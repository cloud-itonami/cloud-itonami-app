# falsify-74 (2026-09-08, 再現性 軸)

falsify-50 で「releases/ 77 ツリー byte 完全一致」を過去形実測に範囲修正し、現在形の再現性
主張を「運行は source classpath、配布は GitHub Releases download + ed25519 署名 manifest
検証 (verify-manifest)」に置き換えた。本反復はその現在形主張を反証試行する。主張:
「配布の ed25519 署名 manifest 検証 (verify-manifest) は実装され、埋め込み公開鍵が実在
し、テスト (updater_test) が署名検証と改変拒否を機械検証している」。反証対象:
verify-manifest が未実装 / 埋め込み鍵が不存在 / テストが署名経路を検証していない。

作業は wt-msloop / bot/maturity-sim-loop で完結、本体 checkout (679572b) 未 touch
(porcelain clean 実測。suite は falsify-63 の identity.clj malformation でコンパイル不能の
ためテスト実行なし、source 静的検証のみ)。

## 実測 (リダイレクト記録、捏造なし)

1. 本体: HEAD = 679572b、porcelain clean (dirty 0)、detached HEAD。dev /health
   (127.0.0.1:1338) → `http_code=200` `{"ok":true,...,"schema":"cloud.itonami.app.health.v1","store":"...","drive-store":"fs"}`
   (稼働継続)。
2. `git ls-tree -r HEAD` → `src/cloud/itonami/app/updater.clj` 実在 (333 行)。
   `updater.clj:61-87` `verify-manifest`: schema (`cloud.itonami.desktop-update.v1`) /
   version (`\d+.\d+.\d+`) / assets 有無を順検証し、`Signature/getInstance "Ed25519"` で
   `.initVerify public-key` → `signable-bytes` (canonical、`:signature` dissoc 後 pr-str、
   updater.clj:49-52) を `.update` → `.verify`、偽なら `:update/signature` ex-info
   fail-closed。`embedded-public-key` は `"cloud-itonami-update-public-key.b64"` を
   `io/resource` で slurp (updater.clj:58-59)。apply 経路 (updater.clj:239,244) は
   package size / digest が署名 manifest と不一致なら throw (fail-closed)。
3. 埋め込み鍵資源: `git ls-tree -r HEAD | grep .b64` →
   `resources/cloud-itonami-update-public-key.b64` (実在)。`git show` 実測 44 文字 base64
   `MCowBQYDK2VwAyEAR5p0Abb2DqRZ+eqPwvUmq0pzps0WxZCrUhd/XVtBiYc=` (ED25519 public key
   DER, 32-byte, X 補正 0x01)。
4. `git ls-tree -r HEAD` → `test/cloud/itonami/app/updater_test.clj` 実在 (155 行、7 deftest)。
   検証項目を実測:
   - `signed-manifests-fail-closed` (updater_test.clj:47-58): 正常署名 round-trip は
     `(= manifest (verify-manifest ...))` で等価。改変 (version "9.0.1" 注入) →
     `thrown-with-msg? "signature is invalid"`。別鍵 bless →
     `thrown? ExceptionInfo`。→ 全 fail-closed 経路を機械検証。
   - `automatic-staging-fails-closed-on-changed-package` (updater_test.clj:tmp): 署名 package
     に対し download-to! で changed-package を書込むと `check-and-stage!` が
     `:error (:status result)` / "does not match" / pending 未作成 — package 改変時の
     digest 不一致拒否を機械検証。
   - `discovery-and-download-stage-only-verified-bytes`: 検証済み bytes のみ stage。
   - `versions-advance-only-forward`: newer-version? forward-only。
   - `applied-receipt-is-visible-but-never-authoritative`: bad edn receipt → :last-applied nil。
   - 単一 version 資源: updater/current-version == Info.plist CFBundleShortVersionString。

## 判定

**SURVIVED**。現在形主張「配布は ed25519 署名 manifest 検証 (verify-manifest) で実施、
埋め込み鍵実在、updater_test が署名検証と改変拒否を機械検証」は成立。
verify-manifest は schema / version / assets / ED25519 署名を全検証し fail-closed、
埋め込み ED25519 公開鍵資源 (resources/cloud-itonami-update-public-key.b64、44 byte
base64) が実在、updater_test 7 deftest は署名 round-trip・単一フィールド改変拒否・
別鍵拒否・package変更時の digest 不一致拒否・排他 (receipt 非権威) を実測検証。
反証候補 (未実装/鍵不在/テスト欠落) はいずれも成立せず。

範囲修正 (新情報): falsify-50 の置き換え後主張を、さらに精緻化する。現在形再現性主張の
配布側は「**検証ロジックの実装+埋め込み鍵+機械テストの実在**」まで確定 (本反復)。ただし
途上にある 2 つの境界 (a) suite が falsify-63 の identity.clj malformation でコンパイル
不能のため本反復は source 静的検証のみでテスト緑は未確認、(b) 署名 manifest の「公開鍵が
実際に org 管理下でローテーション/失効可能である」運用側再現性 (verify 対象の実効) は
未測定 — の範囲は現状の claim に含まず、次反復以降の測定点とする。

再現性 軸 score は **3 のまま** (falsify-50 の範囲修正後の現在形配布主張が本反復で
survived = 裏取りされたが、source 静的検証のみでテスト緑 (falsify-16 相当の完走) の
再測定が suite 赤により不可能なため、score 上昇の根拠なし)。

## 次の 1 アクション

suite が falsify-63 の identity.clj malformation (ns (:import 破損) により再現的に
コンパイル不能) を解消したら、updater_test (7 deftest) を含む単体動行で verify-manifest
の緑を再確認し、さらに署名 manifest 配布の運用実効 (公開鍵管理/ローテーション) を
再現性軸の測定点に回す。identity.clj 修理は Tier 2 kanban/human 判断継続。

## 附帯

本体 checkout は反復中 porcelain clean (dirty 0)、本 bot は touch せず wt-msloop /
bot/maturity-sim-loop で完結。7-軸表 16 行目孤立行 placeholder と falsify-46
mid-sentence cut は残存のまま (operator 復旧待ち、append-only のため編集しない)。
identity.clj malformation (falsify-63..68 build-broken tip) は既知赤として対象外。