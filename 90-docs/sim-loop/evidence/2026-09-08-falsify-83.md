# falsify-83 (2026-09-08, 実装 軸 — HEAD e2b39ba→c792872 前進 (PR #295 initial-launch-grant + #296 wallet-deposits-independent-of-safe merge) が bots-ui 測定面のみに非接触で .kotoba parity 結合水準は生存、2 build-break は未着地継続を git 静的検証 + 一次 compile で確認)

測定日: 2026-09-08 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout は
読み取り専用参照のみ (git status / worktree list)、一切 touch せず。

## 背景 / 反証対象の主張

falsify-82 (head e2b39ba) は実装軸を「merge 前進 6473f6b→e2b39ba (PR #293 safe-capital +
HumanWork .kotoba 移行 2 merge) が新規抽出 human-work-marketplace-core を正しく oracle 配線
しつつ 2 build-break ((B) identity.clj malformation / (A) deps.edn kotoba-net 未宣言) は継続、
src 244 / test 247 に re-anchor」と計測した。本反復で HEAD は **c792872** へ前進した
(git fetch 実測: `e2b39ba..c792872 main`、2 merge #295 initial-launch-grant + #296
wallet-deposits-independent-of-safe)。

反証対象の主張:
1. 「merge 前進 e2b39ba→c792872 が .kotoba parity 測定面 (bot_bounds/bot_slo/human-work 4+1
   core + parity test) に接触し、falsify-78/79/82 の結合水準/配線記録を変化させる」— または
2. 「前進により falsify-63..82 が継続計測してきた 2 build-break ((B) identity.clj (:import
   回復 0 / 裸 [java.* 5 ベクタ、(A) deps.edn kotoba-net 0) が修理着地した」。

両者を独立に falsify する。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

### 1. HEAD 解決と前進幅

`git fetch origin` → `e2b39ba..c792872 main`。`git rev-parse origin/main =
c792872f5c6856359821447fcf64f33e714cbae1`。`git log --oneline origin/main -3`:
c792872 {fix: make wallet deposits independent of Safe setup (#296)} ←
5521098 {Merge PR #295 codex/initial-launch-grant} ← 2bd4580 {Review initial Bot funding
delegation separately from deposits}。falsify-82 の anchor e2b39ba から 2 merge 前進。

### 2. 変化ファイル (`git diff --name-only e2b39ba c792872`)

```
shared/bots-ui/src/cloud/itonami/capital_client.cljs
shared/bots-ui/src/cloud/itonami/capital_ui.cljc
shared/bots-ui/src/cloud/itonami/public_bots.cljc
shared/bots-ui/test/cloud/itonami/funding_role_test.cljs
```

`git diff --stat e2b39ba c792872 -- src test shared/bots-core` = **空** (src/・test/・
shared/bots-core 配下は byte 無変化。4 変化ファイルは全て shared/bots-ui、うち 1 本は
bots-ui 配下の新規 funding_role_test.cljs)。実装軸の .kotoba parity 測定面 (bot_bounds /
bot_slo 4 core + human-work-marketplace-core + 各 parity test、いずれも src/・test/ 配下)
は現前進で **byte 無変化** → falsify-78/79 の 3 値結合水準 (latency/recovery = live #' /
bounds = test-local re-spec / parts = host 結合なし) と falsify-82 の「配線済み新規抽出」
(provider-retry / human-work) は現 HEAD でも生存、および本前進に新規 .kotoba 抽出・配布は
**なし** (`git diff e2b39ba c792872 -- '*.kotoba' '*.kir.edn'` = 空)。

### 3. file-count 再アンカー

`git ls-tree -r --name-only c792872 -- src | wc -l` = **244** / `-- test` = **247**
—— falsify-82 (e2b39ba) の src 244 / test 247 と **同値** (新規 bots-ui テストは bots-ui/
配下で repo-root の src/・test/ 計数に含まれない)。計数は現 HEAD で安定のため再アンカー不要
(同値のまま確認)。

### 4. build-break 生存の独立静的検証 + 一次 compile (claim 2)

`git show c792872:src/cloud/itonami/app/identity.clj` → `grep -c ':import'` = **0**、
ns 形式は `(:require ...)` (28 行閉じ) の**直後に**裸 `[java.*` ベクタ **5** (行 29-33:
[java.nio.charset StandardCharsets] / [java.security MessageDigest SecureRandom] /
[java.time Duration Instant] / [java.util Base64 UUID] / [java.util.concurrent TimeUnit])
を wrap されず残す malformation — falsify-63..82 と完全一致。`git rev-parse
e2b39ba:.../identity.clj` = `3d90156e...` = `c792872:...` (byte 完全同一) — 前進は本ファイル
非接触で malformation は git object レベルで残存。`git show c792872:deps.edn | grep -c
'kotoba-net'` = **0**、`git rev-parse e2b39ba:deps.edn` = `5a36739b...` =
`c792872:deps.edn` (byte 完全同一) — falsify-75/76 の undeclared dependency も残存。

**一次 compile を独立 worktree /private/tmp/mt-msloop83 (head c792872、本体未 touch) で直接
実行** `clojure -M:test -e "(require 'cloud.itonami.app.identity)(println :f83-ok)"` →
**`Syntax error macroexpanding clojure.core/ns at (cloud/itonami/app/identity.clj:1:1)` /
`java.nio.charset - failed: #{:refer-clojure ... :import ...}` EXIT_RC=1** (falsify-63/64/
68/77/80 と同一エラーを新 HEAD c792872 で独立・直接再現)。workspace remove --force / prune
済み、`git worktree list` に mt-msloop83 は残存せず (grep -c = 0)。

## 判定

**REFUTED (claim 2 の修理着地) / SURVIVED (claim 1 の測定面非接触と既存結合/配線記録)**:

- **claim 1 (measurement-surface isolation) SURVIVED**: 前進の変化 4 ファイルは全て
  shared/bots-ui、src/・test/・shared/bots-core の .kotoba parity 測定面は byte 無変化。
  falsify-78/79 の 3 値結合水準と falsify-82 の「配線済み抽出 2 個」は現 HEAD c792872 でも
  生存。本前進に新規 .kotoba 抽出・.kir.edn 配布はなし。src 244 / test 247 は同値で安定
  (re-anchor 不要だが現 HEAD で再確認済み)。
- **claim 2 (build-break 修理着地) REFUTED**: identity.clj (:import opener 0 / 裸 [java.*
  5 ベクタ行 29-33、byte 完全同一 3d90156e) と deps.edn kotoba-net (0、byte 完全同一
  5a36739b) が現 HEAD c792872 でも残存。一次 compile を独立 worktree で直接再現
  EXIT_RC=1 (ns-spec エラー、falsify-63/64/68/77/80 と同一)。falsify-68 の修理着地判定
  3 条件 (HEAD 前進のみ・(:import 復元 未達・compile rc=0 未達) は 1/3 充足、build-break
  生存継続。

実装軸 score は **3 のまま** (build-break 継続で green 化測定不可は不変。本前進は bots-ui
表示面のみで測定面非接触のため score 変動なし。file-count は src 244 / test 247 を現 HEAD
c792872 で確認)。

## スコープ修正 / 次の 1 アクション

- 実装軸・テスト軸 file-count を **src 244 / test 247 (head c792872、falsify-83 実測)**
  として現 HEAD で再確認済み (falsify-82 と同値)。
- 修理案 Tier 2 (kanban/human、本体 checkout 編集が必要で Tier 1 不着地、falsify-68/75/76/
  77/78/79/80/81/82 と不変): (B) identity.clj の 5 裸ベクタを (:import …) で wrap / ns
  形式復元、(A) deps.edn への kotoba-net 宣言 → updater_test 単体緑 (falsify-76 実測済み)。
  両着地後のフル suite green を最終終点に、falsify-62 の bundle/graph 赤 (published-lock
  再発行) もその後再確認。
- 次反復候補: origin/main 前進継続確認 (falsify-76..82 と同型)、または falsify-82 が
  次反復候補とした「bot_bounds / bot_slo を provider-retry / human-work と同水準 (.kir.edn
  + oracle 登録 + host call) へ引き上げる Tier 2 修理案を kanban/human へ正式提出」。

## 附帯

本体 checkout (worktree list 先頭 /Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/
cloud-itonami-app) は detached HEAD 65d3e98 (#290、origin/main c792872 より 2 merge 遅れ)、
`git status --porcelain` 0 行 = porcelain clean (dirty 0)。dev /health 127.0.0.1:1338 ->
**200** 実測。作業は wt-msloop / bot/maturity-sim-loop で完結し、本 bot は本体 checkout を
一切 touch せず。identity.clj malformation / kotoba-net 未宣言は falsify-63..82 と同一で
継続。7-軸表 16 行目孤立行 placeholder / falsify-46 mid-sentence cut は残存のまま
(append-only 規約で本 bot は編集しない)。
(evidence/2026-09-08-falsify-83.md)