# falsify-126 (2026-09-22, テスト 軸)

## 主張 (o)

「origin/main が falsify-75 anchor (679572b) から大幅前進し (324 commits / 829 files
changed / cljk rename wave)、falsify-63 (identity ns 破損) と falsify-75
(kotoba-net 未宣言依存) の 2 既知 build-break が修理されているため、現 tip で
スイートは少なくとも load 段階を通過する」

## 判定: REFUTED (既知 2 赤は修理、しかし新規 load 段階赤で assertion 0 は継続)

### anchor 前進の実測 (読み取りのみ、本体 touch なし)

- 本体 checkout: `/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app`
  (porcelain clean 実測)。HEAD = origin/main = **a3dc8c9** (PR #366 merge 相当)。
  `git rev-list --count 679572b..a3dc8c9` = **324 commits**、
  `git diff --stat 679572b a3dc8c9` = **829 files changed, 34472 insertions, 15190 deletions**。
  ソース全体が .clj/.cljs → **.cljk への rename wave** (identity.clj → identity.cljk 等)。
  ls-tree 実測: **src 256 ファイル / test 261 ファイル** (旧 anchor d226614 測定の
  src 244 / test 247 から再アンカー必要)。
- 本体の fix/ci-install-babashka branch (pre-run script の anchor) は 28105be =
  a3dc8c9 + 2 commits (d2f2e05, 28105be、ci.yml 修正のみ) で origin/main の子孫。

### 既知 2 赤の修理確認 (tip a3dc8c9 の tree 実測)

- **falsify-63 (identity ns 破損) は修理済み**: `git show a3dc8c9:src/cloud/itonami/app/identity.cljk`
  の ns は `(:import [java.net URLEncoder] [java.nio.charset StandardCharsets]
  [java.security MessageDigest SecureRandom] [java.time Duration Instant]
  [java.util Base64 UUID] [java.util.concurrent TimeUnit])` (行 29-34) と
  opener + 名前空間修飾ベクタ形式が復元。裸 java ベクタ 5 件は不在。
- **falsify-75 (kotoba-net 未宣言) は修理済み**: tip deps.edn が
  `io.github.kotoba-lang/http {:git/sha "d54e2a23..."}` を宣言 (行 35)、
  コメント「measured 2026-09-09, the shim had required `kotoba.net.jvm-host`
  since 2026-09-07 and no such namespace existed anywhere」が修理経緯を明記。

### 新規 load 段階赤 (kbb -M:test、detached worktree /tmp/imwt126 で 2 回実行)

isolated worktree (a3dc8c9, 本体未 touch) で `kbb -M:test` を 2 回実行、
**両回とも同一 site で決定論的に rc=1**:

```
Message:  Unable to resolve classname: java.nio.charset.StandardCharsets
Location: /private/tmp/imwt126/src/cloud/itonami/app/authority.cljk:49:3
49:   (:import [java.nio.charset StandardCharsets]
```

- 失敗クラスは falsify-63 と同型だが**対象が JVM の java.nio/java.security
  クラス解決で、環境は nbb (kbb engine, Node.js v26.0.0)** — `kbb -M:test` は
  JVM clojure.main ではなく nbb で走り、JVM :import クラス名を解決できない。
- 加えて kbb 起動時警告: **100 dep(s) are not :local/root and are NOT on the
  classpath** (io.github.kotoba-lang/identity, kotobase, authority, ... の GitHub
  deps が kbb/nbb には解決されない)。
- 現 tip でも **assertion を 1 つも実行できない** (falsify-63 の状態と同値だが
  failure site が identity.clj:1 → authority.cljk:49 へ変化、ランタイムが
  JVM → nbb へ変化)。

### CI 構成の変化 (範囲修正の根拠)

- tip ci.yml は「**Default required path is JVM-free emit. It does not run
  kbb -M:test.** Leftover JVM suite lives in leftover-jvm-tests.yml
  (workflow_dispatch only)」— すなわち現行の suite 赤は PR/push CI の
  required path を塞がない (JVM-free emit + secret-scan + windows smoke のみ)。
  JVM suite は手動 dispatch 専用の leftover workflow に降格。
- したがって「スイートがコンパイル不能」は継続するが、**それが gate を赤にする
  現在形ではなく leftover (手動) path のみ**に範囲修正。

## 附帯測定 (同反復実測)

- df /System/Volumes/Data: `df -h` 実測 **Avail 5.9Gi / 100%**、反復後半の
  再計測で **11284096 KB ≒ 10.8 GiB** — 反復内で ±5 Gi 級の振動を実測。
  緊急フラグ (<100 Gi) は **5 反復連続発火** (falsify-121..125 の系列継続)。
- resident PID **1083** 継続 (**11 反点**, Sep 18 15:52 起動, nbb 経由)。
  export_and_sync 不在 (grep 実測、自助マッチ行除外)。
- dns-resolver annex は本反復未測定 (静止系列の不変は主張しない)。
- 本体 checkout porcelain clean (dirty 0) 実測、本 bot は touch せず
  isolated worktree で完結、worktree は remove --force / prune 済み。

## score

テスト 3 のまま (修理確認と新規赤の同定は測定で裏取れたが、緑の実測が無いため
上昇は規約により不可)。

## 修理案 (Tier 2, kanban/human)

authority.cljk:49 の `(:import [java.nio.charset StandardCharsets] [java.security
MessageDigest] [java.util Base64 UUID])` を nbb で解決可能な形にする
(#?(:bb :import ... / :clj ... の reader 条件分離) か、kbb engine 側の
java-class shim 宣言)。付随して kbb 警告の 100 未解決 deps は nbb.edn 宣言か
suite の nbb 範囲縮小のいずれかの判断が必要。

## 次の 1 アクション

kbb -M:test が authority.cljk を通過できる形に修理された後、suite 全体が
load 〜 完走できるかを単一反復で再測定 (修理着地は kanban/human)。
