# falsify-114 (2026-09-12, 運用/滞留 軸 — 0 ベースの 20 時点観測 / 19 反復連続維持、avail は ±10 Gi 級低下が **3 反復連続発火 → falsify-111 の規約に従い構造的低下と確定** (df 172181 → 159426 MB ≒ 168.1 → 155.7 Gi)、resident は **新 PID 62828 (3:15PM 起動, nbb/node) で再出現**)

測定日: 2026-09-12 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app) は読み取り専用参照のみ、一切 touch せず。
生の出力全体は evidence/2026-09-12-falsify-114-raw.txt に保存。
(前置き: 前反続 falsify-113 は evidence 完成済みのまま未着地だったため、本反復の頭で
commit 3373fff として push してから本測定を開始した。)

## 背景 / 測定対象

falsify-113 の次の 1 アクションは「同 vector + keys + avail を再実測し、0 ベース維持の
**19 反復超の連続持続** or 再蓄積再開を判別。(1) avail の -10 Gi 級低下が 3 反復連続
(構造的低下確定) か否か、(2) resident 不在の継続 (後継プロセスの実測) を観測」だった。
本反復 (falsify-114) はその実行。

**主張**: 本反復の再実測で (a) 3 ベクトルのいずれかに正規 unused が再出現する、または
(b) local annex keys が 786 から増加する、または (c) avail が falsify-113 実測値
(172181 MB ≒ 168.1 Gi) から ±10 Gi 超/反復の大幅変動を起こす (3 反復目の発火)。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD **c80906496cc64cfc60d0b2c600ec9a356f1d9a3b** — falsify-113 と同一、新規前進なし。
porcelain 0 行 = clean)。

### 1-3. main / resident / 全 refs の 3 ベクトル regular unused

- `git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys**、partial
  transfer 残骸 14 chunk (NUMBER 1..14、MD5E-s... 14 行、falsify-85 以降と同一内容) のみ。
  `ok`。
- `git annex unused --used-refspec +refs/heads/resident/dns-resolver` → 同様に正規 unused
  **0 keys**、partial 14 chunk のみ。`ok`。
- `git annex unused` (refspec 指定なし) → 同様に正規 unused **0 keys**、partial 14 chunk
  のみ。孤児 key なし。`ok`。

### 4. git annex info

```
local annex keys: 786        (falsify-113: 786 — 変化 0、20 時点観測 / 19 反復連続維持)
local annex size: 11.68 gigabytes   (不変)
available local disk space: 167.07 gigabytes (+100 megabytes reserved)
  (falsify-113: 180.45 GB — 約 -13.38 GB)
temporary object directory size: 186.98 megabytes (partial chunk 14、不変)
annexed files in working tree: 7143 / 93.02 gigabytes ([b2] 側、不変)
combined annex size of all repositories: 111.12 gigabytes (不変)
```

### 5. df / resident プロセス

- `df -m` 実測: `/dev/disk3s5` (System/Volumes/Data) available **159426 MB ≒ 155.7 Gi**。
  falsify-113 の 172181 MB ≒ 168.1 Gi から **約 -12.4 Gi の低下**。
- `ps aux | grep -E 'itonami-app-resident|export_and_sync' | grep -v grep` → **1 行ヒット
  (rc=0)**: `junkawasaki 62828 ... node ... org-babashka-nbb/cli.js
  /Users/junkawasaki/.gftd/bin/itonami-app-resident.cljs` (S<、**3:15PM 起動**)。
  falsify-113 の「実測不在」は解消、**後継 resident が新 PID 62828 で実測在在**
  (70643 → 62791 → 61573 → 不在 → 62828 の系列)。export_and_sync はヒットなし。

## 判定

**SURVIVED (主張 a/b — 0 ベース) / 成立 (主張 c — avail ステップ変動 3 反復連続)**:
falsify-113 から本反復への独立時点で 3 ベクトルの正規 unused は全て 0 のまま、local
annex keys は 786 → 786 (増加 0、20 時点観測 / 19 反復連続維持) — 主張 (a) 再出現も
(b) 再蓄積も検出されず、falsify-113 指定の閾値「19 反復超の連続持続」を充足し 0 ベース
恒久化傾向を更に精緻化した。

他方、**avail は df 172181 → 159426 MB ≒ 168.1 → 155.7 Gi、annex available 180.45 →
167.07 GB と約 -12.4 Gi / -13.4 GB の低下** — 主張 (c) 成立、**±10 Gi 級ステップ変動検知
の 3 反復連続発火** (低下方向 -10.1 → -10.7 → -12.4 Gi、単調系列で第 3 点は幅が拡大)。
falsify-113 で予告した判定規約「3 反復連続で構造的低下と確定」が本反復で**発動し、
構造的低下を確定**とした。緊急フラグ (<100 Gi) は非該当 (155.7 Gi)。keys 786 /
11.68 GB 不変ゆえ低下は annex 外部 (TCC 遮断で特定不能、operator の du 調査を Tier 2
継続)。観測系列 113.9 → 34.5 → 34.7 → 34.0 → 188.9 → 178.8 → 168.1 → 155.7 Gi。

**resident 再出現は新規事実**: falsify-113 の「実測不在 (後継不明)」に対し、本反復では
新 PID **62828** (3:15PM 起動、nbb 経由で itonami-app-resident.cljs を実行) が実測在在。
短期間で 70643 → 62791 → 61573 → 不在 → 62828 と 4 回の状態遷移が観測されており、
「単一常駐プロセス」ではなく**頻繁に再起動する監視対象**として捉える方が実態に合う。
avail の -12.4 Gi 低下は resident 不在時点 (falsify-113) から在在時点 (本反復) への
遷移中に観測された — 相関候補として記録 (因果特定不能、TCC 遮断)。

運用/滞留 軸 score は **3 のまま** (0 ベース 19 反復超連続維持を記録。実緑測定でのみ
上昇する規約、avail 構造的低下確定は risk-5 記述の再アンカーとして記録)。

## スコープ修正 / 次の 1 アクション

- risk-5 の記述を「滞留は 0 ベースで **falsify-85→114 の 20 時点観測 / 19 反復連続維持**
  (unused 0・keys 786 不動・partial 14 chunk のみ)。**avail は構造的低下と確定**
  (±10 Gi 級低下 3 反復連続 -10.1/-10.7/-12.4 Gi)、annex 外部源 (keys 不変)。緊急フラグ
  (<100 Gi) までの余裕は約 55.7 Gi」へ再アンカー。
- 次の 1 アクション: 同 vector + keys + avail を再実測し、0 ベース維持の
  **20 反復超の連続持続** or 再蓄積再開を判別。あわせて (1) avail の -10 Gi 級低下の
  4 反復目の有無 (発火継続なら 1 反復 -10 Gi 超ペースが実勢と確定、合計低下量から
  緊急フラグ到達 (<100 Gi) の予測時点を算出して Tier 2 提出)、(2) resident PID 62828
  の存続 (3:15PM 起動からの経過観測) を観測。
- 附帯 (実装軸 次反復シグナル): 本体 origin/main は **33037469 のまま**
  (falsify-113 から更新なし、kbb cutover merge 以降 1 反復 no-advance)。本体 HEAD
  33037469 と一致、porcelain 0 行 = clean。次の実装軸反復では 5b2ae3de → 33037469 間の
  parity 測定面 (src/test ファイル数、結線記録) の変化を実測する。実装軸の修理案は
  Tier 2 kanban/human 継続: (1) identity.clj の裸 [java.*] ベクタを `(:import ...)` に
  wrap、(2) kotoba.net 座標を deps.edn に宣言 (git sha 或は :local/root)。
- 附帯 Tier 2: resident 頻繁再起動 (70643→62791→61573→不在→62828) の起動管理主体と
  契機、avail 構造的低下の具体源特定 (TCC 遮断下の du 調査、緊急フラグ到達予測時点の
  算出を含む)、kanban/human へ透過提出継続。

## 附帯

本体 CHECKOUT (worktree list 先頭
`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app`) は
HEAD 33037469dac1237f354c91fbe6378f19ae845de7、`git status --porcelain` 0 行 =
porcelain clean (dirty 0)。本反復は運用軸の falsify-113 次の 1 アクションを測定対象に
選択し、作業は wt-msloop / bot/maturity-sim-loop で完結、本体 checkout を一切 touch せず
(fetch origin main 1 回は origin/main 前進確認のための読み取り、本体 working tree 不変)。
identity.clj malformation / kotoba-net 未宣言 (falsify-63..84) は対象外・既知赤が継続。
7-軸表 16 行目孤立行 placeholder / falsify-46 mid-sentence cut / falsify-91 末尾残骸は
残存のまま (append-only 規約で本 bot は編集しない)。
(evidence/2026-09-12-falsify-114.md)
