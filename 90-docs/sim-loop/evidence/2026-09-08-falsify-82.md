# falsify-82 (2026-09-08, 実装 軸 — HEAD 6473f6b→e2b39ba 前進 (PR #293 codex/safe-capital + HumanWork .kotoba 移行 x2 merge) が新 .kotoba 抽出を正しく oracle 配線しつつ build-break 2 件は継続することを git 静的検証で確認)

測定日: 2026-09-08 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout は
読み取り専用参照のみ (git status / worktree list)、一切 touch せず。

## 背景 / 反証対象の主張

falsify-81 (head 6473f6b) は実装軸を「PR #292 (bot-wallet-pool) は bots-ui 2 ファイルのみで
. kotoba parity 測定面に非接触、build-break (identity.clj malformation (B) / kotoba-net 未宣言
(A)) は継続」と計測した。本反復で HEAD は **e2b39ba** へ前進した (git fetch 実測:
`6473f6b..e2b39ba main`、2 merge #293 safe-capital + HumanWork page-title .kotoba 移行 2 commit)。

反証対象の主張:
1. 「merge 前進 6473f6b→e2b39ba で (falsify-78/79 の枠組みに沿い) 新規 .kotoba 抽出は
   oracle 未登録・.kir.edn 無し・host oracle-ref 0 = 判断権が host に残ったまま (bot_slo 型
   blanket の拡張)」— または
2. 「前進により falsify-63..81 が継続計測してきた 2 build-break ((B) identity.clj (:import 回復
   0 / 裸 [java.* 5 ベクタ、(A) deps.edn kotoba-net 未宣言) が修理着地した」。

両者を独立に falsify する。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

### 1. HEAD 解決と前進幅

`git fetch origin` → `6473f6b..e2b39ba main`。`git log --oneline origin/main -5`:
e2b39ba {migrate: HumanWork page title decisions -> human_work_marketplace_core.kotoba} ←
2e5e5fe {同題 (migration 2 commit)} ← 3a961a9 {Merge PR #293 codex/safe-capital} ←
2d63e41 {Prefill Safe connection address from a review link} ← 405d31d。`git rev-parse
origin/main = e2b39ba9dea6e18d29b1f7e659923c3d0d58446a`。falsify-81 の anchor 6473f6b から
2 merge 前進。ファイル計数は origin/main e2b39ba にアンカーした (falsify-78 の規約どおり)。

### 2. 変化ファイル (`git diff --name-only 6473f6b e2b39ba`)

```
90-docs/adr/2609081200-human-work-marketplace-title-escape-core.edn
resources/cloud/itonami/app/oracle/human-work-marketplace-core.kir.edn   (NEW 資源! .kir.edn 同梱)
shared/bots-ui/src/cloud/itonami/capital_client.cljs
shared/bots-ui/src/cloud/itonami/capital_ui.cljc
shared/bots-ui/src/cloud/itonami/public_bots.cljc
src/cloud/itonami/app/human_work_marketplace.clj
src/cloud/itonami/app/human_work_marketplace_core.kotoba                (NEW 抽出 core)
src/cloud/itonami/app/kotoba_oracle.cljc                                (decision-core-src に 1 行追加)
test/cloud/itonami/app/human_work_marketplace_kotoba_parity_test.clj    (NEW 116 行 parity テスト)
test/cloud/itonami/app/test_runner.clj                                  (+2, parity 登録)
```

`git diff --stat 6473f6b e2b39ba -- src test shared/bots-core`: human_work_marketplace.clj
+29 / human_work_marketplace_core.kotoba +48 / kotoba_oracle.cljc +3 /
human_work_marketplace_kotoba_parity_test.clj +116 / test_runner.clj +2 (計 5 ファイル / 192
insertions)。PR #293 (safe-capital) は bots-ui 3 ファイル (表示面) のみ、.kotoba parity 測定面
(src/test 側) に非接触。falsify-78/79 の bot_bounds / bot_slo 結合面は本 diff に現れず → 3 値
結合水準 (latency/recovery = live #' / bounds = test-local re-spec / parts = guest-only) は
生存 (SURVIVED 側)。

### 3. 新規 .kotoba 抽出 human-work-marketplace-core の oracle 配線 (claim 1 の検証)

`git show origin/main:src/cloud/itonami/app/kotoba_oracle.cljc | diff` —
`decision-core-src` map (id →.kotoba source) に **`:human-work-marketplace-core
"cloud/itonami/app/human_work_marketplace_core.kotoba"`** を追加 (+1 行)。資源
`resources/cloud/itonami/app/oracle/human-work-marketplace-core.kir.edn` が diff 名簿に現れ
**.kir.edn 同梱**。host `human_work_marketplace.clj` (`git show`) は `[cloud.itonami.app.kotoba-oracle
:as oracle]` を require し、**`(oracle/call :human-work-marketplace-core 'pick-brand-name …)`
と `(oracle/call :human-work-marketplace-core 'needs-escape? …)` の 2 本 host 実行時 oracle
呼び出し**を持つ (host oracle-ref > 0)。parity テスト `human_work_marketplace_kotoba_parity_test.clj`
(116 行 / 6 deftest) は pick-brand-name / needs-escape? を host or-default / escape-map に対し
突合し、**かつ oracle 経路 (同梱 KIR artifact) を end-to-end で通す** deftest
(`the-page-still-embeds-the-escaped-title`) と nil 契約行 (`absent-name-answers-wordmark-and-no-escape`)
および native (x86_64/aarch64) / portable (wasm32/js) 全 target の compile 担保を持つ。

→ claim 1 の「新規 .kotoba 抽出は oracle 未登録・.kir.edn 無し・host oracle-ref 0」は
**REFUTED (スコープ修正)**: human-work-marketplace-core は provider-retry-core (falsify-78 で
確認済みの「良い」側) に続く **2 個目の oracle 配線済み抽出**。判断 2 本 (pick-brand-name /
needs-escape?) が同梱 KIR へ移り、host は oracle 経由で実行。falsify-78/79 が確立した「抽出は
結合水準が一様でない (cores により異なる)」に、新たに「正しく oracle 配線された抽出」の実測
データ点を 1 追加。blanket の範囲修正: 「直近の新規 .kotoba 抽出は全て host 権限 (bot_slo 型)」
は成り立たず、実際には配線済み抽出が増えている。

### 4. build-break 生存の独立静的検証 (claim 2)

`git ls-tree -r --name-only origin/main -- src | wc -l = **244**` (falsify-81 243 比 +1 =
human_work_marketplace_core.kotoba) / `-- test = **247**` (246 比 +1 = parity_test)。falsify-81
の src 243 / test 246 を e2b39ba 実測へ re-anchor。

`git show origin/main:src/cloud/itonami/app/identity.clj` → ns 形式は falsify-63..81 と完全一致の
**malformation**: `grep -c ':import'` = **0**、裸 `[java.*` ベクタ = **5** (行 29-33:
[java.nio.charset StandardCharsets] / [java.security MessageDigest SecureRandom] /
[java.time Duration Instant] / [java.util Base64 UUID] / [java.util.concurrent TimeUnit])。
claim 2 の (B) は未着地。`git show origin/main:deps.edn | grep -c 'kotoba-net'` = **0**
(grep_rc=1) — (A) も未着地。git object が falsify-81 (6473f6b) と同一バイト列のため、falsify-68/
77/80/81 で detached worktree に独立直接再現した一次 compile EXIT_RC=1 は**推移的帰結として
継続** (本反復は compile を再実行せず、バイト列同一からの推移的帰結として記録、捏造なし)。

## 判定

**REFUTED (claim 1 の snake-oil blanket + claim 2 の build-break 修理着地) / SURVIVED
(claim 1 側の「配線済み新規抽出」= 質のデータ点、既存 bot_bounds/bot_slo 結合面)**:

- **claim 1-β (健全面)**: human-work-marketplace-core は oracle 登録 (.kir.edn 同梱 /
  kotoba_oracle decision-core-src 追加 / host `oracle/call` ×2 / parity-through-oracle テスト) —
  provider-retry-core に続く 2 個目の正しく配線された新規抽出。falsify-78/79 の「結合一様でない
  (3 水準)」枠組みは生存し、むしろ「配線済み抽出が増加」で強化。SURVIVED。
- **claim 1-α (blanket)**: 「新規抽出は全て host-authority / oracle 未登録」は REFUTED (上の
  通り、配線済み 2 個目を実測)。
- **claim 2 (build-break)**: 「前進で修理着地」は REFUTED — identity.clj (:import 0 / 裸 5
  ベクタ) と deps.edn kotoba-net (0) が現 HEAD e2b39ba でも git object レベルで残存。falsify-68
  の修理着地判定 3 条件未達、build-break 生存継続。

実装軸 score は **3 のまま** (build-break 継続のため green 化測定不可は不変。新規抽出の oracle
配線は質の肯定的データ点だが、suite は identity.clj compile 死で走れず score は green 化でしか
上がらない。file-count は src 244 / test 247 へ re-anchor)。

## スコープ修正 / 次の 1 アクション

- 実装軸・テスト軸 file-count を **src 244 / test 247 (head e2b39ba、falsify-82 実測)** へ
  re-anchor。
- falsify-78/79 の結合水準枠組みの範囲を修正: 「新規 .kotoba 抽出 = host-authority blanket」→
  「配線済み抽出 (provider-retry / human-work-marketplace) と未配線抽出 (bot_bounds / bot_slo)
  が混在、個別コアごとに判定」へ。次反復候補: bot_bounds / bot_slo を provider-retry /
  human-work と同水準 (shipped .kir.edn + oracle 登録 + host call) へ引き上げる Tier 2 修理案
  を kanban/human へ正式提出。
- 修理案 Tier 2 (kanban/human、本体 checkout 編集が必要で Tier 1 不着地、falsify-68/75/76/77/78/
  79/80/81 と不変): (B) identity.clj の 5 裸ベクタを (:import …) で wrap / ns 形式復元、(A)
  deps.edn への kotoba-net 宣言 → updater_test 単体緑 (falsify-76 実測済み)。両着地後のフル
  suite green を最終終点に。
- 次反復候補: origin/main 前進継続確認 (falsify-76..82 と同型) 又は bot_bounds/bot_slo の
  Tier 2 修理案を kanban/human へ正式提出。

## 附帯

本体 checkout (worktree list 先頭 `/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/
cloud-itonami-app`) は detached HEAD 65d3e98 (#290、origin/main e2b39ba より 2 merge 遅れ)、
`git status --porcelain` 0 行 = porcelain clean (pre-run dirty_files 0 と一致)。dev /health
127.0.0.1:1338 -> **200** 実測 (pre-run server_health 200)。本 bot は touch せず wt-msloop /
bot/maturity-sim-loop で完結。identity.clj malformation / kotoba-net 未宣言は falsify-63..81 と
同一で継続。7-軸表 16 行目孤立行 placeholder / falsify-46 mid-sentence cut は残存のまま
(append-only 規約で本 bot は編集しない)。
(evidence/2026-09-08-falsify-82.md)