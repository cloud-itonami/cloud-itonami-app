# falsify-93 (2026-09-08, 運用/滞留 軸 — 0 ベースの 8 反復超連続維持 (9 時点観測跨ぎ、恒久化傾向の更なる精緻化) 判定)

測定日: 2026-09-08 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app) は読み取り専用参照のみ、一切 touch せず。

## 背景 / 測定対象

falsify-92 は 3 ベクトル (main / 全 refs / resident-refspec) の正規 unused 0・local annex
keys 786 不動を falsify-85→92 の 8 時点観測 / 7 反復連続維持で再実測し、0 ベースを
「恒久化傾向の更なる精緻化」へ更新した。その次の 1 アクションは「次反復で同 3 ベクトル +
local annex keys + avail を再実測し、0 ベース維持の **8 反復超の連続持続** or 再蓄積再開を
判別 (8 反復超維持なら恒久化傾向を更に精緻化、再開なら falsify-33..53 語調へ復帰)、あわせて
avail (158 Gi 安定) の帰属 — 滞留 (keys 786) 不変下の外部書き込み源を観測」。あわせて実装軸
附帯で「d3ce2cd の parity 測定面 (3 値結合水準・配線記録) の変化と 2 build-break
(identity.clj / kotoba-net) の生存の実測」が指定されていた。

本反復 (falsify-93) はその次の 1 アクションを執行し、falsify-92 が指定した閾値「0 ベースの
**8 反復超の連続持続 → 恒久化傾向の更なる精緻化**」の充足を falsify する。

**主張**: 本反復の再実測で (a) 3 ベクトルのいずれかに正規 unused が再出現する、または
(b) local annex keys が 786 から増加する (8 反復超への持続は成立せず残留再蓄積を開始する)、
または (c) avail が 150 Gi 未満へ続落する (158 Gi の安定からの下振れが構造的減少へ転じる)。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD detached 566928f67、resident branch は `resident/dns-resolver`)。

### 1. main-refspec regular unused

`git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys** (出力は partial
transfer 残骸 14 chunk のみ、NUMBER 1..14 の MD5E-s... 14 行)。`ok` / EXIT_RC=0。

### 2. resident-refspec (帰属ベクトル)

`git annex unused --used-refspec +refs/heads/resident/dns-resolver` → 同様に正規 unused
**0 keys**、partial 14 chunk のみ。`ok` / EXIT_RC=0。

### 3. 全 refs 走査 (孤児ベクトル)

`git annex unused` (refspec 指定なし) → 同様に正規 unused **0 keys**、partial 14 chunk
のみ。いかなる ref からも到達不能な孤児 key は存在しない。`ok` / EXIT_RC=0。

### 4. git annex info

```
local annex keys: 786        (falsify-93: 786 — 変化 0、9 時点観測 / 8 反復連続維持)
local annex size: 11.68 gigabytes   (不変)
available local disk space: 171.88 gigabytes (+100 megabytes reserved) (falsify-92: 169.95
                                                              GB — 約 +1.93 GB 極小増、上方振動)
temporary object directory size: 186.98 megabytes (partial chunk 14、不変)
annexed files in working tree: 7143 / 93.02 gigabytes ([b2] 側)
combined annex size of all repositories: 111.12 gigabytes
```

3 ベクトルが全て正規 unused 0 のまま。local annex keys は **786 → 786 (増加 0)** — 滞留
蓄積期 (falsify-39..49) の反復あたり +24/+25 単調増加とは対照的に、増分ゼロの状態が
**falsify-85→93 の 9 時点観測 / 8 反復連続維持**で続いている (falsify-92 の 8 時点を
1 時点上回る、falsify-92 指定の閾値「8 反復超」を充足)。

### 5. df avail / 増加源プロセス

`df -h /` → avail **160 Gi / 10%** (falsify-92 の 158 Gi から +2 Gi 上方振動、飽和なし・
100% 標示なし)。annex available local disk space は 171.88 GB (falsify-92 の 169.95 GB 比
約 +1.93 GB 極小増、df と同方向・上方)。avail は falsify-85 (163) → 88 (142) → 90 (159) →
91 (159) → 92 (158) → 本反復 (160) と 142-163 Gi 帯で振動、**150 Gi 未満への続落なし** —
主張 (c) は SURVIVED (158 Gi 安定の下振れはなく上方へ振動、構造的減少へは転じておらず、
滞留 keys 786 不変下の外部書き込み変動帰属と整合)。

`ps aux | grep -E 'itonami-app-resident|export_and_sync'` → 増加源 `node nbb
~/.gftd/bin/itonami-app-resident.cljs` **PID 70643 稼働継続** (falsify-85/86/87/88/89/90/91/92
と同一 PID のまま、連続稼働、export_and_sync は不在)。

### 6. origin/main (実装軸 附帯)

`git fetch origin` → origin/main は **d3ce2cd → c618ef3 へ再前進** (新シグナル)。
`rev-parse origin/main = c618ef334f4979a9477b0c598a07d15d8c29ca73`。`git show --stat -1` →
**migration `appearance_core.kotoba` (ADR-2609082000、merge bot/refactor-20260908-2102)、
変化 7 本 / +439 insertions**: 新規 `2609082000-appearance-mode-cycle-plane-core.edn`
(90 行)・新規 `appearance-core.kir.edn` (75 行)・`appearance.cljc` (17 変更)・新規
`appearance_core.kotoba` (89 行)・`kotoba_oracle.cljc` (3 変更)・新規
`appearance_kotoba_parity_test.clj` (**174 行**)・`test_runner.clj` (2 変更)。
**falsify-89/90/92 が実装軸シグナルとした 52c156c (vf-calibrate) → e91bf0f (vf-authority) →
d3ce2cd (connectors) と同種の kotoba parity 測定面 migration が 4 反復連続で再発** —
今回は appearance mode-cycle plane、parity_test は **174 行 (d3ce2cd の 134 行より更に増)**。
.kotoba 分量の重みが 88→89→90→92→93 と反復ごとに増している。本体 checkout は detached
HEAD b5df93e (#299) / porcelain clean (dirty 0)。

## 判定

**SURVIVED (主張 a/b/c とも不成立) / 0 ベースは 8 反復超の連続持続へ達し「恒久化傾向の更なる
精緻化」を達成**: falsify-92 から本反復への独立時点で 3 ベクトル (main / 全 refs /
resident-refspec) の正規 unused は **全て 0 のまま**、local annex keys は **786 → 786
(増加 0, falsify-85→93 の 9 時点観測 / 8 反復連続維持)**、既知 partial 14 chunk のみ —
主張 (a) の再出現も (b) の再蓄積も検出されなかった。増加源 (resident PID 70643) は稼働
継続のまま、新規 key を参照させる滞留を積まない状態が 8 反復 (観測 9 時点) にわたり維持
されている。avail は 160 Gi (158→160、+2 Gi 上方振動) で安定し、主張 (c) の 150 Gi 未満へ
の続落も不成立。

falsify-92 が指定した閾値「0 ベースの 8 反復超の連続持続」を本反復が充足 — 恒久化傾向を
**更に精緻化**した。ただし増加源 (PID 70643) 稼働継続ゆえ恒久性は保証されず「never
re-accumulate」は主張しない。

運用/滞留 軸 score は **3 のまま** (0 ベースの恒久化傾向精緻化を記録、green 化測定/実緑の
裏付けは本反復では得られない — score は実緑測定でのみ上昇する規約)。

## スコープ修正 / 次の 1 アクション

- risk-5 の記述を「滞留は 0 ベースへ回帰し **falsify-85→93 の 9 時点観測 / 8 反復連続維持**
  (unused 0・keys 786 不動・partial 14 chunk のみ) — **恒久化傾向の更なる精緻化継続**」と
  再アンカーする。ただし増加源 (resident ingest, PID 70643) 稼働継続ゆえ再蓄積の可能性は残る。
- 次の 1 アクション: 次反復で同 vector を再実測し、0 ベース維持の**9 反復超の連続持続** or
  再蓄積再開を判別 (9 反復超維持なら恒久化傾向を更に精緻化、再開なら falsify-33..53 語調へ
  復帰)。あわせて avail (160 Gi 安定) の帰属 — 滞留 (keys 786) 不変下の外部書き込み源
  の特定を観測。
- 附帯 (実装軸 次反復シグナル): origin/main は **d3ce2cd → c618ef3 へ再前進** (appearance_core
  .kotoba、ADR-2609082000)。falsify-89/90/92 の vf-calibrate / vf-authority / connectors に
  続く **同種 kotoba parity 測定面 migration の 4 反復連続再発** (parity_test 174 行、
  d3ce2cd の 134 行より更に増) — 次の実装軸反復で c618ef3 の parity 測定面 (3 値結合
  水準・配線記録) の変化と、2 build-break (identity.clj / kotoba-net) の生存を実測する。
  本体 checkout は detached HEAD b5df93e (#299) / porcelain clean。
- 附帯 Tier 2: falsify-85/86/87/88/89/90/91/92 と不変 — resident プロセス再起動 (94682→70643) の
  契機と滞留回収の帰属 (operator 手動 dropunused/世代削除 vs resident 挙動変化) は継続
  kanban/human へ透過提出。

## 付記 (append-only 規約): falsify-92 追記末尾に新規残骸なし

本反復の wc 実測で maturity.md は行 1272 まで。falsify-92 追記は `(evidence/2026-09-08-falsify-92.md)`
で終わり、新規残骸行は混入していない。falsify-91 追記コマンドの残骸 (行 1237-1238: `MD` /
`echo "append rc=$?" ...`) は append-only 規約により本 bot は編集せず残存のまま。本反復追記は
その後に追加する。

## 附帯

本体 CHECKOUT (worktree list 先頭
`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app`) は
detached HEAD b5df93e (#299)、`git status --porcelain` 0 行 = porcelain clean (dirty 0)。
origin/main は本反復で **d3ce2cd → c618ef3 へ再前進** (appearance_core.kotoba、変化 7 本 /
+439：新規 .kotoba・parity_test 174 行、repo-root src/・test/ 接触 — 実装軸 次反復シグナル)。
本反復は運用軸の falsify-92 次の 1 アクションを測定対象に選択し、作業は wt-msloop /
bot/maturity-sim-loop で完結、本体 checkout を一切 touch せず。identity.clj malformation /
kotoba-net 未宣言 (falsify-63..84) は対象外・既知赤が継続 (c618ef3 の kotoba parity 接触は
本反復の運用軸測定に関する限り影響なし、次反復の実装軸再確認事項)。7-軸表 16 行目孤立行
placeholder / falsify-46 mid-sentence cut / falsify-91 末尾残骸 (本付記参照) は残存のまま
(append-only 規約で本 bot は編集しない)。
(evidence/2026-09-08-falsify-93.md)