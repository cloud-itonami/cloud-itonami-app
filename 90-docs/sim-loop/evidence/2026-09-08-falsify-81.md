# falsify-81 (2026-09-08, 実装 軸 — HEAD 2588c51→6473f6b 前進 (PR #292 codex/bot-wallet-pool) が机上 .kotoba parity 測定面と build-break に影響しないことを一次 compile + git 静的検証で確認)

測定日: 2026-09-08 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout は
読み取り専用参照のみ (git status / worktree list)、一切 touch せず。

## 背景 / 反証対象の主張

falsify-80 (head 2588c51) は実装軸の .kotoba 抽出評価を、falsify-79 で確立した
「parity 結合水準 3 値 (latency/recovery = live #' 結合 / bounds = test-local re-spec /
parts = host 結合なし)」で記録し、2 build-break — (B) identity.clj の (:import 復元 0 /
裸 [java.* 5 ベクタ、一次 compile EXIT_RC=1、(A) deps.edn の kotoba-net 未宣言 —
を EXIT_RC=1 で継続実測した。HEAD は反復毎に前進し、falsify-80 の 2588c51 (#291
yield-funded-rounds) に続き PR #292 (codex/bot-wallet-pool) が merge された。

反証対象の主張: 「PR #292 merge 前進 (2588c51→6473f6b) により、(i) falsify-79 が
確立した bot_bounds / bot_slo parity 結合水準が変化した、または (ii) falsify-63..80 が
継続計測してきた 2 build-break (identity.clj malformation / kotoba-net 未宣言) が
修理着地した」。計測面 (parity テスト・build 対象) が変わらない限り持たない暗黙前提の
ため、両者を独立に falsify する。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

### 1. HEAD 解決と前進幅

`git rev-parse origin/main = 6473f6b` (`git log --oneline origin/main -5`: 6473f6b
{#292 bot-wallet-pool} ← 32c8743 {Show lender Bots / verified pooled capital} ←
2588c51 {#291 yield-funded-rounds} ← c06ae5a ← 65d3e98 {#290 funding-filter})。falsify-80
の anchor 2588c51 から PR #292 の 1 merge (実体 32c8743) 前進。本体 checkout の asserted
HEAD は 65d3e98 (#290、前反復と変わらず) で、origin/main 6473f6b より 2 merge 遅れ。
計数は origin/main 6473f6b にアンカーした (falsify-78 の HEAD 解決規約どおり)。

### 2. 計測面の変化範囲 (git diff 2588c51 6473f6b --name-only)

```
shared/bots-ui/src/cloud/itonami/public_bots.cljc
shared/bots-ui/test/cloud/itonami/public_bots_test.cljs
```

変化は **2 ファイルとも bots-ui のみ** (public_bots.cljc 表示面 + その cljs テスト)。
`git diff 2588c51 6473f6b -- src test shared/bots-core` は **題面 0 行 (空)** — 実装軸の
.kotoba parity 測定面 (bot_bounds.cljc / bot_slo.clj / 抽出 .kotoba + parity test) は
byte 無変化。falsify-79 の parity 結合水準 3 値は現 HEAD 6473f6b でも生存候補
(SURVIVED 側)。

### 3. build-break 生存の独立検証

`git ls-tree -r --name-only origin/main -- src | wc -l = 243` / `-- test = 246`
(falsify-80 実測と同一 re-anchor)。`git show origin/main:src/cloud/itonami/app/identity.clj |
grep -n ':import\|\[java\.'` → **:import line 0 / 裸 [java.* ベクタ 5 (行 29-33:
[java.nio.charset StandardCharsets] / [java.security MessageDigest SecureRandom] /
[java.time Duration Instant] / [java.util Base64 UUID] / [java.util.concurrent
TimeUnit]))** — falsify-63..80 と完全一致。`git show origin/main:deps.edn | grep -c
'kotoba-net'` → **0** (grep_rc=1、kotoba-net 未宣言のまま)。

### 4. 一次 compile (build-break (ii) 側の独立再現)

本体 checkout から独立した detached worktree `/private/tmp/mt-msloop81` (head 6473f6b、
本体未 touch、`git worktree add` rc=0) で `clojure -M:test -e "(require
'cloud.itonami.app.identity)(println :f81-ok)"` → **`Syntax error macroexpanding
clojure.core/ns at (cloud/itonami/app/identity.clj:1:1)` / `java.nio.charset - failed:
#{:refer-clojure}` (ns-refer-clojure / ns-require / ns-import / ns-use / ns-refer / ns-load
/ ns-gen-class の 7 clause 全て) EXIT_RC=1** — falsify-63/64/68/77/80 と同一エラーを現
HEAD 6473f6b で独立・直接再現。

## 判定

**REFUTED (build-break の (ii) 側) — 但し SURVIVED (parity 測定面 i. 側)**:

- **i. side (parity 結合水準)**: PR #292 (bot-wallet-pool) は bots-ui 2 ファイルのみで
  .kotoba parity 測定面に touch せず (diff 空)、falsify-79 の「latency/recovery = live
  #' 結合 / bounds = test-local re-spec / parts = host 結合なき guest-only」の 3 段結合
  水準は現 HEAD 6473f6b でも生存 (SURVIVED)。
- **ii. side (build-break)**: 「#292 merge 前進により build-break 修理着地」は REFUTED —
  identity.clj の (:import 0 / 裸 5 ベクタが現 HEAD でも残存し、一次 compile が
  EXIT_RC=1 (独立直接再現)、kotoba-net deps.edn 未宣言も残存 (grep 0)。falsify-68 の
  修理着地判定 3 条件 (HEAD 前進 / (:import 復元 / 一次 compile EXIT_RC=0) は未達、
  build-break 生存継続。

実装軸 score は **3 のまま** (bot-wallet-pool merge は測定面非接触で score 変動なし、
build-break 継続の質的反証は既知赤の再確認。green 化測定は両 build-break 修理後のため
不可)。

## スコープ修正 / 次の 1 アクション

- 実装軸・テスト軸の file-count を **src 243 / test 246 (head 6473f6b、falsify-81
  実測)** へ re-anchor (origin/main へ再アンカー、falsify-78 の HEAD 解決規約どおり)。
- 修理案 Tier 2 (kanban/human、本体 checkout 編集を要し Tier 1 不着地、falsify-68/75/
  76/77/78/79/80 と不変): (B) identity.clj の 5 裸ベクタを (:import ...) で wrap /
  ns 形式復元、(A) deps.edn への kotoba-net 宣言 → updater_test 単体緑 (falsify-76
  実測済み)。両着地後のフル suite green を最終終点に。
- 次反復候補: falsify-79 の Tier 2 report (bot_bounds parity 張り替え案: bounds を live
  #'admit-spend へ張り替え) を kanban/human へ正式提出、又は origin/main 前進継続確認
  (falsify-76..81 と同型の compile 独立再現) を 1 反復ごとに続行。

## 附帯

本体 checkout は反復中 porcelain clean (dirty 0、`git status --porcelain` 0 行実測)、
dev /health 127.0.0.1:1338 -> **200** 実測。本 bot は touch せず wt-msloop /
bot/maturity-sim-loop で完結。実行 worktree `mt-msloop81` は remove --force / prune 済み
で `git worktree list` に残存せず (/private/tmp/refactor-* 2 worktree を含む worktree
list 7 行確認後、mt-msloop81 のみ削除)。7-軸表 16 行目孤立行 placeholder / falsify-46
mid-sentence cut は残存のまま (operator 復旧待ち、append-only で本 bot は編集しない)。
identity.clj malformation / kotoba-net 未宣言は falsify-63..80 と同一で継続。本体
checkout が origin/main (#292) でなく 65d3e98 (#290) に detach されている状態も記録
(worktree list 実測、コード面の主要差分は bots-ui のみのため計数差なし)。
(evidence/2026-09-08-falsify-81.md)