# falsify-53 — 運用/滞留帰属: "unused ⊆ resident 現在ツリー" の 651-key / 9.7GiB スケール生存確認 (2026-09-07)

測定者: itonami-maint
軸: 運用 (git-annex 滞留帰属)
ターゲット: falsify-25/26/28/29/30/31/33/34/35/36/37/39/40/41/42/43/44/45/46/48/49 が
生存を再確認してきた帰属「main-refspec で unused 判定される滞留が、全件 resident
現在ツリー参照 (= unused − resident = 0)」を、falsify-49 (456 keys / 6.41 GiB) 後の
より大スケールで再検証する。falsify-50/51/52 は再現性軸のため滞留は 3 反復ぶり測定。

## 検証方法

`cloud-itonami-dns-resolver` (DataLad/git-annex データセット) で、従来通り
`git annex unused --used-refspec +refs/heads/main` と
`git annex unused --used-refspec=+refs/heads/resident/dns-resolver` を実行し、
main-refspec 正規 unused と resident-refspec 正規 unused を比較する。

## 測定 (実測、捏造なし)

### main-refspec (`git annex unused --used-refspec +refs/heads/main`)
- 正規 unused key 数: **651** (出力の MD5E-s... 行を機械カウント、/tmp/unused_main.txt)
- `git annex info`: **local annex keys = 651** / **size 9.7 GB**、**unused keys size = 9.7 GB**

### resident-refspec (`git annex unused --used-refspec=+refs/heads/resident/dns-resolver`)
- 正規 unused: **0 keys** (partial chunk 14 個のみ — /tmp/unused_res.txt 実測)
- partial chunk は 14 個 / temp 186.98 MB (falsify-41〜49 と同値、drift なし継続)

### 帰属判定
- main-refspec unused (651) のうち resident 参照 = 651−(0) = 651 → **unused − resident = 0**
- 帰属「滞留 ⊆ resident 現在ツリー」は **651-key / 9.7 GiB スケールで SURVIVED 継続**

### 増加レート
- falsify-49 (456 keys / 6.41 GiB) → falsify-53 (**651 keys / 9.7 GiB**) = **+195 keys / +3.3 GiB**
  (falsify-49 以降 4 反復跨ぎ ≈ 反復あたり ~+49 keys。ただし再現性軸 3 反復 (50/51/52) は
  未測定のため分解不能。レートは falsify-49 単反復 +12 から加速した大増加)

## 副次測定 (疎通・資源)

- dev `/health` 127.0.0.1:1338 → **200** (稼働)
- gateway `/health` 127.0.0.1:8080 → **404** (falsify-48/49 と同値)
- df `/` avail **179Gi / 9%** (falsify-49 記録 136Gi / 85% から大回復、飽和解消のまま)
- 増加源 itonami-app-resident.cljs PID 94682 稼働継続 (falsify-33〜49 と同一 PID、export_and_sync 不在)
- 負荷 load avg 33.22/27.50/33.67 (高負荷)

## 状態変化 (リスク-2 再発)

**本体 checkout が falsify-52 以降 dirty に変化**: `git status --porcelain` で
`?? 90-docs/adr/260907-kl-to-kotoba-migration.edn` / `?? 90-docs/kotoba-migration-progress.md`
の 2 untracked ファイルを検出 (falsify-52 時点では porcelain clean / dirty 0 と記録)。
Kotoba 移行 ADR の追記作業 (他 bot/human の WIP) と推定。SOUL 不変条件に従い本 bot は
本体 checkout を **touch せず**、作業は wt-msloop (branch bot/maturity-sim-loop) で完結。

## 判定

**SURVIVED** — 帰属「unused ⊆ resident 現在ツリー」(unused − resident = 0) が
651-key / 9.7 GiB スケールで生存継続。滞留は増加継続 (+195 keys / 4 反復跨ぎ)。

## 範囲修正 / Tier 2 / NEXT

1. dropunused は引き続き反復緩和であり恒久解消ではない。恒久解消は resident ingest
   世代削除 / 方針見直し (データ削除判断 → Tier 2 operator)。
2. リスク-2 再発を記録: 本体 dirty 中は Tier 1 着地不可能。本反復は測定・記録のみ
   (Tier 2 以内)。
3. 運用軸 score は 3 のまま (帰属の同一主張のスケール拡大であり score へ質的変更なし)。