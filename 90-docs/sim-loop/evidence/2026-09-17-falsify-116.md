# falsify-116 (2026-09-17, 運用/滞留 軸)

## 主張 (反証対象)

主張 (c) 継続形: 「avail の -1 Gi 級減速が定着する」— すなわち ±10 Gi 級ステップ変動検知は 5 反続で発火しない。

## 実測

- 時刻: 2026-09-17 13:17:04 JST (`date` 実測)
- `df -m /System/Volumes/Data`: Filesystem 1M-blocks 948584, Used 838543, **Available 55408**, Capacity 94%
  - falsify-115 実測 158160 MB ≒ 154.5 Gi → **55408 MB ≒ 54.1 Gi、約 -101.5 Gi の大低下**
  - **±10 Gi 級ステップ変動検知は 5 反続目で発火し、しかも 1 桁超 (-1.2 Gi クラス) ではなく 100 Gi クラス** — 主張 (c) 減速定着は REFUTED
  - **緊急フラグ (<100 Gi) が初めて発動**: 54.1 Gi < 100 Gi。余裕は約 54.1 Gi でなく 54.1 Gi 自体が残存値 (誤記防止: 残り 54.1 Gi)
- 増加源 (resident): `ps aux | grep itonami-app-resident | grep -v grep` 実測 1 行 —
  PID **12978** (5:01PM 起動、nbb 経由 `scripts/itonami-app-resident.cljk`、node)。実在の node プロセスであり
  測定ハーネスの自助マッチ行ではない (コマンド行が node 実行)。6 回目の状態遷移
  (70643 → 62791 → 61573 → 不在 → 62828 → 66954 → **12978**)。
- **annex 測定ベクトル (3 unused vector / local annex keys 786) は本反復で再現不能**:
  - `du -sm .../cloud-itonami-app/.git/annex/objects` → No such file or directory (rc=1)
  - `ls .../cloud-itonami-app/data/ledger` → No such file or directory (rc=1)
  - `ls .../cloud-itonami-app/data` 実測: `ledger/` は存在せず、
    `config.edn.bak-20260911-capacity` / `state.edn.bak-signin-20260914` /
    `state.journal.edn.orphan-2026-09-14T080749377363Z` 等の新レイアウト
    (agent-enrollment.key, bot-identity.seed, bot-workspaces, chronicle, drive-issuer.key,
    drive-objects, filecoin-pieces, recovery, state-archive.edn, updates, workforce-authority.seed …)
  - falsify-85 以来の 0 ベース測定系 (unused 3 vector / keys 786 静的) が参照していた
    ledger/annex 配下が消滅 — **0 ベース系列の測定はここで遮断** (21 時点観測 / 20 反続維持で終了、
    22 時点目は「測れなかった」。測れなかった検査を測って問題が無かった検査と同じ値に読まない)
  - annex objects ディレクトリ自体は superproject 配下の annex 対象からも見当たらない
    (find -name objects -path '*annex*' maxdepth 3 → 0 件)
- 附帯 (実装軸): 本体 (cloud-itonami-app) origin/main の advance は本反復で未測定
  (予算尽き、ネットワーク ls-remote 未実施 — 「不変」とは記載しない)。

## 判定

- 主張 (c)「-1 Gi 級減速定着」→ **REFUTED** (df 158160 → 55408 MB、-101.5 Gi)
- 緊急フラグ <100 Gi → **初発動** (54.1 Gi)。これは Tier 2 として kanban/human へ報告する
  測定事実であり、本 bot は緩和・救援・復旧を行わない (Tier 3 禁止)
- falsify-113 の structural-decline モニタは規約通り「構造的低下」判定済みだが、
  今回の低下は -100 Gi クラスで annex keys 測定系とともに消滅した可能性と相関候補
  (data/ledger 消滅が容量を開放したのではなく、op による data 再構成 + 大量外部消費が同時期に発生した可能性 —
  因果は測定不能、捏造しない)
- 測定系自体の遮断を新規事実として記録: 次反復は「annex/ledger 測定ベクトルの代替位置特定」から始める

## 範囲修正

- 「local annex keys 786 静的」は現在形として不成立 (測定対象が消滅)。ledger 行には
  「keys 786 系列 21 時点で遮断、本反復で再測定不能」を明記
- 緊急フラグの意味: 運用軸 score 3 の引上げ根拠にはならない (低下は測定で裏取れたが
  緑確認・修復は人間判断、閾値到達は「報告」義務のみ)

## 附帯

- 0 ベース観測系列: unused 系列は 21 時点で終了 (遮断)。avail 観測系列
  113.9 → 34.5 → 34.7 → 34.0 → 188.9 → 178.8 → 168.1 → 155.7 → 154.5 → **54.1 Gi**
- resident 状態遷移系列: 6 回目 (PID 12978 実測)

## 次の 1 アクション

annex/ledger 測定ベクトルの代替位置特定 (data/ 再構成後の新レイアウトで 786 keys 系列を
どこで取り直せるか、`git-annex` 実体の所在を特定) を最優先とし、そこが復帰するまで
avail 検知と resident 観測だけを続ける。
