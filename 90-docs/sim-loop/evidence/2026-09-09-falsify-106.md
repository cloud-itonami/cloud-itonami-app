# falsify-106 (2026-09-09, 運用/滞留 軸 — 0 ベースの 21 反復超連続維持 (22 時点観測)、avail 低下が 4 反復連続後に停止 (113→114 Gi, annex 121.58→122.16 GB 微増)、100 Gi 未満緊急フラグ未到達)

測定日: 2026-09-09 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app, detached b1b4b85) は読み取り専用参照のみ、
porcelain 0 行 = clean、一切 touch せず。

## 背景 / 測定対象

falsify-105 は 3 ベクトル正規 unused 0・local annex keys 786 不動の 21 時点観測 / 20 反復
連続維持を再実測し、avail の継続低下 (116→113 Gi) を記録した。その次の 1 アクションは
「同 vector + keys + avail を再実測し **20 反復超の連続持続** or 再蓄積再開を判別、avail
113 Gi の更なる継続低下 (100 Gi 未満で緊急フラグ) vs 減速したままの低位定着 (113 Gi 前後)
vs 回復 (148-163 Gi 帯、structural-decline-monitor 再評価) を観測」だった。

本反復 (falsify-106) はその 1 アクションを執行する。

**主張**: 本反復の再実測で (a) 3 ベクトルのいずれかに正規 unused が再出現する、または
(b) local annex keys が 786 から増加する (20 反復超への持続は成立せず残留再蓄積を開始)、
または (c) avail が falsify-101/103/104/105 の低位から **回復して 148-163 Gi 帯へ戻る**
(falsify-101 の -29 Gi ステップダウンが一過性であったことを示す)。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD detached 566928f6701df95d8cf0b62c5455c9d0baf46226、falsify-98..105 と同一、
porcelain 0 行 = clean)。附記: 本反復の `git ls-remote origin main` は
`fatal: 'origin' does not appear to be a git repository` で失敗 (origin/main 前進確認は
本反復は実施できず、origin/main 前進判定は前反復値 (b1b4b85f) の透過としてのみ記載。

### 1. main-refspec regular unused

`git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys** (出力は partial
transfer 残骸 14 chunk のみ、NUMBER 1..14 の MD5E-s... 14 行、falsify-98..105 と同一集合)。
`ok` / EXIT_RC=0。

### 2. 全 refs refspec (主張 (a) ベクトル)

`git annex unused --used-refspec "+refs/*"` → 正規 unused **0 keys** (partial 14 chunk のみ、
同一 14 key 集合)。`ok` / EXIT_RC=0。

### 3. local annex keys (主張 (b) ベクトル)

`git annex info` → **local annex keys: 786** (増加 0)、local annex size **11.68 GB** (不変)、
annexed files in working tree 7143 / 93.02 GB、combined 111.12 GB、
temporary object directory 186.98 MB。available local disk space **122.16 GB**
(falsify-105 の 121.58 GB 比 **+0.58 GB = 微増**)。

### 4. avail (主張 (c) ベクトル)

`df -m /` → avail **116596 MB ≒ 114 Gi** (falsify-105 の df 116081 MB ≒ 113 Gi 比
**約 +0.5 Gi**)。100 Gi 未満の緊急フラグ未到達。148-163 Gi 帯への回復は **発生せず**、
ただし falsify-103/104/105 の -5→-6→-3 Gi の継続低下は **本反復で停止し微増 (+0.5 Gi) に
転換** — 4 反復連続低下の後の低位 (113-114 Gi) 定着シグナルの初観測。

### 5. 滞留増加源

`ps aux | grep -E 'itonami-app-resident|export_and_sync' | grep -v grep` → 増加源
**PID 70643 のみ** (nbb itonami-app-resident.cljs、falsify-85..105 と同一 PID、
export_and_sync 不在)。本反復の測定ハーネス自身の行は grep -v grep で除外し自助マッチ混入なし。

### 6. 附帯 (実装軸)

origin/main 前進確認は本反復の ls-remote 失敗により実施不能 (上記附記の通り)。
本体 checkout porcelain 0 行 = clean。前反復 (falsify-105) 時点では b1b4b85f で 4 反復
連続 no-advance — 本反復の新規前進判定は保留。

## 判定

- **主張 (a) REFUTED**: 正規 unused 0 のまま (partial 14 chunk のみ、再出現なし)。
- **主張 (b) REFUTED**: keys 786 → 786、増加 0。→ falsify-105 指定の閾値「20 反復超の
  連続持続」を充足 (21 反復 > 20)、0 ベース恒久化傾向を更に精緻化 (**SURVIVED**、
  22 時点観測 / falsify-85→106)。
- **主張 (c) REFUTED**: avail の 148-163 Gi 帯回復は発生せず (114 Gi)。ただし継続低下は
  **停止し +0.5 Gi 微増に転換** — falsify-103/104/105 の「継続低下」記述を本反復で範囲修正
  (低下の停止を観測)。structural-decline-monitor は「低下停止・低位定着の持続性観察」へ
  観測モードを更新 (100 Gi 未満で緊急フラグは据え置き)。

score 3 のまま (0 ベース恒久化傾向の更なる精緻化、実緑測定でのみ上昇する規約、
avail 低下停止は risk-5 / structural-decline-monitor の観測モード更新として記録)。

次の 1 アクション: 同 vector + keys + avail を再実測し **21 反復超の連続持続** or 再蓄積
再開を判別、avail 114 Gi の **低下停止 (微増) が持続するか (113-114 Gi 帯の定着) vs 低下
再開 (100 Gi 未満で緊急フラグ) vs 回復 (148-163 Gi 帯) を観測**。加えて origin ls-remote
失敗の原因 (remote 設定消失 or 一時的ネットワーク) を `git remote -v` で特定し、
origin/main 前進確認を復旧する。
