# falsify-70 (2026-09-08, 運用 軸)

OPEN 赤-2 (expiry-alert) の falsify-69 範囲「修理案 A は唯一かつ十分でない（ipld deftype
IEquiv / cacao-delegate / datalog.index が前段に残る）」を反証試行。作業は wt-msloop /
bot/maturity-sim-loop で完結、本体 checkout 未 touch、kagi/vault 非接触 (require probe のみ)。

## 実測 (リダイレクト記録、捏造なし)

1. 本体: HEAD = origin/main = 679572b、porcelain clean、HEAD..origin/main = 0 (falsify-63..69 同一)。
2. expiry-alert: state=not running / runs=1 / last exit code=1 (falsify-58/69 と同値再確認)。
   nbb v1.4.208 / Node.js v26.0.0。
3. plist classpath (launchctl print 実測、15 エントリ) は案 A の 3 src (authority/
   org-nist-sha2/datom-source) 不在。
4. 現行ツリー、plist と同一 classpath で scripts/expiry-alert.cljs を nbb 実行 →
   **唯一 `Could not find namespace: authority.scope` (EXIT_RC=1) の単独終端**。
   Sep-7 ログの deftype IEquiv (ipld/core.cljc:36, ipld/link.cljc:11)・kotoba-rad
   cacao-delegate・datalog.index は**現行ツリーでは再現しない** (/tmp/f70_repro2.txt)。
5. 現行 io-ipld src/ipld/link.cljc は `(defrecord Link [cid] ILink ...)` (deftype なし)。
   git blame: `defrecord Link` = commit 309db3f (2026-08-11, "fix Link equality across
   nbb runtimes")。docstring: 「SCI/nbb cannot resolve built-in IEquiv/IHash on a
   deftype」= deftype IEquiv 失敗は 2026-08-11 より前の旧 io-ipld リビジョン由来で
   現行ツリーでは解決済み。nekko/cacao_delegate.cljc はディスク上実在
   `(ns nekko.cacao-delegate ...)`、kotoba-rad checkout は不在 (ls rc=1) = 旧ログの
   cacao-delegate は kotoba-rad → nekko 移行前の失敗。
6. 案 A 3 src を先行追加した classpath で require 連鎖を直接実行:
   (require 'cloud-itonami.ops-keys 'authority.scope 'ipld.core 'ipld.link
   'nekko.cacao-delegate 'datalog.index) → **:f70-load-ok / EXIT_RC=0**。
   3 src 全てディスク上存在 (dir OK 実測)、authority/scope.cljc / datalog/index.cljc も
   存在 (ls rc=0) (/tmp/f70_probeB.txt)。

## 判定
REFUTED (falsify-69 の前提範囲修正): 現行ツリーでは 15 エントリの plist classpath は
authority.scope 単独終端の失敗を再現し、案 A の 3 src 追加で全 load 連鎖が EXIT_RC=0
で解決する。falsify-69 が「前段に残る」とした ipld deftype / kotoba-rad cacao-delegate
は旧依存リビジョン (io-ipld 309db3f より前 / kotoba-rad 移行前) 由来で現行ツリーの再現
対象ではなく、案 A は現行ツリーに対し唯一かつ十分の範囲へ復帰。ただし「plist 未追記・
実 job 毎発火失敗・未着地・Tier 2」は不変 (案 A 着地 = ops-classpath.sh 修正 + plist
再生成 + launchctl relaunch、本体外・システム変更で Tier 1 不着地)。

運用軸 score は 3 のまま (root cause を 単独→連鎖→「連鎖は依存リビジョン硬化で縮退、
案 A が唯一十分へ復帰」と再範囲修正。job 未修理・未着地のまま記録継続)。

## 次の 1 アクション
OPEN 赤-2 を「現行ツリーの失敗は authority.scope 単独終端 / 案 A 追加で require 連鎖が
EXIT_RC=0」に再範囲修正し、Tier 2 report で案 A 着地ランブックを再提示 (着地後の rc=0
再確認が終点判定、falsify-11 継承)。

## 附帯
本体 checkout は反復中 porcelain clean (dirty 0)、本 bot は touch せず wt-msloop のみで
完結。修理対象は本体外 (network-awai plist classpath / expiry-alert 依存) のため Tier 1
不着地。7-軸表 16 行目孤立行 placeholder と falsify-46 mid-sentence cut は残存のまま
(operator 復旧待ち、append-only のため本 bot は編集しない)。identity.clj malformation
(falsify-63..68 の build-broken tip) は既知赤として本反復対象外。