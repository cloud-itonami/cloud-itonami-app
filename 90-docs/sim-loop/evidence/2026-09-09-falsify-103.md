# falsify-103 (2026-09-09, 運用/滞留 軸 — 0 ベースの 19 時点観測 / 18 反復連続維持で恒久化傾向を更に精緻化、avail の 122 Gi 再低下で「127 Gi 低位回復」仮説は REFUTED)

測定日: 2026-09-09 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (/private/tmp/mt-msloop103 / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app) は読み取り専用参照のみ、一切 touch せず。

## 背景 / 測定対象

falsify-102 (実装軸) は origin/main b1b4b85f (kotoba.lang.text 配線着地) で 2 build-break の生存と
kotoba.text 結線整合を実測し、「次反復は運用/滞留 軸 (falsify-101 の 17 反復超 / avail 127 Gi 監視) へ戻る」
とハンドオフした。falsify-101 (運用軸) は avail が **127 Gi** (falsify-100 の 156 Gi から -29 Gi) へ
再ドロップし、structural-decline-monitor を 2 時点待ち解除で即時再強化した。その次の 1 アクションは
「同 vector + keys + avail を再実測し **17 反復超の連続持続** or 再蓄積再開を判別、avail の **127 Gi 定着
vs 更なる減退 (100 Gi 未満で緊急フラグ) vs 回復**を観測し structural-decline-monitor を経過観測継続」。

本反復 (falsify-103) はその次の 1 アクションを執行し、falsify-101 の閾値「0 ベースの 17 反復超の連続持続」
および「avail 127 Gi の定着 vs 更なる減退 vs 回復」を falsify する。

**主張**: 本反復の再実測で (a) 3 ベクトルのいずれかに正規 unused が再出現する、または
(b) local annex keys が 786 から増加する (17 反復超への持続は成立せず残留再蓄積を開始する)、
または (c) avail が 148-163 Gi 帯へ回復する (falsify-101 の「127 Gi 低位」が一過性の低位振動である —
falsify-96..100 の帯内振動枠組みへ復帰)。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`)。

### 1. main-refspec regular unused

`git annex unused --used-refspec +refs/heads/main` → output の `MD5E|SHA` 行カウント **14** = partial
transfer 残骸 14 chunk のみ (NUMBER 1..14)、正規 unused key **0**。`ok` / EXIT_RC=0。

### 2. 全 refs 走査 (孤児ベクトル)

`git annex unused` (refspec なし) → 同様に正規 unused **0 keys**、partial 14 chunk のみ (`MD5E|SHA` 14 行)。
いかなる ref からも到達不能な孤児 key は存在しない。`ok` / EXIT_RC=0。

### 3. resident-refspec (帰属ベクトル)

`git annex unused --used-refspec +refs/heads/resident/dns-resolver` → 同様に正規 unused **0 keys**、
partial 14 chunk のみ (`MD5E|SHA` 14 行)。`ok` / EXIT_RC=0。

### 4. git annex info

```
local annex keys: 786        (falsify-103: 786 — 変化 0、19 時点観測 / 18 反復連続維持)
local annex size: 11.68 gigabytes   (不変)
available local disk space: 130.49 gigabytes (+100 megabytes reserved) (falsify-101: 136.02
                              GB — 約 -5.53 GB 再低下)
temporary object directory size: 186.98 megabytes (partial chunk 14、不変)
annexed files in working tree: 7143 / 93.02 gigabytes ([b2] 側、不変)
combined annex size of all repositories: 111.12 gigabytes (不変)
```

### 5. df avail (実測)

`df -h /` → avail **122 Gi / 12%** (falsify-101 の 127 Gi から **約 -5 Gi 再低下**)。annex available
local disk space は 130.49 GB (falsify-101 の 136.02 GB 比 約 -5.53 GB、df と同方向)。**falsify-101 の
127 Gi からの回復は認められず、更に -5 Gi 低下** — 主張 (c) の「148-163 Gi 帯へ回復」は本反復で成立せず
(主張 (c) REFUTED)。観測系列は falsify-100 (156) → 101 (127, -29) → 本反復 (**122**, -5) と、falsify-101
の -29 Gi ステップダウン後に更に継続低下。ただし本反復の低下幅 -5 Gi は falsify-101 の -29 Gi より縮小
しており、低下が緩やかながら継続中（structural-decline は続行、100 Gi 未満の緊急フラグには未到達）。

### 6. 増加源プロセス / 帰属

`ps aux | grep -E 'itonami-app-resident|export_and_sync'` → 増加源 `node nbb
~/.gftd/bin/itonami-app-resident.cljs` **PID 70643 稼働継続** (falsify-85..102 と同一 PID のまま、
連続稼働、export_and_sync 不在)。ps 出力の他ヒット (64051 grep 行 / 64049 bash 行) は測定ハーネス自身の
自助マッチで実増加源ではない (runbook falsify-99/100/101 と同様に除外)。local annex keys 786 / 11.68 GB
不変ゆえ **avail の継続低下 (-5 Gi / -5.53 GB) は local annex に帰属しない** — annex 外部の書き込み源に
起因 (falsify-94..101 と同所見)。TCC 遮断 (onedrive / ~/.gftd du 不能) のため具体源特定は引き続き本 bot では
不能 (operator の du 調査を異常系・優先度高で Tier 2 継続)。

### 7. origin/main (実装軸 附帯)

`git fetch origin` → origin/main は **b1b4b85f** (falsify-102 と同一 SHA、kotoba.lang.text 配線着地のまま)。
`rev-parse origin/main = b1b4b85f2103b72746bd5a35aff857092a370aab`。本体 checkout は detached HEAD
b1b4b85 / porcelain clean (dirty 0)。

## 判定

**SURVIVED (主張 a/b) / REFUTED (主張 c — avail 回復)**: falsify-101 から本反復への独立時点で
3 ベクトル (main / 全 refs / resident-refspec) の正規 unused は **全て 0 のまま**、local annex
keys は **786 → 786 (増加 0, falsify-85→103 の 19 時点観測 / 18 反復連続維持)**、既知 partial
14 chunk のみ — 主張 (a) の再出現も (b) の再蓄積も検出されず、falsify-101 指定の閾値「17 反復超の
連続持続」を充足 (18 反復 > 17) し 0 ベース恒久化傾向を**更に精緻化**した。

他方、**avail は df 実測 127 → 122 Gi (約 -5 Gi)、annex available 136.02 → 130.49 GB (約 -5.53 GB) と
falsify-101 の 127 Gi からの回復は認められず継続低下** — 主張 (c) の「148-163 Gi 帯へ回復 (一過性低位)」は
本反復で成立せず REFUTED。falsify-101 の -29 Gi ステップダウンは一過性でなく、継続低下中である
(ただし低下幅は -29 → -5 Gi と減速)。local annex keys 786 / 11.68 GB 不変ゆえ低下は annex 外部の書き込み源に
帰属するが、TCC 遮断により具体源は特定不能。100 Gi 未満の緊急フラグには未到達。

運用/滞留 軸 score は **3 のまま** (0 ベース恒久化傾向の更なる精緻化を記録する一方で、avail の継続低下は
risk-5 / structural-decline-monitor の経過観測継続として記録 — score は実緑測定でのみ上昇する規約、
低下ペナルティも本反復では付さず経過観測)。

## スコープ修正 / 次の 1 アクション

- risk-5 の記述を「滞留は 0 ベースへ回帰し **falsify-85→103 の 19 時点観測 / 18 反復連続維持**
  (unused 0・keys 786 不動・partial 14 chunk のみ) — **恒久化傾向の更なる精緻化継続**。
  ただし増加源 (resident ingest, PID 70643) 稼働継続ゆえ再蓄積の可能性は残る」と再アンカー。
- risk-5 の avail 監視シグナルを更新: df avail は **127 → 122 Gi と継続低下** (annex available 136.02 →
  130.49 GB 約 -5.53 GB も同方向)。falsify-101 の -29 Gi ステップダウン (156→127) は一過性でなく、
  **継続低下中 (-5 Gi で減速継続)**と確定 (回復仮説 REFUTED)。100 Gi 未満で緊急フラグ、150 Gi 定着
  (falsify-100 枠) へ回復したら structural-decline-monitor 解除の判定。consume は annex 外部
  (keys 786 / 11.68 GB 不変) に帰属、TCC 遮断で特定不能 (operator の du 調査を異常系 Tier 2 継続)。
- 次の 1 アクション: 次反復で同 vector + keys + avail を再実測し、0 ベース維持の**18 反復超の
  連続持続** or 再蓄積再開を判別 (18 反復超維持なら恒久化傾向を更に精緻化、再開なら falsify-33..53 語調へ
  復帰)。あわせて **avail 122 Gi の更なる継続低下 (100 Gi 未満で緊急フラグ) vs 減速したままの低位定着
  (122 Gi 前後) vs 回復 (148-163 Gi 帯、structural-decline-monitor 再評価)** を観測。TCC 遮断下の帰属は
  operator の du 調査を異常系・優先度高の Tier 2 として要請継続。
- 附帯 (実装軸 次反復シグナル): origin/main は **b1b4b85f のまま** (falsify-102 と同一、新規前進なし)。
  次の実装軸反復では b1b4b85f の parity 測定面 (src 214 / test 237 anchor、falsify-78/79 の 3 値結合水準)
  と 2 build-break (identity.clj / kotoba-net) の生存を再確認する。
- 附帯 Tier 2: falsify-85..101 と不変 — resident プロセス再起動 (94682→70643) の契機と滞留回収の帰属、
  および falsify-101 以降の avail 継続低下 (127→122 Gi、統合 -34 Gi) の具体源特定 (TCC 遮断) は
  kanban/human へ異常系として透過提出継続。

## 附帯 (記録上の整合)

- 本体 checkout (/private/tmp/mt-msloop103 の親 main)、`git status --porcelain` 0 行 = porcelain clean
  (dirty 0)。
- origin/main は b1b4b85f のまま (falsify-102 と同一、実装軸 新規シグナルなし)。
- 本反復は運用/滞留 軸の falsify-102 ハンドオフ (falsify-101 の next action へ戻る) を測定対象に選択。
- 既知の残骸は残存のまま (append-only 規約で本 bot は編集しない):
  7-軸表 16 行目孤立行 `| 反証候補falsify30PLACEHOLDER` (falsify-33 以降)、falsify-46 mid-sentence cut、
  falsify-91 末尾残骸 (行 1237-1238 `MD` / `echo ...`)。
- identity.clj malformation / kotoba-net 未宣言 (falsify-63..102) は既知赤継続、本反復の対象外。