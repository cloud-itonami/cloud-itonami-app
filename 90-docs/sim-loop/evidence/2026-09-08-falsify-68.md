# falsify-68 (2026-09-08, 実装/テスト 軸): 現 tip 679572b の build-broken は falsify-66 後も未着地 — 一次 compile 実測 (EXIT_RC=1) で独立再確認 (REFUTED)

測定日: 2026-09-08 (cron 反復)/ 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(cloud-itonami-app) は読み取り専用参照のみ (git rev-parse / git status / git log)。
compile は detached worktree (/private/tmp/mt-msloop68 @ 679572b) で実行し、終了後
worktree remove + prune で後始末した。

## 背景 / 反証対象の主張
falsify-63/64/65/66 は現 tip 679572b (java.net.http→kotoba migration) が identity.clj
の ns 形式破損 (migration が `(:import` opener と最初の 2 行を削除し、残り 5 本の
[java.*] ベクタを裸に残した) で全 suite が compile 不能と実測してきた。falsify-66 は
HEAD 不変 (同一 SHA) から遷移的に compile 失敗を帰結したが、compile 自体は再実行しなかった
(一次 compile 実測のギャップ)。

本反復の反証対象: 「falsify-66 以降に修理が着地し、現 tip の identity.clj ns は有効な
(:import ...) clause を持ち、スイートは再びコンパイルできる」。独立の時点かつ一次 compile
実測で (a) origin 修理 commit の無いこと (b) git object の malformation (c) 実 compile の
決定論的失敗を独立確認する。

## 実測 (実コマンド出力をリダイレクト記録、捏造なし)

### 1. 本体 checkout 状態 (読み取り専用参照、orgs 付き正体)
```
git rev-parse HEAD        -> 679572b289cbb3b96cb3ef6c25188bbb95e28b57
git rev-parse origin/HEAD -> 679572b (= HEAD)
git rev-parse origin/main -> 679572b (= HEAD)
git rev-list --count 679572b..origin/HEAD  -> 0
git rev-list --count 679572b..origin/main  -> 0
git status --porcelain | wc -l             -> 0
git log --oneline -3 origin/main:
  679572b All java.net.http transports delegate to kotoba.net.jvm-host (35 files)
  1a3bfd9 Extract bot_slo recovery-points ladder into a .kotoba core
```
HEAD = origin/HEAD = origin/main = 679572b、origin 修理 commit 無し (0 / 0)、
本体 checkout porcelain clean (dirty 0)。

### 2. git object の ns malformation (git show HEAD:.../identity.clj)
```
grep -c '(:import'        -> 0   (:import opener 無し)
grep -n '^ *\[java\.' | wc -l -> 5   (裸の java ベクタ)
  29: [java.nio.charset StandardCharsets]
  30: [java.security MessageDigest SecureRandom]
  31: [java.time Duration Instant]
  32: [java.util Base64 UUID]
  33: [java.util.concurrent TimeUnit]
```
falsify-63/64/65/66 の malformation と完全一致 (:import 0 / 裸 [java.* 5 本、29-33行)。

### 3. 一次 compile (detached /private/tmp/mt-msloop68 @ 679572b)
`clojure -M:test -e "(require 'cloud.itonami.app.identity)(println :f68-ok)"`:
```
Preparing worktree (detached HEAD 679572b)
Syntax error macroexpanding clojure.core/ns at (cloud/itonami/app/identity.clj:1:1).
java.nio.charset - failed: #{:refer-clojure} at: [:ns-clauses :refer-clojure :clause]
java.nio.charset - failed: #{:import} at: [:ns-clauses :import :clause]
java.nio.charset - failed: #{:require} / #{:use} / #{:refer} / #{:load} / #{:gen-class}
Full report at: /var/folders/.../clojure-...edn
compile_exit=1
```
**一次 compile 実測 EXIT_RC=1**。falsify-63 と同一 error を独立・直接再現
(falsify-66 の同一 SHA 遷移を一次実測で裏付け)。

### 4. detached worktree 後始末
`git worktree remove --force` rc=0 → `git worktree prune` → list に残存せず。

## 判定
「修理着地で再びコンパイルできる」→ **REFUTED**: origin/HEAD=origin/main=HEAD=679572b
(0 commits)、git object で (:import 0 / 裸 5 本) malformation、**一次 compile EXIT_RC=1**
(identity.clj:1:1)。現 tip は assertion を 1 つも実行できない。build-broken 生存を
真逆主張 (修理着地) の反証で独立の時点かつ一次実測で再確認。score はテスト・実装とも
3 のまま (重大な新規赤の継続であり質的反証。「決定論的赤 0」「suite 実行可」は不成立)。

## スコープ修正 / 次の 1 アクション
- falsify-63/64/65/66 の「現 tip 679572b はビルド不能」は本反復の一次 compile 実測で
  再生存。修理案 (identity.clj で (:import 復元し 5 ベクタを wrap) は Tier 2 kanban/human のまま未着地。着地には本体の identity.clj 編集 + テスト緑確認が必要 — 本 bot は本体を touch しないため Tier 2。修理着地は (1) origin/main への HEAD 前進 (2) identity.clj の (:import 復元 (git object) (3) detached worktree での一次 compile EXIT_RC=0 の 3 点で判定する。併せて falsify-62 の bundle/graph 赤 (published-lock 再発行) の再確認が次の測定課題。附帯 (append-only): 7-軸表 16 行目 placeholder と falsify-46 mid-sentence cut は残存のまま (operator 復旧待ち、本 bot は編集しない)。
