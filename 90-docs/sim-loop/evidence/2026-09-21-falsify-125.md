# falsify-125 (2026-09-21, 運用/滞留 軸)

## 主張 (m)

「falsify-124 比で avail が更に低下した (-7.0 Gi) 原因は dns-resolver annex の滞留再燃 (keys/du 増加)」

## 判定: REFUTED (annex 静止系列が大低下下で 9 反続生存)

同一 path (`~/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`)
で 2 法測定 (falsify-123 修正法: `git annex unused` + `ls .git/annex/tmp`、glob 単独不使用):

- `find .git/annex/objects -type f | wc -l` = **786** (不変, **9 反復連続静止**)。
- `du -sk .git/annex/objects` = **11403092 KB** — falsify-117..124 全反復と KB 完全同値
  (**9 反復連続**)。
- dns-resolver HEAD = **c8090649** (不変, 9 反復連続)。
- `ls -la .git/annex/tmp`: **14 ファイル実在、total 365232** (512B blocks)、
  mtime すべて **Sep 6 19:12-19:27** (不変) — falsify-117..124 と完全同値。
  partial 14 件系列は falsify-117..125 を通じて**連続実在** (falsify-121/122 の
  glob 誤測 0 は falsify-123 で REFUTED 済み、本反復で再消滅なし)。

annex 側は一切増えていないため、-7.0 Gi の説明に dns-resolver 滞留を帰属させる
主張は REFUTED。低下の増加源は**未測定** (附帯の通り du 帰属試行が 2 連続で
タイムアウトしたため、いかなる帰属数値も主張しない)。

## 主張 (n)

「partial 14 件系列は 5 反復連続 (falsify-117..125) で同一内容 (total 365232 blocks /
各 mtime Sep 6) として持続する」

## 判定: SURVIVED

falsify-124 と同形式の再実測 (ls total 365232、mtime Sep 6 19:12-19:27、14 件)
で同一内容を 8 反復連続確認。Tier 2 dropunused 提案対象は不変 (削除主体未測定)。

## 附帯測定 (同反復実測)

- df /System/Volumes/Data: avail **8545392 KB ≒ 8.1 GiB / 100%**。
  falsify-124 (15738836 KB ≒ 15.0 GiB / 99%) 比 **-7.0 Gi**、緊急フラグ (<100 Gi) は
  **4 反復連続発火**。avail 系列は非単調継続:
  -101.5 → -6.7 → +32.8 → -3.2 → +47.2 → -18.6 → -30.9 → -7.2 → -47.6 → **-7.0 Gi**。
  観測系列 (GiB): 122.3 → 103.75 → 69.8 → 62.6 → 15.0 → **8.1**。
- 帰属試行: `du -x -d1 -m /Users/junkawasaki/github` を実行したが **180 秒でタイムアウト**
  (数値取得不可、本反復の du 出力は未生成)。falsify-124 の 420 秒 timeout に続き、
  帰属は未確立 — 単一帰属主張なし (falsify-118/119 の「du depth-1 で帰属候補を絞る」
  アプローチが 2 連続で収束しないことを記録)。
- resident: PID **1083** 継続 (Fri Sep 18 15:52:10 2026 起動、nbb 経由
  itonami-app-resident.cljk、新規遷移なし、**10 反復継続**)。
  export_and_sync 不在 (grep 実測、測定ハーネス自身のコマンド行ヒットは除外)。
  git-annex プロセス不在 (ps 実測 rc=1)。
- app origin/main は本反復未測定 (不変とは主張しない)。

## score

運用 3 のまま (低下・滞留とも測定で裏取れたが修復判断は人間 = Tier 2)。

## 次の 1 アクション

(1) du 帰属測定を timeout 内に収束させるためスコープ分割 (github 直下の特定サブ
ディレクトリのみ、または depth 0 のみ per-subdir)。
(2) partial 14 件の同一内容持続を 9 反復目で再確認 (Tier 2 dropunused 提案の継続)。
(3) keys 786 / du 11403092 KB / HEAD c8090649 の 10 反復連続静止を判別。
(4) avail 8.1 GiB の低位帯からの回復 vs 更なる低下 (0 近接でディスク機能停止リスク)
を観測 — 回復なし且つ低下継続で 2 反復連続なら緊急フラグを「<100 Gi 発火」から
「<10 Gi 深部」へ再アンカー。
