# falsify-119 (2026-09-18, 運用/滞留 軸)

## 主張 (反証対象)

主張 (f): 「avail の非単調振動 (-101.5 → -6.7 → +32.8 Gi → 本反復) と構造的低位化は、
falsify-18 で帰属確定した m365-archive (onedrive) 系大容量ディレクトリの変化に由来する」
— falsify-118 の「次の 1 アクション (annex 外の大容量ディレクトリの du 変化を附帯測定)」
の初回実行として、m365-archive の現所在地と容量を測定する。

## 実測

- 時刻: 2026-09-18 12:42:39 JST (`date` 実測)
- `df -m /System/Volumes/Data`: 1M-blocks 948584, Used 816887, **Available 78088, Capacity 92%**
  - falsify-118 の 81366 MB ≒ 79.5 Gi → **78088 MB ≒ 76.3 Gi、反復間 -3.2 Gi** (低位化継続)
  - **緊急フラグ (<100 Gi) は 4 反続で発動中** (76.3 Gi < 100 Gi)
- **dns-resolver annex (falsify-117/118 と同一コマンドの再実測)**:
  - `find .git/annex/objects -type f | wc -l` → **786** (falsify-117/118 と同値、増減 0)
  - `du -sm .git/annex` → **11320 MB** (falsify-118 の 11320 MB と同値、増減 0)
  - `du -sm .git/annex/tmp` → **179 MB** (falsify-118 と同値)
  - `git annex unused` (rc=0) → **unused object 0 件**、partially-transferred 一時ファイル
    **14 件** (MD5E-*.edn、falsify-117/118 と同数) — **annex 系列は 3 反続で完全静止**
- **帰属検証 (主張 (f))**: m365-archive の所在地を実測:
  - `ls -d /Users/junkawasaki/m365-archive` → **不存在**
  - `ls -d /Users/junkawasaki/Documents/m365-archive` → **不存在**
  - `du -sm /Users/junkawasaki/*` (depth-1, top15) → top は **github 397651 MB (~388 Gi)**、
    Library 72335 / dsv41-shard11 25858 / Pictures 24205 / models 14941。m365-archive は
    depth-1 top15 に**登場しない**
  - → falsify-18 の帰属先「m365-archive/onedrive 133G」は home depth-1 に現在形で存在しない
    (移設・改名・削除のいずれか、区別は本反復では未測定)。
    **主張 (f)「m365-archive の変化に由来する」は REFUTED** — 帰属先自体が測定不能地点に
    移動した。归属候補として home depth-1 の github (~388 Gi) が最大だが、その内部帰属は
    未測定 (捏造ゼロのため主張しない)。
- 附帯:
  - resident: PID **12978** 稼働継続実測 (ps aux、falsify-116 以降同一 PID、新規遷移なし)
  - dns-resolver HEAD = **c8090649** (不変、rev-parse 実測)
  - app origin/main は本反復では未測定 (予算制約) — 不変とは主張しない

## 判定

- 主張 (f)「avail 振動は m365-archive に由来」→ **REFUTED**
  (m365-archive が home depth-1 の両候補地点に不存在、top15 にも不在)
- 精緻化: dns-resolver annex 系列は 3 反続で完全静止 (786 / 11320 MB / unused 0)。
  avail は -3.2 Gi で低位化継続、緊急フラグ <100 Gi は 4 反続。帰属は annex でも
  m365-archive でも説明できず、単一帰属は確立しない。残る最大の候補は github
  (~388 Gi) 内部だが未測定。
- 次の 1 アクション: 同一測定 (df / find 786 / unused / du tmp) を再実測し緊急フラグ
  5 反続の有無を確認。帰属を絞るため `du -sm /Users/junkawasaki/github/*` (depth-1)
  の上位を附帯測定に加え、github 内部の変化点を探索する。
