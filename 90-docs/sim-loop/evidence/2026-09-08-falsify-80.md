# falsify-80 (2026-09-08, 実装 軸 — HEAD 2606d5e→2588c51 前進 (PR #289..#291) の funding/yield merges が机上 .kotoba parity 測定面 (bot_bounds/slo 4 core) と build-break に影響しないことを一次 compile + git 静的検証で確認)

測定日: 2026-09-08 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout は
読み取り専用参照のみ (git show / status)、一切 touch せず。

## 背景 / 反証対象の主張

falsify-79 (head d5d6e60) は実装軸の .kotoba 抽出評価を「parity 結合水準 (d)」3 値
(live #' 結合 / test-local re-spec / host 結合なし) に精緻化し、score 3 のまま記録した。
falsify-63〜79 は同一の 2 build-break を継続記録してきた — (B) identity.clj の (:import
復元 0 / 裸 [java.* 5 ベクタ (29-33 行)、(A) deps.edn の kotoba-net 未宣言 (jvm-host
transitively pull)。HEAD は反復毎に前進しているが、直前の falsify-79 から PR #289
(mobile-capital-ux) に続き PR #290 (codex/funding-filter)・PR #291 (codex/
yield-funded-rounds) が merge され origin/main が更に前進した。

反証対象の主張: 「PR #289..#291 の funding/yield merge 前進 (d5d6e60→2606→65d3→2588)
により、(i) falsify-79 が実測した bot_bounds / bot_slo 4 core の parity 結合水準が変化
した、または (ii) falsify-63..79 の 2 build-break (identity.clj malformation / kotoba-net
未宣言) が修理着地した」。いずれも計測面 (parity テスト・build 対象) が変わらない限り
持たない暗黙前提なので、両者を独立に falsify する。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

### 1. HEAD 解決と前進幅

wt-msloop の HEAD は ledger branch (bot/maturity-sim-loop = d5bb661) 自身のため、
本体計数は falsify-78 判明どおり origin/main で行う。`git rev-parse origin/main =
origin/HEAD = 2588c516` (`git show` 1 行目、falsify-79 の d5d6e60 から PR #291 まで 3
merge 前進。`git log --oneline origin/main -8`: 2588c51 {#291 yield-funded-rounds} ←
65d3e98 {#290 funding-filter} ← d5d6e60 {#289 mobile-capital-ux})。本体 checkout の
asserted HEAD は **65d3e98** (PR #290、`git worktree list` 実測 detached HEAD) で、
origin/main 2588c51 (#291) より 1 merge 遅れており、計数は 2588c51 にアンカーした。

### 2. 計測面の変化範囲 (git diff d5d6e60..2588c51 --name-only)

```
shared/bots-ui/src/cloud/itonami/capital_client.cljs
shared/bots-ui/src/cloud/itonami/capital_ui.cljc
shared/bots-ui/src/cloud/itonami/public_bots.cljc
```

変化は **3 ファイル全て bots-ui (cljs/cljc) のみ**。実装軸の .kotoba parity 測定面
(bot_bounds.cljc / bot_slo.clj / 4 抽出 .kotoba + parity test) はこの前進で byte 無変化
(git diff に該当パスなし) — falsify-79 の parity 結合水準 3 値 (latency/recovery =
live #' / bounds = test-local re-spec / parts = host 結合なし) は現 HEAD でも生存
(SURVIVED i. 側)。file-count は falsify-79 実測 src 243 / test 246 と同値で re-anchor
(git ls-tree origin/main 243 / 246 行)。

### 3. build-break 生存の独立検証 (i. 側に続き)

`git ls-tree -r --name-only origin/main -- src|test | wc` 実測 **src 243 / test 246**
(falsify-79 と再アンカー一致)。`git show origin/main:src/cloud/itonami/app/identity.clj`
で `(:import` opener **0** / 裸 `[java.*` ベクタ **5** (行 29-33: `[java.nio.charset
StandardCharsets]` 等、`:require [...oauth])` 直後に wrap されず残存) — falsify-63..79
と完全一致。`git show origin/main:deps.edn` で `kotoba-net` grep **0** / `jvm-host` **0**
(falsify-75/76 の undeclared dependency 残存)。`git show origin/main:.../http_client.clj`
は `(:require [kotoba.net.jvm-host :as jvm-host])` (line 16) + `(jvm-host/http-transport
...)` (line 22) を参照却、提供元 kotoba-net 未宣言のまま。

### 4. 一次 compile (build-break (ii) 側の独立再現)

本体 checkout から独立した detached worktree `/private/tmp/mt-msloop80` (head 2588c51、
本体未 touch、worktree add rc=0) で `clojure -M:test -e "(require
'cloud.itonami.app.identity)(println :f80-ok)"` → **`Syntax error macroexpanding
clojure.core/ns at (cloud/itonami/app/identity.clj:1:1)` / `java.nio.charset - failed:
#{:refer-clojure}` (ns-refer-clojure / ns-require / ns-import / ns-use / ns-refer / ns-load
/ ns-gen-class の 7 clause 全て) EXIT_RC=1** — falsify-63/64/68/77 と同一エラーを現 HEAD
で独立・直接再現。

## 判定

**REFUTED (build-break の (ii) 側) — 但し SURVIVED (parity 測定面 i. 側)**:

- **i. side (parity 結合水準)**: FUNDING/yield merges は bots-ui 3 ファイルのみで
  .kotoba parity 測定面に touch せず、falsify-79 の「latency/recovery = live #' 結合の
  checked replica / parts = host 結合なき guest-only formula pin / bounds = test-local
  re-spec とのみ比較」の 3 段結合水準は現 HEAD 2588c51 でも生存 (SURVIVED)。
- **ii. side (build-break)**: 「前進により build-break が修理着地」は REFUTED —
  identity.clj の `(:import` 0 / 裸 5 ベクタが現 HEAD でも残存し、一次 compile が
  EXIT_RC=1 (独立直接再現)、kotoba-net 未宣言も残存 (http_client.clj が jvm-host を
  requireし続ける)。falsify-68 の修理着地判定 3 条件 (HEAD 前進 / (:import 復元 /
  一次 compile EXIT_RC=0) は 1 反復目のみで 2/3 未達、build-break 生存継続。

実装軸 score は **3 のまま** (funding merges は測定面非接触で score 変動なし、build-break
継続の質的反証は既知赤の再確認。green 化測定は両 build-break 修理後のため不可)。

## スコープ修正 / 次の 1 アクション

- 実装軸の file-count を **src 243 / test 246 (head 2588c51、falsify-80 実測)** へ
  re-anchor (origin/main へ再アンカー、falsify-78 の HEAD 解決規約どおり)。
- 修理案 Tier 2 (kanban/human、本体 checkout 編集を要し Tier 1 不着地、falsify-68/75/76/
  77/78/79 と不変): (B) identity.clj の 5 裸ベクタを `(:import ...)` で wrap、(A) deps.edn
  への kotoba-net 宣言 → updater_test 単体緑 (falsify-76 実測済み)。両着地後のフル suite
  green を最終終点に、falsify-62 の bundle/graph 赤 (published-lock 再発行) もその後再確認。
- 次反復候補: ORIGIN/main が #291 に前進したことで README/ADR に funding 契約の混入が
  ないか spec 軸を突合するか、falsify-79 の Tier 2 report (bot_bounds parity 張り替え
  案) を kanban/human へ正式提出。

## 附帯

本体 checkout は反復中 porcelain clean (dirty 0、`git status --porcelain` 0 行実測)、
dev /health 127.0.0.1:1338 -> **200** 実測。本 bot は touch せず wt-msloop /
bot/maturity-sim-loop で完結。実行 worktree `mt-msloop80` は remove --force / prune 済み
で `git worktree list` に残存せず (falsify-62 の annex read-only 残骸は本 head では発生
せず即時削除)。7-軸表 16 行目孤立行 placeholder / falsify-46 mid-sentence cut は残存の
まま (operator 復旧待ち、append-only で本 bot は編集しない)。identity.clj malformation
/ kotoba-net 未宣言は falsify-63..79 と同一で継続。本体 checkout が origin/main (#291)
でなく 65d3e98 (#290) に detach されている状態も記録 (worktree list 実測、コード面の
主要差分は bots-ui のみのため計数差なし)。
(evidence/2026-09-08-falsify-80.md)