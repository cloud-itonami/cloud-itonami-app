# falsify-64 (2026-09-08, テスト/実装 軸): 現 tip 679572b の identity.clj ns 破損は未着地 — falsify-63 の「ビルド不能」が現 HEAD で継続 (REFUTED、前回の発見の生存をコード実測で再確認)

測定日: 2026-09-08 00:56 JST
測定者: itonami-maint (cron 反復)
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app) は
読み取り専用参照のみ (git rev-parse / git show / git worktree add --detach)。テスト実行は
本体から独立した detached worktree `/private/tmp/mt-msloop64` (head 679572b) で実施し、
本体 checkout には一切触れない。終了後 worktree は削除 (chmod -R u+wx + worktree remove
--force + prune、worktree list から消滅確認)。本体 checkout は反復中 porcelain clean
(dirty 0) を維持。

## 背景 / 反証対象の主張

falsify-63 (2026-09-08 00:02) は現 tip 679572b (All java.net.http transports delegate to
kotoba.net.jvm-host, 35 files migration) が identity.clj の ns 形式破損で全スイート compile
不能であることを実測し、level high の新規 OPEN 赤候補 (Tier 2) として記録した。修理案は
「identity.clj の ns で `[java.nio.charset StandardCharsets]` の直前に `(:import` を復元し
5 ベクタを wrap」(着地は本体 checkout 編集 + テスト緑確認で kanban/human 判断、Tier 2)。

本反復の反証対象は、その修理案の暗黙前提「**現 tip のビルド不能状態は (時間経過で誰かが
修理を着地させ) 解決されている**」— すなわち「**現在のヘッドで identity.clj の ns は
有効な `(:import ...)` clause を持ち、スイートは再びコンパイルできる**」。

## 実測 (すべて実コマンド出力をそのまま記録、捏造なし)

### 1. HEAD 状態 (着地が起きたかを判定するための前提)

- `git -C <main> rev-parse HEAD` → **679572b289cbb3b96cb3ef6c25188bbb95e28b57**
- `git -C <main> rev-parse origin/HEAD` → **679572b289cbb3b96cb3ef6c25188bbb95e28b57** (= HEAD)
- `git log --oneline 679572b..origin/HEAD` → **0 commits** (HEAD は falsify-63 と同一、
  修理を着地させる commit は存在しない)
- 本体 checkout porcelain clean (dirty 0) 実測。

### 2. ソースレベルの構造検証 (git object = 決定論的)

`git show HEAD:src/cloud/itonami/app/identity.clj` を実測:

- `(:import` opener の個数: **0**
- 裸の `[java.*` ベクタ (ns 直下 clause として残るもの) の個数: **5**
  (lines 30-34: `[java.nio.charset StandardCharsets]` / `[java.security MessageDigest
   SecureRandom]` / `[java.time Duration Instant]` / `[java.util Base64 UUID]` /
   `[java.util.concurrent TimeUnit])`)、直前 line 29 は `[oauth.model :as oauth])` で
   `:require` clause が閉じた直後、`(:import` が一切無い状態で裸ベクタが続いている

→ falsify-63 が実測した malformation と **完全一致**。ns の form は
`(:require [...oauth])` の後に `(:import` 無しで裸ベクタ 5 本が並び、`:ns-clauses` spec
に適合しない構造が git object レベルで残存。

### 3. コンパイル直接再現 (detached worktree @ 679572b)

`git worktree add --detach /private/tmp/mt-msloop64 679572b` → rc=0
("Preparing worktree (detached HEAD 679572b) / HEAD is now at 679572b ...")

`clojure -M:test -e "(require 'cloud.itonami.app.identity)(println :identity-ok)"`:

```
Syntax error macroexpanding clojure.core/ns at (cloud/itonami/app/identity.clj:1:1).
java.nio.charset - failed: #{:refer-clojure} at: [:ns-clauses :refer-clojure :clause] ...
java.nio.charset - failed: #{:require} at: [:ns-clauses :require :clause] ...
java.nio.charset - failed: #{:import} at: [:ns-clauses :import :clause] ...
java.nio.charset - failed: #{:use} ...
Full report at: /var/folders/h_/.../clojure-....edn
EXIT_RC=1
```

→ falsify-63 と**同一エラー** (`Syntax error macroexpanding clojure.core/ns at
(cloud/itonami/app/identity.clj:1:1)` / `java.nio.charset - failed: #{:import ...}`)。
EXIT_RC=1。build break は現 HEAD で確実に再現する。

### 4. 副次確認: 修理はどの branch にも未着地

- `origin/jvm-host-transport` (00622eb, migration 元流) も identity.clj の裸ベクタ **5**
- `origin/agent/fix-open-red-5-three-bound` (9e5b24a, migration 前 main) は裸ベクタ **6**
  ※ これは `(:import [java.net URI URLEncoder] ...)` に wrap されたままの正当なベクタ
  6 本 (migration 前の完全形) であり、`(:import` opener は存在する
- 現 tip / migration 流では `(:import` = 0 / 裸ベクタ = 5

## 判定

- **「現 tip のビルド不能状態は解決されている (identity.clj ns は有効な `(:import)` を持ち
  スイートはコンパイルできる)」→ REFUTED**: HEAD は falsify-63 と全く同一の 679572b、
  `679572b..origin/HEAD` は 0 commits、identity.clj の `(:import` は 0 / 裸ベクタ 5 本で
  malformation は git object レベルで残存、detached worktree @ 679572b で
  `clojure -M:test -e "(require 'cloud.itonami.app.identity)..."` が
  `Syntax error macroexpanding clojure.core/ns at (cloud/itonami/app/identity.clj:1:1)` /
  `java.nio.charset - failed: #{:import}` EXIT_RC=1 で完全再現。修理案は未着地。
- これは falsify-63 の発見 (build-broken current tip) の**生存**を、真逆の主張 (修理着地済み)
  を反証する形で再確認したもの。falsify-63 と独立に、時間経過後の再実測として記録価値あり。
- **score はテスト・実装とも 3 のまま** (重大な新規赤の継続であり質的反証。file-count
  src 240 / test 243 (tip 679572b) 自体は正しく、修理未着地で「決定論的赤 0」「suite 実行可」
  は現 tip で依然不成立)。

## スコープ修正 (maturity.md テスト/実装行 + OPEN 赤候補の更新)

- falsify-63 の「現 tip 679572b はビルド不能」は**本反復の再実測で生存** (HEAD 不変 / 構造
  残存 / compile 失敗再現)。修理案 (identity.clj ns で `[java.nio.charset StandardCharsets]`
  の直前に `(:import` を復元して 5 ベクタを wrap、`[java.net URI URLEncoder]` /
  `[java.net.http ...]` は migration 目的に沿って生かさない) は Tier 2 kanban/human 判断
  のまま未着地。着地には本体 checkout の identity.clj 編集 + テスト緑確認が必要 — 本 bot は
  本体を touch しないため kanban/human 判断 (Tier 2)。修理後は falsify-62 の bundle/graph 赤
  (bundle published-lock 再発行) の再確認と合わせて決定論的実行の再観測が必要。
- file-count は tip 679572b の src 240 / test 243 (falsify-62 実測) のまま不変
  (HEAD 前進なし)。

## 次の 1 アクション

- 実装/テスト軸の最優先 Tier 2 report (falsify-63/64 で確立): 679572b migration の
  identity.clj ns 形式破損の修理着地 (本体に `(:import` 復元、テスト緑確認)。これにより全
  スイートが再びコンパイル可能になり、falsify-62 の bundle/graph 赤 (bundle published-lock
  再発行) の解消確認が次の測定になる。附帯: 本体 checkout は反復中 porcelain clean
  (dirty 0)、本 bot は touch せず wt-msloop のみで完結。実行 worktree
  /private/tmp/mt-msloop64 は削除・worktree list から消滅確認 (falsify-62 の annex
  read-only 残骸は本 head では発生せず remove --force で即時削除)。7-軸表 16 行目
  孤立行 `| 反証候補falsify30PLACEHOLDER` と falsify-46 mid-sentence truncate
  は残存のまま (operator 復旧待ち、append-only で本 bot は編集しない)。