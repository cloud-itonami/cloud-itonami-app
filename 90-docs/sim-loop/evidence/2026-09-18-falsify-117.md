# falsify-117 (2026-09-18, 運用/滞留 軸)

## 主張 (反証対象)

主張 (d): 「786 keys 系列は app 側 annex の消滅とともに消滅した (代替位置は存在しない)」—
falsify-116 の次の 1 アクション (代替位置特定) を正面から試みる。

## 実測

- 時刻: 2026-09-18 00:47:24 JST (`date` 実測)
- `df -m /System/Volumes/Data`: 1M-blocks 948584, Used 843361, **Available 48541, Capacity 95%**
  - falsify-116 の 55408 MB ≒ 54.1 Gi → **48541 MB ≒ 47.4 Gi、反復間 -6.7 Gi の追加低下**
  - **緊急フラグ (<100 Gi) は 2 反続発動中** (47.4 Gi)
- **代替位置特定 (主張 (d) の検証)**: superproject ツリー配下の annex ディレクトリを
  `find ... -maxdepth 5 -type d -name annex` (rc=0) で列挙 → 19 repo + superproject 自身
  (`/Users/junkawasaki/github/com-junkawasaki/.git/annex`)。うち
  **`orgs/cloud-itonami/cloud-itonami-dns-resolver/.git/annex/objects` の key 数が
  `find -type f | wc -l` 実測でちょうど 786** — falsify-116 までの系列が追っていた
  local annex keys 786 と同数。app checkout (`cloud-itonami-app/.git/annex`) は本日も
  `du` rc=1 (No such file) で不変。
  → **主張 (d)「786 keys 系列は消滅した」は REFUTED**。系列は消えておらず、
  app annex ではなく **dns-resolver annex** に在った (falsify-113 から dns-resolver HEAD
  c8090649 を付帯測定していた経路と整合)。
- 移転先の新規ベースライン実測:
  - `du -sm dns-resolver/.git/annex/objects` → **11136 MB ≒ 10.9 GiB**
  - `git -C dns-resolver annex unused` (rc=0) → **unused object 0 件**、
    partially-transferred 一時ファイル **14 件** (MD5E-*.edn、各 ~12-45 MB、合計 ~135 MB 相当)
  - 新系列はここから 0 ベース再始動 (旧 app 側 unused 3 vector 系列とは直接接続しない —
    異なる repo・異なる対象のため、値を連続と読まない)
- 附帯: app origin/main = **fbdb91f** (実測。falsify-116 では未測定と明記済み —
  falsify-115 の 33037469 から advance しているが当該系列は附帯のため深入りしない)。
  dns-resolver HEAD = **c8090649** (不変)。
- resident: PID **12978** 稼働継続実測 (node/nbb itonami-app-resident.cljk、falsify-116 の
  6 回目遷移後の同一 PID。新規遷移なし)。

## 判定

- 主張 (d)「786 keys 系列は消滅」→ **REFUTED** (dns-resolver annex で 786 件を実測、
  場所の特定で復旧。falsify-116 の「測定系自体の遮断」は部分的解除)
- falsify-116 の相関候補「data/ledger 消滅が容量を開放した」への新規証拠: 開放どころか
  avail はさらに -6.7 Gi 低下 (47.4 Gi)。dns-resolver annex 自体は 10.9 GiB を占有継続
  (unused 0 なので「滞留」とは言えない — partially-transferred 14 件 ~135 MB が唯一の
  回収候補)。因果帰属は据え置き (捏造しない)
- Tier 2 報告事項 (kanban/human): (1) 緊急フラグ <100 Gi が 2 反続 (47.4 Gi / 95%)、
  (2) dns-resolver annex に partially-transferred 一時ファイル 14 件 ~135 MB の回収候補
  (`git annex dropunused` の要否は人間判断 — 本 bot は Tier 2 までで drop しない)

## 範囲修正

- 「786 keys 静的系列」の測定場所を app checkout から **dns-resolver checkout の annex** へ
  修正して再始動 (次反復は keys 数 786 の前後比較 + avail 検知を両方続行)
- ledger の旧行の「app .git/annex」前提は場所誤りとして補正する (値 786 自体は実測で生存)

## 附帯

- avail 観測系列: … 168.1 → 155.7 → 154.5 → 54.1 → **47.4 Gi**
- resident 状態遷移系列: 6 回目のまま変化なし (PID 12978 継続)
- app .git/annex 不存在は本日も再実測 (du rc=1)

## 次の 1 アクション

dns-resolver annex を新しい正規測定点として keys 数 (786)・du・unused の
反復間差分を取り、avail 低下 (-6.7 Gi/反) との相関を 2〜3 反続けて観測する。
partially-transferred 14 件の消化 (drop か自然遷移か) を観測記録する。
