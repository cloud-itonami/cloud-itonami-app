# falsify-65 (2026-09-08, テスト/実装 軸): 現 tip 679572b の identity.clj ns 破損は falsify-64 後も未着地 — HEAD は falsify-63/64 と同一 SHA のため build-broken は決定論的に継続 (REFUTED)

測定日: 2026-09-08 (cron 反復)
測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(cloud-itonami-app) は読み取り専用参照のみ (git rev-parse / git show)。本反復は
source の git object 実測 + HEAD 不変の確認が主であり compile 再実行は行わない。

## 背景 / 反証対象の主張

falsify-63/64 は現 tip 679572b (java.net.http→kotoba migration, 35 files) が identity.clj
の ns 形式破損で全 suite が compile 不能であることを実測した (migration が (:import opener
と最初の 2 行を削除し、残り 5 本の [java.*] import ベクタを裸に残した)。修理案は 5 ベクタを
(:import ...) で wrap 復元 (Tier 2 kanban/human)。

本反復 (falsify-65) の反証対象: 「falsify-64 以降に修理が着地し、現 tip の identity.clj ns は
有効な (:import ...) clause を持ち、スイートは再びコンパイルできる」。(falsify-64 から独立
の時点の再実測)## 実測 (実コマンド出力をリダイレクト記録、捏造なし)

### 1. HEAD 状態
- git -C main rev-parse HEAD -> 679572b289cbb3b96cb3ef6c25188bbb95e28b57
- git -C main rev-parse origin/HEAD -> 679572b289cbb3b96cb3ef6c25188bbb95e28b57 (= HEAD)
- git log --oneline 679572b..origin/HEAD -> 0 commits (修理 commit なし)
- git status --porcelain -> 0 行 (本体 checkout porcelain clean)

### 2. ソースレベルの構造検証 (git object = 決定論的)
git show HEAD:src/cloud/itonami/app/identity.clj (sed -n 25,40p) 実測:

```
            [ed25519.core :as ed]
            [identity.directory :as directory]
            [identity.model :as identity]
            [oauth.model :as oauth])
           [java.nio.charset StandardCharsets]
           [java.security MessageDigest SecureRandom]
           [java.time Duration Instant]
           [java.util Base64 UUID]
           [java.util.concurrent TimeUnit]))
```

- (:import opener の個数 (grep -c): 0
- 裸の [java. ベクタの個数 (grep -c): 5
  (:require [...oauth]) が閉じた直後に (:import 無しで裸ベクタ 5 本が続く —
  falsify-63/64 の malformation と完全一致)

### 3. (明示) compile は本反復で再実行しない
HEAD が falsify-63/64 が実測した commit と同一 SHA (679572b) であり、git commit SHA はツリー内容を一意に特定するため、HEAD 時点の identity.clj のバイト列は
falsify-63/64 が compile (EXIT_RC=1 / Syntax error macroexpanding clojure.core/ns at
(cloud/itonami/app/identity.clj:1:1) / java.nio.charset - failed: #{:import}) した commit
と完全同一。従って compile 結果は決定論的に同一で、compile 再実行は冗長のため行わなかった。
これは同一バイト列からの推移的帰結として記録する (捏造ではない)。

## 判定

- 「falsify-64 以降に修理が着地し、現 tip は再びコンパイルできる」→ REFUTED:
  HEAD = origin/HEAD = 679572b (falsify-63/64 と同一、679572b..origin/HEAD = 0 commits =
  修理 commit なし)、git show HEAD:.../identity.clj で (:import opener 0 / 裸 [java.* ベクタ
  5 の malformation が git object レベルで残存。HEAD が同一 SHA のため、falsify-63/64 が
  実測した compile 失敗 (EXIT_RC=1) は現 HEAD で決定論的に継続する。
- これは falsify-63/64 の発見 (build-broken current tip / 修理未着地) の生存を、真逆の主張
  (修理着地済み) を反証する形で falsify-64 から独立の時点で再確認したもの。
- score はテスト・実装とも 3 のまま (重大な新規赤の継続であり質的反証。file-count
  src 240 / test 243 (tip 679572b, falsify-62 実測) はHEAD 前進なしで不変。修理未着地で「決定論的赤 0」「suite 実行可」は現 tip で依然不成立)。

## スコープ修正 (maturity.md テスト/実装行 + OPEN 赤候補の更新)

- falsify-63/64 の「現 tip 679572b はビルド不能」は本反復 (falsify-64 から独立の時点) で
  再生存 (HEAD 不変 / 構造残存 / compile は同一 SHA により決定論的継続)。修理案
  (identity.clj ns で [java.nio.charset StandardCharsets] の直前に (:import を復元し
  5 ベクタを wrap、[java.net ...] 2 行は migration 目的に沿って生かさない) は Tier 2
  kanban/human 判断のまま未着地。着地には本体 checkout の identity.clj 編集 + テスト緑
  確認が必要 — 本 bot は本体を touch しないため kanban/human 判断 (Tier 2)。修理後は
  falsify-62 の bundle/graph 赤 (bundle published-lock 再発行) の再確認と合わせて決定論的
  実行の再観測が必要。
- file-count は tip 679572b の src 240 / test 243 (falsify-62 実測) のまま不変 (HEAD 前進なし)。
- 附帯 (append-only 既知残存の再確認): 7-軸表 16 行目孤立行 placeholder と falsify-46
  mid-sentence cut は残存のまま (operator 復旧待ち、本 bot は編集しない)。

## 次の 1 アクション

- 実装/テスト軸の最優先 Tier 2 report (falsify-63/64/65 で確立): 679572b migration の
  identity.clj ns 形式破損の修理着地 (本体に (:import 復元、テスト緑確認)。これにより全
  スイートが再びコンパイル可能になり、falsify-62 の bundle/graph 赤 (bundle published-lock
  再発行) の解消確認が次の測定になる。修理が着地した場合、反復で HEAD 前進と identity.clj
  構造復元をまず git object で確認し、その後 full-suite を別 detached worktree で実測する。
- 附帯: 本体 checkout は反復中 porcelain clean (dirty 0)、本 bot は touch せず wt-msloop
  のみで完結。