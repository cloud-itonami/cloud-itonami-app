# falsify-61 (2026-09-07, 実装/テスト 軸): 7-軸表の file-count「src 233 / test 231 (head 9e5b24a)」は現 head 5e3aac9 の現在値ではない — 本体 main が workforce 系 merge で前進し src 233→237 / test 231→240 へ増加

測定日: 2026-09-07 (JST, 21:31)
測定者: itonami-maint (cron 反復)
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app) は
読み取り専用参照のみ (git ls-tree / git diff — 作業ツリーには一切触れない)。

## 背景 / 反証対象の主張

7-軸表 (maturity.md) の 実装 行 と テスト 行 は falsify-55 (2026-09-07) で
「src 233 / test 231 (head 9e5b24a)」に範囲修正した。これは「現在の head では
src 233 / test 231 である」という現在値の主張を伴う。本反復の対象は両行に共通する:

反証対象の主張: 「現時点の本体 head における src / test ファイル数は
src 233 / test 231 (head 9e5b24a を現在とみなす) である」

falsify-53 の教訓 (運用手順) は「7-軸表の file-count は決定論的で 1 反復で再計測
可能、対応軸反復でカウントを随時更新する」なので、本体 head が 9e5b24a より前進して
いるなら stale 化を検出・再アンカーすべき対象。

## 実測 (すべて実コマンド出力をそのまま記録、捏造なし)

### 1. 本体 main check HEAD の前進 (9e5b24a → 5e3aac9)

`git -C <main> log --oneline -3`:
```
5e3aac9 Merge agent/workforce-findings: the workforce runs; next-run-at is the oldest job
6a0e666 adr: the workforce runs; next-run-at is the oldest job, not the next run
2aa14d6 Extract esign retention attestation decision into .kotoba core
```
`git rev-parse HEAD` = 5e3aac92b3f8e8af509740205b1bc91a94cd505b (detached HEAD、
symbolic-ref rc=1)。falsify-55 の anchor 9e5b24a はこの commit graph 上 access (cat-file -t = commit) だが
HEAD は workforce 系 merge で前進している。

### 2. file-count 実測 (git ls-tree, コミットスナップショット == 作業ツリーと一致)

`git ls-tree -r --name-only HEAD -- src | wc -l` = **237**
`git ls-tree -r --name-only HEAD -- test | wc -l` = **240**
(本体 checkout porcelain clean / status rc=0 実測)

### 3. 増分の決定論的特定 (9e5b24a..5e3aac9)

- 追加 src (4): `src/cloud/itonami/app/bot_bounds.cljc`,
  `src/cloud/itonami/app/esign/retention_attestation_core.kotoba`,
  `src/cloud/itonami/app/pure_head_probe.kotoba`,
  `src/cloud/itonami/app/repo_profile.cljc`
- 追加 test (9): `bot_bounds_test.clj`, `esign_retention_kotoba_parity_test.clj`,
  `pure_head_probe_test.clj`, `pure_head_zeroarg_probe_test.clj`,
  `repo_profile_test.clj`, + 4 件の `*_nbb.cljs` editor/skills/splash/wave
- 削除: 0 (breakdown src/test 両方で D=0)

→ falsify-55 の数字が anchor 9e5b24a に対しては正しいが、HEAD が workforce 系 merge
5e3aac9 に前進したため、「現在の head」の値としては stale。増分は私の工程による
追加であり、ソース変更・削除を伴わない (quality への変化なし、数値 drift のみ)。

## 判定

- **「現在の head で src 233 / test 231」→ REFUTED** (範囲修正要)
- 正しい現在値: src **237** / test **240** (head 5e3aac9、worktree 同期済 porcelain clean)
- falsify-55 (anchor 9e5b24a) 自体は不正でなく、その anchor が HEAD でなく過去 head に
  なっただけ。7-軸表の file-count を falsify-53 教訓どおり現在 head 5e3aac9 に再アンカー。
- score は実装・テストとも 3 のまま (数の drift は質の変化ではない、falsify-55 と同様)。

## スコープ修正 (maturity.md 実装行 / テスト行)

- 実装行: 「src 233 (head 9e5b24a)」→「src 237 (head 5e3aac9、falsify-61 実測)」
- テスト行: 「test 231 (head 9e5b24a)」→「test 240 (head 5e3aac9、falsify-61 実測)」

quiet を main check checkout への変更なし。本 bot の作業は wt-msloop のみ。

## 次の 1 アクション

- 実装/テスト軸 score 維持のまま、今後の反復で HEAD 前進に応じ file-count を随時
  再アンカー (falsify-53 教訓の再適用)。次反復候補: 検証済みの current HEAD で
  決定論的テスト実行 (falsify-16 の stage) を再観測し、加点点が green 保持を確認。