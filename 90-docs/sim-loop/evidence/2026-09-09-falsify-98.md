# falsify-98 (2026-09-09, 運用/滞留 軸 — 0 ベースの 13 反復超連続維持 (14 時点観測)、avail は 3 時点連続の帯内定着に延長 (df 158 Gi 微減も 150 Gi 未満への再ドロップなし))

測定日: 2026-09-09 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app) は読み取り専用参照のみ、一切 touch せず。

## 背景 / 測定対象

falsify-97 は 3 ベクトル (main / 全 refs / resident-refspec) の正規 unused 0・local annex
keys 786 不動を falsify-85→97 の 13 時点観測 / 12 反復連続維持で再実測し、0 ベースを
「恒久化傾向の更なる精緻化」へ更新した。その次の 1 アクションは「同 vector + keys + avail を
再実測し、0 ベース維持の **13 反復超の連続持続** or 再蓄積再開を判別 (13 反復超維持なら恒久化
傾向を更に精緻化、再開なら falsify-33..53 語調へ復帰)、あわせて **avail の 159 Gi 定着の持続
(3 時点目) vs 再度の 150 Gi 未満ドロップ** を観測 — 再ドロップなら 148-163 Gi 帯内振動の
周期性を本格検証」。あわせて実装軸附帯で「c618ef3 (appearance_core.kotoba) の parity 測定面の
変化と 2 build-break (identity.clj / kotoba-net) の生存の実測」が指定されていた。

本反復 (falsify-98) はその次の 1 アクションを執行し、falsify-97 が指定した閾値「0 ベースの
**13 反復超の連続持続**」および「avail の 159 Gi 定着の持続 (3 時点目) vs 再度の 150 Gi 未満
ドロップ」を falsify する。

**主張**: 本反復の再実測で (a) 3 ベクトルのいずれかに正規 unused が再出現する、または
(b) local annex keys が 786 から増加する (13 反復超への持続は成立せず残留再蓄積を開始する)、
または (c) avail が 159-163 Gi 帯から **再度 150 Gi 未満へドロップする** (falsify-96/97 の
「159 Gi 定着」説が不成立、148-163 Gi 帯内で周期性のある振動を示す)。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD detached 566928f6701df95d8cf0b62c5455c9d0baf46226、falsify-97 と同一、porcelain clean、
resident branch は `resident/dns-resolver`)。

### 1. main-refspec regular unused

`git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys** (出力は partial
transfer 残骸 14 chunk のみ、NUMBER 1..14 の MD5E-s... 14 行)。`ok` / EXIT_RC=0。

### 2. resident-refspec (帰属ベクトル)

`git annex unused --used-refspec +refs/heads/resident/dns-resolver` → 同様に正規 unused
**0 keys**、partial 14 chunk のみ。`ok` / EXIT_RC=0。

### 3. 全 refs 走査 (孤児ベクトル)

`git annex unused` (refspec 指定なし) → 同様に正規 unused **0 keys**、partial 14 chunk
のみ。いかなる ref からも到達不能な孤児 key は存在しない。`ok` / EXIT_RC=0。

### 4. git annex info

```
local annex keys: 786        (falsify-98: 786 — 変化 0、14 時点観測 / 13 反復連続維持)
local annex size: 11.68 gigabytes   (不変)
available local disk space: 169.89 gigabytes (+100 megabytes reserved) (falsify-97: 170.25
                              GB — 約 -0.36 GB 微減、ほぼ不変の範囲)
temporary object directory size: 186.98 megabytes (partial chunk 14、不変)
annexed files in working tree: 7143 / 93.02 gigabytes ([b2] 側、不変)
combined annex size of all repositories: 111.12 gigabytes (不変)
```

3 ベクトルが全て正規 unused 0 のまま。local annex keys は **786 → 786 (増加 0)** — 滞留
蓄積期 (falsify-39..49) の反復あたり +24/+25 単調増加とは対照的に、増分ゼロの状態が
**falsify-85→98 の 14 時点観測 / 13 反復連続維持**で続いている (falsify-97 の 13 時点を
1 時点上回る、falsify-97 指定の閾値「13 反復超」を充足)。

### 5. df avail (実測)

`df -h /` → avail **158 Gi / 10%** (falsify-97 の 159 Gi 比 約 -1 Gi 微減、159-163 Gi 帯内
に留まり **150 Gi 未満には再ドロップしない**)。annex available local disk space は 169.89 GB
(falsify-97 の 170.25 GB 比 約 -0.36 GB 微減、ほぼ不変の範囲)。**avail は 158 Gi と微減した
が 160 Gi 帯 (159-163) 内の第 3 時点目として定着を延長し、150 Gi 未満を再び下回らない** —
主張 (c) の「再度の 150 Gi 未満ドロップ」は本反復で発生せず (主張 (c) REFUTED)。観測系列は
falsify-85 (163) → 88 (142) → 90 (159) → 91 (159) → 92 (158) → 93 (160) → 94 (149) →
95 (148) → 96 (159) → 97 (159) → 本反復 (**158**) と、falsify-94/95 の一時的低位 (149/148
Gi) の後、falsify-96→98 は **3 反復連続で 159→159→158 Gi、150 Gi 未満への再ドロップなし**。
低位振動からの復帰後は 159 Gi 前後で定着しており、148-163 Gi 帯内に周期性のある振動は (本反復
では 3 時点で) まだ検出されない。1 Gi 級の微減は帯内振動の範囲で、falsify-94/95 の構造的低位
への再陥落ではない。

### 6. 増加源プロセス / 帰属

`ps aux | grep -E 'itonami-app-resident|export_and_sync'` → 増加源 `node nbb
~/.gftd/bin/itonami-app-resident.cljs` **PID 70643 稼働継続** (falsify-85..98 と同一 PID の
まま、連続稼働、export_and_sync は不在)。local annex keys 786 / 11.68 GB 不変ゆえ **1 Gi 級の
avail 微減・0.36 GB 級の annex available 変化は local annex に帰属しない** — annex 外部の
書き込み源の状態変化に起因 (falsify-96/97 と同所見、本反復は avail 158 Gi 微減だが帯内留保)。
TCC 遮断 (onedrive / ~/.gftd du 不能) のため具体源特定は引き続き本 bot では不能 (operator の
du 調査を Tier 2 継続)。

### 7. origin/main (実装軸 附帯)

本体 checkout (orgs/cloud-itonami/cloud-itonami-app) で `git fetch origin` → origin/main は
**c618ef3 のまま** (falsify-93/94/95/96/97 から連続 6 反復にわたり新規前進なし)。`rev-parse
origin/main = c618ef334f4979a9477b0c598a07d15d8c29ca73`。本体 checkout は detached HEAD
b5df93e (#299) / porcelain clean (dirty 0 行)。origin/main は falsify-93..98 で不変ゆえ、
falsify-97 附帯で指定された「c618ef3 の parity 測定面 (3 値結合水準・配線記録) の変化」は
commit 前進がない以上生じておらず、実装軸への引継ぎは本 bot が commit を指す実測のみ。

## 判定

**SURVIVED (主張 a/b — 0 ベース) / REFUTED (主張 c — avail 再ドロップ)**: falsify-97 から本
反復への独立時点で 3 ベクトル (main / 全 refs / resident-refspec) の正規 unused は **全て 0
のまま**、local annex keys は **786 → 786 (増加 0, falsify-85→98 の 14 時点観測 / 13 反復
連続維持)**、既知 partial 14 chunk のみ — 主張 (a) の再出現も (b) の再蓄積も検出されず、
falsify-97 指定の閾値「13 反復超の連続持続」を充足し 0 ベース恒久化傾向を**更に精緻化**した。

他方、**avail は df 実測 159 → 158 Gi (微減、帯内)、annex available 170.25 → 169.89 GB (約
-0.36 GB 微減) と 159-163 Gi 帯内で 3 反復連続の定着を延長した** — 主張 (c) の「再度の 150 Gi
未満ドロップ (148-163 Gi 帯内の周期性振動)」仮説は本反復で発生せず (主張 (c) REFUTED)。
falsify-94/95 の一時的低位 (149/148 Gi) は一過性の低位振動と確定済みのまま、その後の復帰
(159 Gi, falsify-96) が本反復で 158 Gi として持続し、150 Gi 未満への再ドロップはない。1 Gi 級
の帯内微減は構造的低位・継続低下のいずれでもない (avail 158 Gi で帯内安定)。滞留 (keys 786)・
11.68 GB は不変。増加源 PID 70643 が不変のまま avail が 158 Gi で安定している (falsify-96/97
と同所見、回復後の帯内定着を継続確認)。

運用/滞留 軸 score は **3 のまま** (0 ベース恒久化傾向の更なる精緻化 (14 時点観測 / 13 反復
連続維持) を記録、falsify-97 指定の閾値「13 反復超」を充足 — score は実緑測定でのみ上昇する
規約、低下ペナルティも本反復では付さず経過観測)。

## スコープ修正 / 次の 1 アクション

- risk-5 の記述を「滞留は 0 ベースへ回帰し **falsify-85→98 の 14 時点観測 / 13 反復連続維持**
  (unused 0・keys 786 不動・partial 14 chunk のみ) — falsify-97 指定の閾値「13 反復超の
  連続持続」を充足、**恒久化傾向の更なる精緻化継続**。ただし増加源 (resident ingest, PID
  70643) 稼働継続ゆえ再蓄積の可能性は残る」と再アンカー。
- risk-5 の avail 監視シグナルを修正: avail は **158 Gi で 3 反復連続の帯内定着 (df 159→159→
  158 Gi、annex available 170.25→169.89 GB 約 -0.36 GB 微減)**。falsify-94/95 の一時的低位
  (149/148 Gi) からの復帰 (159 Gi, falsify-96) は本反復で 158 Gi として持続し、150 Gi 未満へ
  の再ドロップは発生せず。148-163 Gi 帯内の周期性振動はまだ検出されない (3 時点のみ)。1 Gi 級
  の帯内微減は structural-decline を意味しない。増加源 PID 70643 不変のままの安定ゆえ、消費は
  稼働自体ではなく一時的書き込みバースト/外部解放に帰属継続 (TCC 遮断で具体源特定は継続不能、
  operator の du 調査を Tier 2 継続)。
- 次の 1 アクション: 次反復で同 vector + keys + avail を再実測し、0 ベース維持の**14 反復超の
  連続持続** or 再蓄積再開を判別 (14 反復超維持なら恒久化傾向を更に精緻化、再開なら
  falsify-33..53 語調へ復帰)。あわせて **avail の帯内定着の持続 (158 Gi 第 4 時点目) vs 再度
  の 150 Gi 未満ドロップ** を観測 — 再ドロップなら 148-163 Gi 帯内振動の周期性を本格検証
  (現時点は 3 時点で未検出)。TCC 遮断下の帰属は operator の du 調査を Tier 2 として要請継続。
- 附帯 (実装軸 次反復シグナル): origin/main は **c618ef3 のまま** (falsify-93/94/95/96/97 から
  連続 6 反復、新規前進なし)。次の実装軸反復では、c618ef3 の parity 測定面 (3 値結合水準・
  配線記録) の変化を実測し、2 build-break (identity.clj / kotoba-net) の生存を再確認する
  (本反復は commit 前進なしゆえ parity 変化は生じていない)。
- 附帯 Tier 2: falsify-85..97 と不変 — resident プロセス再起動 (94682→70643) の契機と滞留回収
  の帰属、および falsify-94/95 の avail 一時的低位 (149/148 Gi)→復帰 (159 Gi)→帯内微減 (158
  Gi) 後の具体源特定 (TCC 遮断) は kanban/human へ透過提出。

## 付記 (append-only 規約): falsify-97 追記末尾に新規残骸なし

本反復の wc 実測で maturity.md は行 1527 まで。falsify-97 追記は
`...附帯 (実装軸): origin/main は c618ef3 のまま (falsify-93/94/95/96 から連続 5 反復、新規
前進なし)。` (行 1527) で終わり、新規残骸行は混入していない。falsify-91 追記コマンドの残骸
(行 1237-1238: `MD` / `echo "append rc=$?" ...`) は append-only 規約により本 bot は編集せず
残存のまま。本反復追記はその後に追加する。

## 附帯

本体 CHECKOUT (worktree list 先頭
`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app`) は
detached HEAD b5df93e (#299)、`git status --porcelain` 0 行 = porcelain clean (dirty 0)。
origin/main は本反復で **c618ef3 のまま (falsify-93/94/95/96/97 から連続 6 反復、新規前進
なし)**。本反復は運用軸の falsify-97 次の 1 アクションを測定対象に選択し、作業は wt-msloop /
bot/maturity-sim-loop で完結、本体 checkout を一切 touch せず。identity.clj malformation /
kotoba-net 未宣言 (falsify-63..84) は対象外・既知赤が継続。7-軸表 16 行目孤立行 placeholder /
falsify-46 mid-sentence cut / falsify-91 末尾残骸 (本付記参照) は残存のまま (append-only
規約で本 bot は編集しない)。
(evidence/2026-09-09-falsify-98.md)