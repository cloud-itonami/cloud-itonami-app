# falsify-101 (2026-09-09, 運用/滞留 軸 — 0 ベースの 17 時点観測 / 16 反復連続維持 (SURVIVED)、avail は df 156 → 127 Gi と ~29 Gi 大幅低下 (annex available 166.88 → 136.02 GB 約 -30.9 GB)、150 Gi 未満を大幅に下回る再ドロップを観測 — falsify-100 の「156 Gi 横這い (減退停止)」再アンカー REFUTED)

測定日: 2026-09-09 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app) は読み取り専用参照のみ、一切 touch せず。

## 背景 / 測定対象

falsify-100 は 3 ベクトル (main / 全 refs / resident-refspec) の正規 unused 0・local annex
keys 786 不動を falsify-85→100 の 16 時点観測 / 15 反復連続維持で再実測し、0 ベースを
「恒久化傾向の更なる精緻化」へ更新した。また avail は「df 実測 156 → 156 Gi と横這い
(減退停止)、annex available は -2.17 GB (falsify-99) → -0.84 GB (falsify-100) と減速した
一等級の GB 級微減が続く」と再アンカーし、次の 1 アクションを「同 vector + keys + avail を
再実測し、0 ベース維持の **16 反復超の連続持続** or 再蓄積再開を判別、あわせて **avail の
横這い (156 Gi) の持続 vs 減退再開 or 再安定化** を観測 — 横這いが続けば減退は一過性の低位
振動として確定、減退が再開して 150 Gi 未満へ再ドロップした場合 (または 156 Gi からの減退が
2 時点連続で観測された場合) に限り structural-decline-monitor を再強化し、148-163 Gi 帯内
振動の周期性を本格検証する」とした。

本反復 (falsify-101) はその次の 1 アクションを執行し、falsify-100 が指定した閾値「0 ベースの
**16 反復超の連続持続**」および「avail の横這い (156 Gi) の持続 vs 減退再開」を falsify する。

**主張**: 本反復の再実測で (a) 3 ベクトルのいずれかに正規 unused が再出現する、または
(b) local annex keys が 786 から増加する (16 反復超への持続は成立せず残留再蓄積を開始する)、
または (c) **falsify-100 が再アンカーした「156 Gi で横這い (減退停止)」が持続せず、avail が
減退を再開して 150 Gi 未満へ再ドロップする** (156 Gi からの減退再開が観測され、
structural-decline-monitor 再強化と 148-163 Gi 帯内振動の周期性検証が必要になる)。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD detached 566928f67 / porcelain clean / 本反復同一、resident branch は
`resident/dns-resolver`)。

### 1. main-refspec regular unused

`git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys** (出力は partial
transfer 残骸 14 chunk のみ、NUMBER 1..14 の MD5E-s... 14 行)。`ok` / EXIT_RC=0。

### 2. resident-refspec (帰属ベクトル)

`git annex unused --used-refspec +refs/heads/resident/dns-resolver` → 同様に正規 unused
**0 keys**、partial 14 chunk のみ。`ok` / EXIT_RC=0。

### 3. 全 refs 走査 (孤児ベクトル)

`git annex unused` (refspec 指定なし) → 同様に正規 unused **0 keys**、partial 14 chunk
のみ。いかなる ref からも到達不能な孤児 key は存在しない。`ok` / EXIT_RC=0。

### 4. git annex info

```
local annex keys: 786        (falsify-101: 786 — 変化 0、17 時点観測 / 16 反復連続維持)
local annex size: 11.68 gigabytes   (不変)
available local disk space: 136.02 gigabytes (+100 megabytes reserved) (falsify-100:
                              166.88 GB — 約 -30.86 GB 大幅低下)
temporary object directory size: 186.98 megabytes (partial chunk 14、不変)
annexed files in working tree: 7143 / 93.02 gigabytes ([b2] 側、不変)
combined annex size of all repositories: 111.12 gigabytes (不変)
```

3 ベクトルが全て正規 unused 0 のまま。local annex keys は **786 → 786 (増加 0)** — 滞留
蓄積期 (falsify-39..49) の反復あたり +24/+25 単調増加とは対照的に、増分ゼロの状態が
**falsify-85→101 の 17 時点観測 / 16 反復連続維持**で続いている (falsify-100 の 16 時点を
1 時点上回る、falsify-100 指定の閾値「16 反復超」を充足)。

### 5. df avail (実測、3 回確認)

`df -h /` → avail **127 Gi / 12%** (falsify-100 の 156 Gi 比 **約 -29 Gi 大幅低下**、
`/dev/disk3s1s1`)。3 回連続 (dfa/dfb/dfc) で **127 Gi / 12% で不変** (一時的変動ではない)。
annex available local disk space は 166.88 GB (falsify-100) → **136.02 GB (本反復)** で
約 -30.86 GB — df 実測と整合 (1 Gi ≈ 1.074 GB 換算で 127 Gi ≈ 136.4 GB)。**avail は
falsify-100 の「横這い (減退停止)」から大幅な再減退へ転じ、150 Gi 未満を大きく下回る
127 Gi へ再ドロップ** — これは falsify-96/97/98/99/100 が「発生せず」と 5 反復連続 REFUTED
にしてきた「150 Gi 未満への再ドロップ」が**本反復で実観測された**ことを意味する (+ fetched
増加源は annex 外部)。

### 6. 増加源プロセス / 帰属

`ps aux | grep -E 'itonami-app-resident|export_and_sync'` → 増加源 `node nbb
~/.gftd/bin/itonami-app-resident.cljs` **PID 70643 稼働継続** (falsify-85..101 と同一 PID、
連続稼働、export_and_sync は不在 (grep ヒットなし))。local annex keys 786 / 11.68 GB 不変
ゆえ **avail の ~29 Gi 再減退は local annex に帰属しない** — annex 外部の書き込み源 (または
大量ファイル削除を伴う外部クリーンアップの「used 増加」側) の状態変化に起因 (falsify-96..100
と同所見だが、**今回は減退幅が 1 反復で ~29 Gi と格段に大きく、これまでの -0.36〜-2.17 GB の
GB 級微減とは別格のステップダウン**)。TCC 遮断 (onedrive / ~/.gftd du 不能) のため具体源
特定は引き続き本 bot では不能 (operator の du 調査を Tier 2 継続、ただし今回は異常系として
優先度を上げる)。方法論注: ps 出力は測定ハーネス自身 (grep テキストが自分自身のコマンド行に
一致) を仮ヒット (PID 57065 / 57067) として含む — 実増加源は PID 70643 のみ (falsify-85..101
と同一)、仮ヒットは除外した。

### 7. origin/main (実装軸 附帯)

本体 checkout (orgs/cloud-itonami/cloud-itonami-app) で `git rev-parse origin/main` →
**b1b4b85f2103b72746bd5a35aff857092a370aab** (falsify-100 の c618ef3 から変化、
**8 反復連続の no-advance が解消**)。`git log origin/main -5` → 先頭は
`b1b4b85 Merge jv/deps-repair: the text coordinate lands in the top-level :deps`、
`16b6411 deps.edn: move the text coordinate out of the :build alias`、
`1605024 Merge jv/kotoba-text: clojure.string retired for kotoba.lang.text` 等 —
**kotoba.lang.text 配線が origin/main に着地** (falsify-63..84 の既知 2 build-break のうち
kotoba-net 系に隣接する deps 配線の変化)。diff c618ef3..b1b4b85f は 291 files changed /
+475 -470 (deps.edn 7 行 + ほぼ全 src/test の clojure.string → kotoba.lang.text 置換)。
`/health` server_health:200 (pre-run 実測)。

## 判定

**SURVIVED (主張 a/b — 0 ベース) / REFUTED (主張 c — avail 横這い持続 / 減退再開なし)**:
falsify-100 から本反復への独立時点で 3 ベクトル (main / 全 refs / resident-refspec) の正規
unused は **全て 0 のまま**、local annex keys は **786 → 786 (増加 0, falsify-85→101 の
17 時点観測 / 16 反復連続維持)**、既知 partial 14 chunk のみ — 主張 (a) の再出現も (b) の
再蓄積も検出されず、falsify-100 指定の閾値「16 反復超の連続持続」を充足し 0 ベース恒久化
傾向を**更に精緻化**した。

他方、**avail は df 実測 156 → 127 Gi (約 -29 Gi)、annex available 166.88 → 136.02 GB
(約 -30.86 GB) と、falsify-100 の再アンカー「156 Gi で横這い (減退停止)」を維持できず大幅な
再減退へ転じ、150 Gi 未満を大きく下回る 127 Gi へ再ドロップ** — 主張 (c) は本反復で観測され、
**falsify-100 の「156 Gi 横這い (減退停止)」再アンカーは REFUTED**。falsify-96/97/98/99/100
が「発生せず」としてきた「150 Gi 未満への再ドロップ」が**最初に実観測された**のは本反復
(falsify-94/95 の 149/148 Gi はそれ以前の実測、今回は 127 Gi と更に深い)。減退幅 ~29 Gi は
これまでの GB 級微減 (-0.36〜-2.17 GB) から**別格の単反復ステップダウン**で、falsify-100 が
「2 時点連続の再減退で再強化」と条件付けした structural-decline-monitor を**単一時点の規模
から即時再強化すべき異常系**とみなす (2 時点待ちは帯内微減の誤報を避けるためで、~29 Gi の
ステップダウンは帯内ノイズと誤判定する余地がない)。150 Gi 再ドロップ / 148-163 Gi 帯外への
逸脱を実測したため、「148-163 Gi 帯内振動の周期性」仮説は本反復の 127 Gi で**帯外に外れ**、
帯内振動枠組み自体の再精緻化が必要。

運用/滞留 軸 score は **3 のまま** (0 ベース恒久化傾向の更なる精緻化 (17 時点観測 / 16 反復
連続維持) を記録、falsify-100 指定の閾値「16 反復超」を充足 — score は実緑測定でのみ上昇する
規約。avail の ~29 Gi 再減退は **risk-5 / structural-decline-monitor の再強化トリガ**として
記録し、低下ペナルティは付さず新規監視シグナルとして扱う)。

## スコープ修正 / 次の 1 アクション

- risk-5 の記述を「滞留は 0 ベースへ回帰し **falsify-85→101 の 17 時点観測 / 16 反復連続維持**
  (unused 0・keys 786 不動・partial 14 chunk のみ) — falsify-100 指定の閾値「16 反復超の
  連続持続」を充足、**恒久化傾向の更なる精緻化継続**。ただし増加源 (resident ingest, PID
  70643) 稼働継続ゆえ再蓄積の可能性は残る」と再アンカー。
- risk-5 の avail 監視シグナルを修正 (範囲修正): avail は **156 → 127 Gi (df, 約 -29 Gi / 3
  回確認不変)**、annex available 166.88 → 136.02 GB (約 -30.86 GB) と、falsify-100 の
  「156 Gi 横這い (減退停止)」を維持できず **150 Gi 未満を大きく下回る再ドロップ**へ転じた。
  繰り返される「150 Gi 未満への再ドロップなし」観測 (falsify-96..100) は本反復で反転 —
  **structural-decline-monitor を即時再強化**し、148-163 Gi 帯内振動の周期性検証から
  「127 Gi へのステップダウンを伴う構造的減退」へ監視枠組みを切り替える。減退幅 ~29 Gi の
  具体源は annex 外部 (keys 786 / 11.68 GB 不変) に帰属、TCC 遮断で特定不能 — **operator の
  du 調査を Tier 2 として優先要請** (異常系、通常の経過観測より優先度を上げる)。
- 次の 1 アクション: 次反復で同 vector + keys + avail を再実測し、0 ベース維持の**17 反復超
  の連続持続** or 再蓄積再開を判別 (17 反復超維持なら恒久化傾向を更に精緻化、再開なら
  falsify-33..53 語調へ復帰)。あわせて **avail の 127 Gi 定着 vs 更なる減退 vs 回復** を観測
  — 127 Gi 前後で再び横這いになれば今回の ~29 Gi は一過性の外部解放/書き込みバーストの
  残響として再分類、**更に減退して 100 Gi 未満へ落ちる場合は disk-avail 緊急フラグを立て
  risk-5 を最優先**とする。今回の異常系を受け、**structural-decline-monitor は即時再強化
  (2 時点待ちを解除)**。TCC 遮断下の帰属は operator の du 調査を Tier 2 として要請継続。
- 附帯 (実装軸 次反復シグナル): origin/main は **b1b4b85f** (kotoba.lang.text 配線が着地、
  8 反復連続の no-advance 解消、291 files / +475 -470、deps.edn で text 座標を top-level :deps
  へ移動)。次の実装軸反復では、b1b4b85f の parity 測定面 (3 値結合水準・配線記録) の変化を
  実測し、既知 2 build-break (identity.clj / kotoba-net) の生存 + kotoba.lang.text 配線の
  結線整合を再確認する (c618ef3 から変化があったため、実装軸 parity は本反復で再測定価値)。
- 附帯 Tier 2: falsify-85..100 と不変の附帯 (resident プロセス再起動 94682→70643 の契機と
  滞留回収の帰属) に加え、**falsify-101 の avail ~29 Gi ステップダウンの具体源特定 (TCC 遮断)**
  を異常系として kanban/human へ透過提出 (優先度: 高)。

## 付記 (append-only 規約): falsify-100 追記末尾に新規残骸なし

本反復の wc 実測で maturity.md は行 1532 まで (falsify-100 entry 単独行が末尾、行 1532)。
falsify-100 entry は `...続けた場合にのみ structural-decline-monitor を再強化)。附帯 (実装軸):
origin/main は c618ef3 のまま (falsify-93/94/95/96/97/98/99 から連続 8 反復、新規前進なし)。
(evidence/2026-09-09-falsify-100.md)` で終わり、新規残骸行 (MD / echo 残骸) は混入していない。
falsify-91 追記コマンドの残骸 (行 1237-1238: `MD` / `echo "append rc=$?" ...`) は append-only
規約により本 bot は編集せず残存のまま。本反復追記はその後に追加する。

## 附帯

本体 CHECKOUT (worktree list 先頭
`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app`) は
detached HEAD b5df93e (#299)、`git status --porcelain` 0 行 = porcelain clean (dirty 0)。
origin/main は本反復で **b1b4b85f** (falsify-100 の c618ef3 から更新、**8 反復連続 no-advance
が解消**、kotoba.lang.text 配線着地)。本反復は運用軸の falsify-100 次の 1 アクションを測定
対象に選択し、作業は wt-msloop / bot/maturity-sim-loop で完結、本体 checkout を一切 touch
せず。identity.clj malformation / kotoba-net 未宣言 (falsify-63..84) は対象外・既知赤が継続
(ただし b1b4b85f は kotoba 配線を変更したため、生存は次反復の実装軸で再確認)。7-軸表 16 行目
孤立行 placeholder / falsify-46 mid-sentence cut / falsify-91 末尾残骸 (本付記参照) は残存の
まま (append-only 規約で本 bot は編集しない)。
(evidence/2026-09-09-falsify-101.md)