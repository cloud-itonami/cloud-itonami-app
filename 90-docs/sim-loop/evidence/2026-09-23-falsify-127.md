# falsify-127 (2026-09-23, テスト 軸)

## 主張 (p)

「falsify-126 が観測した load 段階赤 authority.cljk:49
'Unable to resolve classname: java.nio.charset.StandardCharsets' (kbb/nbb,
Node v26.0.0) は一時的または環境起因であり、a3dc8c9 の tree での再実行
(独立 worktree、2 回) で再現しない — すなわち suite は少なくとも load 段階を
通過し、assertion 実行が開始される」

## 判定: REFUTED (赤は決定論的で、2/2 回同一 site で再現。assertion 0 は継続)

### 実測 (isolated worktree /tmp/imwt127, 本体 touch なし)

- 本体 checkout `/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/
  cloud-itonami-app` は porcelain clean (dirty 0 行、反復後再確認)。
  origin/main は **a3dc8c9 のまま** (ls-remote 実測、falsify-126 から前進なし)。
- `git worktree add --detach /tmp/imwt127 a3dc8c9` → 孤立 worktree 作成
  (本体 checkout は不 touch)。
- `kbb -M:test` を 2 回実行 → **両回とも決定論的 rc=1**、同一 failure site:

```
Message:  Unable to resolve classname: java.nio.charset.StandardCharsets
Location: /private/tmp/imwt127/src/cloud/itonami/app/authority.cljk:49:3
49:   (:import [java.nio.charset StandardCharsets]
Error: Unable to resolve classname: java.nio.charset.StandardCharsets
Node.js v26.0.0
EXIT_RC=1
```

- kbb 起動時警告 **100 dep(s) are not :local/root and are NOT on the classpath**
  も falsify-126 と同一 (io.github.kotoba-lang/* 等 GitHub deps が nbb engine
  に解決されない)。
- したがって主張 (p) は REFUTED: 赤は一時的でも環境起因でもなく、tip a3dc8c9
  の source 自体が持つ決定論的 load 赤。falsify-126 の「現 tip でも assertion を
  1 つも実行できない」は現在形として生存。
- worktree は `remove --force` + `prune` 済み (rc=0)。

### score

テスト 3 のまま (修理の実緑測定なし。赤の決定論性の再確認は score を動かさない)。

## 修理案 (Tier 2, kanban/human) — falsify-126 から不変

authority.cljk:49-51 の JVM `(:import ...)` を reader 条件分離
(`#?(:bb ...)` / nbb で java-class shim 宣言) 等で nbb 解決可能にする。
付随して kbb の 100 未解決 deps は nbb.edn 宣言か suite の nbb 範囲縮小の
判断が必要。着地は人間判断。

## 附帯測定 (運用/滞留 軸, 同反復実測)

- df /System/Volumes/Data: avail **5856912 KB ≒ 5.6 GiB / 100%** —
  緊急フラグ (<100 Gi) は **6 反復連続発火** (falsify-121..126 系列継続、
  観測系列 103.75 → 69.8 → 62.6 → 15.0 → 8.1 → 5.6 GiB)。
- dns-resolver annex: HEAD **c8090649** 不変 (10 反続)、objects find -type f
  **786 keys** 不変 (10 反続静止)、`.git/annex/tmp` は **14 件の MD5E-*.edn
  partial 一時ファイル**が連続実在 (2 法測定: `ls` 実在 14 / falsify-123 の
  false negative 教訓に従い glob 単独は不使用) — 静止系列継続、Tier 2
  dropunused 提案不変。
- resident: **PID 1083 継続 (12 反点)**、export_and_sync 不在
  (ps grep -v grep 実測)。
- 本体 checkout porcelain clean、本 bot は touch せず isolated worktree で完結。

## 次の 1 アクション

authority.cljk nbb 解決の修理着地 (kanban/human) を待ち、着地後単一反復で
suite load〜完走を再測定。着地が無い場合、次反復は運用/滞留 軸 (avail 5.6 GiB
の緊急継続観測 + annex 静止系列の 11 反続判別) に戻る。
