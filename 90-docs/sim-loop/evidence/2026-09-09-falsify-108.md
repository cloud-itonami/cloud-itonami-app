# falsify-108 (2026-09-09, 運用/滞留 軸 — 0 ベースの 24 時点観測 / 23 反復連続維持、avail は 36 Gi 帯低位で微減継続 (df 35304 MB ≒ 34.5 Gi / annex info 36.92 GB)、緊急フラグ (<100 Gi) 継続)

測定日: 2026-09-09 12:36 JST (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(cloud-itonami-app, HEAD b1b4b85, porcelain 0 行) は読み取り専用参照のみ、一切 touch せず。

## 背景 / 測定対象

falsify-107 は 0 ベース 22 反復超維持・avail 緊急ステップダウン (114 → 35.7-36.3 Gi、
<100 Gi 緊急フラグ到達) を記録し、次の 1 アクションとして「同 vector + keys + avail
再実測、23 反復超の連続持続判定、avail 36 Gi 帯の更なる低下 vs 定着 vs 回復を観測」
を指定した。本反復はその執行。

**主張**: (a) 3 ベクトルのいずれかに正規 unused が再出現する、または (b) local annex
keys が 786 から増加する、または (c) avail が falsify-107 の 35.7-36.3 Gi 帯から
**回復する (または大きく低下する)** — 低位 36 Gi 帯の定着は成立しない。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD 566928f67… "ingest: resident tick (--source commoncrawl)"、falsify-98..107 と同一、
porcelain 0 行 = clean)。

### 1. main-refspec regular unused

`git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys** (partial
transfer 残骸 14 chunk のみ、NUMBER 1..14 の falsify-85 以来と同一 14 key 集合、
MD5E-s…-C1--….edn)。`ok` / rc=0。

### 2. 全 refs refspec

`git annex unused --used-refspec '+refs/*'` → 正規 unused **0 keys** (同一 partial 14
chunk 集合)。`ok` / rc=0。

### 3. local annex keys

`git annex info` → **local annex keys: 786** (増加 0)、local annex size **11.68 GB**
(不変)、annexed files in working tree 93.02 GB、combined 111.12 GB、temporary object
directory 186.98 MB (不変)。

### 4. avail — 36 Gi 帯低位での微減継続 (緊急フラグ継続)

`df -m /` Available **35304 / 35303 / 35304 MB ≒ 34.5 Gi** (反復内 3 時点、変動 ±1 MB
の実質静止)。falsify-107 の 36580-37158 MB ≒ 35.7-36.3 Gi 比 **約 -1.3〜-1.8 Gi の微減**。
`git annex info` の available local disk space は **36.92 GB (+100 MB reserved)**
(falsify-107 の 38.26-38.86 GB 比 約 -1.3〜-1.9 GB)。**100 Gi 未満の緊急フラグは継続**
(34.5 Gi ≪ 100 Gi)。ただし falsify-107 のような Gi 十台のステップダウンはなく、
反復内 3 時点の df が ±1 MB で実質静止しており **34-36 Gi 帯での低位定着シグナル**を
初観測 (falsify-106 の 113-114 Gi 帯定着シグナルが直後に破られた前例ゆえ、1 反復では
確定しない)。local annex keys 786 / 11.68 GB 不変ゆえ減退は引き続き annex 外部
(TCC 遮断で具体源特定不能、operator の du 調査を異常系・最優先で Tier 2 継続提出)。

### 5. 滞留増加源

`ps aux | grep -E 'itonami-app-resident|export_and_sync' | grep -v grep` → 増加源
**PID 70643 のみ** (node nbb itonami-app-resident.cljs、falsify-85..107 と同一 PID、
export_and_sync 不在)。自助マッチ行は grep -v grep で除外。

### 6. 附帯 (実装軸) — origin/main 前進確認

本体 cloud-itonami-app の `git ls-remote origin main` は
**f54113547ace50de59f0651567551c920f67ce58** を返した (falsify-107 と同一 sha、
**前進なし**)。本体 checkout HEAD は b1b4b85 のまま、porcelain 0 行 = clean
(測定は ls-remote のみで本体 touch なし)。

## 判定

- **主張 (a) REFUTED**: 正規 unused 0 のまま (partial 14 chunk のみ)。
- **主張 (b) REFUTED**: keys 786 → 786。→ 0 ベースは **24 時点観測 / 23 反復連続維持**
  (falsify-85→108)。falsify-107 指定の閾値は「23 反復超」と表記されているが、既存
  パターン (直前反復の連続数を閾値とし、次反復で count > count 前回 で充足:
  falsify-105「19 反復超」←20、falsify-106「20 反復超」←21、falsify-107「21 反復超」←22)
  では falsify-107 の指定は「22 反復超」の表記ゆれと判断され、本反復の 23 連続で
  **充足 (23 > 22)** と記録する (**SURVIVED**)。表記ゆれの可能性も含め透過記録とし、
  次反復の閾値は「24 反復超」とする。
- **主張 (c) REFUTED**: 36 Gi 帯からの回復 (および大幅低下) は発生せず、avail は
  **df 34.5 Gi / annex info 36.92 GB** で falsify-107 比約 -1.5 Gi/GB の微減のまま
  34-36 Gi 帯に滞在。緊急フラグ (<100 Gi) は継続。structural-decline-monitor は
  緊急観測モードを維持しつつ「低位帯定着の持続性観察」を併記 (falsify-106 の
  定着シグナルが 1 反復で破られた前例ゆえ、2 反復連続の定着を持って初めて
  定着と判定する)。減退は annex 外部 (keys 不変)。

score 3 のまま (0 ベース恒久化傾向の更なる精緻化、実緑測定でのみ上昇する規約、
avail 緊急フラグ継続と低位微減は risk-5 / structural-decline-monitor 緊急観測モード
継続として記録)。

次の 1 アクション: 同 vector + keys + avail を再実測し **24 反復超の連続持続** or
再蓄積再開を判別、avail 34-36 Gi 帯の低位定着の持続 (2 反復連続で定着確定) vs
更なる低下 (緊急観測深度判定) vs 回復を観測。加えて: (1) operator du 調査 (avail
緊急フラグ下、最優先 Tier 2 継続)、(2) 本体 origin/main f541135f への追従は
kanban/human へ透過 (本体 touch なし)、(3) dns-resolver remote 名変更 (origin →
cloud-itonami) の operator 確認を Tier 2 継続。
(evidence/2026-09-09-falsify-108.md)
