# falsify-84 (2026-09-08, 実装 軸 — HEAD c792872→d226614 前進 (PR #297 native datetime input synchronize 1 merge) は bots-ui 測定面のみ非接触で .kotoba parity 結合水準は生存、2 build-break は未着地継続を git 静的検証で確認)

測定日: 2026-09-08 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout は
読み取り専用参照のみ (git status / worktree list)、一切 touch せず。

## 背景 / 反証対象の主張

falsify-83 (head c792872) は実装軸を「merge 前進 e2b39ba→c792872 (PR #295 + #296)
が bots-ui の 4 ファイルのみ非接触で 2 build-break は継続、src 244 / test 247 に
re-anchor」と計測した。本反復で HEAD は **d226614** へ前進した
(git fetch 実測: `c792872..d226614 main`、1 merge #297 fix: synchronize native
datetime input events)。

反証対象の主張:
1. 「merge 前進 c792872→d226614 が .kotoba parity 測定面 (bot_bounds/bot_slo/
   human-work 4+1 core + parity test) に接触し、falsify-78/79/82/83 の結合水準/
   配線記録を変化させる」— または
2. 「前進により falsify-63..83 が継続計測してきた 2 build-break ((B) identity.clj
   (:import 回復 0 / 裸 [java.* 5 ベクタ、(A) deps.edn kotoba-net 0) が修理着地した」。

両者を独立に falsify する。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

### 1. HEAD 解決と前進幅

`git fetch origin` → `c792872..d226614 main` (fetch 出力実測: `[new branch]
codex/capital-date-input -> origin/...` + `c792872..d226614 main -> origin/main`)。
`git rev-parse origin/main = d2266149f9b4b350a2caba3ab6717fce3281b3c4`。
`git log --oneline c792872..origin/main` = `d226614 fix: synchronize native
datetime input events (#297)`。falsify-83 の anchor c792872 から **1 merge 前進**
(`git rev-list --count` = 1)。

### 2. 変化ファイル (`git diff --name-only c792872 d226614`)

```
shared/bots-ui/src/cloud/itonami/capital_ui.cljc
```

**単一 cljc のみ**。src/・test/・shared/bots-core 配下は変化なし (name-only に 1 件
のみ = 全て bots-ui)。`.kotoba parity 測定面 (bot_bounds / bot_slo 4 core +
human-work-marketplace-core + 各 parity test、いずれも src/・test/ 配下) は byte
無変化 → falsify-78/79 の 3 値結合水準 (latency/recovery = live #' / bounds =
test-local re-spec / parts = host 結合なし) と falsify-82 の配線済み抽出 2 個、
falsify-83 の「bots-ui 表示面のみ非接触」実測は現 HEAD でも生存。

### 3. file-count 再アンカー

`git ls-tree -r --name-only d226614 -- src | wc -l` = **244** / `-- test` = **247**
—— falsify-83 (c792872) の src 244 / test 247 と **同値** (d226614 の唯一の変化
capital_ui.cljc は shared/bots-ui 配下で repo-root の src/・test/ 計数に含まれない)。
計数は現 HEAD で安定のため再アンカー不要 (同値のまま確認、7-軸表の head を再アンカー
修正のみ実施)。

### 4. build-break 生存の独立静的検証 (claim 2)

`git show d226614:src/cloud/itonami/app/identity.clj` → `grep -c ':import'` = **0**、
裸 `[java.*` ベクタ **5** (行 29-33: [java.nio.charset StandardCharsets] /
[java.security MessageDigest SecureRandom] / [java.time Duration Instant] /
[java.util Base64 UUID] / [java.util.concurrent TimeUnit]) を wrap されず残す
malformation — falsify-63..83 と完全一致。**blob SHA 完全同一**: `git rev-parse
c792872:.../identity.clj` = `3d90156e4944c5431e19d9df2292ef40cb7f0c47` =
`d226614:.../identity.clj` (byte 完全同一)、`git diff --stat c792872 d226614 --
identity.clj deps.edn` は空。`git show d226614:deps.edn | grep -c 'kotoba-net'` =
**0**、blob `5a36739b76a6d06a7ac29d254db5da9c00106c1b` 両 SHA byte 同一 —
falsify-75/76 の undeclared dependency も残存。

一次 compile は falsify-83 が c792872 で独立 worktree 直接実行し
`Syntax error macroexpanding clojure.core/ns ... java.nio.charset - failed: #{
:refer-clojure ... :import}` **EXIT_RC=1** を実測済み。identity.clj blob が
d226614 と byte 完全同一 (上記 3d90156e) ゆえ、同 compile failure は新 HEAD でも
**決定的に保存される** (git object 同一性からの静的帰結、再 compile 不要)。falsify-68
修理着地判定 3 条件 (HEAD 前進のみ・(:import 復元 未達・compile rc=0 未達) は 1/3 充足、
build-break 生存継続。

## 判定

**REFUTED (claim 2 の修理着地) / SURVIVED (claim 1 の測定面非接触と既存結合/配線記録)**:

- **claim 1 (measurement-surface isolation) SURVIVED**: 前進の変化ファイルは
  shared/bots-ui/capital_ui.cljc 単一のみで、src/・test/・shared/bots-core の .kotoba
  parity 測定面は byte 無変化。falsify-78/79 の 3 値結合水準と falsify-82 の「配線済み
  抽出 2 個」は現 HEAD d226614 でも生存。本前進に新規 .kotoba 抽出・.kir.edn 配布は
  なし。src 244 / test 247 は同値で安定 (現 HEAD で再確認、head のみ再アンカー)。
- **claim 2 (build-break 修理着地) REFUTED**: identity.clj (:import opener 0 / 裸
  [java.* 5 ベクタ行 29-33、blob 完全同一 3d90156e) と deps.edn kotoba-net (0、blob
  完全同一 5a36739b) が現 HEAD d226614 でも残存。identity.clj blob が c792872 と byte
  同一ゆえ falsify-83 の独立 compile EXIT_RC=1 (ns-spec エラー) は決定的に保存される
  (falsify-68 修理着地判定 3 条件は 1/3 充足、build-break 生存継続)。

実装軸 score は **3 のまま** (build-break 継続で green 化測定不可は不変。本前進は
bots-ui 表示面のみで測定面非接触のため score 変動なし。file-count は src 244 /
test 247 を現 HEAD d226614 で確認)。

## スコープ修正 / 次の 1 アクション

- 実装軸・テスト軸 file-count を **src 244 / test 247 (head d226614、falsify-84
  実測)** として現 HEAD で再確認済み (falsify-83 と同値、head 表記のみ更新)。
- 修理案 Tier 2 (kanban/human、本体 checkout 編集が必要で Tier 1 不着地、
  falsify-68/75/76/77/78/79/80/81/82/83 と不変): (B) identity.clj の 5 裸ベクタを
  (:import …) で wrap / ns 形式復元、(A) deps.edn への kotoba-net 宣言 →
  updater_test 単体緑 (falsify-76 実測済み)。両着地後のフル suite green を最終終点に、
  falsify-62 の bundle/graph 赤 (published-lock 再発行) もその後再確認。
- 次反復候補: origin/main 前進継続確認 (falsify-76..84 と同型)、または bot_bounds /
  bot_slo を provider-retry / human-work と同水準 (.kir.edn + oracle 登録 + host call)
  へ引き上げる Tier 2 修理案を kanban/human へ正式提出。

## 附帯

本体 checkout (worktree list 先頭
/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app) は
detached HEAD 65d3e98 (#290、origin/main d226614 より遅れ)、
`git status --porcelain` 0 行 = porcelain clean (dirty 0、mainrc=0)。作業は
wt-msloop / bot/maturity-sim-loop で完結し、本 bot は本体 checkout を一切 touch せず。
identity.clj malformation / kotoba-net 未宣言は falsify-63..84 と同一で継続。
7-軸表 16 行目孤立行 placeholder / falsify-46 mid-sentence cut は残存のまま
(append-only 規約で本 bot は編集しない)。
(evidence/2026-09-08-falsify-84.md)