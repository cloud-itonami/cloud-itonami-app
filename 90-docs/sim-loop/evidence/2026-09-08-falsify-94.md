# falsify-94 (2026-09-08, 運用/滞留 軸 — 0 ベースの 9 反復超連続維持 (10 時点観測) で恒久化傾向を更に精緻化、ただし avail は 150 Gi 未満へ低下し falsify-93 の「160 Gi 安定」説を REFUTED)

測定日: 2026-09-08 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app) は読み取り専用参照のみ、一切 touch せず。

## 背景 / 測定対象

falsify-93 は 3 ベクトル (main / 全 refs / resident-refspec) の正規 unused 0・local annex
keys 786 不動を falsify-85→93 の 9 時点観測 / 8 反復連続維持で再実測し、0 ベースを
「恒久化傾向の更なる精緻化」へ更新した。その次の 1 アクションは「次反復で同 vector + keys +
avail を再実測し、0 ベース維持の **9 反復超の連続持続** or 再蓄積再開を判別 (9 反復超維持なら
恒久化傾向を更に精緻化、再開なら falsify-33..53 語調へ復帰)、あわせて avail (160 Gi 安定) の
帰属 — 滞留 (keys 786) 不変下の外部書き込み源の特定を観測」。あわせて実装軸附帯で
「c618ef3 (appearance_core.kotoba) の parity 測定面の変化と 2 build-break (identity.clj /
kotoba-net) の生存の実測」が指定されていた。

本反復 (falsify-94) はその次の 1 アクションを執行し、falsify-93 が指定した閾値「0 ベースの
**9 反復超の連続持続 → 恒久化傾向の更なる精緻化**」および「avail 160 Gi 安定の帰属」を falsify する。

**主張**: 本反復の再実測で (a) 3 ベクトルのいずれかに正規 unused が再出現する、または
(b) local annex keys が 786 から増加する (9 反復超への持続は成立せず残留再蓄積を開始する)、
または (c) avail が 150 Gi 未満へ低下する (falsify-93 の「160 Gi 安定、150 Gi 未満への構造的
減少なし」の帰属が成立しない)。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD detached 566928f67、resident branch は `resident/dns-resolver`)。

### 1. main-refspec regular unused

`git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys** (出力は partial
transfer 残骸 14 chunk のみ、NUMBER 1..14 の MD5E-s... 14 行)。`ok` / EXIT_RC=0。

### 2. resident-refspec (帰属ベクトル)

`git annex unused --used-refspec +refs/heads/resident/dns-resolver` → 同様に正規 unused
**0 keys**、partial 14 chunk のみ。`ok` / EXIT_RC=0。

### 3. 全 refs 走査 (孤児ベクトル)

`git annex unused` (refspec 指定なし) → 同様に正規 unused **0 keys**、partial 14 chunk
のみ。いかなる ref からも到達不能な孤児 key は存在しない。`ok` / EXIT_RC=0。

### 4. git annex info

```
local annex keys: 786        (falsify-94: 786 — 変化 0、10 時点観測 / 9 反復連続維持)
local annex size: 11.68 gigabytes   (不変)
available local disk space: 159.81 gigabytes (+100 megabytes reserved) (falsify-93: 171.88
                              GB — 約 -12.07 GB 低下、150 Gi 台へ)
temporary object directory size: 186.98 megabytes (partial chunk 14、不変)
annexed files in working tree: 7143 / 93.02 gigabytes ([b2] 側、不変)
combined annex size of all repositories: 111.12 gigabytes (不変)
```

3 ベクトルが全て正規 unused 0 のまま。local annex keys は **786 → 786 (増加 0)** — 滞留
蓄積期 (falsify-39..49) の反復あたり +24/+25 単調増加とは対照的に、増分ゼロの状態が
**falsify-85→94 の 10 時点観測 / 9 反復連続維持**で続いている (falsify-93 の 9 時点を
1 時点上回る、falsify-93 指定の閾値「9 反復超」を充足)。

### 5. df avail (2 回独立実測で確認)

`df -h /` → avail **149 Gi / 10%** (falsify-93 の 160 Gi から **約 -11 Gi 低下**、再実測
2 回とも 149 Gi で安定 — 計測誤差ではない)。annex available local disk space は 159.81 GB
(falsify-93 の 171.88 GB 比 **約 -12.07 GB**、df と同方向・下方)。**avail は falsify-93 時点の
160 Gi から **150 Gi 未満 (149 Gi) へ初めて交差して低下** — falsify-93 の「150 Gi 未満への
構造的減少なし」の帰属は本反復で不成立 (主張 (c) REFUTED)。観測系列は falsify-85 (163) →
88 (142) → 90 (159) → 91 (159) → 92 (158) → 93 (160) → 本反復 (**149**) と、159-163 帯から
150 未満へ初めて外れる (falsify-88 の 142 は歴史上の下限、それ以来の再交差)。単一反復での
低下幅 (-11 Gi) は falsify-90..93 の ±1-2 Gi 振動より大きく、一過性振動と構造的減少の判別は
次反復の再実測を要する。

### 6. 増加源プロセス / 帰属

`ps aux | grep -E 'itonami-app-resident|export_and_sync'` → 増加源 `node nbb
~/.gftd/bin/itonami-app-resident.cljs` **PID 70643 稼働継続** (falsify-85/86/87/88/89/90/91/92/93
と同一 PID のまま、連続稼働、export_and_sync は不在)。local annex keys 786 / 11.68 GB 不変
ゆえ **低下・消費分は local annex に帰属しない** — ~11-12 GiB の見かけ上の avail 減少は annex
外部の書き込み源に起因 (滞留不変下の外部書き込み変動、falsify-93 の観測課題と一致)。

`du -sh ~/github/.../m365-archive/onedrive` および `~/.gftd` → **TCC 遮断で 0B / du 不能**
(falsify-19/20 と同状態)。avail 減少分の正確な帰属は TCC 遮断のため本 bot では特定不能 —
operator の du 調査を Tier 2 として要請継続。

### 7. origin/main (実装軸 附帯)

`git fetch origin` → origin/main は **c618ef3 のまま** (falsify-93 から新規前進なし)。
`rev-parse origin/main = c618ef3...ca73`、log 先頭は `merge bot/refactor-20260908-2102
(appearance_core.kotoba, ADR-2609082000)`。falsify-93 が次の実装軸シグナルとした kotoba parity
測定面 migration (parity_test 174 行、appearance mode-cycle plane) は生存継続、2 build-break
(identity.clj / kotoba-net) も既知赤継続 (対象外)。本体 checkout は detached HEAD b5df93e
(#299) / porcelain clean (dirty 0)。

## 判定

**SURVIVED (主張 a/b) / REFUTED (主張 c — avail 低下)**: falsify-93 から本反復への独立時点で
3 ベクトル (main / 全 refs / resident-refspec) の正規 unused は **全て 0 のまま**、local annex
keys は **786 → 786 (増加 0, falsify-85→94 の 10 時点観測 / 9 反復連続維持)**、既知 partial
14 chunk のみ — 主張 (a) の再出現も (b) の再蓄積も検出されず、falsify-93 指定の閾値「9 反復超
の連続持続」を充足し 0 ベース恒久化傾向を**更に精緻化**した。

他方、**avail は df 実測 160 Gi → 149 Gi (約 -11 Gi)、annex available 171.88 → 159.81 GB
(約 -12 GB) へ初めて 150 Gi 未満へ交差して低下** — 主張 (c) が成立し、falsify-93 の
「avail は 160 Gi で安定 / 150 Gi 未満への構造的減少なし」の帰属は**本反復で REFUTED**。
滞留 (keys 786)・11.68 GB は不変ゆえ消費分は annex 外部の書き込み源に帰属するが、TCC 遮断に
より具体源は特定不能。低下幅 (-11 Gi) が falsify-90..93 の ±1-2 Gi 振動を上回るため、一過性
の低位振動 (falsify-88 の 142 Gi) か構造的減少への転じの判別は次反復の再実測を要する。

運用/滞留 軸 score は **3 のまま** (0 ベース恒久化傾向の更なる精緻化を記録する一方で、avail
の 150 Gi 未満への交差は新規監視シグナルとして記録 — score は実緑測定でのみ上昇する規約、低下
ペナルティも本反復では付さず経過観測)。

## スコープ修正 / 次の 1 アクション

- risk-5 の記述を「滞留は 0 ベースへ回帰し **falsify-85→94 の 10 時点観測 / 9 反復連続維持**
  (unused 0・keys 786 不動・partial 14 chunk のみ) — **恒久化傾向の更なる精緻化継続**。
  ただし増加源 (resident ingest, PID 70643) 稼働継続ゆえ再蓄積の可能性は残る」と再アンカー。
- risk-5 の avail に **新規シグナル**を追記: df avail は **160 Gi → 149 Gi (約 -11 Gi)** へ
  150 Gi 未満へ初めて交差 (annex available 171.88 → 159.81 GB 約 -12 GB も同方向)。consume は
  annex 外部 (keys 786 / 11.68 GB 不変) に帰属、TCC 遮断で特定不能。falsify-90..93 の ±1-2 Gi
  振動より大きい低下のため、構造的減少への転じ vs 一過性低位 (falsify-88 142 Gi 先例) を次反復
  で再実測して判別。
- 次の 1 アクション: 次反復で同 vector + keys + avail を再実測し、0 ベース維持の**10 反復超の
  連続持続** or 再蓄積再開を判別 (10 反復超維持なら恒久化傾向を更に精緻化、再開なら
  falsify-33..53 語調へ復帰)。あわせて **avail 149 Gi の続落 (150 Gi 未満への継続低下) vs
  回復**を観測 — 低下継続なら structural-decline と確定し risk-5 を強化、回復なら一過性低位
  振動と確定。TCC 遮断下の帰属は operator の du 調査を Tier 2 として要請継続。
- 附帯 (実装軸 次反復シグナル): origin/main は **c618ef3 のまま** (falsify-93 から新規前進
  なし)。次の実装軸反復では、本反復では対象外とした c618ef3 の parity 測定面 (3 値結合水準・
  配線記録) の変化を実測し、2 build-break (identity.clj / kotoba-net) の生存を再確認する。
- 附帯 Tier 2: falsify-85..93 と不変 — resident プロセス再起動 (94682→70643) の契機と滞留回収
  の帰属、および本反復の avail -11 Gi 低下の具体源特定 (TCC 遮断) は kanban/human へ透過提出。

## 付記 (append-only 規約): falsify-93 追記末尾に新規残骸なし

本反復の wc 実測で maturity.md は行 1306 まで。falsify-93 追記は `(evidence/2026-09-08-falsify-93.md)`
で終わり、新規残骸行は混入していない。falsify-91 追記コマンドの残骸 (行 1237-1238: `MD` /
`echo "append rc=$?" ...`) は append-only 規約により本 bot は編集せず残存のまま。本反復追記は
その後に追加する。

## 附帯

本体 CHECKOUT (worktree list 先頭
`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app`) は
detached HEAD b5df93e (#299)、`git status --porcelain` 0 行 = porcelain clean (dirty 0)。
origin/main は本反復で **c618ef3 のまま (新規前進なし)**。本反復は運用軸の falsify-93 次の
1 アクションを測定対象に選択し、作業は wt-msloop / bot/maturity-sim-loop で完結、本体 checkout
を一切 touch せず。identity.clj malformation / kotoba-net 未宣言 (falsify-63..84) は対象外・
既知赤が継続。7-軸表 16 行目孤立行 placeholder / falsify-46 mid-sentence cut / falsify-91 末尾
残骸 (本付記参照) は残存のまま (append-only 規約で本 bot は編集しない)。
(evidence/2026-09-08-falsify-94.md)