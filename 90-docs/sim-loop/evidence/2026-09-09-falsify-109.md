# falsify-109 (2026-09-09, 運用/滞留 軸 — 0 ベースの 25 時点観測 / 24 反復連続維持、avail は 34-36 Gi 帯低位定着 2 反復連続で確定、緊急フラグ (<100 Gi) 継続)

測定日: 2026-09-09 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(cloud-itonami-app, HEAD 98bec5d, porcelain 0 行) は読み取り専用参照のみ、一切 touch せず。

## 背景 / 測定対象

falsify-108 は 0 ベース 24 時点観測 / 23 反復連続維持・avail 34-36 Gi 帯低位定着
シグナル初観測 (df 35304 MB ≒ 34.5 Gi / annex info 36.92 GB、緊急フラグ継続) を
記録し、次の 1 アクションとして「同 vector + keys + avail 再実測、24 反復超の
連続持続判定、avail 34-36 Gi 帯の低位定着の持続 (2 反復連続で定着確定) vs 更なる
低下 vs 回復を観測」を指定した。本反復はその執行。

**主張**: (a) 3 ベクトルのいずれかに正規 unused が再出現する、または (b) local annex
keys が 786 から増加する、または (c) avail が 34-36 Gi 帯から逸脱する
(更なる大幅低下 or 回復) — 低位帯定着は 2 反徒で確定しない。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD 566928f67… "ingest: resident tick (--source commoncrawl)"、falsify-98..108 と同一、
porcelain 0 行 = clean)。

### 1. main-refspec regular unused

`git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys** (partial
transfer 残骸 14 chunk のみ、NUMBER 1..14 の falsify-85 以来と同一 14 key 集合、
MD5E-s…-C1--….edn)。`ok` / rc=0。

### 2. 全 refs refspec

`git annex unused --used-refspec '+refs/*'` → 正規 unused **0 keys** (同一 partial 14
chunk 集合)。`ok` / rc=0。

### 3. local annex keys

`git annex info` → **local annex keys: 786** (増加 0)、local annex size **11.68 GB**
(不変)、annexed files in working tree 93.02 GB、temporary object directory
186.98 MB (不変)、available local disk space **37.13 GB (+100 MB reserved)**。

### 4. avail — 34-36 Gi 帯低位定着 2 反復連続で確定 (緊急フラグ継続)

`df -m /` Available **35504 / 35504 / 35504 MB ≒ 34.7 Gi** (反復内 3 時点、変動 ±0 MB
の完全静止)。falsify-108 の 35304 MB ≒ 34.5 Gi 比 **約 +0.2 Gi の帯内微増**。
`git annex info` の available local disk space は **37.13 GB** (falsify-108 の
36.92 GB 比 約 +0.21 GB)。**100 Gi 未満の緊急フラグは継続** (34.7 Gi ≪ 100 Gi)。
falsify-108 の定着確定規約 (2 反復連続) に従い、**34-36 Gi 帯の低位定着は本反復で
確定** (35304 → 35504 MB、±1 MB 級の帯内振動のみで帯外逸脱なし)。local annex keys
786 / 11.68 GB 不変ゆえ減退は引き続き annex 外部 (TCC 遮断で具体源特定不能、
operator の du 調査を異常系・最優先で Tier 2 継続提出)。

### 5. 滞留増加源

`ps aux | grep -E 'itonami-app-resident|export_and_sync' | grep -v grep` → 増加源
**PID 70643 のみ** (node nbb itonami-app-resident.cljs、falsify-85..108 と同一 PID、
export_and_sync 不在)。自助マッチ行は grep -v grep で除外。

### 6. 附帯 (実装軸) — origin/main 前進確認

本体 cloud-itonami-app の `git ls-remote origin main` は
**98bec5dc6d833be4848b67023bd5fe81fc44c162** を返した (falsify-107/108 の f541135f から
**前進**、merge commit "Merge pull request #303 from cloud-itonami/feat/async-http-pilot"
相当)。本体 checkout HEAD も **98bec5d** に更新・porcelain 0 行 = clean (他 bot/human
による追従と判断、本 bot は ls-remote/rev-parse の読み取りのみで本体 touch なし)。

## 判定

- **主張 (a) REFUTED**: 正規 unused 0 のまま (partial 14 chunk のみ)。
- **主張 (b) REFUTED**: keys 786 → 786。→ 0 ベースは **25 時点観測 / 24 反復連続維持**
  (falsify-85→109)。falsify-108 指定の閾値は「24 反復超」と表記されているが、既存
  パターン (直前反復の連続数を超えて充足) では 24 連続で **充足 (24 > 23)** と記録
  (**SURVIVED**)。表記ゆれの可能性も含め透過記録とし、次反復の閾値は「25 反復超」
  (パターン準拠) とする。
- **主張 (c) REFUTED**: avail は 34-36 Gi 帯内 (+0.2 Gi 微増) で帯外逸脱なし、
  **34-36 Gi 帯低位定着が 2 反復連続で確定**。緊急フラグ (<100 Gi) は継続のため
  structural-decline-monitor は緊急観測モード維持のまま、観察対象を「低位定着の
  持続性 (3 反復目)」へ更新。減退は annex 外部 (keys 不変)。

score 3 のまま (0 ベース恒久化傾向の更なる精緻化、実緑測定でのみ上昇する規約、
avail 緊急フラグ継続は risk-5 / structural-decline-monitor 緊急観測モード継続として
記録)。

次の 1 アクション: 同 vector + keys + avail を再実測し **25 反復超の連続持続** or
再蓄積再開を判別、avail 34-36 Gi 帯低位定着の持続 (3 反復目) vs 帯外逸脱を観測。
加えて: (1) operator du 調査 (avail 緊急フラグ下、最優先 Tier 2 継続)、(2) 本体
origin/main の f541135f → 98bec5dc 前進 (PR #303 merge) への追従は kanban/human へ
透過済 (本体 touch なし)、(3) dns-resolver remote 名変更 (origin → cloud-itonami)
の operator 確認を Tier 2 継続。
(evidence/2026-09-09-falsify-109.md)
