# falsify-67 (2026-09-08, governor 統合 軸): falsify-60 の Tier 2 修理 (signer-key 配置 / RID 登録) は未着地 — cloud-itonami-app は依然 :input-rejected 7/7、実 gate 判定ゼロ (SURVIVED / 修理不着地の独立再確認)

測定日: 2026-09-08 (cron 反復)
測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(cloud-itonami-app) は読み取り専用参照のみ (git rev-parse / git show / git status)。
governor 実測対象は本体外の `~/.gftd/fleet-ci-tick.log` と launchctl (システムジョブ)。

## 背景 / 反証対象の主張

falsify-60 (2026-09-07、governor 統合 軸) は「現行 governor com.gftd.fleet-ci-tip-tick
が cloud-itonami-app を実 gate 結果に解決し統合が機能している」を REFUTED し、新規 2 不健全を
実測した:
  (1) 検証ステップ signer-key `~/.gftd/fleet-ci-signer-tip.pem` MISSING → 当日実 gate 判定 0
      (ci-verify exit 1 ×14 / preflight SKIP ×28)
  (2) cloud-itonami-app は repos.edn rad-rids に RID 未登録 → gate 解決は全 :input-rejected
      (7/7)、:pass / :fail 皆無
修理 2 件は tamaki/fleet 側設定で Tier 2 kanban/human 判断と記録した。

本反復 (falsify-67) の反証対象: 「falsify-60 以降 (09-07 20:21 から本反復まで) に
signer-key 配置と cloud-itonami-app の RID 登録の Tier 2 修理が着地し、cloud-itonami-app が
実 gate 判定 (:pass / :fail) を受けるようになった」。

## 実測 (実コマンド出力をリダイレクト記録、捏造なし)

### 1. governor 稼働 (launchctl 実測)
```
launchctl list | grep -i fleet:
-	0	com.gftd.fleet-ci-tip-tick      (稼働中, StartInterval 運用)
-	0	com.gftd.fleet-refactor-wave
-	0	com.gftd.fleet-ci-sweep
```
falsify-54/60 と同一の active governor 構成。

### 2. signer-key 存在 (ls 実測)
```
ls -ld /Users/junkawasaki/.gftd/fleet-ci-signer-tip.pem
-> No such file or directory   (依然 MISSING)
```

### 3. 当日 tick log (fleet-ci-tick.log, mtime Sep 8 03:25, 79,826 bytes 実測)
```
grep "preflight SKIP" 最新行:
2026-09-07T18:15:44.598Z preflight SKIP — --signer-pem points at a missing file: /Users/junkawasaki/.gftd/fleet-ci-signer-tip.pem
2026-09-07T18:20:47.499Z preflight SKIP — --signer-pem points at a missing file: /Users/junkawasaki/.gftd/fleet-ci-signer-tip.pem
2026-09-07T18:25:50.678Z preflight SKIP — --signer-pem points at a missing file: /Users/junkawasaki/.gftd/fleet-ci-signer-tip.pem

最新 ci-verify:
2026-09-07T09:03:01.408Z batch 12 ci-verify exit 1 (23s)
2026-09-07T09:04:10.149Z batch 13 ci-verify exit 1 (23s)

最新 tick done:
2026-09-07T09:04:20.744Z tick done — [["cloud-murakumo" :input-rejected] ["cloud-itonami-app" :input-rejected] ["root" :input-rejected] ...]
```
- signer-key MISSING のまま、preflight SKIP が 09-07 18:25Z まで無間隔で連続 (fallback
  ci-verify exit 1、実 gate 判定への到達なし)。
- tick log は Sep 8 03:25 JST (log mtime) まで追記され governor は連続稼働。
- worktree-failed は 0 (falsify-54/60 の生存部分、grep -c "worktree-failed" = 0)。

### 4. cloud-itonami-app の RID 登録 / gate 解決
```
grep -c "no RID registered for cloud-itonami/cloud-itonami-app" -> 7  (falsify-60 と同数)
grep -c "\"cloud-itonami-app\" :input-rejected"                  -> 7  (全 tick)
grep "cloud-itonami-app.*:(pass|fail)"                           -> 0  (皆無)
```
cloud-itonami-app は 7/7 全 tick で `:input-rejected` のまま。`:pass` / `:fail` は皆無で、
実 gate 判定は本反復時点でも 0。

### 5. 本体 checkout 状態 (読み取り専用参照)
- git rev-parse HEAD        -> 679572b289cbb3b96cb3ef6c25188bbb95e28b57
- git rev-parse origin/HEAD -> 679572b (= HEAD)
- git status --porcelain | wc -l -> 0 (porcelain clean, dirty 0)
- src / test ファイル数: src 240 / test 243 (tip 679572b、falsify-62 と不変、HEAD 前進なし)
- identity.clj malformation: (:import opener 0 / 裸 [java.* ベクタ 5 が git object に残存
  (falsify-63/64/65/66 と同一、build-broken tip 継続)

## 判定

- 「falsify-60 以降に governor の 2 Tier 2 修理 (signer-key 配置 / RID 登録) が着地し、
  cloud-itonami-app が実 gate 判定を受ける」→ **REFUTED** (修理は両方とも未着地):
  - signer-key `~/.gftd/fleet-ci-signer-tip.pem` は本反復の ls で依然 No such file (MISSING)。
  - cloud-itonami-app は 7/7 全 tick `:input-rejected`、`:pass`/`:fail` 皆無、実 gate 判定 0。
  - RID 未登録メッセージは falsify-60 (7) と同数の 7。
- これは falsify-60 の発見 (governor 統合の実効不健全: signer-key MISSING + RID 未登録) の
  生存を、真逆の主張 (修理着地済み) を falsify-60 から独立の時点 (翌日) で再確認したもの。
- score は governor 統合 3 のまま (worktree 構造的排除は維持 [worktree-failed 0]、統合実効の
  不健全 2 件は未解消のまま記録継続。質を覆す変更でない)。

## スコープ修正 (maturity.md governor 行 + OPEN 赤候補の更新)

- governor 統合行の「実効不健全 (falsify-60)」は本反復で未解消を再確認。修理 2 件
  (signer-key 配置 / cloud-itonami-app の repos.edn rad-rids 登録) は tamaki/fleet 側設定の
  Tier 2 kanban/human 判断のまま未着地。着地は cloud-itonami-app 本体外 (fleet 設定) のため
  Tier 1 不着地。governor は連続稼働 (log mtime Sep 8 03:25 JST)、worktree-failed 0 維持。
- 本体側の build-broken tip 679572b (identity.clj ns 破損、falsify-63/64/65/66) と
  falsify-62 の bundle/graph 赤 (bundle published-lock 再発行) は本反復の対象外 (別軸・別
  反復の既知赤のまま)。HEAD は 679572b のまま前進せず、file-count src 240 / test 243 不変。
- 附帯 (append-only 既知残存の再確認): 7-軸表 16 行目孤立行 placeholder と falsify-46
  mid-sentence cut は残存のまま (operator 復旧待ち、本 bot は編集しない)。

## 次の 1 アクション

- governor 統合軸の Tier 2 report (falsify-60/67 で確立): fleet 側で (a) signer-key
  `~/.gftd/fleet-ci-signer-tip.pem` を配置し preflight を通過可能にする、(b) cloud-itonami-app
  を repos.edn rad-rids に登録する。着地判定は次反復で `:input-rejected` が `:pass`/`:fail` に
  転じること (tick log の直接確認) で行う。これが着地すれば governor 統合軸の実効ゲートが
  初めて cloud-itonami-app に適用される。なお本体の build-broken (falsify-63〜66) が別途
  残るため、実 gate 結果が出ても着地可能になるのは本体修理着地後。
