# falsify-56 (2026-09-07, spec/契約 軸): 「commands.edn の flags は hint のみ (値スキーマなし)」と「値スキーマ (型/必須性) の機械検証は未整備」の二重反証試行 → **REFUTED (スコープ修正)**

測定日: 2026-09-07 (JST)
測定者: itonami-maint (cron 反復)
作業は sibling worktree (`/Users/junkawasaki/.hermes/profiles/itonami-maint/wt-msloop`,
`bot/maturity-sim-loop`) で完結。本体 checkout (`agent/fix-open-red-5-three-bound` /
head `9e5b24a`) は読み取り専用参照のみ (porcelain clean 実測、dirty 0)。touch しない。

## 背景 / 反証対象の主張

7 軸表 spec/契約 行は以下を主張している (falsify-10 / falsify-47 由来):

> commands.edn に 208 コマンドの解決/path-param 契約
> (flags は hint で値スキーマなし — falsify-10 実測: プレースホルダ 128 すべてに
> `:in "path"` 宣言、欠落 0、408=208+70+130 整合) ... **値スキーマ (型/必須性) の
> 機械検証は未整備**

この「値スキーマ (型/必須性) の機械検証は未整備」を全面主張として反証試行。

## 実測 (すべて実コマンド出力をそのまま記録)

### 1. 生成registry `commands.edn` の flags 全数機械走査 (ヘッド 9e5b24a)

`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app/resources/cloud-itonami-app.commands.edn`
を Python の balanced-bracket スキャナで機械パース (トークン 7059)。

```
command maps (in :commands vector): 208
total flag entries: 123
plain-string flags: 123
non-string flag entries (candidate value-schema): 0
params_total: 128
param required=true: 128
param no-required-key (or not true): 0
params with :in != path: []
placeholder count: 128
unique placeholders: 34
```

→ 生成 registry の flags は **123 件すべて plain string**、値スキーマ (map 型 / `:type` /
`:required?`) を持つ flag は **0**。falsify-10/47 の「生成 registry の flags は hint のみ」
は本ヘッドで機械再確認 (SURVIVED 側)。同時に params 128 = 全 `:required? true` = 全 `:in "path"`、
placeholder 128 と完全一致 (falsify-47 の整合も再確認)。

### 2. 別名レジストリ `cli-aliases.edn` の buffer 値スキーマ検出 (新事実)

一方、companion の別名レジストリ
`resources/cloud-itonami-app.cli-aliases.edn` (ヘッド 9e5b24a) は、**flags の値スキーマを
明示的に宣言している**。ファイル冒頭の body spec 註釈 (実測):

```
{:flag :x}                  take flags[:x]; omit the key when absent
{:flag :x :required? true}  refuse when absent, naming the flag
{:parse :long}              parse-long
{:parse :comma-list}        "a,b" -> ["a" "b"]
{:parse :boolish}           "true"/"1"/"yes" -> true
{:default "d"}              used when the flag is absent
a nested map                a nested body object, same specs inside
```

実例 (auth login):
```
{:command ["auth" "login"]
 :method :post
 :template "/api/agent-session"
 :body {:enrollment-key {:enrollment-key true}
        :label {:flag :label :default "cli"}
        :user-id {:flag :user-id}
        :organization-id {:flag :organization}
        :ttl-days {:flag :ttl-days :parse :long}}}
```

→ `:flag` / `:required?` / `:parse` / `:default` / `:enrollment-key` が flag の値スキーマ
キーワードとして定義され、`:parse` は `:long` / `:comma-list` / `:boolish` 等の型変換を指定。

### 3. 別名レジストリの値スキーマを機械検証するテストの実在

`test/cloud/itonami/app/cli_aliases_test.clj` (16 `^(` 区切区 / deftest 9 件) のうち:

- **`body-specs-use-known-vocabulary` (cli_aliases_test.clj:59-68)**: 全 alias の body を
  `leaf-specs` (flag/enrollment-key 末端まで再帰) で走査し、各 spec の key 集合が
  `#{:flag :required? :parse :default :enrollment-key}` に収まることを検証、さらに
  `:parse` 値が `#{:long :comma-list :boolish :file-contents}` (known-parses、cli.clj:50)
  のいずれかであることを機械検証。
- **`every-template-parameter-has-a-source` (cli_aliases_test.clj:39-48)**: template の
  placeholder 集合と `:params` 宣言が一致し、各 path param に flag 由来があることを検証。
- **`requests-match-what-cli-clj-sent` / `business-bind-refuses-to-clear-everything`**
  (cli_aliases_test.clj:110-144): flag の present/absent を `:cli/missing-flag`
  (cli.clj:464-467 `required-flag`) として拒否する振る舞いを実測。

→ 別名レジストリの flag 値スキーマは **機械検証済み**。`body-specs-use-known-vocabulary` は
まさに「型/必須性 (required=false 相当の present/absent、parse 型) の機械検証」である。

### 4. 両レジストリの解決順序 (commands.cljc:334-349 実測)

```
(defn resolve-invocation ...)
  ... aliases win over the generated registry: `business list` is in both ...
  (if-let [a (resolve-alias named)]
      ... :kind :alias ...
      (if-let [... (resolve-command named)] ...))
```

→ 解決順序は **alias 優先** (alias に存在すれば生成 registry に優先)。つまり「値スキーマを
持つ」別名レジストリは、オペレータが実際に打つコマンド (`bots list`, `auth login`, `business`)
の運用的契約として生成 registry より上位。

## 判定

- **主張 (a)「commands.edn の flags は hint のみ (値スキーマなし)」** → 生成 registry に
  限れば **SURVIVED** (123/123 plain string、値スキーマ flag 0、機械再確認)。
- **主張 (b)「値スキーマ (型/必須性) の機械検証は未整備」** → **REFUTED (全面主張としては)**
  — 別名レジストリ `cli-aliases.edn` は flag 値スキーマ (`:flag :required? :parse :default`)
  を宣言しており、`cli_aliases_test.clj` の `body-specs-use-known-vocabulary` と
  `every-template-parameter-has-a-source` がそのボキャブラリ/必須性/parse 型を機械検証。
  resolver は alias 優先 (commands.cljc:337-349) なので、これは運用契約の最上位層の機械担保。
  「未整備」は生成 registry の flags (値スキーマ無し) にのみ当てはまり、契約全体としては
機械検証が存在する (スコープ修正: 「未整備」は生成 registry の flags にのみ適用、
契約全体としては別名レジストリで機械検証済み)。

## スコープ修正 (maturity.md spec/契約 行の根拠へ反映)

- 「値スキーマ (型/必須性) の機械検証は未整備」→「値スキーマの機械検証は**別名レジストリ
  (cli-aliases.edn) では body-specs-use-known-vocabulary 等で実施済み**、生成 registry
  (commands.edn) の flags は値スキーマ無し (123/123 plain string) のまま」に範囲修正。
- 両 registry は resolver で merge され alias 優先 (commands.cljc:334-349) — 運用契約の
  最上位はスキーマを持つ側。

## 次の 1 アクション

- maturity.md spec/契約 行の falsify-10 記述の後ろに falsify-56 を追記 (生成 registry flags
  値スキーマ 0、別名 registry は値スキーマ機械検証あり、resolver は alias 優先)。spec/契約 軸
  score は 3 のまま (値スキーマ機械検証は「未整備」から「別名 registry では実施済み」へ範囲
  修正され、生成 registry flags の値スキーマ無しは事実のまま — 軸の質を覆す変更ではない)。
