# falsify-43 (2026-09-07): 滞留 (regular unused) の増加継続 (レート減速) + falsify-25 帰属の生存再確認 (321-key / 4.48 GiB scale)

測定日: 2026-09-07 03:55 JST (cron 反復)
測定者: itonami-maint (軸: 反証 + 運用 + リスク-5 + falsify-42 NEXT 残)
対象 repo: cloud-itonami-dns-resolver (read-only コマンドのみ。dropunused/drop/履歴改変/ingest 停止は一切実施しない)
作業は sibling worktree (wt-msloop, bot/maturity-sim-loop) で完結。本体 checkout 未 touch。本体は agent/fix-open-red-5-three-bound で porcelain clean を確認 (status --porcelain = 0 行、head 9e5b24a)。

## 反証すべき主張 (falsify-37/38/39/40/41/42 由来、継続)

1. **「滞留 (main-refspec regular unused) は増加継続し、falsify-25 の帰属 (unused ⊆ resident 現在ツリー)
   は増加後も生存する」**
   - main-refspec unused が増加しつつ resident-refspec regular unused = 0 のままなら帰属 SURVIVED。
   - resident 外の unused が生じるなら帰属破綻 (REFUTED)。
   - falsify-42 で観測された「増加レートの加速 (+62)」が本反復で継続するか、振動するかも併せて観測。

2. **「partial-transfer chunk 数は不変 (drift 停止継続、間欠的)」**
   - falsify-35 の 1 chunk 解決 (15→14) 以降、falsify-36〜42 は 14→14 継続。
   - 本反復で 14 のまま不変 = 間欠的 (SURVIVED)、増加 = drift 再開 (REFUTED)。

3. **「avail の変動は増加源 (PID 94682) の稼働と独立に生じる (単一帰属は未特定のまま)」** ※継続観測

## 実測 (実コマンド出力、2026-09-07 03:55 JST)

1. **df -h /System/Volumes/Data**: `Size 926Gi / Used 740Gi / Avail 146Gi / Capacity 84%`
   (`df -k` avail = 153,266,844 KiB ≈ 146.2Gi)。falsify-42 の 148Gi → 本反復 **146Gi**、
   反復越え -2Gi 漸減。100% 飽和は解消のまま。

2. **git annex info**: local annex keys **321** / size **4.81 gigabytes** (falsify-42 の 296 /
   4.45 GB から **+25 keys / +0.36 GB**)。temporary object directory size **186.98 megabytes**
   (falsify-42 と同値、14 チャンク temp)。working tree annex 33.59 GB (不変)。

3. **git annex unused --used-refspec=+refs/heads/main** (rc=0):
   - 正規 unused key: **321 個** / bytes **4810182003** (≈4.48 GiB、awk 集計実測 /
     falsify-42 の 296 keys / 4,444,966,515 bytes から **+25 keys / +0.34 GiB**、
     増加継続。増加レートは falsify-42 の加速 (+62) から **+25 へ減速** = 単反復の加速は
     継続せず、falsify-40 (+25) / falsify-41 (+24) と同水準に回帰)
   - partially transferred data: **14 個** (MD5E-s*-S52428800-C1-- チャンク temp、falsify-42 と同数)

4. **git annex unused --used-refspec=+refs/heads/resident/dns-resolver** (rc=0):
   - 正規 unused key: **0 個** (partially transferred のみ 14 個)
   - → 帰属「unused ⊆ resident 現在ツリー (data/ledger/)」が 321-key / 4.48 GiB スケールで
     生存継続。main-refspec unused 321 全件が resident-refspec では used。
   - partial-transfer 14 件は falsify-42 と同一 key (temp 186.98MB 不変、annex info 整合)。

5. **増加源の直接観測 (ps)**: `itonami-app-resident.cljs` PID **94682** 稼働継続
   (falsify-33〜42 と同一 PID、nbb 経由、`node nbb .../itonami-app-resident.cljs`)。`export_and_sync.cljs`:
   該当行なし (不在)。

6. **dev server**: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:1338/health` = **200** (稼働)。
   gateway 相当 (3000) は **000** (応答なし、未起動)。

7. **負荷**: load averages 38.17 / 40.11 / 38.13 (falsify-42 の 40.82 台と同水準の高負荷継続)。

## 判定

### 主張 1 (帰属生存 + 増加継続、レートは減速) → **SURVIVED (帰属不変、増加は継続・レート減速)**
- main-refspec regular unused は 321 keys / 4.48 GiB へ**増加** (falsify-42 296 → **+25 keys**)。
- 同一時刻の resident-refspec regular unused = **0** — 増加後も 321 全件が resident 現在ツリー
  (data/ledger/) 参照のまま。falsify-25 の帰属は 321-key / 4.48 GiB スケールで**生存継続**。
- 増加源 itonami-app-resident.cljs (PID 94682) は同一 PID で稼働継続 — 帰属妥当。
- **増加レートの振動を観測**: falsify-42 の単反復加速 (+62) は継続せず **+25 へ減速**、
  falsify-40 (+25) / falsify-41 (+24) 水準に回帰。「加速は持続的な単調上昇」説に反し、
  レートは変動 (振動) する。空床実測の累積: 128(+25)/153(+25)/171(+18)/185(+14)/210(+25)/
  234(+24)/296(+62)/321(+25) — 単調ではなく ~14〜62 の間で振動。

### 主張 2 (chunk drift 停止継続) → **SURVIVED (drift は間欠的のまま)**
- chunk 数は falsify-42 の **14 → 14** で不変。temp 合計 186.98MB (annex info、falsify-42 同値)。
- falsify-35 の 1 chunk 解決 (15→14) 以降、連続 8 反復で drift なし —「drift は毎反復継続する
  単調現象」は引き続き REFUTED、間欠的である。

### 主張 3 (avail 単一帰属は未特定のまま) → **観測継続 (本反復でも特定不能)**
- avail は falsify-38 170Gi → 39:154 → 40:152 → 41:150 → 42:148 → 本反復 **146Gi / 84%**
  と漸減 (反復越え -2Gi)。増加源 (PID 94682) は稼働継続のままの消費 —「avail 回復 = 増加源停止」は
  falsify-37 で既に反証済みのまま不変。解放源 (falsify-30/37 の大回復) の単一起因帰属は依然未特定。

## Tier 2 提言 (unchanged)

- 恒久解消には増加源 (resident ingest の data/ledger 世代削除/方針見直し) が必須。
  データ削除判断を伴うため operator/kanban 判断 (Tier 2)。dropunused は反復緩和として
  引用継続可能。本反復の実測は read-only で Tier 2 以内に留めた (着地なし)。
- 増加レートは falsify-42 の加速 (+62) から +25 へ減速したが増加は継続。単反復ごとの
  レート振動 (14〜62) が実測され、単調上昇ではない。恒久解消 (Tier 2) の優先度は据え置き。
- avail 146Gi / 84% は引続きフルスイート実行可能域 (falsify-38 は 170Gi で 0 failures 実測済み)。

## 附帯

- 7 軸テーブル 16 行目の孤立行 `| 反証候補falsify30PLACEHOLDER` は残存のまま
  (append-only 規約で本 bot は編集せず、operator 復旧待ち — falsify-33〜42 report 済み)。
- 本体 checkout は agent/fix-open-red-5-three-bound で porcelain clean (status --porcelain 0 行) を確認。