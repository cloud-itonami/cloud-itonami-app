# falsify-123 (2026-09-20, 運用/滞留 軸)

## 主張 (k)

「falsify-121/122 が実測した partial 一時ファイル 0 は実在の 0 を反映する (14 件は消滅したまま)」

## 判定: REFUTED (測定ハーネスの false negative)

同一 path (`~/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`)
で再実測:

- `git annex unused` は **14 件の partially transferred 一時ファイルを再列挙**
  (NUMBER 1..14、MD5E-*-S52428800-C1--*.edn、rc=0)。
- `ls -la .git/annex/tmp` 実測: **14 ファイル実在、mtime すべて Sep 6 19:12-19:27**
  (falsify-117 の測定時点から不変)。`total 365232` (512B blocks) ≈ 約 174 MiB。
- falsify-121/122 の `find .git/annex \( -name '*.tmp' -o -name '*.part' \)` は
  **`.edn` 拡張子に一致しない** — 0 は実在 0 でなく glob 不一致による false negative。
- falsify-121 の「falsify-117 Tier 2 dropunused 候補 (~135 MB) は消滅済み・削除主体未測定」
  は REFUTED — 候補は消滅しておらず、列挙方法の変更が見かけ上の消滅を作った。
- 範囲修正: partial 14 件系列は falsify-117..123 を通じて**連続実在** (消滅観測は無効)、
  falsify-117 の Tier 2 提案 (dropunused 14 件) は有効のまま再提出。

## 附帯測定 (同反復実測)

- df /System/Volumes/Data: avail **65652648 KB ≒ 62.6 GiB / 94%**。
  falsify-122 (73188864 KB ≒ 69.8 Gi) 比 **-7.2 Gi**、緊急フラグ (<100 Gi) は
  **2 反続発火**。avail 系列は非単調継続:
  -101.5 → -6.7 → +32.8 → -3.2 → +47.2 → -18.6 → -30.9 → **-7.2 Gi**、単一帰属未確立。
- annex keys: `find .git/annex/objects -type f | wc -l` = **786** (不変, **7 反続静止**)。
- du -sk .git/annex/objects = **11403092 KB** — falsify-121/122 と KB 完全同値 (**7 反続**)。
- dns-resolver HEAD = **c8090649** (不変, 7 反続)。remote は cloud-itonami / kotobase
  (falsify-107 の remote 変更状態を維持)。
- resident: PID **1083** 継続 (Fri03PM 起動、新規遷移なし)。
- app origin/main は本反復未測定 (不変とは主張しない)。

## score

運用 3 のまま (低下・滞留とも測定で裏取れたが修復判断は人間 = Tier 2)。

## 次の 1 アクション

(1) partial 14 件の実在を `git annex unused` + `ls .git/annex/tmp` の 2 法で再実測し
系列を修復、(2) 緊急フラグ 3 反続の有無、(3) keys 786 / du 同値 / HEAD の 8 反続静止
を判別。測定法は falsify-123 で修正した 2 法のみを使用 (glob 単独は禁止)。
