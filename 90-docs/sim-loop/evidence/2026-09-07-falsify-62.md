# falsify-62 (2026-09-07, テスト 軸): 現 anchor head 5e3aac9 の全スイート決定論的実行 — workforce 加加点 5 ns は green 保持 (SURVIVED) だが、bundle_test ×2 FAIL / graph_test 1 ERROR の新規決定論的赤を実測 (「決定論的赤 0」は不成立)

測定日: 2026-09-07 (JST)
測定者: itonami-maint (cron 反復)
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app) は
読み取り専用参照のみ (git ls-tree / git log)。テスト実行は本体から独立した detached
worktree `/private/tmp/mt-msloop62` (head 5e3aac9) で実施し、本体 checkout には一切
触れない。終了後 worktree は削除 (chmod -R u+wx + rm、git worktree list からも消滅確認)。

## 背景 / 反証対象の主張

falsify-61 (2026-09-07) は 7-軸表の file-count を head 5e3aac9 に再アンカーし
(src 237 / test 240)、次反復候補として「検証済みの current HEAD で決定論的テスト実行を
再観測し、加加点が green 保持を確認」を挙げた。

反証対象の主張 (2 層):
1. (狭義・falsify-61 の次アクション) 「現 anchor head 5e3aac9 で workforce merge の
   加加点 (bot_bounds / esign_retention_kotoba_parity / pure_head_probe /
   pure_head_zeroarg / repo_profile の 5 clj テスト ns) は green を保持している」
2. (広義・テスト軸表の現在形) 「現 head で全スイートは 0 failures (決定論的赤 0)」

## 実測 (すべて実コマンド出力をそのまま記録、捏造なし)

### 1. 実行環境

- detached worktree `/private/tmp/mt-msloop62` @ **5e3aac9** (Merge agent/workforce-findings)
- `clojure -M:test` は :test alias の `:main-opts ["-m" "cloud.itonami.app.test-runner"]`
  を解決し、全 240 ns の明示リストを実行 (test-runner -main が走ったことを stack trace で確認)。
- 実行時 disk: `/` avail **216Gi / 7%** (falsify-17 の飽和リスクなし)。
- 本体 checkout は head **679572b** へ前進 (反復中に別 merge が着地、5e3aac9 → 679572b)。

### 2. 全スイート結果 (実測、/tmp/f62.log に保存)

```
Ran 2342 tests containing 14116 assertions.
2 failures, 1 errors.
exit=1
```

- **加加点 5 ns は全て green**: cloud.itonami.app.bot-bounds-test /
  cloud.itonami.app.repo-profile-test / cloud.itonami.app.pure-head-probe-test /
  cloud.itonami.app.pure-head-zeroarg-probe-test /
  cloud.itonami.app.esign-retention-kotoba-parity-test は各「Testing ...」の後
  FAIL/ERROR ブロックなしで実行終了。→ 狭義の主張 1 は SURVIVED。

### 3. 失敗 2 + error 1 (いずれも workforce 加加点でない既存 ns)

- **bundle_test.clj:45/47 `the-published-lock-matches-the-current-document` (FAIL ×2)**:
  ```
  source changed; republish before landing
  expected: (= cid (:kotoba.app/bundle-cid published))
    actual: (not (= "bafkreiabj4eq52t6ujh2gfcebqefis6mortdn3hmwc5o5npgzeanqsn2ry"
                    "bafkreiabxxpmnzydzr2iidu7u63mdrcai2a56b6r4qz2rdjzv4lv7fgbny"))
  ```
  (embed-url も同様に published vs manifest 不一致)
- **graph_test.clj:117 `publish-points-at-the-chain-cid` (ERROR)**:
  ```
  clojure.lang.ExceptionInfo: manifest invalid after graph
  {:problems [{:attrs [:kotoba.app/bundle-cid :kotoba.app/embed-url],
               :error :bundle-cid-embed-url-mismatch}]}
  ```
  → bundle_test と graph_test は同一根因 (生成 bundle の chain CID が checked-in
  published-lock と不一致)。

### 4. 新規性の確認 (falsify-38 の 0 failures からの出現)

`git merge-base --is-ancestor cc7a17d 5e3aac9` = **rc 0** (cc7a17d は 5e3aac9 の祖先)。
falsify-38 (2026-09-07, cc7a17d) は **Ran 2291 / 13895 assertions / 0 failures** を実測。
cc7a17d..5e3aac9 には Kotoba pure-head / compiler advance (amu→78cdc2e) / esign
retention extraction / wave 描画等の bundle 内容に影響しうる merge 群が含まれる。
→ この 2 failures + 1 error は cc7a17d..5e3aac9 間で新規出現した決定論的赤。

## 判定

- **狭義 (加加点 green 保持) → SURVIVED**: 5 加加点 ns は 5e3aac9 の決定論的実行で
  全て green。
- **広義 (全スイート 0 failures / 決定論的赤 0) → REFUTED**: 現 anchor head 5e3aac9 で
  2 failures + 1 error (bundle_test ×2 FAIL / graph_test 1 ERROR) を実測。falsify-38
  (cc7a17d) の 0 failures から新規の決定論的赤が出現しており、テスト軸表の「決定論的赤 0」
  は現在形として成立しない。
- score は 3 のまま (測定駆動の範囲修正であり、スイートは 2342/14116 のうち 2f+1e のみ。
  赤は記録・修理提案に留める)。

## スコープ修正 (maturity.md テスト行 + 新規 OPEN 赤)

- テスト行の「決定論的赤 0」を「falsify-38 (cc7a17d) までは 0 failures だったが、
  falsify-62 (5e3aac9) で bundle_test ×2 FAIL / graph_test 1 ERROR の新規決定論的赤を実測」
  へ範囲修正。
- 新規 OPEN 赤候補 (Tier 2): 「bundle published-lock が bundle 内容変化 (cc7a17d..5e3aac9
  の Kotoba/design-core merge) 後に再発行されておらず、bundle_test と graph_test が
  決定論的に赤」。修理案 = bundle 再発行 / published-lock fixture 再生成 (リポジトリ内の
  bundle 生成・lock 更新手順に従う)。着地は kanban/human 判断 (Tier 2)。
- 附帯 (falsify-53 教訓の再適用): 反復中に本体 HEAD が 5e3aac9 → **679572b** へ前進。
  `git ls-tree -r --name-only 679572b -- src|test | wc -l` = **src 240 / test 243**
  (5e3aac9 の 237/240 比 +3/+3)。7-軸表の file-count を tip 679572b へ再アンカー。
- 附帯: 実行 worktree `/private/tmp/mt-msloop62` の target/test-data に git-annex
  read-only 残骸が残り `git worktree remove` が Permission denied で失敗 — falsify-16 が
  NEXT-9 で Tier 2 runbook 提案済みの既知 fail モード (test-data の annex read-only 残骸)
  の実例。本反復は提案済み手順 (chmod -R u+wx) で退避・削除し、worktree 登録も消滅確認
  (本体 checkout には影響なし)。
- 本体 checkout は反復中も porcelain clean (dirty 0) を維持、本 bot は touch せず
  wt-msloop のみで完結。7-軸表 16 行目孤立行 `| 反証候補falsify30PLACEHOLDER` と
  falsify-46 mid-sentence cut は残存のまま (operator 復旧待ち、append-only)。

## 次の 1 アクション

- テスト軸: bundle published-lock の再発行 (bundle_test / graph_test の新規決定論的赤の
  解消) を Tier 2 で提起。falsify-53 教訓どおり、file-count を tip 679572b
  (src 240 / test 243) に再アンカー。
