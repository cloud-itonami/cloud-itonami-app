# falsify-77 (2026-09-08, 実装/テスト 軸 — 新 HEAD での build-break 生存再確認)

測定日: 2026-09-08 (cron 反復)/ 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app) は読み取り専用参照のみ (git rev-parse / show /
ls-tree / worktree add --detach)。compile は detached worktree
(/private/tmp/mt-msloop77) で実行し、終了後 remove --force + prune で後始末した。

## 背景 / 反証対象の主張

falsify-63..76 は現 tip 679572b の identity.clj ns 形式破損 (migration が `(:import`
opener を削除し残り 5 本の [java.*] ベクタを裸のままに残した) で全 suite compile 不能、
さらに falsify-75/76 で第二の undeclared build-break (kotoba-net) を確定してきた。falsify-68
の修理着地判定 3 条件は (1) origin/main への HEAD 前進 (2) identity.clj の (:import 復元
(git object) (3) detached worktree 一次 compile EXIT_RC=0。

本反復で本体 HEAD が **679572b → e2e3bd8** へ前進した (falsify-68 条件 (1) が生起)。
反証対象: 「HEAD 前進に伴い falsify-68 の修理が着地し、現 tip (e2e3bd8) の identity.clj は
再び有効な (:import ...) を持ち、一次 compile EXIT_RC=0 でコンパイルできる」。

## 実測 (実コマンド出力をリダイレクト記録、捏造なし)

### 1. 本体 checkout 状態 (新 HEAD)
```
git rev-parse HEAD        -> e2e3bd8f4211583786b0b7ecb938cec4016bf9da
git rev-parse origin/main -> e2e3bd8 (= HEAD)
git rev-parse origin/HEAD -> e2e3bd8 (= HEAD)
git status --porcelain | wc -l -> 0 (dirty 0)
git ls-tree -r --name-only HEAD -- src | wc -l -> 242
git ls-tree -r --name-only HEAD -- test | wc -l -> 245
```
HEAD = origin/main = origin/HEAD = **e2e3bd8**、本体 porcelain clean (dirty 0)、
src 242 / test 245 (falsify-62 実測 240/243 から +2/+2、bot_bounds/bot_slo `.kotoba`
抽出 4 commit 分)。HEAD 前進 = `git log 679572b..HEAD` で確定:
```
e2e3bd8 Extract bot_bounds spend-admission decision into a .kotoba core
b83e85b feat: extract bot_bounds spend-admission decision into a .kotoba core
365745d Extract bot_slo stability-part limbs into .kotoba
f1c7290 feat: extract bot_slo stability-part limbs into .kotoba
```
(679572b 直後の追加 commit 4 本。transport migration 以後の bot_bounds / bot_slo 抽出
であり、いずれも identity.clj 修理を含むかは以下で判定。)

### 2. git object の identity.clj ns 状態 (新 HEAD)
```
git show HEAD:.../identity.clj | grep -c '(:import'   -> 0   (:import opener 無し)
git show HEAD:.../identity.clj | grep -n '^ *\[java\.' -> 5   (裸の java ベクタ)
  29: [java.nio.charset StandardCharsets]
  30: [java.security MessageDigest SecureRandom]
  31: [java.time Duration Instant]
  32: [java.util Base64 UUID]
  33: [java.util.concurrent TimeUnit]))
```
**falsify-63..76 の malformation と git object レベルで完全一致** (HEAD 前進しても
`(:import` = 0 / 裸 [java.* 5 本、29-33 行 — 修理 commit は入っていない)。falsify-68
補足: origin/jvm-host-transport や migration 前 branch は他形式だが、新 HEAD (main 先端
e2e3bd8) はこの 4 本の bot_bounds/bot_slo 抽出 commit を 679572b に積んだだけで
identity.clj へは未 touch。

### 3. deps.edn の kotoba-net 未宣言 (第二 build-break)
`git show HEAD:deps.edn | grep -c 'kotoba-net'` = **0** — falsify-75/76 が確定した
undeclared dependency (kotoba-net) も HEAD 前進後も解消せず残存。

### 4. 一次 compile (detached /private/tmp/mt-msloop77 @ e2e3bd8)
`clojure -M:test -e "(require 'cloud.itonami.app.identity)(println :f77-ok)"`:
```
Syntax error macroexpanding clojure.core/ns at (cloud/itonami/app/identity.clj:1:1).
java.nio.charset - failed: #{:refer-clojure} at: [:ns-clauses :refer-clojure :clause]
java.nio.charset - failed: #{:require} at: [:ns-clauses :require :clause]
java.nio.charset - failed: #{:import} at: [:ns-clauses :import :clause]
java.nio.charset - failed: #{:use} at: [:ns-clauses :use :clause]
java.nio.charset - failed: #{:refer} at: [:ns-clauses :refer :clause]
java.nio.charset - failed: #{:load} at: [:ns-clauses :load :clause]
java.nio.charset - failed: #{:gen-class} at: [:ns-clauses :gen-class :clause]
compile_exit=1
```
**一次 compile 実測 EXIT_RC=1**、falsify-63/64/68 と同一の ns spec エラーが新 HEAD
でも独立・直接再現。現 tip (e2e3bd8) は依然 identity を transitively に pull する全
テストが load 段階で停止し、assertion を実行できない。

### 5. detached worktree 後始末
`git worktree remove --force` rc=0 → `git worktree prune` → `worktree list | grep -c
mt-msloop77` = 0 (残存せず)。

## 判定

**REFUTED** — 「HEAD 前進に伴い修理が着地し現 tip が再びコンパイルできる」は成立しない:
HEAD は e2e3bd8 へ前進したが (falsify-68 条件 (1) のみ生起)、(2) identity.clj の
`(:import` 復元は git object で未着地 (0 / 裸 5 本のまま)、(3) 一次 compile は
EXIT_RC=1。falsify-68 の 3 条件中 1/3 充足、build-break 生存。deps.edn の kotoba-net
未宣言 (falsify-75/76) も残存。テスト軸・実装軸 score は **3 のまま** (重大な新規赤の継続
であり質的反証。新 HEAD での 「決定論的赤 0」「suite 実行可」は依然不成立)。

## スコープ修正 / 次の 1 アクション

- file-count を新 HEAD へ再アンカー (falsify-55/61/62 教訓の再適用): 実装行
  「src 240」→ **242**、テスト行「test 243」→ **245** (e2e3bd8 実測)。
- build-break の生存を新 HEAD (HEAD 前進後) でも一次 compile 実測で再確認。修理案は
  falsify-68/75/76 と不変: (B) identity.clj の 5 裸ベクタを `(:import ...)` で wrap
  (Tier 2 kanban/human)、(A) deps.edn への kotoba-net 宣言 (:git/sha 或は :local/root)
  → updater_test 単体緑 (falsify-76 で実測済み)。両着地後のフル suite green を最終終点、
  falsify-62 の bundle/graph 赤 (published-lock 再発行) もその後再確認。

## 附帯

本体 checkout は反復中 porcelain clean (dirty 0)、本 bot は touch せず wt-msloop /
bot/maturity-sim-loop で完結、実行 worktree mt-msloop77 は remove --force / prune 済みで
worktree list に残存せず。7-軸表 16 行目孤立行 placeholder と falsify-46 mid-sentence cut
は残存のまま (operator 復旧待ち、append-only のため本 bot は編集しない)。bot_bounds /
bot_slo の `.kotoba` 抽出 4 commit は board が分離環境で投入したとみられるが、本反復は
build-break 生存の一次実測のみでそれ自体の質は未検証 (次反復候補: 抽出の構文/往来整合)。