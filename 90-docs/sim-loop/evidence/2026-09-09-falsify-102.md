# falsify-102 (2026-09-09, 実装 軸 — origin/main b1b4b85f で既知 2 build-break (identity.clj ns 生成形 / kotoba-net 未宣言) の生存を再実測、**両者とも SURVIVED** (kotoba.text 配線着地は build-break 修復へ繋がらなかった)、他方 **kotoba.lang.text 配線の結線整合は SURVIVED (新規実測)** — src 214 ファイル / test 237 ファイル)

測定日: 2026-09-09 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app) は読み取り専用参照のみ、一切 touch せず。

## 背景 / 測定対象

falsify-101 は運用/滞留 軸で 0 ベースの 17 時点観測 / 16 反復連続維持 (SURVIVED) と avail
大幅減退を記録し、附帯 (実装軸 次反復シグナル) で「origin/main は **b1b4b85f** (kotoba.lang.
text 配線が着地、8 反復連続の no-advance 解消、291 files / +475 -470、deps.edn で text 座標を
top-level :deps へ移動)」「次の実装軸反復では、b1b4b85f の parity 測定面の変化を実測し、既知
2 build-break (identity.clj / kotoba-net) の生存 + kotoba.lang.text 配線の結線整合を再確認する
(c618ef3 から変化があったため、実装軸 parity は本反復で再測定価値)」とした。

本反復 (falsify-102) はその附帯を執行し、実装軸の parity 再測定 + 2 build-break 生存確認 +
kotoba.lang.text 配線の結線整合を falsify する。

**主張**:
(a) kotoba.text 配線着地 (b1b4b85f) により **build-break #1 (identity.clj の ns 生成形破損) が
    修復されている** — 6f9fd0d が identity.clj を書き換え (kotoba.lang.text 移行)、b1b4b85f の
    同 ns は well-formed な Clojure Form として macroexpand できる。
(b) kotoba.text 配線着地により **build-break #2 (kotoba-net 未宣言) が修復されている** —
    http_client.clj:16 が require する kotoba.net.jvm-host の座標が deps.edn (全 alias 含む) に
    宣言されている。
(c) **kotoba.lang.text 配線は結線整合している** — deps.edn top-level :deps に text 座標 (io.github.
    kotoba-lang/text, sha 73bdb13) が宣言され、src の移行 (clojure.string → kotoba.lang.text) が
    完了し (src 内 clojure.string 参照 0)、sibling kotoba-lang/text が存在する。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: 本体 cloud-itonami-app。origin/main = **b1b4b85f2103b72746bd5a35aff857092a370aab**
(本体 checkout detached HEAD b5df93e (#299) / porcelain clean / dirty 0 — 読み取り参照のみ)。
全て `git show b1b4b85f:...` でキャノニカルツリーから読取 (checkout 未 touch)。

### 1. parity 測定 (src / test ファイル数、b1b4b85f)

`git ls-tree -r --name-only b1b4b85f src | grep -cE '\.(clj|cljc)$'` → **214** ファイル
`git ls-tree -r --name-only b1b4b85f test | grep -cE '\.(clj|cljc)$'` → **237** ファイル
(count 方法: 本 bot が falsify-84 (d226614) で用いた clj/cljc 限定とは同種、falsify-101 の
291 files / +475 -470 は c618ef3..b1b4b85f の full-tree diff — 実装面は kotoba.text 置換を
含めて増減あり。score 根拠の「src 244 / test 247 (d226614)」からの差分はリビジョン差 (kotoba.
text 移行で src/test 内置換) を含むため、本反復では b1b4b85f 時点の実測値を新 anchor とする)。

### 2. build-break #1 — identity.clj の ns 生成形 (b1b4b85f)

`git show b1b4b85f:src/cloud/itonami/app/identity.clj` 先頭 34 行の ns 生成形:

```
(ns cloud.itonami.app.identity
  "Local account, organization membership, and delegated OAuth connections.
   ..."
  (:require [cloud.itonami.app.config :as config]
            ...
            [kotoba.lang.text :as str]
            [ed25519.core :as ed]
            [identity.directory :as directory]
            [identity.model :as identity]
            [oauth.model :as oauth])
  [java.nio.charset StandardCharsets]
  [java.security MessageDigest SecureRandom]
  [java.time Duration Instant]
  [java.util Base64 UUID]
  [java.util.concurrent TimeUnit])
```

— ns 本体内、`(:require ...)` の閉じ括弧 (行 29) の**後**に `[java.* ...]` の裸ベクタが
5 個、`(:import ...)` ラッパー無しで並ぶ (行 30-34)。falsify-63 が 679572b で実測した破損形
((:import opener 削除 + 5 裸 java ベクタ) と**構造的に同一**。6f9fd0d の kotoba.lang.text 移行は
clojure.string → kotoba.lang.text の文字列置換のみで (commit message「nothing but the
substitutions moved」)、ns の裸 java ベクタ生成形には触れていない)。

**macroexpand 実測 (2 系統で照合)**:
- babashka (bb, sci ns は lenient): `macroexpand-1` → `MACROEXPAND_OK` (誤判定。sci の ns 実装は
  JVM Clojure と等価でないため)。
- **JVM Clojure 1.12.5 (本 bot が deps.edn と同一 Clojure mvn 1.12.5 を :deps に置いた
  /tmp/probedeps で実行)**: `macroexpand-1` → `CLJ_ERR: Syntax error macroexpanding
  clojure.core/ns at (...)`。**falsify-63 と同一のエラー署名**。

→ **build-break #1 は SURVIVED** (宣言 (a) REFUTED)。JVM Clojure (アプリが実際に走る実装) では
b1b4b85f の identity.clj は依然 ns 生成形エラーで macroexpand 失敗。6f9fd0d / b1b4b85f (deps-
repair) は kotoba.text 座標の移動と文字列置換を行ったが、既知赤の ns 生成形修復には至らなかった。
(commit 6f9fd0d stats 先頭: deps.edn +7-1 等、identity.clj を含む 290 file 置換 — 文字列置換のみ)。

### 3. build-break #2 — kotoba-net 未宣言 (b1b4b85f)

- `git show b1b4b85f:src/cloud/itonami/app/http_client.clj` 16 行目: `(:require [kotoba.net.jvm-host
  :as jvm-host])` — **kotoba.net.jvm-host を require 継続** (3-5 行 docstring「delegates to
  kotoba.net.jvm-host (the workspace's single java.net.http site)」)。
- kotoba.net.jvm-host への require は repository 内で **http_client.clj のみ** (`git grep -l` →
  1 file)。
- `git show b1b4b85f:deps.edn` を **kotoba-net / kotoba_net / jvm-host / jvm_host で grep →
  ヒット 0** (rc=1)。**:dev alias の :override-deps / :local/root リストにも kotoba-net なし**
  (git show deps.edn | grep kotoba-net → rc_local=1)。→ falsify-75 の「kotoba-net 未宣言」は
  **b1b4b85f でも不変に生存**。
- 他方 sibling `kotoba-lang/kotoba-net/src/kotoba/net/jvm_host.clj` は実在 (本ワークスペースの
  実参照先)。つまり参照対象ライブラリは存在するが、このアプリの deps.edn には宣言されていない。

→ **build-break #2 は SURVIVED** (宣言 (b) REFUTED)。http_client.clj:16 の require に対し
kotoba-net 座標は deps.edn 全 alias に未宣言のままで、falsify-75 の FileNotFoundException
(kotoba/net/jvm_host を locate 不可) は解消していない。

### 4. kotoba.lang.text 配線の結線整合 (b1b4b85f, 新規実測)

- **宣言 (top-level :deps)**: git show deps.edn 行 7 → `io.github.kotoba-lang/text
  {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}` + 行 4-6 comment「kotoba.lang.text,
  not clojure.string. Pinned as an explicit floor...」。→ falsify-101 の「text 座標を top-level
  :deps へ移動」が**実在** (deps-repair b1b4b85f の中心変更)。
- **参照側**: src 内 `kotoba.lang.text` require は **132 ファイル** (`git grep -l` → 132)。
- **残 clojure.string**: src 内 `clojure.string` 参照は **0** (`git grep -l` → 0)。移行完了。
- **提供側 sibling**: `/Users/junkawasaki/github/com-junkawasaki/orgs/kotoba-lang/text` 実在。

→ **kotoba.lang.text 配線の結線整合は SURVIVED (宣言 (c) SURVIVED, 新規実測)**。宣言座標
(io.github.kotoba-lang/text, top-level :deps) ・参照 (kotoba.lang.text :as str, src 132 file) ・
移行完了 (clojure.string 残 0) ・提供側 (sibling kotoba-lang/text) の 4 側すべてで結線が揃う。
ただし deps.edn の text 座標は git/sha 指定のみで :git/url が無い (tools.deps は lib 名から Git
repo を導出する規約) — これは deps.edn の他 git-dep (io.github.kotoba-lang/text 等の古参書式) と
同様の記法で、結線整合の新規阻害は実測されなかった。

### 5. 増加源 / avail (運用軸 附帯 — falsify-101 の異常系継続確認)

本反復は実装軸のため avail / unused は本測定対象外。ただし増加源 PID 70643
(itonami-app-resident.cljs) 稼働継続は pre-run サーバ疎通 (server_health:200) と整合
(falsify-101 の異常系 du 調査 Tier 2 要請は継続)。

## 判定

**実装軸**: 宣言 (a) identity.clj ns 修復 — **REFUTED** (JVM Clojure で still Syntax error)。
宣言 (b) kotoba-net 宣言 — **REFUTED** (deps.edn 全 alias に未宣言のまま、http_client.clj:16
require 不変)。宣言 (c) kotoba.lang.text 結線整合 — **SURVIVED (新規実測)**。

→ falsify-101 附帯の「既知 2 build-break の生存再確認」は **両 build-break 生存 (SURVIVED)**
で確定、b1b4b85f の kotoba.text 配線着地は build-break 修復へ「繋がらなかった」。
他方、同一の b1b4b85f で **kotoba.lang.text 配線の結線整合 (宣言/参照/移行残0/提供側) が初めて
独立実測で裏取れた** (実装軸の新規 positive、score 根拠の一部を強化) — ただし JVM suite が
2 build-break で走らないため「実緑測定」にはならず、score は **3 のまま** (実装軸 score は
実緑/checkout 全コンパイル成功でのみ上昇する規約)。

## スコープ修正 / 次の 1 アクション

- 実装軸 score 根拠の anchor を更新: src/test ファイル数は **b1b4b85f 時点で src 214 / test 237**
  (falsify-84 の d226614 基準からリビジョン差を含む実測 — 次の実装軸反復は b1b4b85f を anchor)。
- build-break 記述を再アンカー: 既知 2 build-break (identity.clj 裸 java ベクタ ns 生成形 /
  kotoba-net 未宣言) は **b1b4b85f でも生存** (falsify-63..84 の既知赤継続、kotoba.text 配線
  着地 b1b4b85f では修復されず)。修理案は既存の Tier 2 継続:
  (1) identity.clj の裸 `[java.*]` ベクタを `(:import [java.nio.charset StandardCharsets] ...
  [java.util.concurrent TimeUnit])` に wrap (生成形修復);
  (2) http_client.clj:16 が require する kotoba.net の座標を deps.edn に宣言
  (git sha 或は :dev で :local/root "../../kotoba-lang/kotoba-net")。
  いずれも kanban/human 判断 (Tier 2 提案のみ、本 bot は着地しない)。
- 次の 1 アクション: 次反復は運用/滞留 軸の falsify-101 次の 1 アクション (0 ベース 17 反復超 /
  avail 127 Gi 定着 vs 更なる減退 vs 回復 + structural-decline-monitor) へ戻る。実装軸の次回は
  b1b4b85f を anchor に 2 build-break の生存再確認 (JVM Clojure 1.12.5 macroexpand 検証 +
  kotoba-net deps 宣言 grep) と kotoba.text 結線整合の維持を再実測。
- 附帯 Tier 2: falsify-101 の avail ~29 Gi ステップダウン具体源特定 (TCC 遮断) を異常系として
  kanban/human へ透過提出継続。本反復は「2 build-break が b1b4b85f でも生存」を追加報告。

## 付記 (append-only 規約): falsify-101 追記末尾に新規残骸なし

本反復は maturity.md への追記を作成する時点で wc 実測し、falsify-101 entry の末尾
(evidence/2026-09-09-falsify-101.md) 以降に既存残骸が無いことを確認する。falsify-91 の残骸
(行 1237-1238: `MD` / `echo "append rc=$?" ...`) は append-only 規約により不触。

## 附帯

本体 CHECKOUT (worktree list 先頭
`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app`) は
detached HEAD b5df93e (#299)、`git status --porcelain` 0 行 = porcelain clean (dirty 0)。
origin/main は本反復で **b1b4b85f** (kotoba.text 配線着地、falsify-101 と同値、新規前進なし —
falsify-102 は b1b4b85f を anchor に実装軸を測定)。本反復は実装軸の falsify-101 附帯を測定対象に
選択し、作業は wt-msloop / bot/maturity-sim-loop で完結、本体 checkout を一切 touch せず。
identity.clj malformation / kotoba-net 未宣言 (falsify-63..84) は**本反復で再確認 (両者生存)**、
対応する修理案は上記 Tier 2。7-軸表 16 行目孤立行 placeholder / falsify-46 mid-sentence cut /
falsify-91 末尾残骸 (falsify-101 付記参照) は残存のまま (append-only 規約で本 bot は編集しない)。
(evidence/2026-09-09-falsify-102.md)