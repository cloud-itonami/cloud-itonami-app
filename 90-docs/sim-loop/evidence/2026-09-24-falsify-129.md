# falsify-129 (2026-09-24, テスト 軸)

## 経緯

falsify-128 の次の 1 アクション分岐の前提ステップとして、origin/main diff により
falsify-126/127 の Tier 2 修理 (authority.cljk の JVM :import を nbb 解決可能にする
shim) の着地有無を確認した。運用/滞留 軸の測定ベクトル (df / annex 2 法 / ps) は
本反復では**未実行** — 測定用の結合コマンドがセキュリティスキャン (Tirith) により
実行前に BLOCK された (exit -1、コマンドは発行されず)。捏造ゼロ規約により運用軸の
数値は一切記録しない (緊急フラグ 8 反続判定・annex 静止系列 12 反続判定は本反復では
行わない = 測定ゼロ)。

## 主張 (r)

「origin/main の前進 (414f907 → c4d3e08) により falsify-126/127 の Tier 2 修理
(authority.cljk の nbb :import shim) が着地した」

## 判定: REFUTED

### 実測 (wt-msloop / bot/maturity-sim-loop から読み取りのみ、本体 checkout は一切 touch せず)

- `git fetch origin main` rc=0、origin/main は 414f907 → **c4d3e08** へ前進
  (2 commits: 7023a68 "cljk: rename 1 Clojure source file(s) to .cljk" +
  c4d3e08 "Merge cljk rename (adr-2609111500-cljk-rename-all-clojure-source)")。
- `git diff --stat 414f907..c4d3e08` = **build.cljc => build.cljc.cljk (R100,
  内容変更 0)** + **cljk-origin.edn (521 行変化: -520 / +1)** のみ。
  authority.cljk への変更は diff 中に存在しない。
- `git log --oneline 414f907..c4d3e08 -- '*authority*'` = **0 行** (rc=0、
  前進 2 commits の中に authority 系ファイルへ触れた commit は無し)。
- tip 内容の直接確認 `git show origin/main:src/cloud/itonami/app/authority.cljk`:
  - 行 49: `(:import [java.nio.charset StandardCharsets]` — **bare JVM :import が
    reader 条件分離なしで残存** (falsify-126/127 が赤の原因とした同一 site)。
  - 行 121: `(.digest (.getBytes s StandardCharsets/UTF_8))` — 使用側も不変。
- よって主張 (r) は REFUTED: nbb (Node v26.0.0) で
  `Unable to resolve classname: java.nio.charset.StandardCharsets`
  (authority.cljk:49) が出る決定論的 load 赤の source 状態は tip c4d3e08 でも不変で、
  falsify-126/127 の「assertion 0 実行可能」は現在形として生存。
  suite load〜完走の再測定前提は未充足のまま。

### 附帯測定 (同反復実測、すべて実コマンド出力)

- 本体 checkout: 28105be [fix/ci-install-babashka]、`git status --porcelain` 0 行
  = clean (pre-run script の dirty_files: 0 と整合)。
  `git merge-base --is-ancestor 28105be origin/main` rc=1 — 本体 branch は
  origin/main に未包含 (未着地 branch)。
- wt-msloop: HEAD 72f449b (falsify-128)、porcelain clean、HEAD..origin/main は
  **283 commits** (本体追従は本 bot の管轄外、読み取りのみ)。
- `git worktree list` 実測 3 worktree: 本体 (28105be) /
  /private/tmp/refactor-20260920-1028 (a3dc8c9) / wt-msloop (72f449b)。

## score

テスト 3 のまま (修理未着地の確認は score を動かさない。suite 実緑測定でのみ
上昇する規約)。

## 次の 1 アクション

(1) origin/main diff で authority.cljk 修理着地を再確認 (着地済みなら isolated
worktree で suite load〜完走を再測定)。(2) 未着地なら運用/滞留 軸の測定を
コマンド単純分割 (Tirith のネスト解析上限を避ける) で実施し、緊急フラグ
8 反続の有無と annex 静止系列 12 反続を判別する。
