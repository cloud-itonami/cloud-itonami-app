# falsify-124 (2026-09-20, 運用/滞留 軸)

## 主張 (l)

「df avail の大暴落 (-47.6 Gi) は dns-resolver annex の滞留再燃 (増加) を反映する」

## 判定: REFUTED (annex 静止系列が大暴落下で生存)

同一 path (`~/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`)
で 2 法測定 (falsify-123 修正法: `git annex unused` + `ls .git/annex/tmp`、glob 単独不使用):

- `find .git/annex/objects -type f | wc -l` = **786** (不変, **8 反続静止**)。
- `du -sk .git/annex/objects` = **11403092 KB** — falsify-117..123 全反復と KB 完全同値
  (**8 反続**)。
- dns-resolver HEAD = **c8090649** (不変, 8 反続)。
- `git annex unused`: **14 件の partially transferred 一時ファイルを再列挙** (rc=0)、
  `ls -la .git/annex/tmp`: **14 ファイル実在、mtime すべて Sep 6 19:12-19:27** (不変)、
  `total 365232` (512B blocks) — falsify-123 と完全同値。partial 14 件系列は
  falsify-117..124 を通じて**連続実在** (消滅観測なし)。

annex 側は一切増えていないため、-47.6 Gi の説明に dns-resolver 滞留を帰属させる
主張は REFUTED。大暴落の増加源は**未測定** (下記附帯の通り du 帰属試行がタイムアウト
中断したため、いかなる帰属数値も主張しない)。

## 附帯測定 (同反復実測)

- df /System/Volumes/Data: avail **15738836 KB ≒ 15.0 GiB / 99%**。
  falsify-123 (65652648 KB ≒ 62.6 Gi) 比 **-47.6 Gi**、緊急フラグ (<100 Gi) は
  **3 反続発火**。avail 系列は非単調継続:
  -101.5 → -6.7 → +32.8 → -3.2 → +47.2 → -18.6 → -30.9 → -7.2 → **-47.6 Gi**。
- 帰属試行: `du -x -d1 -k /Users/junkawasaki` を depth-1 実行したが
  **420 秒でタイムアウト中断** (数値取得不可)。帰属は未確立 — 単一帰属主張なし。
- resident: PID **1083** 継続 (Fri03PM 起動、新規遷移なし、9 反点)。
  export_and_sync は不在 (grep 実測、測定ハーネス自身のコマンド行ヒットは除外)。
- app origin/main は本反復未測定 (不変とは主張しない)。

## score

運用 3 のまま (低下・滞留とも測定で裏取れたが修復判断は人間 = Tier 2)。

## 次の 1 アクション

(1) avail 15 GiB / 99% は falsify-17 系の「テスト・スイート実行がディスク飽和に脆弱」
リスクへ直結 — 次反復は du 帰属を timeout 内に収まる浅い測定へ分割するか別手法で
増加源を特定。(2) partial 14 件の 2 法再実測継続、
(3) keys 786 / du 同値 / HEAD の 9 反続静止を判別。
