# falsify-60 (2026-09-07, governor 統合 軸): 「現行 governor は cloud-itonami-app を実 gate 結果に解決し統合が機能している」— 今日の fleet-ci 検証ステップは signer-key 欠損で全数を出せず、cloud-itonami-app は RID 未登録で :input-rejected のみ

測定日: 2026-09-07 (JST, 20:18)
測定者: itonami-maint (cron 反復)
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(agent/fix-open-red-5-three-bound / head 9e5b24a) は読み取り専用参照のみ
(git status porcelain clean / dirty 0)。fleet-ci tick は本体の別系 (~/.gftd + LaunchAgents)
の読み取り参照。touch しない。

## 背景 / 反証対象の主張

maturity.md governor 統合 軸行は falsify-6 (2026-08-14 由来) の「tamaki tick は 1430 repo
を 900s 間隔でスキャン継続、1559 連続 worktree-failed、着地 0 landed 継続」を**現在形のまま**
記したままだ。falsify-54 (governor 軸) は「1559 連続 worktree-failed は過去形に範囲修正」を
結論したが、7-軸 table 行自体は更新されておらず (append-only のため)、行が現在形のまま
陳腐化していた。さらに本反復は falsify-54 後 (同日・別フィールド) の**新規事実**を実測した。

反証対象の主張: 「統合 governor (com.gftd.fleet-ci-tip-tick) は今日も cloud-itonami-app を
CI 検証し、実 gate 結果 (:pass / :input-rejected / :fail) に解決する。統合は機能している
(falsify-54 の 'worktree-failed 0' を sign とする現在形)」— この「検証が実走し
cloud-itonami-app が実 gate 判定を受ける」を再検証する。

本反復で判明したのは、**今日の fleet-ci 検証ステップが全数失敗**しており、かつ
cloud-itonami-app は RID 未登録のため gate 判定にすら載らない、という二つの不健全状態。

## 実測 (すべて実コマンド出力をそのまま記録、捏造なし)

### 1. active governor と現在のログ (falsify-54 と同一: com.gftd.fleet-ci-tip-tick)

`launchctl list | grep -i tamaki` = 0 一致。active は `com.gftd.fleet-ci-tip-tick`
(plist 実測: program /opt/homebrew/bin/nbb、stdout/stderr = ~/.gftd/fleet-ci-tip-tick.stdout.log、
StartInterval 運用)。本体 log `fleet-ci-tick.log` (68,738 B, mtime `Sep 7 20:21:03 2026`) が
本反復時点で流れ続けている。

### 2. 新失敗: signer-key 欠損 → 全 batch ci-verify exit 1 / no receipt

`~/.gftd/fleet-ci-signer-tip.pem` は **MISSING** (`if [ -f ]` 実測 false)。
同日ログ実測:
- `ci-verify exit 1` 回数 = **14**
- `no receipt produced` 回数 = **14**
- `preflight SKIP — --signer-pem points at a missing file` 回数 = **28**
- signer-pem 言及 = 69、mtime 20:10→20:21 で増加継続
- 実証サンプル (fleet-ci-tick.log):
  `Message: ENOENT: no such file or directory, open '/Users/junkawasaki/.gftd/fleet-ci-signer-tip.pem'`

→ 検証 (= green/red 判定を生むステップ) が signer-key 欠如のため**全 batch で失敗**しており、
今日は実 gate 判定が生成されていない。preflight SKIP 側では verify 自体が始まらない。

### 3. cloud-itonami-app は RID 未登録 → gate に載らず :input-rejected のみ

`grep -oE "\[\"cloud-itonami-app\" :[a-z-]+\]"` 実測:
```
   7 ["cloud-itonami-app" :input-rejected]
```
かつ `no RID registered for cloud-itonami/cloud-itonami-app — skipped` = **7 回**
(uniq 集合は cloud-itonami-app / network-awai の 2 種のみ)。
→ falsify-54 が「cloud-itonami-app は :pass/:input-rejected/:fail に解」としたが、
**今日の全 7 gate は :input-rejected 固定**、:pass / :fail はゼロ。「統合が機能している」は
cloud-itonami-app について成立していない。

### 4. worktree-failed は依然 0 (falsify-54 の生存部分)

`grep -icE "worktree.*fail|already exists|worktree add"` fleet-ci-tick.log = **0**。
falsify-54 の「現行実装 (prepare-landing-worktree! --no-checkout+sparse / cleanup --force+prune)
で worktree-failed 0」は本反復でも不変。

## 判定

- **「今日の governor が cloud-itonami-app を実 gate 検証し実結果に解決 (統合継続)」→ REFUTED**
  理由 2:
  (a) 検証ステップは signer-key 欠損 (fleet-ci-signer-tip.pem MISSING) で全 batch `ci-verify
     exit 1` / `no receipt` — 今日、実 gate 判定が生成されていない;
  (b) cloud-itonami-app は RID 未登録 (`no RID registered` ×7) で gate は全 :input-rejected
    固定 (:pass/:fail 皆無)。falsify-54 の「実 gate 結果に解決」は cloud-itonami-app について
    成立せず範囲修正要。
- 7-軸 table 行の「1559 連続 worktree-failed / 毎 tick / 着地 0 継続」は falsify-54 の範囲修正
  が行に未反映で現在形のまま陳腐化 (append-only のため本 bot は既存行を削除せず、根拠セルへ
  falsify-60 の実測を追記更新する)。
- governor 統合 軸 score は 3 のまま。worktree 構造的排除は維持 (falsify-54 継続) する一方、
  検証 step の signer 欠如と RID 未登録が統合実効値を下げる不健全状態を新規記録。score は
  実 gate 生成ゼロを反映すべきほどの低下を即断せず、測定・提言に留める。

## スコープ修正 (maturity.md governor 統合 行へ反映 + 新リスク)

governor 統合 行 根拠セルへ追記更新:
「(1559 連続 whole-tree fail は falsify-54 で過去史と確定、現 head 構造は worktree-failed 0。
新規 2 不健全 — falsify-60):
  (1) fleet 検証ステップ signer-key (fleet-ci-signer-tip.pem) MISSING → 全 batch ci-verify
      exit 1 / no receipt (14/14/28)、当日実 gate 判定 0;
  (2) cloud-itonami-app は repos.edn rad-rids に RID 未登録 → gate は全 :input-rejected
      (7/7)、:pass/:fail 皆無。」

新規 Tier 2 提案 (両方 tamaki/fleet 側設定・kanban/human 判断、本 bot は plist/repos.edn
非 touch):
  A) fleet-ci-signer-tip.pem の配置 (または --signer-pem 指し替え) — 検証ステップの全数失敗を
     解消;
  B) cloud-itonami-app を repos.edn rad-rids に登録 — 現在 governor 統合 (issue 登録等) の
     対象外で、再開には RID 登録が必要。

## 次の 1 アクション

- governor 統合の実効を復旧する Tier 2 提起 (fleet signer-key 配置 + cloud-itonami-app の RID
  登録。両者とも tamaki/fleet 側設定、operator 判断)。本体 checkout には一切触れない。