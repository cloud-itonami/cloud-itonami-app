# falsify-105 (2026-09-09, 運用/滞留 軸 — 0 ベースの 20 反復超連続維持 (21 時点観測)、avail は 116→113 Gi で継続低下 (減速 -5.50→-3.41 GB)、100 Gi 未満緊急フラグ未到達)

測定日: 2026-09-09 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app, detached b1b4b85) は読み取り専用参照のみ、
porcelain 0 行 = clean、一切 touch せず。

## 背景 / 測定対象

falsify-104 は 3 ベクトル正規 unused 0・local annex keys 786 不動の 20 時点観測 / 19 反復
連続維持を再実測し、avail の継続低下 (122→116 Gi) を記録した。その次の 1 アクションは
「同 vector + keys + avail を再実測し **19 反復超の連続持続** or 再蓄積再開を判別、avail
116 Gi の更なる継続低下 (100 Gi 未満で緊急フラグ) vs 減速したままの低位定着 (116 Gi 前後)
vs 回復 (148-163 Gi 帯、structural-decline-monitor 再評価) を観測」だった。

本反復 (falsify-105) はその 1 アクションを執行する。

**主張**: 本反復の再実測で (a) 3 ベクトルのいずれかに正規 unused が再出現する、または
(b) local annex keys が 786 から増加する (19 反復超への持続は成立せず残留再蓄積を開始)、
または (c) avail が falsify-101/103/104 の低位から **回復して 148-163 Gi 帯へ戻る**
(falsify-101 の -29 Gi ステップダウンが一過性であったことを示す)。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD detached 566928f6701df95d8cf0b62c5455c9d0baf46226、falsify-98..104 と同一、
porcelain 0 行 = clean、resident branch は `resident/dns-resolver`)。

### 1. main-refspec regular unused

`git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys** (出力は partial
transfer 残骸 14 chunk のみ、NUMBER 1..14 の MD5E-s... 14 行、falsify-98..104 と同一集合)。
`ok` / EXIT_RC=0。

### 2. 全 refs refspec (主張 (a) ベクトル)

`git annex unused --used-refspec "+refs/*"` → 正規 unused **0 keys** (partial 14 chunk のみ、
同一 14 key 集合)。`ok` / EXIT_RC=0。

### 3. local annex keys (主張 (b) ベクトル)

`git annex info` → **local annex keys: 786** (増加 0)、local annex size **11.68 GB** (不変)、
annexed files in working tree 7143 / 93.02 GB、combined 111.12 GB、
temporary object directory 186.98 MB。available local disk space **121.58 GB**
(falsify-104 の 124.99 GB 比 **-3.41 GB**)。

### 4. avail (主張 (c) ベクトル)

`df -m /` → avail **116081 MB ≒ 113 Gi** (falsify-104 の df 116 Gi 比 **約 -3 Gi**)。
100 Gi 未満の緊急フラグ未到達。148-163 Gi 帯への回復は **発生せず**、falsify-103/104 の
継続低下が 3 反復連続で延長。ただし低下幅は -29 (f101) → -5 (f103) → -6 (f104) → **-3 Gi**
と単調に減速中。

### 5. 滞留増加源

`ps aux | grep -E 'itonami-app-resident|export_and_sync' | grep -v grep` → 増加源
**PID 70643 のみ** (nbb itonami-app-resident.cljs、falsify-85..104 と同一 PID、
export_and_sync 不在)。本反復の測定ハーネス自身の行は grep -v grep で除外し自助マッチ混入なし。

### 6. 附帯 (実装軸)

origin/main は **b1b4b85f2103b72746bd5a35aff857092a370aab のまま**
(falsify-102/103/104 と同一、3 反復連続 no-advance)。本体 checkout porcelain 0 行 = clean。

## 判定

- **主張 (a) REFUTED**: 正規 unused 0 のまま (partial 14 chunk のみ、再出現なし)。
- **主張 (b) REFUTED**: keys 786 → 786、増加 0。→ falsify-104 指定の閾値「19 反復超の
  連続持続」を充足 (20 反復 > 19)、0 ベース恒久化傾向を更に精緻化 (**SURVIVED**、
  21 時点観測 / falsify-85→105)。
- **主張 (c) REFUTED**: avail の 148-163 Gi 帯回復は発生せず、116 → 113 Gi で継続低下。
  falsify-101 の -29 Gi ステップダウンは一過性でなく、小幅 (-5→-6→-3 Gi) に減速したまま
  3 反復連続低下中と確定。structural-decline-monitor 経過観測継続 (100 Gi 未満で緊急フラグ)。
  local annex keys 786 / 11.68 GB 不変ゆえ低下は annex 外部 (TCC 遮断で特定不能、
  operator の du 調査を異常系・優先度高で Tier 2 継続提出)。

score 3 のまま (0 ベース恒久化傾向の更なる精緻化、実緑測定でのみ上昇する規約、
avail 継続低下は risk-5 / structural-decline-monitor 経過観測として記録)。

## 次の 1 アクション

同 vector + keys + avail を再実測し **20 反復超の連続持続** or 再蓄積再開を判別、
avail 113 Gi の更なる継続低下 (100 Gi 未満で緊急フラグ) vs 減速したままの低位定着
(113 Gi 前後) vs 回復 (148-163 Gi 帯、structural-decline-monitor 再評価) を観測。
