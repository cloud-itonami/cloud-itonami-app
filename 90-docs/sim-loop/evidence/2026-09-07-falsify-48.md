# falsify-48 — 運用/滞留帰属: "unused ⊆ resident 現在ツリー" が 444-key / 6.24 GiB スケールで生存、増加継続 (2026-09-07)

測定者: itonami-maint
軸: 運用 (・レジストリ機能の実体は `cloud-itonami-dns-resolver` annex)
ターゲット: falsify-25/26/28/29/30/31/34/35/36/37/39/40/41/42/43/44/45/46 の帰属主張
「main-refspec で unused 化した滞留は全件が resident/dns-resolver 現在ツリー (data/ledger/) 参照」
検証方法: 実リポジトリ `/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`
で `git annex unused` を 3 通りの refspec で実行し、正規 unused key 数・総バイトを機械集計。

## 測定 (実測値、捏造なし)

リポジトリ: cloud-itonami-dns-resolver (git head `1bc43874c [DATALAD] new dataset`)

| 測定項目 | 実測値 |
|---|---|
| `git annex info` local annex keys | **444** |
| `git annex info` local annex size | **6.7 GB** (working tree 33.59 GB / temp 186.98 MB) |
| main-refspec (`+refs/heads/main`) 正規 unused | **444 keys / 6,699,301,080 bytes (≈6.24 GiB)** |
| resident-refspec (`+refs/heads/resident/dns-resolver`) 正規 unused | **0** |
| 全 refs 走査 (`git annex unused` refspec なし) 正規 unused | **0** (いかなる ref からも到達不能な孤児 0) |
| partial-chunk (temporary) 数 | **14** (temp 186.98MB、不変) |
| 増加源 itonami-app-resident.cljs | PID **94682** 稼働継続 (export_and_sync 不在) |
| dev /health 127.0.0.1:1338 | 200 |
| gateway /health 127.0.0.1:8080 | 404 (本反復実測。前反復記録では 502、経緯は継続観測対象) |
| df avail (/System/Volumes/Data) | **140Gi / 85%** |
| load avg | 60.88/64.71/58.63 (高負荷) |

集計ロジック: `git annex unused --used-refspec ...` の出力から正規 key 行
(`MD5E-s<N>--...`) のみ awk で ≪ partial-chunk (`-S*-C*-`) を除外 ≫ して key 数と
`MD5E-s<N>` の N (size marker) を合計。合計 = 6,699,301,080 bytes、key 数 = 444。

## 判定

**SURVIVED (帰属不変 + 増加継続)**
- main-refspec 正規 unused は **444 keys / 6.24 GiB** (falsify-46 の 394 keys / 5.53 GiB から
  **+50 keys**)。falsify-47 は spec 軸だったため retention 測定は falsify-46→48 の 2 反復跨ぎ
  で +50 keys ≈ 反復あたり +25 (falsify-40/41/43/44/45 の +24/+25 帯と整合、+62 加速への回帰なし)。
- resident-refspec 正規 unused = **0** → 滞留の全件が resident 現在ツリー参照 (unused ⊆ resident、
  unused−resident = 0)。falsify-25 以来の帰属が 444-key / 6.24 GiB スケールで生存継続。
- 全 refs 走査 (refspec なし) の正規 unused = **0** → main-refspec で unused 判定された key のうち
  いかなる ref からも到達不能な孤児は存在しない (falsify-46 の独立ベクトルを再確認)。
- partial chunk は **14 → 14** / temp 186.98MB で不変 (falsify-35 の 1 chunk 解決以降 **連続 12 反復
  drift なし**、間欠的のまま)。

## 次の 1 アクション

引続き 1 tick あたり ≈ +25 keys の増加を観測する (増加源 PID 94682 は稼働継続)。恒久解消は
変わらず resident ingest の世代削除/方針見直し (データ削除判断 → Tier 2、operator)。df avail 140Gi
はフルスイート実行可能域を維持。falsify-17 の最小 avail 閾値下界実測は次の候補のまま。

## 附帯

- 7 軸テーブル 16 行目孤立行 `| 反証候補falsify30PLACEHOLDER` 残存のまま (operator 復旧待ち、
  falsify-33/34 Tier 2 report 済み、append-only で本 bot は編集しない)。
- 前反復 falsify-46 の本体記録は mid-sentence で truncate (maturity.md 456 行目
  `df avail は 141Gi / 85` で途切れ、falsify-47 で報告済み)。本反復は falsify-48 として
  完全文で記録する。
- 本体 checkout は agent/fix-open-red-5-three-bound / PORCELAIN CLEAN (dirty 0 実測)、
  head 9e5b24a。