# falsify-41 (2026-09-07): 滞留 (regular unused) の増加継続 + falsify-25 帰属の生存再確認 (234-key / 3.31 GiB scale)

測定日: 2026-09-07 01:35 JST (cron 反復)
測定者: itonami-maint (軸: 反証 + 運用 + リスク-5 + falsify-40 NEXT 残)
対象 repo: cloud-itonami-dns-resolver (read-only コマンドのみ。dropunused/drop/履歴改変/ingest 停止は一切実施しない)
作業は sibling worktree (wt-msloop, bot/maturity-sim-loop) で完結。本体 checkout 未 touch。本体は agent/fix-open-red-5-three-bound で porcelain clean を確認 (pre-run dirty_files 0)。

## 反証すべき主張 (falsify-37/38/39/40 由来、継続)

1. **「滞留 (main-refspec regular unused) は増加継続し、falsify-25 の帰属 (unused ⊆ resident 現在ツリー)
   は増加後も生存する」**
   - main-refspec unused が増加しつつ resident-refspec regular unused = 0 のままなら帰属 SURVIVED。
   - resident 外の unused が生じるなら帰属破綻 (REFUTED)。

2. **「partial-transfer chunk 数は不変 (drift 停止継続、間欠的)」**
   - falsify-35 の 1 chunk 解決 (15→14) 以降、falsify-36〜40 は 14→14 継続。
   - 本反復で 14 のまま不変 = 間欠的 (SURVIVED)、増加 = drift 再開 (REFUTED)。

3. **「avail の変動は増加源 (PID 94682) の稼働と独立に生じる (単一帰属は未特定のまま)」** ※継続観測

## 実測 (実コマンド出力、2026-09-07 01:35 JST)

1. **df -h /System/Volumes/Data**: `Size 926Gi / Used 736Gi / Avail 150Gi / Capacity 84%`
   (`df -k` avail = 157,591,588 KiB ≈ 150.3Gi)。falsify-40 の 152Gi → 本反復 **150Gi**、
   反復越え -2Gi 漸減。100% 飽和は解消のまま。

2. **git annex info**: local annex keys **234** / size **3.55 gigabytes** (falsify-40 の 210 /
   3.18 GB から **+24 keys / +0.37 GB**)。temporary object directory size **186.98 megabytes**
   (falsify-40 と同値、14 チャンク temp)。working tree annex 33.59 GB (不変)。

3. **git annex unused --used-refspec=+refs/heads/main** (rc=0):
   - 正規 unused key: **234 個** / bytes **3551064161** (≈3.31 GiB、python/grep 集計実測 /
     falsify-40 の 210 keys / 3,184,346,441 bytes から **+24 keys / +0.34 GiB**、増加継続)
   - partially transferred data: **14 個** (MD5E-s*-S52428800-C1-- チャンク temp、falsify-40 と同数)

4. **git annex unused --used-refspec=+refs/heads/resident/dns-resolver** (rc=0):
   - 正規 unused key: **0 個** (partially transferred のみ 14 個)
   - → 帰属「unused ⊆ resident 現在ツリー (data/ledger/)」が 234-key / 3.31 GiB スケールで
     生存継続。main-refspec unused 234 全件が resident-refspec では used。
   - partial-transfer 14 件の合計 bytes = 186,977,095 (python 集計、annex info の temp 186.98MB と整合)

5. **増加源の直接観測 (ps)**: `itonami-app-resident.cljs` PID **94682** 稼働継続
   (falsify-33〜40 と同一 PID、nbb 経由)。`export_and_sync.cljs`: 該当なし (grep_rc=1)。

6. **dev server**: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:1338/health` = **200** (稼働)。

## 判定

### 主張 1 (帰属生存 + 増加継続) → **SURVIVED (帰属不変、増加継続)**
- main-refspec regular unused は 234 keys / 3.31 GiB へ**増加** (falsify-40 210 → +24 keys)。
- 同一時刻の resident-refspec regular unused = **0** — 増加後も 234 全件が resident 現在ツリー
  (data/ledger/) 参照のまま。falsify-25 の帰属は 234-key / 3.31 GiB スケールで**生存継続**。
- 増加源 itonami-app-resident.cljs (PID 94682) は同一 PID で稼働継続 — 帰属妥当。

### 主張 2 (chunk drift 停止継続) → **SURVIVED (drift は間欠的のまま)**
- chunk 数は falsify-40 の **14 → 14** で不変。temp 合計 186.98MB (annex info、falsify-40 同値)。
- falsify-35 の 1 chunk 解決 (15→14) 以降、連続 6 反復で drift なし —「drift は毎反復継続する
  単調現象」は引き続き REFUTED、間欠的である。

### 主張 3 (avail 単一帰属は未特定のまま) → **観測継続 (本反復でも特定不能)**
- avail は falsify-38 170Gi → falsify-39 154Gi → falsify-40 152Gi → 本反復 **150Gi / 84%** と漸減
  (反復越え -2Gi)。増加源 (PID 94682) は稼働継続のままの消費 —「avail 回復 = 増加源停止」は
  falsify-37 で既に反証済みのまま不変。解放源 (falsify-30/37 の大回復) の単一起因帰属は依然未特定。

## Tier 2 提言 (unchanged)

- 恒久解消には増加源 (resident ingest の data/ledger 世代削除/方針見直し) が必須。
  データ削除判断を伴うため operator/kanban 判断 (Tier 2)。dropunused は反復緩和として
  引用継続可能。本反復の実測は read-only で Tier 2 以内に留めた (着地なし)。
- avail 150Gi / 84% は引続きフルスイート実行可能域 (falsify-38 は 170Gi で 0 failures 実測済み)。

## 附帯

- 7 軸テーブル 16 行目の孤立行 `| 反証候補falsify30PLACEHOLDER` は残存のまま
  (append-only 規約で本 bot は編集せず、operator 復旧待ち — falsify-33〜40 report 済み)。
- 本体 checkout は agent/fix-open-red-5-three-bound で porcelain clean (dirty 0) を確認。
- 本反復の負荷: load averages 55.26 / 55.99 / 48.65 (環境の高負荷は継続、工程中に変動)。