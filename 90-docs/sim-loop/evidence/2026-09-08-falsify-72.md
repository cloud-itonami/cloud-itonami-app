# falsify-72 (2026-09-08, spec/契約 軸)

falsify-71 (head 679572b) は transport migration 後のコマンド契約表面 (commands.edn
208→209 / placeholder 128=128 / cli-aliases.edn byte 一致) を再アンカーし、次アクション
として「追加コマンド workspace resources の gate :app 契約が server.clj ルート
(routes 408→409) と一致する」の突合を挙げた。本反復でその突合を実行した。

**主張: 追加コマンド workspace resources の :gate :app 契約は、server.clj ルート
(routes 408→409、migration で +1 された単一ルート) の gate と一致する。**

## 実測 (リダイレクト記録、捏造なし)

HEAD = origin/main = 679572b (rev-parse 実測、falsify-63..71 と同一 SHA、本体 checkout
porcelain clean / dirty 0)。

1. `git diff 9e5b24a 679572b -- src/cloud/itonami/app/server.clj` (実測): 単一 hunk
   `@@ -4390,6 +4390,17 @@` のみ — **`;; ── resources ──` コメント付きで
   `(and (= method "GET") (= path "/api/workspace/resources"))` + `(let [session
   (require-app-session! exchange)] (send! exchange 200 (bots/resources session)))`
   が (drive-snapshot と esign の間、4398-4401 行目) 新規追加。これが routes
   408→409 の +1 ルートであり、追加コマンド 1 件 (workspace resources、falsify-71)
   と一対一対応。
2. `route_scan.cljc` の gate 導出を実測: `gate-of` (route_scan.cljc:131-145) が
   body 文字列の `require-app-session!` 含有 → `:app` (行 144-145)、
   `routes` (行 265) が各ルートに `(merge {:method method :route path :gate
   (gate-of body) ...})` を付与 (行 282)、`registry` は `:counts {:routes (count all)
   ...}` (行 310-313) と `:app`/`:session` gate のみをコマンド公開 (行 303)。追加ルート
   の body は `require-app-session!` を含むため generically `:app` に導出される。
3. チェックイン済み commands.edn (tip 679572b、hash c44cc35 = falsify-71 実測) の
   workspace resources エントリを機械抽出: `{:command ["workspace" "resources"],
   :method "GET", :template "/api/workspace/resources", :params [], :flags [],
   :gate :app, :route "/api/workspace/resources"}` — **ちょうど 1 件** (tr 分割 grep
   実測 count=1)。
4. **実行可能検証 (detached worktree /private/tmp/mt-msloop72、head 679572b、本体
   checkout 未 touch)**: 同一の scanner (route-scan) を clojure で直接実行し
   `(scan/registry (slurp sf) sf)` を評価 → `:counts {:routes 409, :commands 209,
   :human-only 70, :unauthenticated 130}` (チェックイン済みと一致)、
   `workspace-resources-cmd` = `{:command ["workspace" "resources"], :method "GET",
   :template "/api/workspace/resources", :params [], :flags [], :gate :app, :route
   "/api/workspace/resources"}`、`(scan/routes src)` の workspace resources ルート =
   `{:method "GET", :route "/api/workspace/resources", :gate :app, ...}` (EXIT_RC=0、
   /tmp/wr9.txt 実測)。
5. **complete 等価性 (commands-test の gate そのもの)**: `fresh (scan/registry (slurp
   sf) sf)` とチェックイン済み `commands.edn` (edn/read-string) を clojure で比較 →
   `:equal true` / 両方の workspace resources gate `:app` (EXIT_RC=0、/tmp/wrA.txt
   実測)。scan を tip server.clj に再実行しても リポジトリは完全一致 = `commands-test
   fails when this file no longer matches the routes` の不変量が現 head で成立。

## 判定

SURVIVED。追加コマンド workspace resources の `:gate :app` 契約は server.clj ルート
(GET /api/workspace/resources、require-app-session!、routes 408→409 の +1 単一ルート)
と完全一致 — ① git diff で +1 ルートの実体特定、② route_scan の gate-of
(require-app-session!→:app) 機械導出、③ workspace resources エントリ gate :app ちょうど
1 件、④ 実行可能な scan→registry 生成で counts/gate 一致、⑤ fresh 再スキャンと
チェックイン済み commands.edn の `= true` 等価、の 5 層で突合完了。falsify-71 の再アンカー
(209 コマンド / placeholder 128=128 / cli-aliases byte 一致) に加え、追加コマンド自体が
server.clj ルートと機械ゲート一致することを決定論的に確認。spec/契約 軸 score は 3 のまま
(falsify-56/71 の範囲修正に本突合を追記、質を覆す変更なし)。

注記 (捏造なし・既知赤): `commands_test.clj:39-47 the-registry-matches-the-routes` が
この不変量の設計上の機械ガードだが、現 tip は identity.clj の既知 build-broken
(falsify-63..68) により :test フルスイートがコンパイル不能で green 実行は不可。本反復は
同一 scanner を直接実行して不変量を実測 (④⑤) — テスト green 実行の代用ではなく本体の
不変量を直接確かめたもの であり、上記起動は detached worktree の clojure -M 単発で
identity を transitively に pull しない (route-scan は clojure.string のみ require、
commands は clojure.edn のみ)。

## 次の 1 アクション

spec 軸は「契約表面 ↔ server.clj ルートの機械ゲート一致」まで突合完了。次の反復は
OPEN 赤-2 (expiry-alert) の案 A 着地後 rc=0 再確認 (falsify-11 終点判定継承)、
または falsify-62 の bundle/graph 赤 (bundle published-lock 再発行) 解消確認
(identity.clj 修理後に実行可能となる) へ。identity.clj malformation (falsify-63..68
の build-broken tip、origin SHA 不変) は Tier 2 kanban/human 修理待ちの既知赤のまま。

## 附帯

本体 checkout は反復中 porcelain clean (dirty 0)、本 bot は touch せず wt-msloop /
bot/maturity-sim-loop のみで完結。実行 worktree /tmp/mt-msloop72 は remove --force /
prune 済みで worktree list に残存せず (本体は 679572b detached HEAD のまま)。