# falsify-121 (2026-09-19, 運用/滞留 軸)

## 主張 (h)

「dns-resolver annex の partially-transferred 一時ファイル 14 件は系列静止の一部として持続する
(falsify-117/118/119/120 の 3 反続同一実測の現在形)」

## 判定: REFUTED

同 path (`github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver/.git/annex`) での
同一コマンド再実測:

- `find .git/annex -name '*.tmp' -o -name '*.part' | wc -l` = **0** (前反復まで 14 件、3 反続で同一)
- keys: `find .git/annex/objects -type f | wc -l` = **786** (不変, 5 反続静止)
- du -sk objects = **11403092 KB ≒ 11136 MiB ≒ 10.9 GiB** (falsify-118 の 11320 MB 比 +約 85 MB,
  帯内 — falsify-117 の 11136 MB 表記と同一量級)
- dns-resolver HEAD = **c8090649** (不変, 5 反続)

14 件の一時ファイルは消滅していた。地上の前提「3 反続で同一だった一時ファイル集合は静的資産」は
現在形として成立しない。dropunused 判断候補 (~135 MB) は本反復時点で実在しない — falsify-117 で
人間へ Tier 2 提案した dropunused 候補は消滅済みとして記録 (削除主体は未測定)。

## 附帯測定 (同反復実測)

- df /System/Volumes/Data: avail **108807940 KB ≒ 103.75 GiB / 89%**。
  falsify-120 (125298 MB ≒ 122.3 Gi) から **-18.6 Gi** の低下方向へ反転。
  緊急フラグ (<100 Gi) は **不発火 2 反続** (103.75 Gi > 100 Gi、余裕約 3.75 Gi で閾値近傍に再接近)。
  avail 系列は引き続き非単調: -101.5 → -6.7 → +32.8 → -3.2 → +47.2 → **-18.6 Gi**。
  単一帰属は未確立のまま (falsify-118/120 継続、捏造ゼロ)。
- resident: PID **1083** 継続 (falsify-120 実測から新規遷移なし)。ps 実測
  `node .../org-babashka-nbb/cli.js .../scripts/itonami-app-resident.cljk`。

## score

運用 3 のまま (低下は測定で裏取れたが修復判断は人間 = Tier 2)。

## 次の 1 アクション

同測定を再実測し、(1) -18.6 Gi 減速が定着するか / 100 Gi を切って緊急フラグが再発火するか、
(2) partial temp 0 が定着するか、(3) keys 786 静止が 6 反続に達するかを判別。
