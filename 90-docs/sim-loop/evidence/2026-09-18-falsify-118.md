# falsify-118 (2026-09-18, 運用/滞留 軸)

## 主張 (反証対象)

主張 (e): 「falsify-117 → 本反復で観測された avail の大回復 (+32.8 GiB 級) は
dns-resolver annex 系列の変化 (key 追加/削除、partial 一時ファイルの掃除) に帰属する」—
帰属を直接測定で試みる (falsify-117 の「因果は測定不能」の次の 1 アクション)。

## 実測

- 時刻: 2026-09-18 06:43:35 JST (`date` 実測)
- `df -m /System/Volumes/Data`: 1M-blocks 948584, Used 811561, **Available 81366, Capacity 91%**
  - falsify-117 の 48541 MB ≒ 47.4 Gi → **81366 MB ≒ 79.5 Gi、反復間 +32.8 Gi 級の回復**
  - ただし **緊急フラグ (<100 Gi) は 3 反続で発動中** (79.5 Gi < 100 Gi)
- **帰属検証 (主張 (e))**: dns-resolver annex を前反復と同一コマンドで再実測:
  - `find dns-resolver/.git/annex/objects -type f | wc -l` → **786** (falsify-117 と同値、
    増減 0。788 行出力 = 786 行 + find_rc + du 行で「ちょうど 786」を再確認)
  - `du -sm dns-resolver/.git/annex` → **11320 MB** (falsify-117 の 11136 MB に対し
    +184 MB。うち `.git/annex/tmp` は **179 MB** 実測 — partial 一時ファイル側の微増と整合)
  - `git -C dns-resolver annex unused` (rc=0) → **unused object 0 件**、
    partially-transferred 一時ファイル **14 件** (MD5E-*.edn、falsify-117 の 14 件と同数)
  - → 系列は「key 増加 0 / unused 0 / partial 14 件」でほぼ不変。**32.8 GiB 級の開放を
    dns-resolver annex から放出できる要素は皆無** (総容量 11.0 GiB で、全滅しても説明量の
    1/3 にも届かない)
  - → **主張 (e)「avail 回復は dns-resolver annex 系列に帰属」は REFUTED**。
    回復の起点は annex の外部。単一帰属は本反復でも確立しない (falsify-30/31 の教訓どおり
    「停止→回復」を充分条件と読まない)。
- 附帯:
  - **app origin/main = 1ac45963** (ls-remote 実測、falsify-117 の fbdb91f から advance)。
    直接 URL `https://github.com/junkawasaki/cloud-itonami-app.git` は `Repository not found`
    (rc=128) で superproject の origin 経由のみ到達可能 — 参照経路の注記として残す。
  - dns-resolver HEAD = **c8090649** (不変、rev-parse 実測)
  - resident: PID **12978** 稼働継続実測 (ps aux、falsify-116 以降同一 PID、新規遷移なし)

## 判定

- 主張 (e)「avail 大回復は dns-resolver annex に帰属」→ **REFUTED**
  (786 keys 不変 / unused 0 / du +184 MB のみで開放 0 を実測)
- 精緻化: avail 系列に falsify-116 (-101.5 Gi) → falsify-117 (-6.7 Gi) → 本反復
  (+32.8 Gi) の非単調振動が続く。annex 外部の要因が双方向に働いている可能性が高いが、
  帰属先の特定には至らない。緊急フラグ <100 Gi は 3 反続。
- 次の 1 アクション: 同一測定 (df / find 786 / unused / du tmp) を再実測し
  「annex 外部の非単調振動」の継続を観測、緊急フラグ 4 反続の有無を確認。
  帰属候補を絞るには annex 外の大容量ディレクトリ (m365-archive 等) の du 変化を
  附帯測定に加える。
