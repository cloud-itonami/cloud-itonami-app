# falsify-36 (2026-09-06): falsify-35 が残した「partial chunk は drift 継続で正規鍵へ解決」+ 「滞留は増加継続」を反証試行 + falsify-25 帰属の再生存確認

測定日: 2026-09-06 22:28 JST (cron 反復)
測定者: itonami-maint (軸: 運用 + リスク-5 + falsify-35 NEXT 残)
対象 repo: cloud-itonami-dns-resolver (read-only コマンドのみ。dropunused/drop/履歴改変/ingest 停止は一切実施しない)
作業は sibling worktree (wt-msloop, bot/maturity-sim-loop) で完結。本体 checkout 未 touch。

## 反証すべき主張 (falsify-35 由来)

1. **「partial-transfer chunk の drift は継続し、14 個の変化が見られるか」**
   → falsify-35 は chunk 15→14 (1 chunk 解決) を実測し「drift は低速だが実在」と判定。
   → 本反復で chunk 数がさらに減る (14→13 以下) = drift 継続 SURVIVED、
      不変 (14 のまま) = この反復では drift 停止 (前反復での解決は一時的/間欠)。

2. **「滞留 (regular unused) は増加継続し、falsify-25 の帰属 (unused ⊆ resident 現在ツリー)
   は増加後も生存する」**
   → main-refspec unused が増加しつつ resident-refspec unused = 0 のままなら帰属 SURVIVED。
   → resident 外の unused が生じるなら帰属破綻。

## 実測 (実コマンド出力、2026-09-06 22:28 JST)

作業ファイル /tmp/unused.txt に git annex 出力の全記録を保存。

1. **df -h /System/Volumes/Data**: `Size 926Gi / Used 813Gi / Avail 73Gi / Capacity 92%`
   (falsify-35 終了時 avail 74Gi / 92% → 本反復 73Gi / 92%、-1GiB 微減)

2. **git annex info**: local annex keys **99** / size **1.48 gigabytes** (falsify-35 の 75 /
   1.11GB から **+24 keys / +0.37 GB**)。temporary object directory 186.98MB。

3. **git annex unused --used-refspec=+refs/heads/main** (rc=0):
   - 正規 unused key: **99 個** / bytes **1482952762** (≈1.38 GiB、falsify-35 の 75 から +24)
   - partially transferred data: **14 個** (S52428800-C1-- チャンク temp、falsify-35 と同数)
   - tmp ディレクトリ実測: `du -sh .git/annex/tmp` = **178M** / ファイル数 **14** (不変)

4. **git annex unused --used-refspec=+refs/heads/resident/dns-resolver** (rc=0):
   - 正規 unused key: **0 個** (partially transferred のみ 14 個)

5. **増加源の直接観測 (ps)**: `itonami-app-resident.cljs` PID **94682** 稼働継続
   (falsify-33/34/35 と同一 PID)。`export_and_sync.cljs`: 該当なし (grep_rc=1)。

6. **dev server**: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:1338/health` = **200** (稼働)。

## 判定

### 主張 1 (chunk drift 継続) → **REFUTED (本反復では drift 停止)**
- chunk 数は falsify-35 の **14 → 14** で不変。tmp du も 178M / 14 ファイルで変化なし。
- 前反復 (falsify-35) で観測した 15→14 の 1 chunk 解決は、本反復では反復されず、
  drift は**間欠的**である (毎反復連続する単調現象ではない)。
- ただし regular unused は +24 と増加しており、滞留増加は継続 (主張 2 参照)。

### 主張 2 (帰属生存 + 増加継続) → **SURVIVED (帰属不変)**
- main-refspec unused は 99 keys / 1.38 GiB へ**増加** (falsify-35 75 → +24)。
- 同一時刻の resident-refspec unused = **0** — 増加後も 99 全件が resident 現在ツリー
  (data/ledger/) 参照のまま。falsify-25 の帰属は 99-key / 1.38 GiB スケールで**生存継続**。
- 増加源 itonami-app-resident.cljs (PID 94682) は同一 PID で稼働継続 — 帰属妥当。

総合: **falsify-25 の帰属は再蓄積 scale (99 keys / 1.38 GiB) で SURVIVED 継続**。
chunk drift は間欠的 (本反復では停止)。dropunused は引き続き反復緩和であり、
増加源 (resident ingest) が存続する限り滞留の増加と再蓄積は続く。

## Tier 2 提言 (unchanged)

- 恒久解消には増加源 (resident ingest の data/ledger 世代削除/方針見直し) が必須。
  データ削除判断を伴うため operator/kanban 判断 (Tier 2)。dropunused は反復緩和として
  引用継続可能。本反復の実測は read-only で Tier 2 以内に留めた (着地なし)。

## 附帯

- 7 軸テーブル 16 行目の孤立行 `| 反証候補falsify30PLACEHOLDER` は残存のまま
  (append-only 規約で本 bot は編集せず、operator 復旧待ち — falsify-33/34/35 report 済み)。
- avail 73Gi / 92% はフルスイート実行可能域継続 (falsify-31 の最小 avail 閾値実測は
  引き続き現実的選択肢)。