# falsify-59 (2026-09-07, テスト 軸): OPEN 赤-4 (launcher WORKSPACE_ROOT shadow) が head 9e5b24a で決定論的に再現 — 修理案 A 着地説 REFUTED

測定日: 2026-09-07 (JST, 19:16)
測定者: itonami-maint (cron 反復)
作業は sibling worktree (`/Users/junkawasaki/.hermes/profiles/itonami-maint/wt-msloop`,
`bot/maturity-sim-loop`) で完結。本体 checkout (`agent/fix-open-red-5-three-bound` / head
`9e5b24a`) は読み取り専用参照のみ (確認: `git status --porcelain` 0 行 / porcelain clean)。
touch しない。

## 背景 / 反証対象の主張

OPEN 赤-4 (launcher_test.clj:148 `resident-clone-resolves-shell-from-workspace-root`) は
falsify-8 (2026-09-04) で「実バグ」と帰属確定済み: `bin/cloud-itonami-app` の shell 解決が
repo 相対分支 (`$app_dir/../../kotoba-lang/shell`) を `CLOUD_ITONAMI_WORKSPACE_ROOT` より
優先し、WORKSPACE_ROOT 必勝のテスト契約と矛盾。修理案 A (launcher を WORKSPACE_ROOT 優先に)
/ 案 B (テストを repo 相対優先契約に改訂) は kanban/human 判断待ち (Tier 2) と記録され、
falsify-16 の緑は /private/tmp worktree (shadow レイアウト無し) のため反証ではないとされた。

本反証の対象主張:
**「head 9e5b24a で修理案 A が着地し、launcher は WORKSPACE_ROOT を優先するようになった
(OPEN 赤-4 は解決済み)」** — この着地仮説を反証試行する。

## 実測 (すべて実コマンド出力をそのまま記録、捏造なし)

### 1. bin/cloud-itonami-app の shell 解決順序 (head 9e5b24a、実コード)

`sed -n '30,70p' bin/cloud-itonami-app` 実測 (抜粋):

```bash
shell_dir="${CLOUD_ITONAMI_SHELL_DIR:-}"
if [[ -z "$shell_dir" && -d "$app_dir/../../kotoba-lang/shell" ]]; then   # repo 相対が先
  shell_dir="$(CDPATH= cd -- "$app_dir/../../kotoba-lang/shell" && pwd)"
fi
if [[ -z "$shell_dir" && -n "${CLOUD_ITONAMI_WORKSPACE_ROOT:-}" &&
      -d "$CLOUD_ITONAMI_WORKSPACE_ROOT/orgs/kotoba-lang/shell" ]]; then  # WORKSPACE_ROOT は後
  shell_dir="$(CDPATH= cd -- \
    "$CLOUD_ITONAMI_WORKSPACE_ROOT/orgs/kotoba-lang/shell" && pwd)"
fi
```

repo 相対分支が依然として WORKSPACE_ROOT より先に評価される (案 A の優先順位反転は実装されて
いない)。`app_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"` (launcher 21 行目)
のため、テストが `bash bin/cloud-itonami-app` を repo root から起動するとき `app_dir` = repo
root となり、`$app_dir/../../kotoba-lang/shell` = 実在の shell ツリーを指す。

### 2. 実在の 2 階層上 shell が存在 (repo 相対分支が発火する前提)

`ls -d "$MC/../../kotoba-lang/shell"` → `/Users/junkawasaki/github/com-junkawasaki/orgs/kotoba-lang/shell`
(rc_real=0)。repo 相対分支の `-d` 検査は真。

### 3. 実実行: WORKSPACE_ROOT を合成 root に設定しても repo 相対 shell が返る (赤の直接再現)

合成 root `/tmp/ci-resident-root-59` に `orgs/kotoba-lang/shell` を作成し、
`CLOUD_ITONAMI_WORKSPACE_ROOT=/tmp/ci-resident-root-59` を export、repo root から
`bash bin/cloud-itonami-app --print-shell-dir` を実行 (`--print-shell-dir` は launcher
`if [[ "${1:-}" == "--print-shell-dir" ]]` 分支で印刷後 exit 0、server/GUI 起動前に終了)。

実測出力:

```
WORKSPACE_ROOT=/tmp/ci-resident-root-59
expected-if-test-passes(workspace shell): /tmp/ci-resident-root-59/orgs/kotoba-lang/shell
--- actual --print-shell-dir output ---
/Users/junkawasaki/github/com-junkawasaki/orgs/kotoba-lang/shell
exit=0
--- real 2-levels-up shell ---
/Users/junkawasaki/github/com-junkawasaki/orgs/kotoba-lang/shell
rc=0
```

出力は WORKSPACE_ROOT 配下の合成 shell (`/tmp/.../orgs/kotoba-lang/shell`) ではなく repo 相対の
実 shell を返した。launcher_test.clj の必然断言
`(is (= (.getCanonicalPath shell) (str/trim stdout)))` は `/tmp` 配下 ≠ `/Users` 配下で**赤**。
falsify-8 の赤の失敗条件が head でも完全再現。

## 判定

- **主張「head で修理案 A が着地し launcher は WORKSPACE_ROOT 優先」→ REFUTED**。実コードの
  評価順序は repo 相対→WORKSPACE_ROOT のまま反転しておらず、実実行でも WORKSPACE_ROOT 無視を
  決定論的に再現。OPEN 赤-4 は head 9e5b24a で**未解決のまま** (falsify-8 帰属不変)。
- 範囲修正: falsify-16 の緑は /private/tmp (shadow 無し) の反証外である事実に加え、今回 repo
  root 起動というテスト本来のレイアウトで直接再現。OPEN 赤-4 の実体は「テスト契約
  (WORKSPACE_ROOT 必勝) と launcher 実装 (repo 相対優先) の 13c45a5 (2026-08-10) 以来の
  不一致」で、head 9e5b24a でも生存。
- テスト軸 score は 3 のまま (根拠に「OPEN 赤-4 未解決の決定論的再現を falsify-59 で確認、修理
  案 A 着地は未確認」を追記。既知の未解決を再確認したもので質を覆す変更ではない)。

## スコープ修正 (maturity.md OPEN 赤-4 行へ反映)

- OPEN 赤-4 行: falsify-16「/private/tmp で緑」に加え、**falsify-59 で repo root 起動の直接
  再現 (WORKSPACE_ROOT 無視を `--print-shell-dir` 実実行で観測)** を追記。修理案 A/B は Tier 2
  kanban/human 判断のまま。

## 次の 1 アクション

- OPEN 赤-4 の修理着地 (案 A: launcher を WORKSPACE_ROOT 優先に、bin