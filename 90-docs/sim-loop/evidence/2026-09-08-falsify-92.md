# falsify-92 (2026-09-08, 運用/滞留 軸 — 0 ベースの 7 反復超連続維持 (8 時点観測跨ぎ、恒久化傾向の更なる精緻化) 判定)

測定日: 2026-09-08 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app) は読み取り専用参照のみ、一切 touch せず。

## 背景 / 測定対象

falsify-91 は 3 ベクトル (main / 全 refs / resident-refspec) の正規 unused 0・local annex
keys 786 不動を falsify-85 以降 7 時点観測跨ぎで再実測し、0 ベースを「恒久化傾向の更なる
精緻化」へ更新した。その次の 1 アクションは「次反復で同 3 ベクトル + local annex keys +
avail を再実測し、0 ベース維持の **7 反復超の連続持続** or 再蓄積再開を判別 (7 反復超維持
なら恒久化傾向を更に精緻化、再開なら falsify-33..53 語調へ復帰)、あわせて avail (159 Gi
安定) の帰属 — 滞留 (keys 786) 不変下の外部書き込み源を観測」。あわせて実装軸附帯で
「e91bf0f の .kotoba parity 測定面と 2 build-break の生存再確認」が指定されていた。

本反復 (falsify-92) はその次の 1 アクションを執行し、falsify-91 が指定した閾値「0 ベースの
**7 反復超の連続持続 → 恒久化傾向の更なる精緻化**」の充足を falsify する。

**主張**: 本反復の再実測で (a) 3 ベクトルのいずれかに正規 unused が再出現する、または
(b) local annex keys が 786 から増加する (7 反復超への持続は成立せず残留再蓄積を開始する)、
または (c) avail が 150 Gi 未満へ続落する (159 Gi の安定からの下振れが構造的減少へ転じる)。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD detached 566928f67、resident branch は `resident/dns-resolver`)。

### 1. main-refspec regular unused

`git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys** (出力は partial
transfer 残骸 14 chunk のみ、NUMBER 1..14 の MD5E-s... 14 行)。`ok` / EXIT_RC=0。

### 2. resident-refspec (帰属ベクトル)

`git annex unused --used-refspec +refs/heads/resident/dns-resolver` → 同様に正規 unused
**0 keys**、partial 14 chunk のみ。`ok` / EXIT_RC=0。

### 3. 全 refs 走査 (孤児ベクトル)

`git annex unused` (refspec 指定なし) → 同様に正規 unused **0 keys**、partial 14 chunk
のみ。いかなる ref からも到達不能な孤児 key は存在しない。`ok` / EXIT_RC=0。

### 4. git annex info

```
local annex keys: 786        (falsify-92: 786 — 変化 0、8 時点観測 / 7 反復連続維持)
local annex size: 11.68 gigabytes   (不変)
available local disk space: 169.95 gigabytes (falsify-91: 170.65 GB — 約 -0.70 GB 極小減)
temporary object directory size: 186.98 megabytes (partial chunk 14、不変)
annexed files in working tree: 7143 / 93.02 gigabytes ([b2] 側)
combined annex size of all repositories: 111.12 gigabytes
```

3 ベクトルが全て正規 unused 0 のまま。local annex keys は **786 → 786 (増加 0)** — 滞留
蓄積期 (falsify-39..49) の反復あたり +24/+25 単調増加とは対照的に、増分ゼロの状態が
**falsify-85→92 の 8 時点観測 / 7 反復連続維持**で続いている (falsify-91 の 7 時点を
1 時点上回る、falsify-91 指定の閾値「7 反復超」を充足)。

### 5. df avail / 増加源プロセス

`df -h /` → avail **158 Gi / 10%** (falsify-91 の 159 Gi から -1 Gi 極小減、飽和なし・
100% 標示なし)。annex available local disk space は 169.95 GB (falsify-91 の 170.65 GB 比
約 -0.70 GB 極小減、df と同方向)。avail は falsify-85 (163) → 88 (142) → 90 (159) → 91
(159) → 本反復 (158) と 158-163 Gi 帯で振動、**150 Gi 未満への続落なし** — 主張 (c) は
SURVIVED (159 Gi 安定の下振れは極小 -1 Gi で構造的減少へ転じておらず、滞留 keys 786 不変
下の外部書き込み変動帰属と整合)。

`ps aux | grep -E 'itonami-app-resident|export_and_sync'` → 増加源 `node nbb
~/.gftd/bin/itonami-app-resident.cljs` **PID 70643 稼働継続** (falsify-85/86/87/88/89/90/91
と同一 PID のまま、連続稼働、export_and_sync は不在)。

### 6. origin/main (実装軸 附帯)

`git fetch origin` → origin/main は **e91bf0f → d3ce2cd へ再前進** (新シグナル)。
`rev-parse origin/main = d3ce2cddbbaecc1531ae52c4735e9dffd4408c3a`。`git show --stat -1` →
**migration `connectors scope-implication table -> connectors_core.kotoba`
(ADR-2609081800)、変化 8 本 / +339 insertions**: 新規 `connectors-core.kir.edn` (28 行)・
新規 `connectors_core.kotoba` (55 行)・新規 `connectors_kotoba_parity_test.clj` (134 行)・
`connectors.clj`/`kotoba_oracle.cljc`/`test_runner.clj`/`connectors_test.clj` 変更。
**falsify-89/90 が実装軸シグナルとした 52c156c (vf-calibrate) → e91bf0f (vf-authority) と
同種の kotoba parity 測定面 migration が 3 反復連続で再発** — 今回は connectors scope
implication 表、parity_test は 134 行 (e91bf0f の 101 行より増)。.kotoba 分量の重みが
88→89→90→92 と反復ごとに増している。本体 checkout は detached HEAD b5df93e (#299) /
porcelain clean (dirty 0)。

## 判定

**SURVIVED (主張 a/b/c とも不成立) / 0 ベースは 7 反復超の連続持続へ達し「恒久化傾向の更なる
精緻化」を達成**: falsify-91 から本反復への独立時点で 3 ベクトル (main / 全 refs /
resident-refspec) の正規 unused は **全て 0 のまま**、local annex keys は **786 → 786
(増加 0, falsify-85→92 の 8 時点観測 / 7 反復連続維持)**、既知 partial 14 chunk のみ —
主張 (a) の再出現も (b) の再蓄積も検出されなかった。増加源 (resident PID 70643) は稼働
継続のまま、新規 key を参照させる滞留を積まない状態が 7 反復 (観測 8 時点) にわたり維持
されている。avail は 158 Gi (159→158、-1 Gi 極小) で安定し、主張 (c) の 150 Gi 未満への
続落も不成立。

falsify-91 が指定した閾値「0 ベースの 7 反復超の連続持続」を本反復が充足 — 恒久化傾向を
**更に精緻化**した。ただし増加源 (PID 70643) 稼働継続ゆえ恒久性は保証されず「never
re-accumulate」は主張しない。

運用/滞留 軸 score は **3 のまま** (0 ベースの恒久化傾向精緻化を記録、green 化測定/実緑の
裏付けは本反復では得られない — score は実緑測定でのみ上昇する規約)。

## スコープ修正 / 次の 1 アクション

- risk-5 の記述を「滞留は 0 ベースへ回帰し **falsify-85→92 の 8 時点観測 / 7 反復連続維持**
  (unused 0・keys 786 不動・partial 14 chunk のみ) — **恒久化傾向の更なる精緻化継続**」と
  再アンカーする。ただし増加源 (resident ingest, PID 70643) 稼働継続ゆえ再蓄積の可能性は残る。
- 次の 1 アクション: 次反復で同 vector を再実測し、0 ベース維持の**8 反復超の連続持続** or
  再蓄積再開を判別 (8 反復超維持なら恒久化傾向を更に精緻化、再開なら falsify-33..53 語調へ
  復帰)。あわせて avail (158 Gi 安定) の帰属 — 滞留 (keys 786) 不変下の外部書き込み源
  の特定を観測。
- 附帯 (実装軸 次反復シグナル): origin/main は **e91bf0f → d3ce2cd へ再前進** (connectors
  scope-implication -> connectors_core.kotoba、ADR-2609081800)。falsify-89/90 の vf-calibrate
  / vf-authority に続く **同種 kotoba parity 測定面 migration の 3 反復連続再発** (parity_test
  134 行、e91bf0f の 101 行より増) — 次の実装軸反復で d3ce2cd の parity 測定面 (3 値結合
  水準・配線記録) の変化と、2 build-break (identity.clj / kotoba-net) の生存を実測する。
  本体 checkout は detached HEAD b5df93e (#299) / porcelain clean。
- 附帯 Tier 2: falsify-85/86/87/88/89/90/91 と不変 — resident プロセス再起動 (94682→70643) の
  契機と滞留回収の帰属 (operator 手動 dropunused/世代削除 vs resident 挙動変化) は継続
  kanban/human へ透過提出。

## 付記 (append-only 規約): falsify-91 追記末尾の残骸

maturity.md の末尾 (falsify-91 追記 `(evidence/2026-09-08-falsify-91.md)` の後) に、直前の
falsify-91 追記が用いたシェル append コマンドの残骸と見られる 2 行
(`MD` / `echo "append rc=$?" > /tmp/f91append.txt; wc -l "$F" >> /tmp/f91append.txt`) が
混入している。これは追記作業のコマンドテキストが誤ってファイルへ書き込まれたもの (本反復
wc 実測: 計 1238 行、行 1237-1238 が残骸)。append-only 規約 (既存行の編集・削除禁止) に
従い本 bot はこれを削除・編集せず、本反復追記をその後に追加する。ガバナンス上は記録の
追記が汚損されている旨を kanban/human へ透過報告対象とする。

## 附帯

本体 CHECKOUT (worktree list 先頭
`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app`) は
detached HEAD b5df93e (#299)、`git status --porcelain` 0 行 = porcelain clean (dirty 0)。
origin/main は本反復で **e91bf0f → d3ce2cd へ再前進** (connectors scope-implication ->
connectors_core.kotoba、変化 8 本 / +339：新規 .kotoba・parity_test 134 行、repo-root
src/・test/ 接触 — 実装軸 次反復シグナル)。本反復は運用軸の falsify-91 次の 1 アクションを
測定対象に選択し、作業は wt-msloop / bot/maturity-sim-loop で完結、本体 checkout を一切
touch せず。identity.clj malformation / kotoba-net 未宣言 (falsify-63..84) は対象外・既知赤
が継続 (d3ce2cd の kotoba parity 接触は本反復の運用軸測定に関する限り影響なし、次反復の
実装軸再確認事項)。7-軸表 16 行目孤立行 placeholder / falsify-46 mid-sentence cut / 
falsify-91 末尾残骸 (本付記参照) は残存のまま (append-only 規約で本 bot は編集しない)。
(evidence/2026-09-08-falsify-92.md)