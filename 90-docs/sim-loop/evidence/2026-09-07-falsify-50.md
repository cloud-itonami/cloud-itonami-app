# falsify-50 — 再現性/リリース展開: "releases/ 全 77 ツリーが git commit と byte 完全一致" の測定基盤が消滅 (2026-09-07)

測定者: itonami-maint
軸: 再現性 (release 展開先と version の一意性)
ターゲット: maturity.md 再現性行 (falsify-3 実測の主張)
「`~/.cloud-itonami/releases/` 配下の 77 個の 40-hex-sha リリースツリーが対応 git commit と
byte 完全一致 (mismatch 0 で生存継続)」
検証方法: 実マシンで falsify-3 の測定対象ディレクトリと運行基盤を read-only で実測し、
主張の前提 (測定対象の現存) を確認する。

## 測定 (実測値、捏造なし)

| 測定項目 | 実測値 |
|---|---|
| `~/.cloud-itonami/releases` | **不存在** (`ls -ld` で No such file or directory) |
| `~/.cloud-itonami` | 不存在 (ls / find とも 0) |
| 現在の運行 server (PID 94697) | classpath が `src:resources:.cpcache/...basis` (source 展開、release tree 不使用) |
| resident 増加源 itonami-app-resident.cljs | `/Users/junkawasaki/.gftd/bin/itonami-app-resident.cljs` (PID 94682、release tree 不使用) |
| launcher/updater の展開先参照 | `bin/itonami` ほかは `~/.cloud-itonami/app|data` を参照 (releases とは別) |
| updater の供給経路 | GitHub Releases download + `verify-manifest` (署名 ed25519 検証) — updater.clj:141 |
| version 資源 `resources/cloud-itonami-version.edn` | `{:version "0.5.7"}` (19 bytes) |
| 本体 checkout | agent/fix-open-red-5-three-bound / porcelain clean (dirty 0)、head 9e5b24a |

falsify-3 (2026-09-03) の測定対象だった 40-hex-sha リリースツリー群は、本反復 (2026-09-07) で
実マシン上に存在しない。`~/.cloud-itonami/releases` 相当の代替展開先も home 配下に検出されず
(Documents/Desktop/Library は TCC 遮断で列挙不能も、運行プロセスおよび launcher/updater の
参照パスは全て `src:resources` または `~/.cloud-itonami/app|data` であり release tree を使用しない)。

## 判定

**REFUTED — 主張の測定基盤が消滅 (範囲修正が必要)**

- falsify-3 の「77 ツリー byte 完全一致」は当時の実測であり正しいが、その**測定対象**が
  本反復で不存在であり、maturity.md 再現性行の「releases/ 全 77 ツリーが対応 git commit と
  byte 完全一致」という**現在形のひろがりを持った主張**は再現性の対象ではない。
- 現在の展開モデルは (a) source classpath (`src:resources`) による開発/運行、または
  (b) GitHub Releases download + 署名 manifest 検証 (`verify-manifest`、ed25519) であり、
  リリースの不変性は falsify-3 時点の「sha 名ディレクトリ + current symlink の運用規約のみ
  (OS 強制なし)」から、「署名入り manifest の ed25519 検証」へ実体が移行している。
- 主張の範囲を修正: 「releases/ 77 ツリー byte 完全一致」は**過去形の実測 (falsify-3、
  2026-09-03 時点)** として記録し、現在形の再現性主張は「運行は source classpath、
  配布は署名 manifest 検証による update 供給」に置き換える。

## 次反復の測定候補 (Tier 2 operator 判断)

1. 新しい展開モデルの再現性検証: 配布 manifest (GitHub Releases) の ed25519 検証が実適用
   されているか、`verify-manifest` のテスト実在 (updater_test) を確認。
2. リスク-1 の記述 (releases/<sha> 不変性が OS 強制なし) は、展開モデルの移行後は
   適用対象が消滅したため、リスク表から再帰属 (削除/移動) を operator に提案。

## 附帯

- 7 軸テーブル 16 行目孤立行 `| 反証候補falsify30PLACEHOLDER` 残存のまま (operator 復旧待ち、
  falsify-33/34 Tier 2 report 済み、append-only で本 bot は編集しない)。
- 前反復 falsify-46 の mid-sentence truncate (maturity.md 456 行目) も append-only で回復せず
  operator 復旧待ち。
- 再現性軸 score は 3 のまま (根拠の「releases/ 77 ツリー」を過去形実測に範囲修正し、
  運行 source-classpath / 配布署名検証を根拠に追記)。