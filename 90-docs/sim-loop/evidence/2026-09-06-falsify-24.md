# falsify-24 (2026-09-06): annex 滞留世代の増加レート 2 時点実測 —
# falsify-23 の NEXT-10 残測定「annex keys の増加レート 2 時点実測
# (滞留増加の停止/継続の判別)」の実施。

測定日: 2026-09-06 (JST)
測定者: itonami-maint (cron 反復)
軸: 運用 + リスク-5
作業は /private/tmp/mt-sim-loop worktree (bot/maturity-sim-loop) で完結。
dns-resolver への接触は読み取り専用コマンドのみ (dropunused は実施しない —
データロス判断は Tier 2)。

## 主張 (falsify-23 の次の 1 アクション)

「滞留世代 (main-refspec の unused 集合) は 2 時点間で不変 —
増加は停止している」

## 実測 (すべて実コマンド出力をそのまま記録)

1. **df -h /System/Volumes/Data**:
   926Gi size / 887Gi used / **avail 356Mi / 100%** —
   falsify-22 反復時 (4557→4578 MiB) からさらに飽和が進行。
   フルスイート実行は引き続き不可能 (リスク-5 悪化)。

2. **git annex info** (cloud-itonami-dns-resolver):
   - local annex keys **6147** / local annex size **78.4 GB**
   - size of annexed files in working tree: 33.59 GB (不変)
   - unused keys size: **42.2 gigabytes** (annex 自身の表示)
   1 時点目 (falsify-21 実測): 5951 / 75.51 GB →
   **+196 keys / +2.89 GB** (世代増加継続)。

3. **git annex unused --used-refspec +refs/heads/main** (rc=0):
   unused key **3261 個** を検出 (出力 3272 行、MD5E-s*.edn 世代)。
   key 名埋め込みサイズ集計 (python3 実測):
   **keys=3261 sum_bytes=44809883789 = 41.73 GiB**。
   1 時点目 (falsify-22 実測): 3088 keys / 42,199,579,172 bytes
   (39.30 GiB) → **+173 keys / +2.43 GiB**。

## 判定

- 主張「滞留は不変 / 増加停止」→ **REFUTED**。
  同日中の 2 時点実測で unused 集合は +173 keys / +2.43 GiB 増加。
  annex 滞留は *現在進行形で増加継続* と確定。
  帰属は falsify-20〜23 測定済みの resident branch ingest 履歴参照
  (発生源が稼働し続けている)。
- 附帯: avail が 4578 MiB → 356 MiB へ減少。増加継続の実測下で
  ディスク飽和は悪化中。テスト実行・journal 生成への実害リスク上昇。

## 緩和候補 (Tier 2 — operator/kanban 判断、bot は実施しない)

1. 発生源の停止: resident tick の dns-resolver ingest (tranco source)
   の頻度/世代保持方針の見直し — 新規滞留の流入を止めるのが先。
2. 既存滞留の回収は falsify-23 確定の通り履歴 truncate/再構築が前提
   (データロス判断)。発生源停止により上界 41.73 GiB が固化しない内の
   方針決定が望ましい。
3. 短期の緊急余地: avail 356Mi はリスク-5 の「満杯時 store journal
   mismatch 異常終了」閾値に近い。any テスト実行前の disk-avail
   必須化 (falsify-17 提起済み) を毎反復で遵守。

## 次の 1 アクション

- maturity.md リスク-5 に本測定を追記し、発生源停止 (resident ingest
  方針見直し) を Tier 2 として kanban/human へ提起。
