# falsify-38 (2026-09-06): フルスイートの最小 avail 閾値実測 + 現行 main (cc7a17d) の緑再確認

測定日: 2026-09-06 23:36-23:44 JST (cron 反復)
測定者: itonami-maint (軸: テスト + リスク-5 / falsify-31・falsify-37 NEXT)
対象 repo: cloud-itonami-app 現行 main (cc7a17d)
作業は sibling worktree (/private/tmp/mt-msloop38、detached) で完結。
本体 checkout (agent/fix-open-red-5-three-bound) 未 touch。

## 反証すべき主張 (falsify-31/37 NEXT 由来)

falsify-31 (2026-09-06 18:42):「disk-avail が 50Gi まで回復したため、次反復で
フルスイート実行 (falsify-17 の閾値検証) が現実的」。falsify-37 (23:02):
「avail 171Gi / 81% はフルスイート実行可能域であり、falsify-31 の最小 avail
閾値実測が再び現実的選択肢となった (次反復の実行候補)」。

本反復の主張: 「**avail 約 170Gi / 81% の健康帯で、現行 main (cc7a17d、
harness-plugins コミットを含む) のフル JVM スイートが負荷環境下でも
0 failures で完走する**」。
→ 0 failures なら (a) avail 170Gi はフルスイート実行可能 (最小閾値の上界確定、
  falsify-17 のディスク飽和 fail モードがこの avail では発火しない)、
  (b) 現行 main に回帰なし (harness-plugins コミットの緑が再確認)。
→ 赤が出れば 現行 main の回帰 (新規の決定論的赤) を記録する。

## 実測 (実コマンド出力をそのまま記録)

1. **事前 df -h /System/Volumes/Data** (23:36):
   `Size 926Gi / Used 715Gi / Avail 170Gi / Capacity 81%` (falsify-37 終了時
   171Gi とほぼ同水準、健康帯維持)。

2. **負荷条件**: 開始時 load averages 24.37 / 29.52 / 34.04 (falsify-16 と
   同等の高負荷環境)、終了時 27.28 / 26.83 / 30.24。resident JVM 多数走行。

3. **測定環境**: `git worktree add --detach /private/tmp/mt-msloop38 cc7a17d`
   (本体 checkout 未 touch、porcelain clean)。現行 main = cc7a17d
   (harness: everything is a plugin — ADR-2609042200)。

4. **フルスイート実行** (23:36-23:44、所要約 4分48秒、warm cpcache):
   `cd /private/tmp/mt-msloop38; clojure -M:test -m cloud.itonami.app.test-runner`
   結果 (log 末尾、実出力):
   ```
   Ran 2291 tests containing 13895 assertions.
   0 failures, 0 errors.
   ```
   EXIT: test-runner が 0 failures を出力 (wrapper は defunct だが結果は
   これが正)。ログ全文は /tmp/f38-full2.log。

5. **事前/事後の avail 変化**: 170Gi (開始) → 170Gi (終了)、ディスク飽和の
   兆候なし。スイートの state.edn/journal 警告は出力されたが、すべて正常系
   (write-bound 接近・orphan journal 退避) でありテスト失敗には至らない。

## 判定

### 主張 (170Gi avail で負荷下フルスイート 0 failures) → **SURVIVED**

- **Ran 2291 tests / 13895 assertions / 0 failures, 0 errors** を実測。
- 現行 main (cc7a17d) は決定論的赤ゼロで完走。harness-plugins コミットに
  よる回帰は生じていない (緑再確認)。
- avail 170Gi / 81% はフルスイート実行可能域であることが確定。falsify-17 が
  「avail 2.2Gi で journal mismatch 異常終了」を実測した時と異なり、この avail
  帯ではディスク飽和 fail モードは発火しない。
- 最小 avail 閾値の厳密な下界は未確定 (170Gi は「確実に動く」上界)。
  falsify-16 は avail 16Gi でも完走実測済み (cache 整理後) なので、
  閾値は 16Gi 以下〜170Gi の間にあると分かるが、単点測定のため本反復で
  下界を詰めることはしない。

## テスト軸スコアへの影響

- 変更要なし (score 3 のまま)。ただし根拠を更新: 現行 main (cc7a17d,
  harness-plugins コミット含有) でフルスイート 0 failures を負荷環境で
  再確認。決定論的赤 0 は不変。3 止まりの理由 (flake リトライ機構なし /
  OPEN 赤-4 未解決) は変わらず。

## 次の 1 アクション

- maturity.md のテスト軸根拠とリスク-5 に falsify-38 を追加記録
  (avail 170Gi でフルスイート 0 failures、現行 main 緑再確認)。
- 最小 avail 閾値の下界測定 (16Gi 未満でどこまで下げられるか) と
  flake リトライ機構 (テスト軸 4 への道) は継続候補。