# falsify-73 (2026-09-08, 運用 軸)

OPEN 赤-2 (expiry-alert) の falsify-70/72 next「修理案 A 着地後 rc=0 再確認 (falsify-11
終点判定継承)」を先行して反証試行。主張は「案 A の必須 3 src が既にインストール済み /
ロード済み job classpath に追加されたので、次回 (2026-09-14 Mon 09:00) 発火で rc=0 に
復旧する (案 A 着地済み)」とした。作業は wt-msloop / bot/maturity-sim-loop で完結、
本体 checkout 未 touch、kagi/vault 非接触。

## 実測 (リダイレクト記録、捏造なし)

1. 本体: HEAD = origin/main = 679572b (falsify-63..72 と同一 SHA、porcelain clean)。
   `git remote -v` 実測 = `git@github.com:cloud-itonami/cloud-itonami-app.git`。
   `git ls-tree -r 679572b --name-only | grep -i 'expiry\|ops-classpath'` → ヒット **0**
   (rc=1) = cloud-itonami-app には `scripts/expiry-alert.cljs` も `scripts/\
   ops-classpath.sh` も存在しない。job の WorkingDirectory / classpath は
   `/Users/junkawasaki/github/com-junkawasaki/orgs/network-awai/cloud-itonami`
   (**別リポジトリ checkout**) を指す。
2. `launchctl list | grep -i expiry` 実測 → `-\t1\tcom.gftdcojp.itonami.expiry-alert`
   (exit 1 = not running)。
3. `launchctl print gui/$(id -u)/com.gftdcojp.itonami.expiry-alert` 実測 →
   `state = not running` / `runs = 1` / `last exit code = 1`、event trigger
   `{Minute 0, Hour 9, Weekday 1}` (毎月曜 09:00)。**ロード済み classpath (arguments、
   15 エントリ) に authority / org-nist-sha2 / datom-source の 3 src は一切含まれない**。
4. インストール済み plist (~/Library/LaunchAgents/com.gftdcojp.itonami.expiry-alert.plist、
   mtime Aug 13 2026、6672 B) の classpath を git 製 comment (行 3-32、web deploy 手順)
   と併読 → 15 エントリで西 DECLARED checkouts (bonsai/nekko/arrangement/.../datalog)。
   `grep -c 'authority\|nist-sha2\|datom-source' plist` → **0** (rc=1)。3 src 全て不在。
5. ログ (~/.itonami/logs/expiry-alert.log、8547 B、mtime **Sep 7 09:00** = 前回月曜
   発火) tail 実測 → `Error: Could not find namespace: authority.scope` (Node.js
   v26.0.0) — falsify-70 で確定した「現行ツリーの単独終端」と同一エラーが当該発火でも出力。

## 判定

**REFUTED**。「案 A の 3 src がインストール済み/ロード済み classpath に追加済み → 次回
発火 rc=0」の主張は不成立: ロード済み job (launchctl print) とインストール済み plist
(grep -c=0) いずれも 3 src 全欠で、前回発火 (Sep 7 09:00) は `authority.scope` 単独終端
のまま失敗 (runs=1 / last exit code=1 / not running)。**案 A は依旧未着地**で、次回
(2026-09-14 Mon 09:00) も同一エラーで失敗する見込み (再確認はその後の rc)。

範囲修正 (新情報): falsify-70 の「案 A 着地 = ops-classpath.sh 修正 + plist 再生成 +
launchctl relaunch」を一歩具体化 — **その両対象 (`scripts/ops-classpath.sh` と
`scripts/expiry-alert.cljs`) は cloud-itonami-app に存在せず、job が参照する別リポジトリ
`network-awai/cloud-itonami` 側にあるため、修理は対象リポジトリ本体外 (sibling repo) に
完結し、本 bot の Tier 1 着地範囲には入らない**。Tier 2 kanban/human 判断のランブック
(op 対象: network-awai/cloud-itonami の ops-classpath.sh に authority/src +
org-nist-sha2/src + datom-source/src 追加 → plist 再生成 → launchctl bootout/bootstrap)
を再提示。着地後の rc=0 再確認が終点判定 (falsify-11 継承、falsify-70 の当初 next は
本反復で「未着地のまま」と確定)。

運用 軸 score は **3 のまま** (root cause は falsify-70 で単独終端に確定済み、案 A の
十分性 (require 連鎖 EXIT_RC=0) も falsify-70 で実測済み — 本反復は「未着地・次回も失敗
見込み + 修理対象が sibling repo に完結」を確定・精緻化しただけで、green 化の測定なし)。

## 次の 1 アクション

OPEN 赤-2 を「案 A 未着地 (3 src 全欠、次回 Mon 09:00 も authority.scope 単独終端で失敗
見込み)、修理対象は network-awai/cloud-itonami に完結 (Tier 2)」と確定したので、Tier 2
report で案 A 着地ランブック (network-awai/cloud-itonami 側 ops-classpath.sh 修正 + plist
再生成 + bootout/bootstrap) を提示し、着地後の rc=0 再確認を次の測定に回す。別線として:
falsify-72 の次アクション「falsify-62 の bundle/graph 赤 (published-lock 再発行) 解消
確認」は identity.clj 修理 (Tier 2 kanban/human) 後に実行可能。

## 附帯

本体 checkout は反復中 porcelain clean (dirty 0)、本 bot は touch せず wt-msloop /
bot/maturity-sim-loop で完結。修理対象は本体外 (sibling repo network-awai/cloud-itonami
の launchd job) のため Tier 1 不着地。7-軸表 16 行目孤立行 placeholder と falsify-46
mid-sentence cut は残存のまま (operator 復旧待ち、append-only のため本 bot は編集
しない)。identity.clj malformation (falsify-63..68 build-broken tip) は既知赤として対象外。