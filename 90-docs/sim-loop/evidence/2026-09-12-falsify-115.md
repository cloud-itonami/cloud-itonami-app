# falsify-115 (2026-09-12, 運用/滞留 軸 — 0 ベースの 21 時点観測 / 20 反復連続維持、avail は ±10 Gi 級低下が **4 反復目は不発火** (df 159426 → 158160 MB ≒ 155.7 → 154.5 Gi、約 -1.2 Gi 帯内微減)、resident は falsify-114 の PID 62828 が消滅し **新 PID 66954 (11:21PM 起動, nbb/node) で再出現 — 5 回目の状態遷移**)

測定日: 2026-09-12 23:21 JST (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app) は読み取り専用参照のみ、一切 touch せず。
生の出力全体は evidence/2026-09-12-falsify-115-raw.txt に保存。

## 背景 / 測定対象

falsify-114 の次の 1 アクションは「同 vector + keys + avail を再実測し、0 ベース維持の
**20 反復超の連続持続** or 再蓄積再開を判別。あわせて (1) avail の -10 Gi 級低下の
4 反復目の有無 (発火継続なら 1 反復 -10 Gi 超ペースが実勢と確定、緊急フラグ到達 (<100 Gi)
の予測時点を算出して Tier 2 提出)、(2) resident PID 62828 の存続 (3:15PM 起動からの
経過観測)」だった。本反復 (falsify-115) はその実行。

**主張**: 本反復の再実測で (a) 3 ベクトルのいずれかに正規 unused が再出現する、または
(b) local annex keys が 786 から増加する、または (c) avail が falsify-114 実測値
(159426 MB ≒ 155.7 Gi) から ±10 Gi 超/反復の大幅変動を起こす (4 反復目の発火)。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定日時: `Sat Sep 12 23:21:20 JST 2026` (date 実測)。
測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD **c80906496cc64cfc60d0b2c600ec9a356f1d9a3b** — falsify-113/114 と同一、新規前進なし。
porcelain 0 行 = clean)。

### 1-3. main / resident / 全 refs の 3 ベクトル regular unused

- `git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys**、partial
  transfer 残骸 14 chunk (NUMBER 1..14、MD5E-s... 14 行、falsify-85 以降と同一内容) のみ。
  `ok`。
- `git annex unused --used-refspec +refs/heads/resident/dns-resolver` → 同様に正規 unused
  **0 keys**、partial 14 chunk のみ。`ok`。
- `git annex unused` (refspec 指定なし) → 同様に正規 unused **0 keys**、partial 14 chunk
  のみ。孤児 key なし。`ok`。

### 4. git annex info

```
local annex keys: 786        (falsify-114: 786 — 変化 0、21 時点観測 / 20 反復連続維持)
local annex size: 11.68 gigabytes   (不変)
available local disk space: 165.74 gigabytes (+100 megabytes reserved)
  (falsify-114: 167.07 GB — 約 -1.33 GB)
temporary object directory size: 186.98 megabytes (partial chunk 14、不変)
annexed files in working tree: 7143 / 93.02 gigabytes ([b2] 側、不変)
combined annex size of all repositories: 111.12 gigabytes (不変)
```

### 5. df / resident プロセス

- `df -m` 実測: `/dev/disk3s5` (System/Volumes/Data) available **158160 MB ≒ 154.5 Gi**
  (annex info の 165.74 GB ≒ df MB 値に ~1024 換算 + reserve 差の記録差は前反と同様)。
  falsify-114 の 159426 MB ≒ 155.7 Gi から **約 -1.2 Gi の帯内微減**。
- `ps aux | grep -E 'itonami-app-resident|export_and_sync' | grep -v grep` → **1 行ヒット**:
  `junkawasaki 66954 ... node ... org-babashka-nbb/cli.js
  /Users/junkawasaki/.gftd/bin/itonami-app-resident.cljs` (S<、**11:21PM 起動**)。
  falsify-114 実測の PID 62828 は消滅、**後継 resident が新 PID 66954 で実測在在**
  (70643 → 62791 → 61573 → 不在 → 62828 → 66954 の系列)。export_and_sync はヒットなし。

## 判定

**SURVIVED (主張 a/b — 0 ベース) / 不成立 (主張 c — 4 反復目不発火)**:
falsify-114 から本反復への独立時点で 3 ベクトルの正規 unused は全て 0 のまま、local
annex keys は 786 → 786 (増加 0、21 時点観測 / 20 反復連続維持) — 主張 (a) 再出現も
(b) 再蓄積も検出されず、falsify-114 指定の閾値「20 反復超の連続持続」を充足し 0 ベース
恒久化傾向を更に精緻化した。

他方、**avail は df 159426 → 158160 MB ≒ 155.7 → 154.5 Gi、annex available 167.07 →
165.74 GB と約 -1.2 Gi / -1.3 GB の帯内微減** — 主張 (c)「±10 Gi 超/反復の大幅変動
(4 反復目)」は本反復で発生せず (REFUTED)。falsify-114 で確定した「構造的低下」判定
(3 反復連続 -10.1/-10.7/-12.4 Gi) に対し、本反復は低下幅が **1 桁縮小して -1.2 Gi** で、
1 反復 -10 Gi 超ペースは実勢と確定しなかった — 予測時点算出の前提が崩れたため緊急フラグ
到達予測時点の算出は**未実施のまま記録** (捏造したペース外挿を書かない、捏造ゼロ)。
緊急フラグ (<100 Gi) は非該当 (154.5 Gi、余裕約 54.5 Gi)。keys 786 / 11.68 GB 不変ゆえ
低下は annex 外部 (TCC 遮断で特定不能、operator の du 調査を Tier 2 継続)。観測系列
113.9 → 34.5 → 34.7 → 34.0 → 188.9 → 178.8 → 168.1 → 155.7 → 154.5 Gi。
範囲修正: 「構造的低下」確定の説明力を「低下が **-10 Gi 級 → -1 Gi 級へ減速した状態で
継続**」へ精緻化 (4 反復連続低下方向は維持: -10.1 → -10.7 → -12.4 → -1.2 Gi)。

**resident 5 回目の状態遷移は新規事実**: falsify-114 実測の PID 62828 (3:15PM 起動) は
本反復時点で消滅、新 PID **66954** (11:21PM 起動、nbb 経由で itonami-app-resident.cljs を
実行) が実測在在。70643 → 62791 → 61573 → 不在 → 62828 → 66954 の **5 回状態遷移**を
観測 — 「頻繁に再起動する監視対象」という falsify-114 の記述を更に支持する。PID 62828
の存続 (3:15PM 起動からの経過観測) を falsify-114 が観測対象に指定したが、実測は存続
ではなく再起動だった。起動管理主体と契機は operator 確認事項 Tier 2 継続。

運用/滞留 軸 score は **3 のまま** (0 ベース 20 反復超連続維持を記録。実緑測定でのみ
上昇する規約、avail 減速継続は risk-5 記述の再アンカーとして記録)。

## スコープ修正 / 次の 1 アクション

- risk-5 の記述を「滞留は 0 ベースで **falsify-85→115 の 21 時点観測 / 20 反復連続維持**
  (unused 0・keys 786 不動・partial 14 chunk のみ)。**avail は低下方向 4 反復連続だが
  -10 Gi 級 → -1 Gi 級へ減速** (-10.1/-10.7/-12.4/-1.2 Gi)、annex 外部源 (keys 不変)。
  緊急フラグ (<100 Gi) までの余裕は約 54.5 Gi」へ再アンカー。
- 次の 1 アクション: 同 vector + keys + avail を再実測し、0 ベース維持の
  **21 反復超の連続持続** or 再蓄積再開を判別。あわせて (1) avail が -1 Gi 級減速を
  維持する (減速定着) か -10 Gi 級へ再加速するか (再加速なら 1 反復 -10 Gi 超ペースを
  実勢と確定し緊急フラグ到達予測時点を Tier 2 算出)、(2) resident PID 66954 の存続
  (11:21PM 起動からの経過観測、6 回目の状態遷移の有無) を観測。
- 附帯 (実装軸 次反復シグナル): 本体 origin/main は **33037469 のまま**
  (falsify-113/114 から 3 反復連続 no-advance、ls-remote 33037469dac 実測)。本体 HEAD
  33037469 と一致、porcelain 0 行 = clean。次の実装軸反復では 5b2ae3de → 33037469 間の
  parity 測定面 (src/test ファイル数、結線記録) の変化を実測する。実装軸の修理案は
  Tier 2 kanban/human 継続: (1) identity.clj の裸 [java.*] ベクタを `(:import ...)` に
  wrap、(2) kotoba.net 座標を deps.edn に宣言 (git sha 或は :local/root)。
- 附帯 Tier 2: resident 頻繁再起動 (…→ 不在 → 62828 → 66954) の起動管理主体と契機、
  avail 低下の具体源特定 (TCC 遮断下の du 調査)、kanban/human へ透過提出継続。

## 附帯

本体 CHECKOUT (worktree list 先頭
`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app`) は
HEAD 33037469dac1237f354c91fbe6378f19ae845de7、`git status --porcelain` 0 行 =
porcelain clean (dirty 0)。本反復は運用軸の falsify-114 次の 1 アクションを測定対象に
選択し、作業は wt-msloop / bot/maturity-sim-loop で完結、本体 checkout を一切 touch せず
(ls-remote 1 回は origin/main 前進確認のための読み取り、本体 working tree 不変)。
identity.clj malformation / kotoba-net 未宣言 (falsify-63..84) は対象外・既知赤が継続。
7-軸表 16 行目孤立行 placeholder / falsify-46 mid-sentence cut / falsify-91 末尾残骸は
残存のまま (append-only 規約で本 bot は編集しない)。
(evidence/2026-09-12-falsify-115.md)
