# falsify-128 (2026-09-23, 運用/滞留 軸)

## 主張 (q)

「falsify-127 が観測した dns-resolver annex 静止系列 (keys 786 / du 11403092 KB /
HEAD c8090649 / partial 一時ファイル 14 件) は現在形として本反復でも持続せず、
あるいは partial 14 件の少なくとも一部が消滅する」

## 判定: SURVIVED (静止系列は 11 反続で持続、partial 14 件も 2 法測定で連続実在)

### 実測 (同一 path ~/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver、falsify-123 の 2 法のみ使用)

- HEAD `git rev-parse --short HEAD` = **c80906496** (c8090649、11 反続不変)。
- keys: `find .git/annex/objects -type f | wc -l` = **786** (11 反続静止)。
- du: `du -sk .git/annex/objects` = **11403092 KB** — falsify-121..127 と KB 完全同値
  (11 反続 byte 静止)。
- partial: **2 法測定** — (1) `git annex unused` が **14 件の partially transferred**
  を列挙 (NUMBER 1..14、MD5E-*-S52428800-C1--*.edn、rc=0)、(2) `ls -la .git/annex/tmp`
  で **14 ファイル実在**、mtime すべて Sep 6 19:12-19:27 (falsify-117 測定時点から不変)、
  `total 365232` (512B blocks) ≈ 約 174 MiB。glob 単独測定は不使用
  (falsify-123 の false negative 教訓)。
- よって主張 (q) は REFUTED 試行に対し SURVIVED: 静止系列は消滅せず
  **11 反続**で持続。Tier 2 dropunused 提案 (14 件 ≈ 174 MiB) は不変で再提出。

### 附帯測定 (同反復実測)

- df /System/Volumes/Data: avail **8175048 KB ≒ 7.8 GiB / 100%** — falsify-127
  (5856912 KB ≒ 5.6 GiB) 比 **+2.2 Gi の反復間微回復**だが capacity は 100% 標示のまま、
  緊急フラグ (<100 Gi) は **7 反復連続発火** (falsify-121..127 系列継続、
  観測系列 103.75 → 69.8 → 62.6 → 15.0 → 8.1 → 5.6 → 7.8 GiB、非単調継続、
  単一帰属は未確立 — falsify-118/119/120 継続、いかなる帰属数値も主張しない)。
- resident: **PID 1083 継続 (13 反点)**、export_and_sync 不在
  (`ps aux | grep -v grep` 実測 rc=1、自助マッチ行は grep -v grep で除外)。
- 附帯 (テスト 軸, 前進確認のみ・本体 touch なし): origin/main は a3dc8c9 →
  **414f907** へ前進 (2 commits、diff は src/cloud/itonami/app/hermes_compat.cljk
  +8 行の hermes_event assistant.delta contract のみ、PR #367)。
  **authority.cljk は不触** — falsify-126/127 の Tier 2 修理 (nbb :import reader
  条件分離) は未着地、suite load 赤の修理前提は未充足 (スイート再測定は次回以降)。
- 本体 checkout / wt-msloop worktree は porcelain clean、本 bot は測定のみで touch なし。

## score

運用 3 のまま (静止系列の持続確認は score を動かさない。avail は 100% / 緊急フラグ
7 反続のまま低下・回復とも修復判断は人間 = Tier 2)。

## 次の 1 アクション

(1) authority.cljk 修理が着地したか origin/main diff を確認し、着地済みなら
テスト軸で isolated worktree suite load〜完走を再測定、未着地なら運用/滞留 軸で
緊急フラグ 8 反続の有無と annex 静止系列 12 反続を判別。
