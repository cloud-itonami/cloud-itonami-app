# falsify-71 (2026-09-08, spec/契約 軸)

falsify-56 (head 9e5b24a) 以来の spec 軸再測定。HEAD が 679572b (java.net.http→
kotoba transport migration、35 files) へ前進したため、反証可能主張を立てた:

**主張: transport migration はコマンド契約表面 (commands.edn / cli-aliases.edn) を
変更し、placeholder<->params 整合不変量を破る。**

## 実測 (リダイレクト記録、捏造なし)

1. origin/main = origin/HEAD = 679572b (rev-parse 実測)、falsify-63..70 と同一 SHA。
   ledger branch HEAD = 9c8c688 (falsify-70 commit)。
2. `git hash-object` で両 registry の SHA 比較:
   - commands.edn: 9e5b24a=72c4dd2 / 679572b=c44cc35 → **変化**
   - cli-aliases.edn: 9e5b24a=5301368 / 679572b=5301368 → **byte 完全一致**
3. `git diff 9e5b24a 679572b -- commands.edn`: 1 insertion / 1 deletion
   (単一行 edn 全置換)。`tr ',' '\n'` 分割 diff で変化を特定 — **:counts
   {:routes 408→409 :commands 208→209} と、1 コマンド追加**: `{:command
   ["workspace" "resources"], :method "GET", :template "/api/workspace/resources",
   :params [], :flags [], :gate :app}` (workspace scheduler の直前に挿入)。
   追加コマンドは placeholder なし / params 空 / 値スキーマなし (生成 registry 既定)。
4. tip 679572b の commands.edn を Python で機械解析:
   templates 209 / placeholder-bearing 113 / placeholder tokens 128 /
   宣言 params 128 / placeholder 中 params 未宣言の欠落 **0** / schema
   "cloud.itonami.app.commands.v1" 不変 (falsify-47/56 の方法を踏襲)。
5. 追加 1 コマンド `workspace resources` は templates 中にちょうど 1 回存在
   (grep 実測)、params 空ゆえ placeholder/params 整合には寄与しない。

## 判定

SURVIVED (不変量生存を範囲拡大確認 + 実施範囲を明示)。途中仮説「migration が
契約表面を変えた」は hash レベルでは成立 (commands.edn 変化) だが、変化の実体は
**単一の params なし GET コマンド追加 (208→209)** であり、placeholder<->params
整合 (128=128、欠落 0) と cli-aliases.edn byte 一致は migration 後も完全に保持。
schema も不変。falsify-47/56 (head 9e5b24a) の整合主張は現在 head 679572b でも
生存継続 — spec/契約 軸の根拠を「208 コマンド (9e5b24a)」から「209 コマンド
(tip 679572b)」へ再アンカーし、placeholder 128 は不変。

細部注意 (附帯 Tier 2 候補): transport migration (java.net.http → kotoba、
35 files) が同時に `workspace resources` コマンドを新規追加した —
transport リファクタとコマンド表面追加が同一コミットに混在。分離 git history
の可読性 / リリース差分の因果特定性に影響しうる (値スキーマの欠如は falsify-56
の範囲修正どおり生成 registry の既定で、本反復では質の変化なし)。

spec/契約 軸 score は 3 のまま (根拠を頭 9e5b24a→tip 679572b へ再アンカー、
整合不変量の生存を拡大再確認。値スキーマ検証が別名 registry のみで生成 registry
の flag に無いのは falsify-56 記録のまま不変、score を覆す質的変更なし)。

## 次の 1 アクション

spec 軸は次反復で「追加コマンド workspace resources の gate :app 契約が
server.clj ルート (routes 408→409) と一致する」を突合 (gen_commands.cljs の
commands-test 緑が経路継続担保か)。または OPEN 赤-2 (expiry-alert) の案 A
着地後 rc=0 再確認 (falsify-11 終点判定継承)。他軸は前反復記録のまま:
identity.clj build-broken (falsify-63..68、origin 同一 SHA で継続) は修理
Tier 2 未着地。

## 附帯

本体 checkout は反復中 porcelain clean (dirty 0, 前段 head 679572b 実測)、
本 bot は touch せず wt-msloop / bot/maturity-sim-loop のみで完結。identity.clj
malformation (falsify-63..68 の build-broken tip) は origin SHA 不変ゆえ
git object に残存のまま (本反復対象外、既知赤)。7-軸表 16 行目孤立行
placeholder と falsify-46 mid-sentence cut は残存のまま (operator 復旧待ち、
append-only のため本 bot は編集しない)。