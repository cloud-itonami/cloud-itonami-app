# falsify-69 (2026-09-08, 運用 軸): OPEN 赤-2 (expiry-alert) fail を falsify-58 の「authority.scope 単独原因」から複数 load 失敗連鎖へ範囲修正 (REFUTED)

測定日: 2026-09-08 (cron) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(cloud-itonami-app) は読み取り専用参照のみ。修理対象は本体外 (network-awai リポ
plist classpath / expiry-alert.cljs 依存連鎖) のため Tier 2 kanban/human 判断、
本 bot は touch しない (測定・読取のみ)。

## 背景 / 反証対象
falsify-58 (09-07) は OPEN 赤-2 (expiry-alert、Sep 7 09:00 発火 exit=1) の root
cause を「authority.scope の classpath 経路漏れという単独原因」とし、修理案 A
(3 src: authority/src / org-nist-sha2/src / datom-source/src + plist 再生成) を
唯一修正とした。

反証対象: 「失敗は authority.scope 単独で説明でき、修理案 A が唯一かつ十分」。

## 実測 (リダイレクト記録、捏造なし)
### 1. 本体 checkout
- HEAD = origin/main = 679572b (`rev-parse`、`rev-list` 0)、porcelain clean
- identity.clj (:import 0 / bare-java 5) — falsify-63..68 既知 build-broken、本書対象外

### 2. launchd 状態 (falsify-58 の独立再確認)
- state = not running, runs = 1, last exit code = 1
- program nbb, args { --classpath ... scripts/expiry-alert.cljs }

### 3. ログ全文 (109 行, mtime Sep 7 09:00)——成功 1 行 (2026-07-14 OK) の後、
複数の異なる load 失敗連鎖 (nbb):
- `Could not find namespace: kotoba-rad.cacao-delegate` ×2
- `Protocol not found: IEquiv` @ ipld/core.cljc:36 (deftype Link)
- `Protocol not found: IEquiv` @ ipld/link.cljc:11 (deftype Link)
- `Could not find namespace: datalog.index` ×1
- `Could not find namespace: authority.scope` ×3 (終端)

### 4. plist classpath (installed 実測) と 3 src 不在
- classpath に bonsai/nekko/arrangement/prolly-tree/io-ipld/io-multiformats/chain/
  org-ietf-cbor/org-ietf-ed25519/org-chainagnostic-cacao/mail/mailer/datalog の src
- 案A の 3 src (authority/src / org-nist-sha2/src / datom-source/src) は全て
  grep -c = 0 (不在)

### 5. 依存ネームスペース実在 (ディスク上)
- nekko/src/nekko/cacao_delegate.cljc 実在 (ops-keys は nekko.cacao-delegate require)
- io-ipld/src/ipld/{core,link}.cljc 実在 (IEquiv を reader 条件で使用)
- kotoba-lang/kotoba-rad checkout 不在 (ls 実測、com-radixx 等のみ)
- authority/src/authority/scope.cljc 実在、datalog/src/datalog/index.cljc 実在

## 判定
「単独原因 authority.scope」→ **REFUTED (スコープ修正)**: 同一ログ内で
authority.scope の前段に ipld deftype `Protocol not found: IEquiv`、
kotoba-rad.cacao-delegate、datalog.index という**異なる failure クラスが実在**。
authority.scope は load 連鎖の終端。plist classpath は既に nekko/io-ipld/cacao/
datalog を含むが同一で失敗しており、**修理案 A の 3 src 追加だけでは ipld IEquiv
(load 段階) と cacao-delegate (解決) には触れないため「唯一かつ十分」でない**。
score 3 のまま (root cause を単独→連鎖へ範囲修正、job は未修理のまま)。

## スコープ修正 / 次の 1 アクション
- falsify-58 の単独原因記述を「複数 load 失敗の連鎖 (終端 = authority.scope)」に範囲修正
- 次の測定: nbb+Node v26 下での ipld `#?@(:clj ...)` reader 条件 / IEquiv protocol
  解決の個別再現 (Tier 2 附帯提案): repair は 3-src 案 A + 「連鎖全体の精査 / nbb
  compatibility 再確認」