# falsify-104 (2026-09-09, 運用/滞留 軸 — 0 ベースの 20 時点観測 / 19 反復連続維持で恒久化傾向を更に精緻化、avail の 116 Gi 再低下で「低位回復」仮説は REFUTED)

測定日: 2026-09-09 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (/Users/junkawasaki/.hermes/profiles/itonami-maint/wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app) は読み取り専用参照のみ、一切 touch せず。

## 背景 / 測定対象

falsify-103 (運用軸) は 3 ベクトルの正規 unused 0 / local annex keys 786 (19 時点観測 / 18 反復連続維持) を実測し
falsify-101 指定の閾値「17 反復超」を充足、avail は df 実測 127 → **122 Gi** (annex available 136.02 → 130.49 GB、
約 -5 Gi / -5.53 GB) と falsify-101 の 127 Gi からの回復は認められず継続低下を観測した。次の 1 アクションは
「同 vector + keys + avail を再実測し **18 反復超の連続持続** or 再蓄積再開を判別、avail **122 Gi の更なる継続低下
(100 Gi 未満で緊急フラグ) vs 減速したままの低位定着 (122 Gi 前後) vs 回復 (148-163 Gi 帯)** を観測」。

本反復 (falsify-104) はその次の 1 アクションを執行し、0 ベースの「18 反復超の連続持続」と avail の 122 Gi 定着を falsify する。

**主張**: 本反復の再実測で (a) 3 ベクトルのいずれかに正規 unused が再出現する、または
(b) local annex keys が 786 から増加する (18 反復超への持続は成立せず残留再蓄積を開始する)、
または (c) avail が 148-163 Gi 帯へ回復する (falsify-101/103 の低位が一過性の低位振動である)。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`)。

### 1. main-refspec regular unused

`git annex unused --used-refspec +refs/heads/main` → output の `MD5E|SHA` 行カウント **14** = partial
transfer 残骸 14 chunk のみ (NUMBER 1..14)、正規 unused key **0**。`ok` / EXIT_RC=0。

### 2. 全 refs 走査 (孤児ベクトル)

`git annex unused` (refspec なし) → 同様に正規 unused **0 keys**、partial 14 chunk のみ (`MD5E|SHA` 14 行)。
いかなる ref からも到達不能な孤児 key は存在しない。`ok`。

### 3. resident-refspec (帰属ベクトル)

`git annex unused --used-refspec +refs/heads/resident/dns-resolver` → 同様に正規 unused **0 keys**、
partial 14 chunk のみ (`MD5E|SHA` 14 行)。`ok` / EXIT_RC=0。

### 4. git annex info

```
local annex keys: 786        (falsify-104: 786 — 変化 0、20 時点観測 / 19 反復連続維持)
local annex size: 11.68 gigabytes   (不変)
available local disk space: 124.99 gigabytes (+100 megabytes reserved) (falsify-103: 130.49
                              GB — 約 -5.50 GB 再低下)
temporary object directory size: 186.98 megabytes (partial chunk 14、不変)
annexed files in working tree: 7143 / 93.02 gigabytes ([b2] 側、不変)
combined annex size of all repositories: 111.12 gigabytes (不変)
```

### 5. df avail (実測)

`df -h /` → avail **116 Gi / 13%** (falsify-103 の 122 Gi から **約 -6 Gi 再低下**)。annex available
local disk space は 124.99 GB (falsify-103 の 130.49 GB 比 約 -5.50 GB、df と同方向)。**falsify-103 の
122 Gi からの回復は認められず、更に -6 Gi 低下** — 主張 (c) の「148-163 Gi 帯へ回復」は本反復で成立せず
(主張 (c) REFUTED)。観測系列は falsify-100 (156) → 101 (127, -29) → 103 (122, -5) → 本反復 (**116**, -6)
と、falsify-101 の -29 Gi ステップダウン後に軽減幅 (-5 〜 -6 Gi) で継続低下が続く。低下幅は -29 Gi の大幅
ステップから小幅 (≈-5〜-6 Gi) に減速したまま継続中（structural-decline は続行、100 Gi 未満の緊急フラグには
未到達）。

### 6. 増加源プロセス / 帰属

`ps aux | grep -E 'itonami-app-resident|export_and_sync'` → 増加源 `node nbb
~/.gftd/bin/itonami-app-resident.cljs` **PID 70643 稼働継続** (falsify-85..103 と同一 PID のまま、
連続稼働、export_and_sync 不在)。ps 出力の他ヒットは測定ハーネス自身の自助マッチのみで実増加源ではない
(runbook falsify-99..103 と同様に除外)。local annex keys 786 / 11.68 GB 不変ゆえ **avail の継続低下
(-6 Gi / -5.50 GB) は local annex に帰属しない** — annex 外部の書き込み源に起因 (falsify-94..103 と同所見)。
TCC 遮断 (onedrive / ~/.gftd du 不能) のため具体源特定は引き続き本 bot では不能 (operator の du 調査を
異常系・優先度高で Tier 2 継続)。

### 7. origin/main (実装軸 附帯)

`git fetch origin` → origin/main は **b1b4b85f** (falsify-102/103 と同一 SHA、kotoba.lang.text 配線着地のまま、
`rev-list --count b1b4b85..origin/main` = 0)。本体 checkout は detached HEAD b1b4b85 / porcelain clean
(`status --porcelain` 0 行)。

## 判定

**SURVIVED (主張 a/b) / REFUTED (主張 c — avail 回復)**: falsify-103 から本反復への独立時点で
3 ベクトル (main / 全 refs / resident-refspec) の正規 unused は **全て 0 のまま**、local annex
keys は **786 → 786 (増加 0, falsify-85→104 の 20 時点観測 / 19 反復連続維持)**、既知 partial
14 chunk のみ — 主張 (a) の再出現も (b) の再蓄積も検出されず、falsify-103 指定の閾値「18 反復超の
連続持続」を充足 (19 反復 > 18) し 0 ベース恒久化傾向を**更に精緻化**した。

他方、**avail は df 実測 122 → 116 Gi (約 -6 Gi)、annex available 130.49 → 124.99 GB (約 -5.50 GB) と
falsify-103 の 122 Gi からの回復は認められず継続低下** — 主張 (c) の「148-163 Gi 帯へ回復 (一過性低位)」は
本反復で成立せず REFUTED。falsify-101 の -29 Gi ステップダウンの低位は一過性でなく継続低下中である
(ただし低下幅は -29 → -5 → -6 Gi と小幅に減速継続)。local annex keys 786 / 11.68 GB 不変ゆえ低下は
annex 外部の書き込み源に帰属するが、TCC 遮断により具体源は特定不能。100 Gi 未満の緊急フラグには未到達。

運用/滞留 軸 score は **3 のまま** (0 ベース恒久化傾向の更なる精緻化を記録する一方で、avail の継続低下は
risk-5 / structural-decline-monitor の経過観測継続として記録 — score は実緑測定でのみ上昇する規約、
低下ペナルティも本反復では付さず経過観測)。

## スコープ修正 / 次の 1 アクション

- risk-5 の記述を「滞留は 0 ベースへ回帰し **falsify-85→104 の 20 時点観測 / 19 反復連続維持**
  (unused 0・keys 786 不動・partial 14 chunk のみ) — **恒久化傾向の更なる精緻化継続**。
  ただし増加源 (resident ingest, PID 70643) 稼働継続ゆえ再蓄積の可能性は残る」と再アンカー。
- risk-5 の avail 監視シグナルを更新: df avail は **122 → 116 Gi と継続低下** (annex available 130.49 →
  124.99 GB 約 -5.50 GB も同方向)。falsify-101 の -29 Gi ステップダウン (156→127) は一過性でなく、
  **小幅 (-5〜-6 Gi) に減速したまま継続低下中** と確定 (回復仮説 REFUTED)。100 Gi 未満で緊急フラグ、
  150 Gi 定着 (falsify-100 枠) へ回復したら structural-decline-monitor 解除の判定。consume は annex 外部
  (keys 786 / 11.68 GB 不変) に帰属、TCC 遮断で特定不能 (operator の du 調査を異常系 Tier 2 継続)。
- 次の 1 アクション: 次反復で同 vector + keys + avail を再実測し、0 ベース維持の**19 反復超の
  連続持続** or 再蓄積再開を判別 (19 反復超維持なら恒久化傾向を更に精緻化、再開なら falsify-33..53 語調へ
  復帰)。あわせて **avail 116 Gi の更なる継続低下 (100 Gi 未満で緊急フラグ) vs 減速したままの低位定着
  (116 Gi 前後) vs 回復 (148-163 Gi 帯、structural-decline-monitor 再評価)** を観測。TCC 遮断下の帰属は
  operator の du 調査を異常系・優先度高の Tier 2 として要請継続。
- 附帯 (実装軸 次反復シグナル): origin/main は **b1b4b85f のまま** (falsify-102/103 と同一、新規前進なし)。
  次の実装軸反復では b1b4b85f の parity 測定面 (src 214 / test 237 anchor、falsify-102) と 2 build-break
  (identity.clj / kotoba-net) の生存を再確認する。
- 附帯 Tier 2: falsify-85..103 と不変 — resident プロセス再起動 (94682→70643) の契機と滞留回収の帰属、
  および falsify-101 以降の avail 継続低下 (127→122→116 Gi、統合 -40 Gi) の具体源特定 (TCC 遮断) は
  kanban/human へ異常系として透過提出継続。

## 附帯 (記録上の整合)

- 本体 checkout (orgs/cloud-itonami/cloud-itonami-app)、`git status --porcelain` 0 行 = porcelain clean
  (dirty 0)。
- origin/main は b1b4b85f のまま (falsify-102/103 と同一、実装軸 新規シグナルなし)。
- 本反復は運用/滞留 軸の falsify-103 ハンドオフ (次の 1 アクションへ戻る) を測定対象に選択。
- 既知の残骸は残存のまま (append-only 規約で本 bot は編集しない):
  7-軸表 16 行目孤立行 `| 反証候補falsify30PLACEHOLDER` (falsify-33 以降)、falsify-46 mid-sentence cut、
  falsify-91 末尾残骸 (行 1237-1238 `MD` / `echo ...`)。
- identity.clj malformation / kotoba-net 未宣言 (falsify-63..102) は既知赤継続、本反復の対象外。