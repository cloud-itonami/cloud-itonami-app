# falsify-107 (2026-09-09, 運用/滞留 軸 — 0 ベースの 22 反復超連続維持 (23 時点観測)、avail が単反復で 114 Gi → 36 Gi 帯へ大規模ステップダウン (約 -78 Gi)、**100 Gi 未満緊急フラグ到達**、origin 失踪は「origin なし remote 名変更」で解明)

測定日: 2026-09-09 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(cloud-itonami-app, HEAD b1b4b85f) は読み取り専用参照のみ、一切 touch せず。

## 背景 / 測定対象

falsify-106 は 0 ベース 21 反復超維持・avail 低下停止 (113→114 Gi 微増) を記録し、
次の 1 アクションとして「同 vector + keys + avail 再実測、21 反復超の連続持続判定、
avail 114 Gi の低下停止持続 vs 低下再開 vs 回復を観測。加えて origin ls-remote 失敗の
原因を `git remote -v` で特定し origin/main 前進確認を復旧」を指定した。本反復はその執行。

**主張**: (a) 3 ベクトルのいずれかに正規 unused が再出現する、または (b) local annex
keys が 786 から増加する、または (c) avail が falsify-106 の 114 Gi から **低下再開せず
113-114 Gi 帯に定着する** (低下停止の持続)。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD 566928f6701df95d8cf0b62c5455c9d0baf46226、falsify-98..106 と同一、porcelain 0 行 = clean)。

### 1. main-refspec regular unused

`git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys** (partial
transfer 残骸 14 chunk のみ、NUMBER 1..14 の同一 14 key 集合)。`ok` / rc=0。

### 2. 全 refs refspec

`git annex unused --used-refspec '+refs/*'` → 正規 unused **0 keys** (同一 partial 14
chunk 集合)。`ok` / rc=0。

### 3. local annex keys

`git annex info` → **local annex keys: 786** (増加 0)、local annex size **11.68 GB**
(不変)、annexed files in working tree 7143 / 93.02 GB、combined 111.12 GB、temporary
object directory 186.98 MB。

### 4. avail — **大規模ステップダウンを観測**

`df -m /` は本反復内の複数時点で **Available 36580 / 36596 / 36851 / 36870 / 36876 /
37158 MB ≒ 35.7-36.3 Gi** (falsify-106 の df 116596 MB ≒ 113.9 Gi 比 **約 -77.6〜-78.1
Gi の単反復ステップダウン**)。`git annex info` の available local disk space は
**38.26〜38.86 GB** の間で振動 (falsify-106 の 122.16 GB 比 **約 -83.3〜-83.9 GB**)。
**100 Gi 未満の緊急フラグに到達した** (35-36 Gi ≪ 100 Gi)。falsify-106 の「低下停止・
微増転換 / 113-114 Gi 帯定着シグナル」は本反復で帯外逸脱し成立しなくなった。
5-20 秒間隔の反復 df で 36.0→35.7 Gi と短期間でも微減が継続 (減速後も動的)。
local annex keys 786 / 11.68 GB 不変ゆえ減退は annex 外部 (TCC 遮断で具体源特定不能、
operator の du 調査を異常系・最優先で Tier 2 提出 — falsify-101 以来の提案を強度アップで再提出)。

### 5. 滞留増加源

`ps aux | grep -E 'itonami-app-resident|export_and_sync' | grep -v grep` → 増加源
**PID 70643 のみ** (node nbb itonami-app-resident.cljs、falsify-85..106 と同一 PID、
export_and_sync 不在)。自助マッチ行は grep -v grep で除外。

### 6. origin 失踪の原因特定 (falsify-106 の指定アクション、実施)

`git remote -v` → **origin は存在しない**。実在 remote は `cloud-itonami`
(git@github.com:cloud-itonami/cloud-itonami-dns-resolver) と `kotobase` のみ —
remote 名の変更/削除が原因で、「一時的ネットワーク」ではなかった。
代替として `git ls-remote cloud-itonami main` は成功し
**566928f6701df95d8cf0b62c5455c9d0baf46226** (測定 repo の HEAD と同一 sha) を返した。

### 7. 附帯 (実装軸) — origin/main 前進確認の復旧

本体 cloud-itonami-app には origin が実在し、`git ls-remote origin main` は成功:
**f54113547ace50de59f0651567551c920f67ce58**。本体 checkout HEAD は b1b4b85f のまま —
**origin/main は b1b4b85f から前進している** (falsify-102..105 の 4 反復連続
no-advance 解消、測定は ls-remote のみで本体 touch なし)。

## 判定

- **主張 (a) REFUTED**: 正規 unused 0 のまま (partial 14 chunk のみ)。
- **主張 (b) REFUTED**: keys 786 → 786。→ falsify-106 指定の閾値「21 反復超の連続持続」
  を充足 (22 反復 > 21)、0 ベース恒久化傾向を更に精緻化 (**SURVIVED**、23 時点観測 /
  falsify-85→107)。
- **主張 (c) REFUTED**: 113-114 Gi 帯定着は成立せず、**avail は 114 Gi → 35.7-36.3 Gi
  (df) / 122.16 → 38.3-38.9 GB (annex info) への約 -78 Gi / -83 GB ステップダウン**を
  実観測。falsify-101 の -29 Gi を大幅に上回る史上最大の単反復減退で、**100 Gi 未満
  緊急フラグに到達**。structural-decline-monitor を緊急観測モードへ移行 (低下の持続性
  観察から即時性重視へ)。減退は annex 外部 (keys 不変)。
- **origin 失踪は解決**: dns-resolver repo の remote は origin→cloud-itonami に
  変更/再構成済み (ls-remote cloud-itonami は成功)。一時的ネットワーク障害ではなかった。

score 3 のまま (0 ベース恒久化傾向の更なる精緻化、実緑測定でのみ上昇する規約、
avail ステップダウンと緊急フラグ到達は risk-5 / structural-decline-monitor 緊急観測
モード移行として記録)。

次の 1 アクション: 同 vector + keys + avail を再実測し **23 反復超の連続持続** or 再蓄積
再開を判別、avail 36 Gi 帯の更なる低下 (緊急観測の継続深度判定) vs 定着 vs 回復を観測。
加えて: (1) 本体 app の origin/main f541135f への前進追従は本体 checkout が他 bot WIP
に触れない範囲でのみ可能 — kanban/human へ透過、(2) dns-resolver の remote 名変更
(origin → cloud-itonami) の意図を operator 確認事項として Tier 2 記録、(3) avail 緊急
フラグ下での operator du 調査を最優先 Tier 2 として再提出。
(evidence/2026-09-09-falsify-107.md)
