# falsify-91 (2026-09-08, 運用/滞留 軸 — 0 ベースの 6 反復超連続維持 (恒久化傾向の更なる精緻化) 判定)

測定日: 2026-09-08 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app) は読み取り専用参照のみ、一切 touch せず。

## 背景 / 測定対象

falsify-90 は 3 ベクトル (main / 全 refs / resident-refspec) の正規 unused 0・local annex
keys 786 不動を falsify-85 以降 6 時点観測で再実測し、0 ベースを「恒久化傾向の精緻化」へ
更新した。その次の 1 アクションは「次反復で同 3 ベクトル + local annex keys + avail を
再実測し、0 ベース維持の **6 反復超の連続持続** or 再蓄積再開を判別 (6 反復超維持なら恒久化
傾向を更に精緻化)、あわせて df avail の振動 (162→156→142→159 Gi) の帰属 (滞留不変下の
外部書き込み源) を観測」。あわせて実装軸附帯で「e91bf0f (vf-authority migration) の parity
測定面と 2 build-break の生存を次反復で再確認」と指定されていた。

本反復 (falsify-91) はその falsify-90 次の 1 アクションを執行し、falsify-90 が指定した閾値
「0 ベースの **6 反復超の連続維持** → 恒久化傾向の更なる精緻化」の充足を falsify する。

**主張**: 本反復の再実測で (a) 3 ベクトルのいずれかに正規 unused が再出現する、または
(b) local annex keys が 786 から増加する (6 反復超越への持続は成立せず残留再蓄積を開始する)、
または (c) avail が 162→156→142→159 の振動幅を超えて低位 (150 Gi 未満) へ続落する。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD detached 566928f67、resident branch は `resident/dns-resolver`)。

### 1. main-refspec regular unused

`git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys** (出力は partial
transfer 残骸 14 chunk のみ、NUMBER 1..14 の MD5E-s... 14 行)。`ok` / EXIT_RC=0。

### 2. resident-refspec (帰属ベクトル)

`git annex unused --used-refspec +refs/heads/resident/dns-resolver` → 同様に正規 unused
**0 keys**、partial 14 chunk のみ。`ok` / EXIT_RC=0。

### 3. 全 refs 走査 (孤児ベクトル)

`git annex unused` (refspec 指定なし) → 同様に正規 unused **0 keys**、partial 14 chunk
のみ。いかなる ref からも到達不能な孤児 key は存在しない。`ok` / EXIT_RC=0。

### 4. git annex info

```
local annex keys: 786        (falsify-91: 786 — 変化 0、7 時点観測 / 6 反復連続維持)
local annex size: 11.68 gigabytes   (不変)
available local disk space: 170.65 gigabytes (falsify-91: +0.54 GB 微増、stable)
temporary object directory size: 186.98 megabytes (partial chunk 14、不変)
annexed files in working tree: 7143 / 93.02 gigabytes ([b2] 側)
combined annex size of all repositories: 111.12 gigabytes
```

3 ベクトルが全て正規 unused 0 のまま。local annex keys は **786 → 786 (増加 0)** — 滞留
蓄積期 (falsify-39..49) の反復あたり +24/+25 単調増加とは対照的に、増分ゼロの状態が
**falsify-85→91 の 7 時点観測 / 6 反復連続維持**で続いている (falsify-90 の 6 時点を 1 時点上回る)。

### 5. df avail / 増加源プロセス

`df -h /` → avail **159 Gi / 10%** (falsify-90 の 159 Gi と同値、飽和なし・100% 標示なし)。
annex available local disk space は 170.65 GB (falsify-90 の 170.11 GB 比 +0.54 GB、df と
同方向の極小増)。avail は falsify-85 (163) → 90 (159) → 本反復 (159) で 159 Gi へ安定、
falsify-89 で観測された 2 反復跨ぎ減少 (162→156→142) はトレンドでなく、続落せず維持を再確認。

→ **主張 (c) は SURVIVED (avail は 159 Gi で安定、150 Gi 未満への続落なし)**: 滞留量 (keys
786) が不変かつ avail が 9 Gi 帯に安定している以上、帰属は falsify-90 の「滞留不変下の外部
書き込み変動」と整合し続ける。外部書き込み源の特定は引き続き継続観測対象。

`ps aux | grep -E 'itonami-app-resident|export_and_sync'` → 増加源 `node nbb
~/.gftd/bin/itonami-app-resident.cljs` **PID 70643 稼働継続** (falsify-85/86/87/88/89/90 と
同一 PID のまま、連続稼働、export_and_sync は不在)。

### 6. origin/main (実装軸 附帯)

`git fetch origin` → origin/main は **e91bf0f のまま前進 0** (falsify-90 と同一 SHA、
falsify-89→90 の 52c156c→e91bf0f 前進以降は静止)。`rev-parse origin/main = e91bf0f...`。
`git status --porcelain` = 0 行 (本体 porcelain clean、dirty 0)。HEAD 65d3e98 (#290) 不変。

## 判定

**0 ベースは 6 反復超の連続維持 (7 時点観測跨ぎ) へ達し「恒久化傾向の更なる精緻化」を達成、
主張 a/b/c はいずれも不成立**:
falsify-90 から本反復への独立時点で 3 ベクトルの正規 unused は **全て 0 のまま**、local
annex keys は **786 → 786 (増加 0, falsify-85→91 の 7 時点観測 / 6 反復連続維持)**、既知
partial 14 chunk のみ — 主張 (a) の再出現も (b) の再蓄積も検出されなかった。増加源 (resident
PID 70643) は稼働継続のまま、新規 key を参照させる滞留を積まない状態が 6 反復 (観測 7 時点)
にわたり維持されている。avail は 159 Gi で安定し、主張 (c) の 150 Gi 未満への続落も不成立。

falsify-90 が指定した閾値「0 ベースの 6 反復超の連続維持」を本反復が充足 — 恒久化傾向を
**更に精緻化**した。ただし増加源 (PID 70643) 稼働継続ゆえ恒久性は保証されず「never
re-accumulate」は主張しない。

運用/滞留 軸 score は **3 のまま** (0 ベースの恒久化傾向精緻化を記録、green 化測定/実緑の
裏付けは本反復では得られない — score は実緑測定でのみ上昇する規約)。

## スコープ修正 / 次の 1 アクション

- risk-5 の記述を「滞留は 0 ベースへ回帰し **falsify-85→91 の 7 時点観測 / 6 反復連続維持**
  (unused 0・keys 786 不動・partial 14 chunk のみ) — **恒久化傾向の更なる精緻化継続**」と
  再アンカーする。ただし増加源 (resident ingest, PID 70643) 稼働継続ゆえ再蓄積の可能性は残る。
- 次の 1 アクション: 次反復で同 vector を再実測し、0 ベース維持の**7 反復超の連続持続** or
  再蓄積再開を判別 (7 反復超維持なら恒久化傾向を更に精緻化、再開なら falsify-33..53 語調へ
  復帰)。あわせて avail (159 Gi 安定) の帰属 — 滞留 (keys 786) 不変下の repos示す外部書き込み源
  の特定を観測。
- 附帯 (実装軸 次反復シグナル): origin/main は **e91bf0f のまま前進 0** (falsify-90 と同一)。
  falsify-90 附帯で「e91bf0f の parity 測定面・2 build-break (identity.clj / kotoba-net) を
  実装軸で再確認」が指定されたままであり、次の実装軸反復で e91bf0f の主要素系 (parity 握手で
  3 値結合水準・配線記録の変化有無) と build-break 不信を実測する。本体 checkout は detached
  HEAD 65d3e98 (#290) / porcelain clean。
- 附帯 Tier 2: falsify-85/86/87/88/89/90 と不変 — resident プロセス再起動 (94682→70643) の
  契機と滞留回収の掃属 (operator 手動 dropunused/世代削除 vs resident 挙動変化) は継続
  kanban/human へ透過提出。

## 附帯

本体 CHECKOUT (worktree list 先頭
`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app`) は
detached HEAD 65d3e98 (#290)、`git status --porcelain` 0 行 = clean (dirty 0)。origin/main は
本反復で前進 0 (e91bf0f のまま) — 実装軸次反復シグナル (仮の附出武装) として、falsify-90 が
指定した e91bf0f の parity 測定面と 2 build-break の再確認を次反復へ繰り越し。本反復は運用軸
の falsify-90 次の 1 を測定対象に選択し、作業は wt-msloop / bot/maturity-sim-loop で完結、
本体 checkout を一切 touch せず。identity.clj malformation / kotoba-net 未宣言 (falsify-63..84)
は対象外・既知赤継続 (e91bf0f の build-break 接触は本反復の運用軸測定に関する限り影響なし)。
7-軸表 16 行目未動行 placeholder / falsify-46 mid-sentence cut は残存のまま (append-only 規約で
本 bot は編集しない)。(evidence/2026-09-08-falsify-91.md)