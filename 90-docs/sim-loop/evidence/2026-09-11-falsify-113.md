# falsify-113 (2026-09-11, 運用/滞留 軸 — 0 ベースの 18 反復超連続維持 (19 時点観測)、avail は ±10 Gi 級ステップ低下が **2 反復連続発火** (df 183066 → 172181 MB ≒ 178.8 → 168.1 Gi)、resident プロセスは **実測不在** (61573 消滅、後継不明)、dns-resolver HEAD fd9aa865 → c8090649 へ前進)

測定日: 2026-09-11 (cron 反復) / 測定者: itonami-maint
作業は sibling worktree (wt-msloop / bot/maturity-sim-loop) で完結。本体 checkout
(orgs/cloud-itonami/cloud-itonami-app) は読み取り専用参照のみ、一切 touch せず。
生の出力全体は evidence/2026-09-11-falsify-113-raw.txt に保存。

## 背景 / 測定対象

falsify-112 の次の 1 アクションは「同 vector + keys + avail を再実測し、0 ベース維持の
**18 反復超の連続持続** or 再蓄積再開を判別、avail の 178.8 Gi 帯の維持 vs ±10 Gi 級
ステップ変動の継続/再発を観測」だった。本反復 (falsify-113) はその実行。

**主張**: 本反復の再実測で (a) 3 ベクトルのいずれかに正規 unused が再出現する、または
(b) local annex keys が 786 から増加する (再蓄積再開)、または (c) avail が
falsify-112 実測値 (183066/183048 MB ≒ 178.8 Gi) から ±10 Gi 超/反復の大幅変動を
起こす (ステップ変動検知の継続/再発)。

## 実測 (実コマンド出力をリダイレクト、捏造なし)

測定リポジトリ: cloud-itonami-dns-resolver
(`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-dns-resolver`、
HEAD **c80906496cc64cfc60d0b2c600ec9a356f1d9a3b** — falsify-111/112 の fd9aa865 から
**前進** (新規観測)。porcelain 0 行 = clean、branch 表示なし (detached HEAD 相当))。

### 1-3. main / resident / 全 refs の 3 ベクトル regular unused

- `git annex unused --used-refspec +refs/heads/main` → 正規 unused **0 keys**、partial
  transfer 残骸 14 chunk (NUMBER 1..14、MD5E-s... 14 行、falsify-85 以降と同一内容) のみ。
  `ok`。
- `git annex unused --used-refspec +refs/heads/resident/dns-resolver` → 同様に正規 unused
  **0 keys**、partial 14 chunk のみ。`ok`。
- `git annex unused` (refspec 指定なし) → 同様に正規 unused **0 keys**、partial 14 chunk
  のみ。孤児 key なし。`ok`。

### 4. git annex info

```
local annex keys: 786        (falsify-112: 786 — 変化 0、19 時点観測 / 18 反復連続維持)
local annex size: 11.68 gigabytes   (不変)
available local disk space: 180.45 gigabytes (+100 megabytes reserved) (falsify-112: 191.86 GB — 約 -11.4 GB)
temporary object directory size: 186.98 megabytes (partial chunk 14、不変)
annexed files in working tree: 7143 / 93.02 gigabytes ([b2] 側、不変)
combined annex size of all repositories: 111.12 gigabytes (不変)
```

### 5. df / resident プロセス

- `df -m` 実測: `/dev/disk3s5` (System/Volumes/Data) available **172181 MB ≒ 168.1 Gi**。
  falsify-112 の 183066/183048 MB ≒ 178.8 Gi から **約 -10.7 Gi の低下**。
- `ps aux | grep itonami-app-resident | grep -v grep` → **ヒット 0 行 (rc=1)** —
  falsify-112 実測の PID 61573 (12:42PM 起動) は本反復時点で**消滅し、後継 resident
  プロセスは実測不在**。falsify-85..112 では常時「増加源が在る」か「在る (PID 変化)」
  だったため、実測不在は**新規観測事実** (ただし本 grep は resident 文字列のみ、
  export_and_sync は別パターンで未併走 — 次反復で併走する)。

## 判定

**SURVIVED (主張 a/b — 0 ベース) / 成立 (主張 c — avail ステップ変動 2 反復連続)**:
falsify-112 から本反復への独立時点で 3 ベクトルの正規 unused は全て 0 のまま、local
annex keys は 786 → 786 (増加 0、19 時点観測 / 18 反復連続維持) — 主張 (a) 再出現も
(b) 再蓄積も検出されず、falsify-112 指定の閾値「18 反復超の連続持続」を充足し 0 ベース
恒久化傾向を更に精緻化した。

他方、**avail は df 183066/183048 → 172181 MB ≒ 178.8 → 168.1 Gi、annex available
191.86 → 180.45 GB と約 -10.7 Gi / -11.4 GB の低下** — 主張 (c) 成立、falsify-112
に続く **±10 Gi 級ステップ変動検知の 2 反復連続発火** (低下方向 -10.1 → -10.7 Gi、
ほぼ同規模の単調系列)。2 反復連続の同方向・同規模低下は falsify-111 の「外部源起因の
揺れ範囲内」単発解釈では説明が弱く、**短周期の構造的低下再開シグナル**として
structural-decline-monitor を「±10 Gi 級低下の継続観察」モードへ更新 (緊急フラグ
(<100 Gi) は非該当)。keys 786 / 11.68 GB 不変ゆえ低下は annex 外部 (TCC 遮断で
特定不能、operator の du 調査を Tier 2 継続)。観測系列 113.9 → 34.5 → 34.7 → 34.0 →
188.9 → 178.8 → 168.1 Gi。

**増加源 (resident) の実測不在は新規事実**: falsify-111 の 62791 → falsify-112 の
61573 と短期再起動が続いた後、本反復では resident プロセス自体が実測不在 (grep rc=1)。
70643 → 62791 → 61573 → (不在) の系列は「常駐が常態」の前提自体を揺るがす観測であり、
avail の -10.7 Gi 低下と resident 停止が同時点に観測されたことは相関候補として記録
(因果は特定不能、TCC 遮断下の帰属は operator の du 調査 Tier 2 継続)。

運用/滞留 軸 score は **3 のまま** (0 ベース 18 反復超連続維持を記録。実緑測定でのみ
上昇する規約、avail 低下は新規観測として記録)。

## スコープ修正 / 次の 1 アクション

- risk-5 の記述を「滞留は 0 ベースで **falsify-85→113 の 19 時点観測 / 18 反復連続維持**
  (unused 0・keys 786 不動・partial 14 chunk のみ) — falsify-112 指定の閾値「18 反復超」
  を充足、恒久化傾向の更なる精緻化継続。ただし resident 実測不在時点での avail 低下が
  同時観測されており、恒久化の前提 (resident ingest) 自体が変化した可能性を付記」へ
  再アンカー。
- avail 監視は falsify-111 の簡素化枠組み (単時点実測値の忠実記録 + ±10 Gi 超/反復の
  大幅変動検知) を継承し、本反復で **2 反復連続発火** (-10.1 → -10.7 Gi) を確認 —
  structural-decline-monitor を「±10 Gi 級低下の継続観察」モードへ更新、3 反復連続で
  構造的低下と確定判定。観測系列 113.9 → 34.5 → 34.7 → 34.0 → 188.9 → 178.8 → 168.1 Gi。
- 次の 1 アクション: 同 vector + keys + avail を再実測し、0 ベース維持の
  **19 反復超の連続持続** or 再蓄積再開を判別。あわせて (1) avail の -10 Gi 級低下が
  3 反復連続 (構造的低下確定) か否か、(2) resident 不在の継続 (後継プロセスの実測) を
  観測 — `ps aux | grep -E 'itonami-app-resident|export_and_sync'` を併走し自助マッチ行は
  grep -v grep で除外。(3) dns-resolver HEAD が fd9aa865 → c8090649 へ前進した要因
  (resident 停止と同時期の fetch/pull 実行者) は operator 確認事項として Tier 2 提出。
- 附帯 (実装軸 次反復シグナル): 本体 origin/main は **33037469 へ前進** (5b2ae3de から
  更新、`git log --oneline -1` 相当は "Merge kbb cutover
  (adr-2609112000-kbb-cutover-clojure-cli-text)")。本体 HEAD 33037469 と一致、porcelain
  0 行 = clean。次の実装軸反復では 5b2ae3de → 33037469 間の parity 測定面 (src/test
  ファイル数、結線記録) の変化を実測する。実装軸の修理案は Tier 2 kanban/human 継続:
  (1) identity.clj の裸 [java.*] ベクタを `(:import ...)` に wrap、(2) kotoba.net 座標を
  deps.edn に宣言 (git sha 或は :local/root)。
- 附帯 Tier 2: resident 不在の契機と後継 (itonami-app-resident 起動管理は誰が担うか)、
  avail 変動の具体源特定 (TCC 遮断)、dns-resolver HEAD 前進の実行者、kanban/human へ
  透過提出継続。

## 附帯

本体 CHECKOUT (worktree list 先頭
`/Users/junkawasaki/github/com-junkawasaki/orgs/cloud-itonami/cloud-itonami-app`) は
HEAD 33037469dac1237f354c91fbe6378f19ae845de7、`git status --porcelain | wc -l` = 0 =
porcelain clean (dirty 0)。本反復は運用軸の falsify-112 次の 1 アクションを測定対象に
選択し、作業は wt-msloop / bot/maturity-sim-loop で完結、本体 checkout を一切 touch せず
(fetch origin main 1 回は origin/main 前進確認のための読み取り、本体 working tree 不変)。
identity.clj malformation / kotoba-net 未宣言 (falsify-63..84) は対象外・既知赤が継続。
7-軸表 16 行目孤立行 placeholder / falsify-46 mid-sentence cut / falsify-91 末尾残骸は
残存のまま (append-only 規約で本 bot は編集しない)。
(evidence/2026-09-11-falsify-113.md)
