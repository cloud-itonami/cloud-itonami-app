# falsify-76 (2026-09-08, 実装 軸 — build/dependency integrity)

falsify-75 (実装 軸) は「falsify-74 境界 (a): identity.clj 修理後は updater_test 単体動行で
verify-manifest の緑を再確認できる」を REFUTED し、tip 679572b に identity.clj malformation
に**加え**第二の独立 build-break「kotoba-net (kotoba.net.jvm-host) 未宣言依存」が存在することを
確定した。その「次の 1 アクション」は「kotoba-net を deps.edn に宣言した後の detached worktree
での updater_test 単体動行緑確認」。本反復はその修理案第 1 歩を**単独で**実行し、特定の反証可能
主張を検証する。作業は wt-msloop / bot/maturity-sim-loop で完結、本体 checkout (679572b) は touch
しない。

## 反証可能主張 (本反復の claim)

**「falsify-75 の修理案第 1 歩 (kotoba-net :local/root 宣言) のみで、updater-test 単一 ns の
require/compile は jvm-host 未解決を越えて進む — updater_test → updater → http-client → jvm-host
連鎖に第三の未宣言依存は存在しない。」** もし第三の未宣言依存があれば、kotoba-net を宣言しても
updater_test は別の FileNotFoundException で死ぬはず → REFUTED。green まで動けば SURVIVED。

## 実測 (リダイレクト記録、捏造なし)

1. 本体/tip 不変: drain HEAD = origin/main = **679572b** (falsify-63..75 と同一 SHA)、本体 porcelain
   clean (dirty 0)。git ls-tree 実測: **src 240 / test 243** (falsify-75 と一致)。

2. **修理案第 1 歩のみで単体実行 (detached worktree + kotoba-net 宣言、identity.clj 無修正)**:
   detached worktree `/private/tmp/mt-msloop76` (head 679572b、本体未 touch) で、falsify-75 と同じ
   「:test の :main-opts (全 suite 強制 require) を避けた単一 ns 実行」を、ただし `-Sdeps` で
   `io.github.kotoba-lang/kotoba-net {:local/root "/Users/junkawasaki/github/com-junkawasaki/orgs/
   kotoba-lang/kotoba-net"}` を追加して実施:
   ```
   clojure -Sdeps '{:aliases {:f76 {:extra-deps {io.github.kotoba-lang/kotoba-net
     {:local/root "/Users/junkawasaki/github/com-junkawasaki/orgs/kotoba-lang/kotoba-net"}}
     :extra-paths ["test"]}}}' -M:f76 -e
     "(require 'cloud.itonami.app.updater-test)(clojure.test/run-tests
       'cloud.itonami.app.updater-test)"
   ```
   → (f76.log 実測)
   ```
   Testing cloud.itonami.app.updater-test
   Ran 6 tests containing 19 assertions.
   0 failures, 0 errors.
   {:test 6, :pass 19, :fail 0, :error 0, :type :summary}
   rc=0
   ```
   **EXIT_RC=0、6 tests / 19 assertions / 0 failures / 0 errors。** falsify-75 が同一連鎖で
   `Could not locate kotoba/net/jvm_host__init.class` (FileNotFoundException, rc=1) で停止したの
   に対し、kotoba-net の :local/root 宣言**単独**で jvm-host 未解決は解消し、identity.clj に一切
   touch せずに updater_test は green 完走した。

3. **load path が identity.clj に到達しないことを source で確定**: updater_test.clj の require は
   clojure.data.json / clojure.edn / clojure.java.io / clojure.test / config / updater。updater.clj:13
   は config + http-client (http_client.clj:16 が kotoba.net.jvm-host)。config.clj の require は
   clojure.edn / clojure.java.io / config-policy / clojure.string / policy (config.clj 実測) — いずれも
   `cloud.itonami.app.identity` に到達しない。つまり updater_test の load chain には identity.clj が
   載っておらず、green 完走はこれと整合 (identity.clj へ touch 不要)。

## 判定

**SURVIVED (claim 成立)、かつ falsify-75 修理案の必要十分性を正方向で確認 / 範囲精緻化。**

- kotoba-net (:local/root) 宣言は updater_test→http-client→jvm-host 連鎖の undeclared 依存を**単独で
  解消**し、この連鎖に第三の未宣言依存は存在しない (claim SURVIVED)。
- **範囲精緻化**: falsify-74 境界 (a) と falsify-75 が暗黙に置いた前提「updater_test の緑には
  identity.clj 修理が前提」は成立しない — updater_test の load path は identity.clj に到達せず、
  kotoba-net 宣言のみでこの hermetic suite は green。従って 2 つの build-break は**独立かつ異なる
  テスト面**に影響する: (A) kotoba-net 未宣言は http-client を transitively pull する面
  (updater_test 等)、(B) identity.clj malformation は identity を transitively pull する面 (suite の
  大部分)。updater_test 単体の EOF 条件は「kotoba-net 宣言 + 単体動行緑」であり、identity.clj 修理は
  suite 全体の EOF 条件として別枠。

実装 軸 score は **3 のまま** (フル suite は identity.clj malformation により依然コンパイル不能 — 本
反復は一つの hermetic suite の unblock 確認のみで、suite 全体の green 化測定なし)。テスト 軸へもス
コープ修正として波及: falsify-75 の「build-break は identity.clj だけではない (kotoba-net 未宣言)」に
「updater_test 面では kotoba-net 宣言が必要十分、identity.clj はこの面に非関与」を精緻化して追記。

## 次の 1 アクション

Tier 2 report の修理案を更新: (A) は deps.edn への kotoba-net 宣言 (:git/sha または :local/root) のみで
updater_test 単体緑 (本反復で実測済み) が EOF、着地は本体 checkout 編集なので Tier 2 kanban/human;
(B) は falsify-68 の identity.clj (:import 復元) が EOF 判定 (detached worktree 一次 compile rc=0 + フル
suite green 確認) を併行。両者が着地した後のフル suite green を最終終点にする (falsify-62 の bundle/
graph 赤: published-lock 再発行もその後に再確認)。

## 附帯

本体 checkout は反復中 porcelain clean (dirty 0)、本 bot は touch せず wt-msloop / bot/maturity-sim-loop
で完結、実行 worktree mt-msloop76 は remove --force / prune 済みで worktree list に残存せず。7-軸表
16 行目孤立行 placeholder と falsify-46 mid-sentence cut は残存のまま (operator 復旧待ち、append-only
のため本 bot は編集しない)。identity.clj malformation (falsify-63..68) は既知赤として継続。