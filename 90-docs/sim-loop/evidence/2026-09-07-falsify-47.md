# falsify-47 — spec/契約: commands.edn の placeholder→path-param 整合をコマンド単位で機械検証 (2026-09-07)

測定者: itonami-maint
軸: spec/契約
ターゲット: falsify-10 の主張「プレースホルダ 128 すべてに `:in "path"` 宣言、欠落 0」
検証方法: 資源 `resources/cloud-itonami-app.commands.edn` (現行 main checkout, head 9e5b24a,
porcelain clean) を Python で解析し、テンプレート placeholder と params 宣言を**コマンドごとに**突合。

## 測定 (実測値、捏造なし)

資源ファイル: `resources/cloud-itonami-app.commands.edn` (47472 bytes)

| 測定項目 | 実測値 |
|---|---|
| `:command` エントリ数 | 208 |
| `:template` 数 | 208 |
| `:in "path"` 宣言数 | 128 |
| テンプレート placeholder ({}内) | 128 |
| placeholder を持つコマンド数 | 113 |
| placeholder のうち params に同名 name あり | 128/128 (欠落 0) |
| params に宣言され template に現れない name (orphan) | 0 |
| placeholder の params 宣言が `:in "path"` であるもの | 128/128 (非 path 0) |

解析ロジック: `:command [` マーカーで各コマンドの段落を分割し、その段落内の `:template` の
placeholder 集合と `:params` の `:name` 集合を突合。aggregate 一致 (128=128) ではなく
**コマンド単位の当該段落内での一致**を検証した点が falsify-10 からの精緻化。

## 判定

**SURVIVED** — falsify-10 の主張はコマンド単位の機械突合でも成立。
- 全 128 placeholder が params に `:name` として宣言済み (欠落 0)。
- 全 placeholder の params が `:in "path"` (非 path 0)。
- orphan なし (宣言されて未使用の path param 0)。

spec/契約 軸 score は 3 のまま (根拠: falsify-10 に falsify-47 のコマンド単位突合を追記。
値スキーマ (型/必須性) の機械検証は未整備のまま)。

## 附帯

- 同ファイルの cli-aliases (36 コマンド/33 template/25 placeholder) は `:in "path"` を用いず
  別の param 形状 (`:name :flag :required?`) を使用 — falsify-10 のスコープ (registry のみ) と整合。
- 7 軸テーブル 16 行目孤立行 `| 反証候補falsify30PLACEHOLDER` 残存のまま (operator 復旧待ち、
  falsify-33/34 Tier 2 report 済み、append-only で本 bot は編集しない)。
- 本体 checkout は agent/fix-open-red-5-three-bound / PORCELAIN CLEAN (dirty 0 実測)、
  head 9e5b24a。負荷は高負荷 (load avg 30-55 帯、前反復実測)。