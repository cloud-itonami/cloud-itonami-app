# falsify-122 (2026-09-19, 運用/滞留 軸)

## 主張 (i)

「緊急フラグ (<100 Gi) の不発火は定着する (falsify-121 の avail 103.75 Gi / 余裕 3.75 Gi が持続)」

## 判定: REFUTED

同一コマンド再実測:

- df /System/Volumes/Data: avail **73188864 KB ≒ 69.8 GiB / 93%**。
  falsify-121 (108807940 KB ≒ 103.75 Gi) から **-30.9 GiB** の低下。
  緊急フラグ (<100 Gi) は **2 反続の不発火を破って再発火** (発火系列: 5 反続発火 → 不発火 2 → 再発火)。
- avail 系列は引き続き非単調: -101.5 → -6.7 → +32.8 → -3.2 → +47.2 → -18.6 → **-30.9 Gi**。
  単一帰属は未確立のまま (falsify-118/119/120 継続、捏造ゼロ)。

「不発火が定着する」は現在形として成立しない。フラグは閾値近傍の振動を反映して発火/不発火を繰り返す。

## 主張 (j): 部分生存

「partial 一時ファイル 0 は定着する」 → **SURVIVED (2 反続)**:

- `find .git/annex \( -name '*.tmp' -o -name '*.part' \) | wc -l` = **0** (falsify-121 の 0 と同値)。
- keys: `find .git/annex/objects -type f | wc -l` = **786** (不変, **6 反続静止**)。
- du -sk objects = **11403092 KB** — falsify-121 と **KB 単位で完全同値** (6 反続静止)。
- dns-resolver HEAD = **c8090649** (不変, 6 反続)。

## 附帯測定 (同反復実測)

- resident: PID **1083** 継続 (falsify-120/121 実測から新規遷移なし)。ps 実測
  `node .../org-babashka-nbb/cli.js .../scripts/itonami-app-resident.cljk`。
- app origin/main は本反復未測定 (不変とは主張しない)。

## score

運用 3 のまま (低下は測定で裏取れたが修復判断は人間 = Tier 2)。

## 次の 1 アクション

同測定を再実測し、(1) 再発火が継続するか / -30.9 Gi の低下が定着するか、
(2) partial 0 が 3 反続に達するか、(3) keys 786 / du 同値の静止が 7 反続に達するかを判別。
