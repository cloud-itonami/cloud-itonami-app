# falsify-13 (2026-09-05) — NEXT-4「expiry-alert.log が更新継続中」説の反証試行

## 反証可能主張 (maturity.md NEXT-4 より)

> 2026-09-04 20:47 再確認: 同一エラー (`Could not find namespace:
> authority.scope`) で log が更新継続中 — 未修理のまま。

すなわち「expiry-alert.log は現在も更新され続けている」。

## 測定方法 (すべて実測コマンド)

1. `stat -f '%m %N' ~/.itonami/logs/expiry-alert.log`
   → mtime = 1788134405
2. `date -r 1788134405`
   → **Mon Aug 31 09:00:05 JST 2026** (週次発火 (月曜 09:00) の直後 5 秒)
3. `tail -5 ~/.itonami/logs/expiry-alert.log`
   → 末尾は `Error: Could not find namespace: authority.scope` +
   `Node.js v26.0.0` (falsify-9 と同一エラー、内容は不変)
4. `launchctl list | grep expiry`
   → `-\t1\tcom.gftdcojp.itonami.expiry-alert` (last exit 1、not running — 不変)
5. `bash scripts/ops-classpath.sh` (network-awai/cloud-itonami)
   → 出力に `authority/src` を含まない (bonsai/nekko/…/datalog の 15 項目、
   HEAD bf4e50ec でも未追従) → falsify-9/-11 の帰属は再確認で SURVIVED
6. インストール済み plist
   (`~/Library/LaunchAgents/com.gftdcojp.itonami.expiry-alert.plist`) の
   `--classpath` も同一の 15 項目 — 修正未着地を確認
7. `find orgs/kotoba-lang/authority/src -name '*.cljc'`
   → chain.cljc / scope.cljc / grant.cljc が存在 (案 A の修理材料は在位)

## 判定

**REFUTED (範囲修正)**: 「log が更新継続中」は不正確。log は最後の定刻発火
2026-08-31 09:00:05 以降静止している (次回発火は 2026-09-07 月曜 09:00)。
「2026-09-04 20:47 再確認」で観たエラー行は同じ行の再読であって、
log への新規追記ではなかった。

帰属自体は変化なし: 未修理のまま (generator も plist も authority/src を
持たない) であり、案 A (3 src 追加 + plist 再生成 + bootout/bootstrap)
は falsify-11 の検証内容のまま有効。

## 副次確認 (governor 統合 / NEXT-3)

- tamaki HEAD は 1aa9307 (PR #21) まで進んでいるが、
  `src/kotoba/tamaki/runners.clj` の `prepare-worktree!` は
  `git worktree add --detach` 失敗時の chmod/再試行を含まず、
  maintenance.clj も `git worktree remove` のみ →
  worktree-failed 根因修正 (chmod -R u+wx) は未着地。NEXT-3 は有効のまま。
- tamaki 側 worktree 残骸の実在確認は
  `git -C orgs/cloud-itonami worktree list` では検出されず
  (tamaki worktree は現在 0)、1559 連続 failed の現況 tick ログ確認は今回未実施
  (次回反復で tick 側 log を直接確認する)。
