# falsify-15 (2026-09-05) — NEXT-9: OPEN red-5 fix attempted as Tier 1 (test/ only)

## 主張 (反証対象)

「OPEN 赤-5 の修理は 3-bound 整合 (falsify-12) で test/ に着地でき、
着地後の負荷下フルスイートで flake サイトが緑になる」

## 測定

1. **本体 checkout の porcelain 再確認** (falsify-14 の前提条件):
   `git status --short` = 空 (cc7a17d)。Tier 1 着地条件を満たす。
2. **決定論的変換 (test/ のみ)**: bots_test.clj の test 側 `entered` 待ち
   3 箇所すべて 2000ms → 5000ms (worker 側 release deref 3000ms を超える)。
   bin/, src/ への変更はゼロ。
3. **機械検証の追加**: 新 deftest
   `goal-test-concurrency-bounds-are-internally-consistent` —
   ソースから bounds を正規表現で読み出し、invariant `entered > release`
   が破られたら赤にする。**このテスト自体が開発中に残存 2000ms
   (cancel テスト) を検出した** — gate が機能することの実測。
4. **変更後テスト緑確認 (負荷環境下)**: 同時走行する resident JVM
   (load 測定環境) で bots-test namespace 全体を実行:
   `Ran 2292 tests containing 13880 assertions. 1 failures, 0 errors.`
   唯一の failure は既知 OPEN 赤-4 (launcher_test.clj:162, falsify-8 で
   実バグ確定済み、main に元から存在)。flake サイト
   `durable-goal-detaches-plans-runs-read-actions-in-parallel-and-verifies`
   は負荷下で緑。
   (注: 1 回目の run は data-dir override 副作用で data_isolation_test が
   1 failure — 測定環境の artifact でありコード変更とは無関係。2 回目の
   適正な data-dir での run で消えた。)
5. **コミット + PR**: agent/fix-open-red-5-three-bound (9e5b24a) →
   PR #280 (https://github.com/cloud-itonami/cloud-itonami-app/pull/280)。

## 結論

- 主張 SURVIVED: 3-bound 整合の修理は test/ に決定論的に着地でき、
  負荷下 1 完走で flake サイト緑を確認。
- ただし flake リトライ機構はまだなく、単 1 回の負荷下完走は
  flake が消えたことの十分証明ではない → テスト軸 score 3 を据え置き。
  OPEN 赤-5 を「fix は PR #280 で提出、着地 + 追加 1 負荷下完走で CLOSED」
  に状態遷移。
- landing (merge) は kanban/human の判断 (Tier 2)。
