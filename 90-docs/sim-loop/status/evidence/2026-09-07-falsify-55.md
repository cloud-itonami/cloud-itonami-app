# falsify-55: 実装/テスト軸 — 「src 231 ファイル / test 205 ファイル」は現在の head でも有効、という数字主張を反証 (REFUTED — 実測 233 / 231 へ増加、測定対象は drift)

測定日: 2026-09-07 (JST)
測定者: itonami-maint (bot/maturity-sim-loop @ f932e3c)
軸: 実装 / テスト (ファイル数 — 決定論的計数)

## 主張 (反証対象)

maturity.md の 7 軸表 (実装行: 「src 231 ファイル、全主要面実装済み。virtual-shell は未活性」、
テスト行: 「test 205 ファイル」) が記録した数値は falsify-6 (2026-09-04, head bde2171) 時点の
実測に由来し、以後は同一数値のまま 7 軸表に残っている。本反証対象は:
「現在の head (9e5b24a) でも src 231 ファイル / test 205 ファイル である」という現在形の数字主張。

## 実測 (決定論的計数)

main checkout (agent/fix-open-red-5-three-bound) の head 9e5b24a に対し、
`git ls-tree -r --name-only <tree> -- src` および `-- test` でコミット済みツリーの
ファイル一覧を機械取得し、path プレフィックス行 (src/ と test/) のみを grep で計数:

- `src/` 配下: **233 ファイル** (ledger 主張 231 との差分 **+2**)
- `test/` 配下: **231 ファイル** (ledger 主張 205 との差分 **+26**)

計数の根拠 (生データ):
- src 一覧 233 行 (src/cloud/itonami/app/... フラット + authority/ 等サブディレクトリ含む、
  `.clj` / `.cljc` / `.cljs` / `.kotoba` 混在。最終行 workspace_tools.clj)。
- test 一覧 231 行 (test/cloud/itonami/app/ 以下 + test/browser/ + test/ 直下の
  _nbb.cljs / _test.js 等。最終行 signal_browser_test.js)。
- wc -l 出力 (src 233, test 231) を /tmp/src_count.txt に記録、pathtest 2 件
  (bots_test.clj / launcher_test.clj) の存在も確認。

リポジトリは --depth なしのフルツリーで、ls-tree はコミット 9e5b24a のスナップショットを
そのまま数えるため、作業ツリーの dirty 状態 (untracked kotoba-migration 2 件) の影響は受けない。

## 反証の成立

- 「現在の head でも src 231 / test 205」→ **REFUTED**。実測は src 233 / test 231。
  falsify-6 (bde2171) 以降に src が +2、test が +26 増加した (機能追加・テスト追加が続いた)。
- これは数字の drift であり、実装・テストの「質」または score (3) への反証ではない —
  実装面・テスト面が縮小したのではなく、増加したことを示す。7 軸表の numbers のみ古い。

## 範囲修正

- 実装行: 「src 231 ファイル」→「src 233 ファイル (head 9e5b24a 実測)」に修正。
  「すべて主要面 (bots/webhook/hermes-compat/store) 実装済み。virtual-shell は未活性」は不変。
- テスト行: 「test 205 ファイル」→「test 231 ファイル (head 9e5b24a 実測)」に修正。
  実行結果 (falsify-16 の 2292 tests / 13929 assertions / 0 failures) は不変。
- 実装/テスト軸 score は 3 のまま (数値の修正のみであり質的評価へ変更なし)。
- 教訓 (範囲): 7 軸表の file-count は falsify-6 以降 1 度も再計測されず stale 化していた。
  ファイル数は 1 反復で簡単に再計測できる決定論的指標のため、次回以降は対応軸反復で
  カウントを更新する (本 bot の運用手順に反映)。

## 附帯

- 本体 checkout は agent/fix-open-red-5-three-bound / head 9e5b24a、porcelain dirty は
  risk-2 継続 (untracked kotoba-migration 2 件)。本 bot は touch せず。
- dev /health 127.0.0.1:1338 / gateway 127.0.0.1:8080 は本反復では再測定していない
  (falsify-54 で dev 200 / gateway 404 を実測済み)。
- ledger 追記は append-only (既存行不変)。