# falsify-16 (2026-09-05) — OPEN red-5 CLOSED 条件の実測: 着地後の負荷下フルスイート完走

## 主張 (反証対象)

「PR #280 の merge 後、merged main 上で追加 1 回の負荷下フルスイートを
実行しても flake サイト (durable-goal 並行 deref) は赤にならない」
(maturity.md の OPEN 赤-5 CLOSED 遷移条件)

## 測定

1. **着地確認**: `gh pr view 280 --repo cloud-itonami/cloud-itonami-app`
   → `state: MERGED, mergedAt: 2026-09-05T06:52:58Z,
   mergeCommit: 1905580f8c067facbcb98a7a215b0fac9355cccb` (実測)。
2. **測定環境**: `git worktree add --detach /private/tmp/mt-merged-main
   1905580` (本体 checkout は touch しない)。porcelain clean。
3. **負荷条件**: 実行時 load averages 11.08 / 19.46 / 27.68、resident JVM
   5 プロセス同時走行 (falsify-15 と同等以上の負荷環境)。
4. **阻害事象の記録 (2 回の空振り run)**:
   - run 1/2 は `cloud.itonami.app.store/apply-op (store.clj:95)
     "state journal append does not fit this state"` でスイート開始前に
     異常終了 (EXIT=1)。原因はディスク満杯
     (/System/Volumes/Data 100%、avail 2.2Gi — canvas-test が
     `No space left on device` を出力) による journal 断片化ではなく、
     汚染された target/test-data の残骸。
   - `.cache/codex-runtimes` (1.6G) と huggingface hub の未使用モデル
     (Meta-Llama-3.1-8B 15G) を削除し avail 16GB を確保。
     これは operator cache の整理であって repo 変更ではない。
   - target/test-data は rm が git-annex read-only object で失敗 →
     `chmod -R u+wx` 後に削除成功 (falsify-6 で特定した tamaki tick の
     worktree-failed と同一の fail モードが test-data 後始末でも再現)。
5. **負荷下フルスイート完走 (run 3)**:
   `clojure -M:test -m cloud.itonami.app.test-runner` @ 1905580
   → `Ran 2292 tests containing 13929 assertions.
   0 failures, 0 errors.` EXIT=0 (所要 約15分、開始 19:16 JST / 終了 19:31 JST)。
   flake サイト `durable-goal-detaches-plans-runs-read-actions-in-parallel-
   and-verifies` を含む bots-test は負荷下で緑。

## 新規観測 (帰属に注意)

- **OPEN 赤-4 (launcher_test.clj:162) も本 run では赤にならなかった**
  (0 failures)。falsify-8 の帰属は「実バグ (bin/ の 2 階層上に
  kotoba-lang/shell が存在すると WORKSPACE_ROOT を shadow)」であり、
  本測定は /private/tmp の worktree でその shadow レイアウトが存在
  しないため緑と説明できる — 赤-4 の実バグ帰属は不変、
  本 run は赤-4 の反証ではない。

## 結論

- 主張 SURVIVED: merged main (1905580) で負荷下フルスイート 0 failures
  を実測。OPEN 赤-5 の CLOSED 条件 (着地 + 着地後の負荷下完走 1 回) を
  満たす → **OPEN 赤-5 を CLOSED に遷移**。
- テスト軸の根拠を更新: 決定論的赤 0、flake 修理が着地済み。
  ただし flake リトライ機構は依然なし → score は 3 のまま
  (根拠の文言を更新)。
- 運用軸に新規観測: ディスク満杯 (avail 2.2Gi) がスイートを
  2 回殺した。テスト実行の前提条件として disk avail の確認を
  反復手順に追加することを提案 (Tier 2)。
