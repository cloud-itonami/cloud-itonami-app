# falsify-14 (2026-09-05) — リスク-2「本体 checkout が dirty」前提の反証

## 主張 (反証対象)

maturity.md 既知リスク-2: 「本体 checkout が他 bot の WIP で dirty の間、
Tier 1 着地が不可能 → テスト緑の定期保証がない」。
cron 前処理スクリプトも `dirty_files: 0` (HEAD 95f80f3) を報告していた。

## 反証手順と実測

1. `git -C <本体> status --porcelain` → **空出力 (0 行) = clean**。
   `rev-parse HEAD` → `95f80f35dda759a06a8b1ca39bd950fb1b046797`、branch `main`。
2. dirty の由来だった bot.cljc face-hash 系 WIP の行方を実測:
   - commit f4f2964 (2026-09-03 14:25:02 +0900)
     `restore bot/face: the deterministic Bot avatar origin/main's bots.clj already calls`
     変更: src/cloud/itonami/app/bot.cljc (+35/-1) と
     test/cloud/itonami/app/bot_test.clj (+15)。**着地済み**。
   - PR #279 (31bfa88, 2026-09-04 23:55 merge) の cleanup land 0b6d7b7 は
     `.hermes/plans/winfix-1.md` 1 ファイルのみで、コード本体への接触なし。
3. clean main でのテスト緑確認 (変更前テストの代わりに HEAD 実測):
   `clojure -M:test` 完走 → **Ran 2291 tests containing 13893 assertions.
   1 failures, 0 errors. (rc=1)**
   唯一の failure は launcher_test.clj:162
   resident-clone-resolves-shell-from-workspace-root = 既知の OPEN 赤-4
   (falsify-8 で実バグと帰属確定、修理案 A/B は kanban/human 判断待ち)。
   赤数は falsify-7 (1 failure) と同一で、決定論的赤は赤-4 のみ。

## 判定

**REFUTED** — dirty 前提は少なくとも 2026-09-04 23:55 (PR #279 merge)
以降は不正確。リスク-2 の範囲を修正する:

- リスク-2 (修正後): 本体 checkout は現時点 clean であり、
  Tier 1 着地の構造的障害は解消済み。ただし「dirty 状態は再び生じうる」
  ので、反復ごとの porcelain 再確認を前提条件とする。
  テスト緑の定期保証が不可能だった期間は 2026-09-03〜09-04 に限定される。
- 帰結: OPEN 赤-5 の修理 (test/ の 3 bound 整合) と赤-4 の修理案 A/B は
  着地判断を kanban/human に仰ぐ Tier 2 であることに変わりはないが、
  「着地不能」の理由は消滅した。着地可否の判断材料が揃った状態。

## Tier

Tier 2 (測定・範囲修正・report)。本体への変更なし。
