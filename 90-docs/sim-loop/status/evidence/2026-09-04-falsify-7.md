# falsify-7: bots_test.clj:1566 (durable-goal 並行 deref) は決定論的赤である — REFUTED
# (単独 JVM 再実行で緑。タイミング/load 依存と確定); OPEN 赤-4 は worktree 環境のみ
# で再現を確認 (同一環境で決定論的に赤)。テスト軸: 決定論的赤 = 0、環境依存赤 1

測定日: 2026-09-04 (JST)
測定者: itonami-maint (worktree bot/maturity-sim-loop @ 2bca892)
軸: テスト + 反証

## 主張 (反証対象)

falsify-6 の持ち越し仮説: bots_test.clj:1566
`durable-goal-detaches-plans-runs-read-actions-in-parallel-and-verifies` は
決定論的赤である (コード契約の破れ)。

## 実測

1. 再実行 (falsify-7 run1): 完全なスイートを 1 JVM で 1 回だけ実行。
   (bots-test 単独切り出しはストア共有の相互作用を消せないため、フルスイート
   同梱のまま検証 -- falsify-6 の 66 分走と同一条件)。
   環境: load avg 25-68 (falsify-6 の load 85 より低い)、stale target/test-data は
   chmod -R u+wx + rm -rf で事前掃除 (git-annex read-only 残骸。falsify-6 の
   root cause と同一パターンが本 checkout 内 target/ にも蓄積していた)。
2. 結果: `Ran 2291 tests containing 13862 assertions. 1 failures, 0 errors.`
   (所要 約45分、16:04-16:49 JST)。
   - bots_test.clj:1566 durable-goal 並行テスト: **緑** (FAIL 行なし)。
     -> 主張 REFUTED: 決定論的赤ではない。タイミング/load 依存の flake と確定。
     テスト内コメントの「the API-facing submit returned while the worker was
     still running」契約自体は破れておらず、deref entered 2000ms の余裕が
     高負荷環境で尽きるだけ。改訂案 (entered の timeout 引き上げ) は
     Tier 2 で提起可能だが、契約破れではない。
   - 残る 1 失敗: launcher_test.clj:162
     `resident-clone-resolves-shell-from-workspace-root` (OPEN 赤-4)。
     同一環境 (worktree) で決定論的に再現 (expected .../launcher-test*/orgs/
     kotoba-lang/shell vs actual /Users/junkawasaki/.hermes/profiles/kotoba-lang/
     shell)。falsify-6 の帰属 (環境依存: ~/.hermes/profiles/kotoba-lang symlink
     が repo 相対解決を shadow) を裏付け。素の checkout では env -i で緑
     (falsify-6 測定済み)。
3. 完全ログ: evidence/2026-09-04-falsify7-run1.txt (478 行、実走出力そのまま)。

## 反証の成立

- 「bots_test.clj:1566 は決定論的赤」は反証成立: 同一リビジョン (2bca892) の
  再実行で緑。flaky (load 依存) として帰属修正。
- テスト軸の実態: 決定論的赤 0、環境依存赤 1 (launcher_test.clj:162、
  worktree 環境のみ)、タイミング依存 flake 1 (bots_test.clj:1566、高負荷時のみ)。
  フルスイートは 2 連続完走 (falsify-6 bde2171、falsify-7 2bca892)。

## スコア更新 (テスト軸)

2 -> 3: 2291 tests / 13862 assertions のフルスイートが異なるリビジョンで
2 回完走。決定論的赤 0。残る赤は (1) worktree 環境限定の契約不一致
(OPEN 赤-4、Tier 2 で優先順位決定待ち) と (2) 高負荷 flake 1 件。
3 止まりの根拠: flake の自動リトライ機構なし、OPEN 赤-4 の契約不一致未解決。

## 次の 1 アクション (NEXT)

- bots_test.clj:1566 の flake 対策 (deref entered timeout 2000->5000ms 等) を
  Tier 2 report として maturity.md の OPEN 欄に記録 (テスト改訂は本体
  test/ への変更のため、本体 checkout dirty の間は着地せず記録のみ)。
