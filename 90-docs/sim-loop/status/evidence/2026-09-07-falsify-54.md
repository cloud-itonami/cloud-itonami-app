# falsify-54: governor 軸 — falsify-6 の「tamaki tick 1559 連続 worktree-failed、毎 tick、着地 0 landed 継続」は現在の測定対象として REFUTED (スキャナが fleet-ci 実装へ移行、cloud-itonami-app は実 gate 結果へ解決)

測定日: 2026-09-07 (JST)
測定者: itonami-maint (worktree bot/maturity-sim-loop @ origin/bot/maturity-sim-loop 3670da5、本体 checkout 未 touch)
軸: governor 統合

## 主張 (反証対象)

falsify-6 (2026-09-04) + governor 軸行 (maturity.md) が記録した状態:
「tamaki tick は 1430 repo を 900s 間隔でスキャン継続。ただし **1559 連続 worktree-failed** (2026-08-14〜、毎 tick) — tick の `rm -rf` が git-annex read-only 残骸を取りこぼし → `git worktree add` が永久 `already exists`。修理案 (chmod -R u+wx) は tamaki リポ側で Tier 2 提起のみ、**着地 0 landed は継続**」

本反復の反証対象: 「cloud-itonami-app は現在も毎 tick `:worktree-failed` が継続し、governor の着地 0 が続いている」という現在形の状態。

## 実測

1. **falsify-6 の測定基盤 (itonami-maturity-tick.stdout.log) は不存在**:
   - `ls ~/.gftd/itonami-maturity-tick*` → No such file (全一致 0)。
   - `ls ~/.gftd/*maturity*tick*stdout*` → No such file。
   - `launchctl list | grep -i maturity` → **一致 0** (maturity-tick ジョブはロードされていない)。
   - `launchctl list | grep -iE "tick|fleet|gov"` → 稼働 tick 一覧 (下記) に itonami-maturity-tick は無し。
   - → falsify-6 が解析したログも、それを産む tick ジョブも現在は存在しない (運用変更 or 退役)。

2. **現行 governor スキャナー (com.gftd.fleet-ci-tip-tick) は稼働**:
   - `launchctl list` → `57431 0 com.gftd.fleet-ci-tip-tick` (PID 57431、exit 0 = 正常)。
   - plist ProgramArguments: `nbb ../scripts/fleet-ci/tick.cljs` (fleet-ci 実装、StartInterval 300s)。
   - `fleet-ci-tick.log` (28,670,975 bytes、mtime **Sep 7 14:00** = 現在も活動中) の `grep -c "worktree-failed"` → **0**。
   - `fleet-ci-tip-tick.stdout.log` (31,759,545 bytes、Sep 7 13:45) の `grep -c "worktree-failed"` → **0**。
   - cloud-itonami-app は最新 tick (`2026-09-07T04:40:12Z tick done — [["cloud-itonami-app" :fail] ... ["cloud-itonami-app" :pass] ... ["cloud-itonami-app" :input-rejected]`) と実 gate 結果 (`:pass` / `:input-rejected` / `:fail`) に解決 — `:worktree-failed` ではない。

3. **tick 実装は worktree 失敗モードを回避する形へ改修済み** (`scripts/fleet-ci/tick.cljs`, 131,797 bytes, mtime Sep 7 12:20):
   - `prepare-landing-worktree!` (tick.cljs:1411-) は `git worktree add --detach --no-checkout --quiet` + `sparse-checkout init/set --no-cone` (tick.cljs:1420-1422)。
   - → 全体ルートの checkout を行わないため、falsify-6 の「テスト内で git-annex 初期化→read-only 残骸→rm -rf 取残し」連鎖の対象 (test-data/projects/.../git-annex objects) を worktree に現出させない。
   - `cleanup-worktree!` (tick.cljs:1397-1407) は `worktree remove --force` (git-annex read-only 残骸を許容) + `worktree prune --expire now` で missing metadata を即時プルーニング。
   - grep for `chmod|u\+w|read-only|git-annex|annex` in tick.cljs → 0 (chmod 案は未導入だが、no-checkout+sparse 化で同 failure mode 自体が不要化)。

## 反証の成立

- 「cloud-itonami-app は現在も毎 tick `:worktree-failed` 継続」→ **REFUTED** (falsify-6 の現在形主張)。
  - 測定根拠 (maturity-tick.log + そのジョブ) は不存在。
  - 現在活動中の governor (fleet-ci-tip-tick) は cloud-itonami-app を実 gate 結果 (`:pass`/`:input-rejected`/`:fail`) に解決、両稼働ログで `worktree-failed` 出現 **0**。
  - tick 実装は `--no-checkout` + sparse-checkout 化で worktree-failed 誘発構造を排除済み (`prepare-landing-worktree!`)。
- ただし注意: falsify-6 の「tamaki 側で Tier 2 提起された chmod 修理案が着地したか」は本反復では確認していない (tamaki リポ管理外)。着地 0 か否かは別主張であり、本反証は「現在形の worktree-failed 継続」に限定する。

## 範囲修正

- governor 統合行: 「1559 連続 worktree-failed (2026-08-14〜、毎 tick) 継続」→「**過去の計測**。現在の governor は fleet-ci 実装 (com.gftd.fleet-ci-tip-tick) で、cloud-itonami-app は実 gate 結果へ解決 (worktree-failed 0)、tick 実装は --no-checkout+sparse で誘発モードを排除」と再記述。
- governor 統合 score は 3 のまま (skip-root は別途存在、tick 稼働は継続且つ実結果を出すが、tamaki chmod 修理案の着地確認は未実施のため質的変更なし)。

## 附帯

- dev /health 127.0.0.1:1338 -> 200、gateway /health 127.0.0.1:8080 -> 404 (本反復実測)。
- df / avail 174Gi / 9% (100% 飽和は解消のまま)。
- load avg 19.15/20.95/24.23 (高負荷)。
- 本体 checkout は agent/fix-open-red-5-three-bound / head 9e5b24a で porcelain dirty { untracked 2 (90-docs/adr/260907-kl-to-kotoba-migration.edn, 90-docs/kotoba-migration-progress.md) } = risk-2 継続 (他 bot/human WIP)。SOUL 不変条件に従い本 bot は touch せず、作業は wt-msloop / bot/maturity-sim-loop で完結。