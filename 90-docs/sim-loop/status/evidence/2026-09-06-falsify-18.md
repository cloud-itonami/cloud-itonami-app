# falsify-18 (2026-09-06) — NEXT-8 実行: ディスク増加源の du ツリー計測
# (リスク-5 の反証可能性確定 — 運用軸、測定のみ Tier 2)

## 主張 (反証対象)

falsify-17 の前提「Data volume の used 879Gi の内訳は未測定、増加源は
未同定」— すなわち、ディスク再飽和の原因が du ツリー計測の到達範囲外
(システム側/隠し領域) である可能性が残る。

## 実測 (du、2026-09-06 JST)

- `df -h /System/Volumes/Data`: 926Gi / 880Gi used / **5.2Gi avail / 100%**
  (falsify-17 計測時の 1.9Gi から 3.3Gi 回復 — 誰かの削除または
  自動清掃。この増分の帰属は未確定、記録のみ)
- `du -sh` トップレベル:
  - `~/github/com-junkawasaki/orgs` = **299G** (全体 880G の 34%)
    - `gftdcojp` = 147G
    - `cloud-itonami` = 75G
    - `com-junkawasaki` = 40G
    - `kotoba-lang` = 22G
    - `network-awai` = 11G
  - `~/.hermes` = 14G、`~/.codex-worktrees` = 1.1G
- さらに掘削: `gftdcojp/m365-archive` = **139G**、その内訳
  `onedrive` = **133G**:
  - `onedrive/レコーディング` = **83G**
  - `onedrive/From G Suite Drive` = **47G**
  - mail 3.4G / teams 804M

## 結論: SURVIVED (帰属確定)

- 「du の到達範囲外」という可能性は反証された。ディスク支配項は
  ローカル repo ツリー内の **m365-archive/onedrive 133G
  (全体 880G の 15%)** と断定できる。単一ディレクトリ。
- システム側の隠れた増加源説は不採用。falsify-17 の「14GB 再増分の
  内訳不明」は残るが、支配項は静的なアーカイブであり、**増分** は
  ワークツリー/ログ等の小さい項の寄せ集めと整合する (未確定のまま)。
- リスク-5 の緩和候補 (人間判断、Tier 2): m365-archive/onedrive を
  外部ストレージへ退避すれば +133Gi (avail が 5.2Gi → 約 138Gi)。
  本 bot はアーカイブデータには接触しない。

## 副次測定 (OPEN 赤-2 状態再確認)

- expiry-alert: launchctl print 実測 `state = not running`,
  `runs = 0`, `last exit code = (never exited)` (bolstered plist は
  再 bootstrap 待ち)。log mtime Aug 31 09:00 静止のまま、次回発火
  2026-09-07 (Mon) 09:00。falsify-13/17 の帰属は不変。
- network-awai origin/main (dd34f563) ops-classpath.sh の
  authority/src 欠落は falsify-17 で再確認済みのため本反復は grep で
  再確認のみ (count = 0)。
