# falsify-12 (2026-09-05): bots_test.clj:1566 flake「時間切れ型のみで hang/deadlock
# 構造を含まない」説の反証試行 — SURVIVED (静的解析)。ただし測定可能な
# 競合窓を 1 点同定した (修理案の範囲修正、着地なし)。

測定日: 2026-09-05 (JST)
測定者: itonami-maint (worktree bot/maturity-sim-loop)
軸: 反証 + テスト

## 主張 (反証対象)

falsify-7 の帰属: bots_test.clj:1566
`durable-goal-detaches-plans-runs-read-actions-in-parallel-and-verifies` の
deref entered 2000ms 赤は「flake は時間切れ型のみで、テスト構造上の
hang/deadlock 要因を含まない」— すなわち load が下がれば必ず緑になり、
テスト改訂 (entered timeout 引き上げ) 以外の構造的修理は不要である。

## 実測 (静的解析。本体 checkout dirty につき実行はせず、ソース閲覧のみ)

### 測定 1: producer 側の deliver 順序 (決定論的)

test/cloud/itonami/app/bots_test.clj:1526-1595 (全行実読):
- producer は `provider/agent-turn` の with-redefs 内 case 1 であり、
  最初のアクションが `(deliver entered true)` (bots_test.clj:1544)。
  deliver より前にブロックする処理は存在しない。
- producer の `(deref release 3000 nil)` (bots_test.clj:1545) は
  timeout 付き deref で、3000ms 後に nil を受けて続行する
  (無限ブロックではない)。

### 測定 2: consumer 側の bound (決定論的)

- consumer の `(deref entered 2000 false)` (bots_test.clj:1566) は
  timeout 付き。赤になる形は「2000ms 内に entered が deliver されない」
  の 1 通りで、hang ではない。
- 完了待ちポーリングループは 100 回 × Thread/sleep 25ms = 2.5s 上限
  (bots_test.clj:1570-1579)。
- `with-store` は store/transact! を swap! 置換 (bots_test.clj:42-46) で、
  永続ロックを介さない。turn-lock / active-turns の lock も
  テスト内では単一スレッド経路で競合しない。
- ソース上に無限待ち・相互待ち (デッドロック) の構造は存在しない。
  goal-executor は daemon-pool 3 (src/cloud/itonami/app/bots.clj:120-121)、
  submit-goal! → enqueue-goal! → Future 提出であり、pool が 3 本すべて
  埋まっていれば producer の開始自体が遅れうる — これは時間切れ型の
  帰属 (falsify-7) と整合し、hang ではない。

### 測定 3: 見つけた競合窓 (新規観察、反証により同定)

- release の deliver (bots_test.clj:1569) は consumer が
  (1) deref entered 2000ms、(2) submit 返却確認、
  (3) ポーリングループ最大 2.5s、を通った後に来る。
  producer は deref release を 3000ms で諦めるため、consumer 側の
  (1)+(3) の合計が 3000ms を超えて遅延した場合、producer は release が
  deliver されないまま case 2 に進む。この経路は即赤ではなく
  「高負荷時に長引く緑寄りの経路」だが、entered (2000ms)・poll (2.5s)・
  release (3000ms) の 3 つの bound が相互に非同期で設計されており、
  許容負荷は最小の entered 2000ms で律速される。
- 帰結: 「entered の timeout を 2000→5000ms に引き上げる」だけの
  修理 (OPEN 赤-5 の現行案) では、poll ループ 2.5s と release 3000ms の
  整合が未検証のまま残る。引き上げ後も entered+poll 合計が release の
  3000ms を跨ぐ時間帯が存在し、flake が消えない可能性が残る
  (未実測 — 次回反復で実行系が取れたら検証)。

## 結論

- 反証不成立: 「flake は時間切れ型のみ」は survived。hang/deadlock 構造は
  ソース上に存在しない。falsify-7 の帰属は修正不要。
- OPEN 赤-5 の修理案に範囲修正を追記: 「entered 2000→5000ms」に加え、
  poll ループ (2.5s)・release deref (3000ms) との整合確認をセットで
  行うこと。いずれも test/ への変更のため、本体 checkout dirty の間は
  着地せず記録のみ (Tier 2)。
- テスト軸 score は 3 のまま (変更要なし): 静的解析のみで実行計測が
  取れておらず、flake 発生率の定量化には至っていない。

## 次の 1 アクション (NEXT)

- 実行環境が取れた反復で bots_test.clj:1566 を高負荷再現条件下で
  再実行し、測定 3 の競合窓 (entered+poll vs release 3000ms) の
  実際の発火を確認する。発火すれば修理案の範囲を確定させる。
