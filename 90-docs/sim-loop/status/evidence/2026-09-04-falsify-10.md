# falsify-10 (2026-09-04): spec/契約軸「commands.edn 208 コマンドに schema がある」説の反証

## 主張 (反証対象)

maturity.md spec/契約 軸: 「commands.edn に 208 コマンドの schema」。
→ すなわち「レジストリが 208 コマンドそれぞれに対する呼び出しスキーマ
(パラメータの完全な宣言) を機械検証可能に保持している」説。

## 反証の結果: SURVIVED (ただし範囲を正確に修正)

本体 checkout は dirty (他 bot WIP) のため閲覧のみ。実測はすべて
本体 checkout (e97b6ed, resources/cloud-itonami-app.commands.edn, 47472 bytes) で実施。

### 測定 1: カウント整合

- `:command [` の出現 = **208** (:counts {:routes 408, :commands 208,
  :human-only 70, :unauthenticated 130} と一致、408 = 208+70+130)

### 測定 2: path パラメータの完全性 (決定論的検査)

template 中の `{placeholder}` 数と `:in "path"` 宣言数をレコードごとに照合:

- 208 コマンド中、テンプレートにプレースホルダを持つもの全件で
  対応する `:name "<ph>", :in "path"` 宣言が存在 (**欠落 0**)
- `:in "path"` の総数 128 = テンプレートプレースホルダ総数 128
- `:params []` (パスパラメータなし) は 95 コマンド、残り 113 が 1..3 個

### 測定 3: 機械検証リンクの実在

test/cloud/itonami/app/commands_test.clj (16 deftest) が
server.clj から routes を再スキャンし checked-in レジストリと比較
(the-registry-matches-the-routes)。route 追加で再生成しないと suite が落ちる
= 契約→テストの機械検証リンクは実在し、falsify-6/7 のフルスイート 2 回完走
で実際に走っている。

### 範囲修正 (survived だが主張を狭める)

- `:flags` は **schema ではない** — commands.cljc 自身が
  "They are hints for help output, NOT a schema" と明言。
  unknown flags は通過する。
- したがって正確な主張は「208 コマンドの **名前解決・path パラメータ**
  契約が機械検証つきで保持されている」であり、**入力値スキーマ
  (型・必須性の flags/body 検証) は存在しない**。
- maturity.md の「208 コマンドの schema」表記はこの点で過大 →
  「208 コマンドの解決/path-param 契約 (flags は hint, 値スキーマなし)」に修正。

## 結論

- 反証試行の結果: **survived** (path-param 契約の完全性は決定論的検査で確認)
- spec/契約 score 3 は維持 (上げない): 機械検証リンクは実在するが、
  それが検証するのは解決契約のみで値スキーマの機械検証は未整備。
