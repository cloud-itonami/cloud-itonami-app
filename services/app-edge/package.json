{
  "name": "cloud-itonami-app-edge",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "cloud-itonami-app read surface on Cloudflare Workers (ClojureScript). ADR-2608081500.",
  "type": "module",
  "scripts": {
    "catalog": "mkdir -p public && cp ../../resources/itonami-fleet-catalog.edn public/",
    "build": "npm run catalog && node ../../../../../scripts/resource-guard.mjs run build -- npx shadow-cljs release entry",
    "dev": "npm run catalog && wrangler dev",
    "dry-run": "npm run build && wrangler deploy --dry-run --outdir dist",
    "deploy": "npm run build && wrangler deploy",
    "test": "nbb --classpath \"src:../../src\" test/fleet_core_parity.cljs"
  },
  "devDependencies": {
    "shadow-cljs": "3.4.11",
    "wrangler": "4.118.0"
  }
}
