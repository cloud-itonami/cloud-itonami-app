{:format :kotoba.kir/v4,
 :signature {:params [], :result :i64},
 :functions
 [{:name status-disabled,
   :params [],
   :result :i64,
   :effects #{},
   :body 0,
   :param-types []}
  {:name status-idle,
   :params [],
   :result :i64,
   :effects #{},
   :body 1,
   :param-types []}
  {:name status-running,
   :params [],
   :result :i64,
   :effects #{},
   :body 2,
   :param-types []}
  {:name status-waiting-approval,
   :params [],
   :result :i64,
   :effects #{},
   :body 3,
   :param-types []}
  {:name status-stale,
   :params [],
   :result :i64,
   :effects #{},
   :body 4,
   :param-types []}
  {:name stale?,
   :params [p],
   :result :bool,
   :effects #{},
   :body
   (<
    (record-get
     [:record
      :routine/presence
      [[:enabled :bool]
       [:held-run :bool]
       [:active-run :bool]
       [:steps-admitted :i64]
       [:steps-recorded :i64]]]
     p
     :steps-admitted)
    (record-get
     [:record
      :routine/presence
      [[:enabled :bool]
       [:held-run :bool]
       [:active-run :bool]
       [:steps-admitted :i64]
       [:steps-recorded :i64]]]
     p
     :steps-recorded)),
   :param-types [[:ref :routine/presence]]}
  {:name may-start?,
   :params [p],
   :result :bool,
   :effects #{},
   :body
   (if
    (record-get
     [:record
      :routine/presence
      [[:enabled :bool]
       [:held-run :bool]
       [:active-run :bool]
       [:steps-admitted :i64]
       [:steps-recorded :i64]]]
     p
     :enabled)
    (if
     (stale? p)
     false
     (if
      (record-get
       [:record
        :routine/presence
        [[:enabled :bool]
         [:held-run :bool]
         [:active-run :bool]
         [:steps-admitted :i64]
         [:steps-recorded :i64]]]
       p
       :active-run)
      false
      true))
    false),
   :param-types [[:ref :routine/presence]]}
  {:name may-fire?,
   :params [p],
   :result :bool,
   :effects #{},
   :body
   (if
    (may-start? p)
    (if
     (record-get
      [:record
       :routine/presence
       [[:enabled :bool]
        [:held-run :bool]
        [:active-run :bool]
        [:steps-admitted :i64]
        [:steps-recorded :i64]]]
      p
      :held-run)
     false
     true)
    false),
   :param-types [[:ref :routine/presence]]}
  {:name status,
   :params [p],
   :result :i64,
   :effects #{},
   :body
   (if
    (record-get
     [:record
      :routine/presence
      [[:enabled :bool]
       [:held-run :bool]
       [:active-run :bool]
       [:steps-admitted :i64]
       [:steps-recorded :i64]]]
     p
     :enabled)
    (if
     (record-get
      [:record
       :routine/presence
       [[:enabled :bool]
        [:held-run :bool]
        [:active-run :bool]
        [:steps-admitted :i64]
        [:steps-recorded :i64]]]
      p
      :held-run)
     (status-waiting-approval)
     (if
      (stale? p)
      (status-stale)
      (if
       (record-get
        [:record
         :routine/presence
         [[:enabled :bool]
          [:held-run :bool]
          [:active-run :bool]
          [:steps-admitted :i64]
          [:steps-recorded :i64]]]
        p
        :active-run)
       (status-running)
       (status-idle))))
    (status-disabled)),
   :param-types [[:ref :routine/presence]]}
  {:name tick-admitted?,
   :params [t session-kind],
   :result :bool,
   :effects #{},
   :body
   (if
    (string=? session-kind "agent")
    false
    (if
     (record-get
      [:record
       :routine/tick
       [[:tick-enabled :bool] [:session-live :bool]]]
      t
      :tick-enabled)
     (if
      (record-get
       [:record
        :routine/tick
        [[:tick-enabled :bool] [:session-live :bool]]]
       t
       :session-live)
      true
      false)
     false)),
   :param-types [[:ref :routine/tick] :string]}
  {:name main,
   :params [],
   :result :i64,
   :effects #{},
   :body 0,
   :param-types []}],
 :oracle-value 0,
 :schema-identities
 #:routine{:presence
           "68f6c7eb34de718c080fec8b0e114c9a8f18c128a15e811591eb0751088f49e9",
           :tick
           "34078cd3ecc8906b53558b4bccd18cef6dfcbff5e1e0a7c9fc0ced1b14086594"},
 :exports
 [status-disabled
  status-idle
  status-running
  status-waiting-approval
  status-stale
  stale?
  may-fire?
  may-start?
  status
  tick-admitted?
  main],
 :schemas
 #:routine{:presence
           [:record
            :routine/presence
            [[:enabled :bool]
             [:held-run :bool]
             [:active-run :bool]
             [:steps-admitted :i64]
             [:steps-recorded :i64]]],
           :tick
           [:record
            :routine/tick
            [[:tick-enabled :bool] [:session-live :bool]]]},
 :effects #{},
 :blocks [{:id 0, :instructions [[:const.i64 0] [:return]]}],
 :entry main}
